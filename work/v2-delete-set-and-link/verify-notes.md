# v2 — 🗑 ลบชุดเนื้อร้อง + ลิงก์แชร์ใส่รหัสชุดเสมอ · หลักฐานการตรวจ

Branch `v2-delete-set-and-link` (from `origin/main` = d42fce3). Dev server 5471,
own Chromium :9411 + own `--user-data-dir` (ไม่แตะ 9222/9335/5393).
เพลงที่ใช้ทดสอบ = **เพลง 717 จาก DB จริง (อ่านอย่างเดียว · ไม่มีการ save ใด ๆ)** —
2 ชุด มี id ถาวรทั้งคู่ (`s0a7d42c7e7`, `s3b87476c4f`).

## B — ลิงก์แชร์ (เบราว์เซอร์จริง)

| ตรวจ | ผล |
|---|---|
| แชร์ตอนอยู่ **ชุดแรก** | `…/#/song/b036d020…?set=s0a7d42c7e7` — **มี `?set=`** และเป็น **id ถาวร ไม่ใช่ตำแหน่ง** |
| เปิดลิงก์นั้น | ลงที่ `เนื้อร้องที่ 1` · sheet fingerprint ตรงกันเป๊ะ (`740:2522022432` ทั้งสองแท็บ) |
| ลิงก์ที่ id ยังอยู่ | **ไม่ขึ้นข้อความเตือน** (เงียบ = ถูก) |
| ลิงก์ที่ id ไม่มีในเพลง | เปิดเพลงได้ + ขึ้น `เนื้อร้องชุดที่ลิงก์นี้ชี้ไว้ถูกลบไปแล้ว · กำลังแสดงเนื้อร้องชุดแรกแทน` · `role="status"` · กด ปิด ได้ |
| เพลงธรรมดา / เพลงชุดเดียว | ลิงก์สะอาด ไม่มี `?set=` (unit test) |
| `key` + `set` มาด้วยกัน | ได้ทั้งคู่ (`?set=` ถูกใช้ก่อนบล็อก `?key=` ที่ `return` ออก — กับดักเดิมยังไม่หาย) |

## A — ปุ่มลบชุด (เบราว์เซอร์จริง · วัดจาก content object ที่แอปถืออยู่)

| ตรวจ | ผล |
|---|---|
| โหมดแก้ไข | แถบชุด **กางค้าง** · ไม่มีปุ่มยุบ |
| ＋ เพิ่มชุด | `เนื้อร้องที่ 1 / 2 / 3` |
| 🗑 aria-label | `ลบ เนื้อร้องที่ 2` (ระบุชุดที่จะลบ) |
| กด 🗑 | ถามยืนยัน `ลบ “เนื้อร้องที่ 2” ?` + `ทำนองยังอยู่` · **ยังไม่ลบอะไรเลย** |
| โฟกัสตอนเปิด | `lset-confirm-cancel` (ปุ่มปลอดภัย ไม่ใช่ปุ่มแดง) |
| หลังยืนยัน | แท็บเหลือ `เนื้อร้องที่ 1 / 2` (**เรียงใหม่ ไม่ใช่ 1·3**) · อยู่ที่ชุดก่อนหน้า |
| aria-live | `ลบ เนื้อร้องที่ 2 แล้ว · เหลือ 2 ชุด` |
| **ทำนอง** | `content.stanzas` **byte-identical** ก่อน/หลังลบ และหลัง collapse |
| ท่อนที่ใช้ร่วมทุกชุด (ไม่มี `set`) | ยังอยู่ |
| ลบจนเหลือชุดเดียว | `'lyricSets' in content === false` (ไม่ใช่ `[]`) · แถวไม่เหลือ key `set` เลย · แถบหาย · ปุ่ม ＋ เบา ๆ กลับมา · ไม่มี 🗑 ให้กดซ้ำ |
| 360px | ไม่มี overflow (`scrollWidth == 360`) · ทุกปุ่ม 44px · ลบได้ครบเหมือน desktop |

## เจอเองระหว่าง verify แล้วแก้ในก้อนนี้

**กล่องยืนยันถูกตัดหัวที่ 360px** — ตอนแรกทำเป็นการ์ด inline ใต้แถบชุด แต่ในโหมดแก้
แผ่นเพลงมี scroll region ของตัวเอง (`.sv-doc`) ซึ่งที่ 360×780 **สูงแค่ 31px**
(แถบสถานะ/บันทึกกิน 253px) → คำถามเลื่อนพ้นจอ เหลือให้เห็นแค่ปุ่มแดง
(`elementFromPoint` ที่กลางหัวเรื่อง โดน `.sv-save-bar`).
แก้เป็น **modal จริง** ทรงเดียวกับ ShareSheet ของแอป (scrim `--z-modal` · การ์ดกลางจอ ·
`aria-modal="true"` จริง · Esc / คลิก scrim = ยกเลิก · Tab วนแค่ 2 ปุ่ม) →
วัดซ้ำแล้ว `elementFromPoint` โดน `lset-confirm-t` = หัวเรื่องเห็นจริง

## G-VERIFY (adversarial) — `G-adversarial-review.md`

G ยิงมา 4 ข้อ · **ตรวจซ้ำที่ source เองทุกข้อ**:

1. **[G ว่า High] ชุดที่ยังไม่ได้ save ไม่มี id → ลิงก์ไม่มี `?set=`** — *ไม่ใช่บั๊ก · เป็นสเปก*
   (ชุดยังไม่ save ก็ยังไม่มีใน DB คนเปิดลิงก์เห็นไม่ได้อยู่ดี · ใส่เลขตำแหน่งแทน = สิ่งที่ดีไซน์นี้ห้าม)
   มี unit test คุมไว้แล้ว
2. **[G ว่า High] watcher เคลียร์ `linkSetPending` ก่อน content โหลดครบ** — *ไม่จริง*:
   `loadSong` (Studio.vue:101) set `liveSong` ทีเดียวพร้อม content ที่ migrate แล้ว
   ไม่มี state ที่ content มาไม่ครบ · ส่วนร่างเปล่าจาก editor มี guard `s.number != null || title_th` กันอยู่แล้ว
3. **[G ว่า Medium] ข้อความเตือนค้างข้ามเพลง** — ✅ **จริง · แก้แล้ว**
   `linkSetMissing` เป็น boolean ที่ไม่เคย reset · shell ตัวเดิมโหลดเพลงใหม่ได้ →
   เตือนค้างบนเพลงที่ไม่เกี่ยวกับลิงก์เลย · เขียนเทสให้ **แดงก่อน** แล้วเปลี่ยนเป็น
   `linkSetMissingFor` (เก็บ id เพลงที่เตือน) + computed → หายเองเมื่อเพลงเปลี่ยน
4. **[G ว่า Medium] focus trap ไม่ดู shiftKey** — *ไม่เป็นปัญหาจริง*: มี 2 ปุ่ม
   Tab และ Shift+Tab ก็ไปที่ "อีกปุ่ม" เหมือนกัน · โฟกัสหลุดออกนอก scrim ไม่ได้ (scrim คลุมทั้งจอ)

## เจตนาที่ต่างจาก v1 (ตั้งใจ · align up)

- v1 โฟกัสที่ปุ่ม **ลบ** และผูก Enter = ลบ → กด Enter ค้างจากแถบแท็บลบชุดได้โดยไม่ตั้งใจ
  /v2 โฟกัสที่ **ยกเลิก** (Apple HIG: destructive → ปุ่มปลอดภัยเป็น default) และไม่ผูก Enter
- v1 ใส่ `aria-modal="true"` บนการ์ด inline (ไม่จริง) · /v2 เป็น modal จริงจึงประกาศได้ตรง

## นอกขอบเขต — ฝากไว้ให้ PM

- **โหมดแก้ที่ 360×780 เหลือพื้นที่แผ่นเพลงแค่ 31px** (แถบสถานะ+บันทึกกิน 253px) ·
  ที่ 412×915 ได้ 254px · desktop 522px · **เป็นของเดิม ไม่ใช่จากงานนี้** แต่แปลว่า
  แก้เพลงบนมือถือจอ 360 แทบใช้ไม่ได้
- **`/` (v1) ไม่มีระบบ `?set=` เลย** (ไม่มี `buildSongUrl` / ไม่อ่าน `query.set`) →
  ลิงก์เพลงจาก v1 เปิดที่ชุดแรกเสมอ · v1 จึงไม่มีบั๊ก "ลิงก์เปลี่ยนความหมายเงียบ ๆ"
  จากงานนี้ แต่ก็แชร์เจาะชุดไม่ได้
