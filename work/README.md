# work/ — งานค้างของเพลง (1 งาน = 1 folder)

**กติกาของ work/ + closed/ อยู่ที่ [`pair/pair-sop.md §1.1`](../pair/pair-sop.md)** — ไม่เขียนซ้ำที่นี่
`ls work/` = งานค้างทั้งหมด · ว่าง = ไม่มีค้าง · เสร็จแล้วลบ folder (ประวัติอยู่ใน git)

**เฉพาะเพลง (ที่ต่างจากพระคำ):**
- **LIVE = รอบ 37 `b3d188f`** (20 ก.ค.) · verify ด้วย commit stamp ใน bundle ไม่ใช่ชื่อไฟล์
- **ฐาน branch = `studio-shell-redesign`** (ไม่ใช่ `main`) · **`main` = auto-deploy** merge ผิด = ขึ้นเว็บทันที
- deploy = cherry-pick commit เข้า branch จาก `main` ผ่าน worktree แยก แล้ว push (ห้าม full-merge `studio-shell-redesign`)
- งานใหญ่ที่ยังไม่แตก (multi-voice ลูกคู่ · D.C./Fine/Segno resolver · measure grid · DockKey redesign) → `docs/backlog.md`
