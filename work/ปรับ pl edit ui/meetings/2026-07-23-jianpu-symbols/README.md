# วง G — ชุดสัญลักษณ์เจียนพู่ (23 ก.ค. 2026)

**ชื่อ chat ที่ตั้งใน Gemini:** `pleng-jianpu-symbols-2026-07-23`
(= slug ของโฟลเดอร์นี้ ตามธรรมเนียม meeting-room `<work-folder>/meetings/<date>-<slug>/` — P'Aim ค้นชื่อนี้ในเบราว์เซอร์ตัวเองเจอทันที ไม่ต้องแย่งจอกับ session ไหน)

**ผู้วางคำถาม:** P'Aim เอง (Research seat ไม่ขับเบราว์เซอร์ของ P'Aim — กฎ §4)

## สถานะ — ✅ คุยกับ G จบแล้ว 5 รอบ

- Chromium ของ session นี้เอง: **port 9351 + user-data-dir แยก** (ไม่แตะ :9222 ของ P'Aim · ไม่แตะ server :5480/:5312/:5326/:5335)
- P'Aim กด Sign in ให้ → ส่ง/ดึงเองได้ทั้งหมด 5 รอบ
- ปมเทคนิค 2 อย่างที่ต้องเลี่ยง (บันทึกไว้ให้ session ถัดไป): Playwright `connect_over_cdp` **ค้าง**กับ browser ตัวนี้ → ใช้ CDP ดิบผ่าน websocket แทน (`scratchpad/cdp.py`) · หน้าต่างอยู่สถานะ hidden ทำให้ Angular ไม่ render บทสนทนาและ mouse/key จริงไม่เข้า → แก้ด้วย `Emulation.setFocusEmulationEnabled` (หลอกว่าโฟกัส **โดยไม่ดึงหน้าต่างขึ้นมาแย่งจอ**) + ส่งข้อความด้วย synthetic KeyboardEvent

## ไฟล์

| ไฟล์ | คือ |
|---|---|
| `01-sent-to-G.md` | ข้อความที่ส่งให้ G **ทั้งดุ้น** (ข้อความเปิด + คำถาม 8 ข้อ + สิ่งที่ให้ค้าน) — ตรงกับที่อยู่ใน `docs/reports/g-brief-jianpu-symbols.md` |
| `02-reply-from-G.md` | คำตอบเต็มของ G **ทั้งดุ้น ไม่ย่อ** ครบ 5 รอบ |
| `03-sent-to-G-round2.md` | ข้อความรอบ 2 (คำตอบ P'Aim 8 ข้อ + คำถาม A-K) |
| `04-sent-to-G-rounds3-5.md` | ข้อความรอบ 3-5 (ไล่ค้าน G ทีละจุด) |
| `05-สรุปส่งต่อ-SA.md` | **ข้อสรุปส่ง SA** — G ยืนยันอะไร / ค้านอะไร / อะไรที่ยังไม่ลงตัว |

## กฎที่ใช้กับโฟลเดอร์นี้ (`C:\gl\CLAUDE.md` §4.5 ข้อ 10)

- **"เล่าว่า G ว่าไง ไม่นับ"** — ข้อสรุปที่ไม่มี `02-reply-from-G.md` = ไม่ใช่หลักฐาน · PM จะไม่ปิด gate
- ย่อได้ แต่ต้องย่อ**จาก**ไฟล์ 02 ที่เซฟไว้ ไม่ใช่จากความจำ
