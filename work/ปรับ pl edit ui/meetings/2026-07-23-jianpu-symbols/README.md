# วง G — ชุดสัญลักษณ์เจียนพู่ (23 ก.ค. 2026)

**ชื่อ chat ที่ตั้งใน Gemini:** `pleng-jianpu-symbols-2026-07-23`
(= slug ของโฟลเดอร์นี้ ตามธรรมเนียม meeting-room `<work-folder>/meetings/<date>-<slug>/` — P'Aim ค้นชื่อนี้ในเบราว์เซอร์ตัวเองเจอทันที ไม่ต้องแย่งจอกับ session ไหน)

**ผู้วางคำถาม:** P'Aim เอง (Research seat ไม่ขับเบราว์เซอร์ของ P'Aim — กฎ §4)

## สถานะ (23 ก.ค. 19:5x) — 🔴 ติด blocker: ล็อกอิน

P'Aim สั่งให้ Research seat **คุยกับ G เองผ่าน meeting tool** แต่ยังส่งไม่ได้:

- ✅ เปิด Chromium ของ session นี้เองแล้ว — **port 9351 + user-data-dir แยก** (ไม่แตะ :9222 ของ P'Aim · ไม่แตะ server :5480/:5312/:5326)
- ✅ ต่อ CDP + เอนจิน `BrowserAgent` ของ meeting-room ได้ (override `CDP_URL` เป็น 9351 · ไม่แก้ไฟล์ tool กลาง)
- 🔴 **Chromium ของ session นี้ยังไม่ได้ล็อกอิน Google** → Gemini ขึ้นปุ่ม "Sign in" · โคลนโปรไฟล์ 2 รอบ (แบบเลือกไฟล์ + แบบเต็ม) **ไม่พาการล็อกอินมาด้วย** เพราะ Chromium ของ P'Aim เปิดอยู่ ไฟล์ฐานข้อมูลถูกล็อก + คุกกี้ผูกอุปกรณ์ (Device Bound Sessions)
- ⛔ **Research seat ล็อกอินเองไม่ได้** — การกรอกรหัสผ่าน/ยืนยันตัวตนเป็นสิ่งที่ agent ห้ามทำ ต้องเป็น P'Aim กดเอง

**ทางปลดล็อก (เลือกทางเดียว):**
1. **P'Aim กด Sign in ในหน้าต่าง Chromium ของ session นี้** (เปิดค้างไว้แล้วที่ gemini.google.com · เป็นคนละหน้าต่างกับของ P'Aim ไม่รบกวนกัน) → หลังจากนั้น Research seat ส่ง/ดึงคำตอบเองได้ทั้งหมด
2. **P'Aim ก๊อปวางเองเหมือนเดิม** — ข้อความรอบ 2 พร้อมวางแล้วที่ `03-sent-to-G-round2.md`

## ไฟล์

| ไฟล์ | คือ |
|---|---|
| `01-sent-to-G.md` | ข้อความที่ส่งให้ G **ทั้งดุ้น** (ข้อความเปิด + คำถาม 8 ข้อ + สิ่งที่ให้ค้าน) — ตรงกับที่อยู่ใน `docs/reports/g-brief-jianpu-symbols.md` |
| `02-reply-from-G.md` | คำตอบเต็มของ G **ทั้งดุ้น ไม่ย่อ** — Research seat เซฟลงตรงนี้ก่อนสรุปเสมอ (ยังว่าง = ยังไม่ได้คำตอบ) |
| `03-สรุปส่งต่อ-SA.md` | ข้อสรุป ≤1 หน้า (G ยืนยันอะไร / ค้านอะไร / อะไรที่ P'Aim ต้องเคาะ) — สร้างหลังมี 02 เท่านั้น |

## กฎที่ใช้กับโฟลเดอร์นี้ (`C:\gl\CLAUDE.md` §4.5 ข้อ 10)

- **"เล่าว่า G ว่าไง ไม่นับ"** — ข้อสรุปที่ไม่มี `02-reply-from-G.md` = ไม่ใช่หลักฐาน · PM จะไม่ปิด gate
- ย่อได้ แต่ต้องย่อ**จาก**ไฟล์ 02 ที่เซฟไว้ ไม่ใช่จากความจำ
