# pair/ — การทำงานร่วม Claude Code ⟷ Antigravity

**👉 กติกาทั้งหมดอยู่ที่ [`pair-sop.md`](pair-sop.md) — ฉบับเดียว ใช้ทั้ง 2 โปรเจกต์ ทุก session อ่านอันนี้**
หลักฐานเบื้องหลังแต่ละกฎ → [`evidence.md`](evidence.md) (อ่านเฉพาะตอนจะแก้หรือเถียงกฎ)

| ไฟล์ | ใช้ตอนไหน |
|---|---|
| **`pair-sop.md`** | ทุกครั้งที่เปิด session — กฎล้วน |
| `evidence.md` | จะแก้/ลบ/เถียงกฎข้อไหน |
| `<task-id>.md` | 1 งาน = 1 ไฟล์ (เช่น `B118-slur-continuous.md`) · งานจบแล้วไม่ต้องอ่านอีก |

ฝั่งพระคำชี้มาที่ไฟล์เดียวกันนี้ → `phrakham.life2/pair/README.md`
สถานะงานปัจจุบัน → `docs/pm/board.md` (อ่านหัว **▶ RESUME**)

*(ก่อน 20 ก.ค. 2026 ไฟล์นี้เคยเป็นตัวโปรโตคอลเอง — เนื้อหาถูกยุบเข้า `pair-sop.md` + `evidence.md` ทั้งหมดแล้ว ดูของเดิมได้จาก git history)*
