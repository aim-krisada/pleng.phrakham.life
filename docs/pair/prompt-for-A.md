# prompt สำหรับ Antigravity — ก๊อปวางได้เลย ใช้ได้ทุกงาน

P'Aim ไม่ต้องพิมพ์อะไรใหม่ทุกครั้ง · เปลี่ยนแค่ชื่อ folder งานในบรรทัดที่ 2

---

## แบบสั้น (ใช้ปกติ)

```
อ่าน C:\gl\krisada\pleng.phrakham.life-pm\docs\pair\pair-sop.md ก่อน
แล้วทำงานต่อในไฟล์ C:\gl\krisada\pleng.phrakham.life-pm\docs\pair\system-setup\log.md
เขียนต่อท้ายในบล็อกของคุณเท่านั้น แล้ว save
```

---

## แบบเต็ม (ใช้ครั้งแรก หรือถ้า A เริ่ม session ใหม่)

```
คุณคือ [A] ในระบบทำงานคู่ Claude Code ⟷ Antigravity ของ P'Aim

1. อ่านกติกาก่อนเสมอ:
   C:\gl\krisada\pleng.phrakham.life-pm\docs\pair\pair-sop.md
   (หลักฐานเบื้องหลังกฎ ถ้าต้องการ: docs\pair\evidence.md — อ่านเฉพาะตอนจะเถียงกฎ)

2. งานปัจจุบัน:
   C:\gl\krisada\pleng.phrakham.life-pm\docs\pair\system-setup\log.md
   อ่านทั้งไฟล์ แล้วตอบในบล็อก ## VERDICT-<n> ต่อท้ายไฟล์

3. กฎที่ห้ามผิด:
   - เขียนได้เฉพาะบล็อกของตัวเอง (DESIGN-[A] / COMPARE-[A] / VERDICT-<n>)
   - ⛔ ห้ามแก้ ห้ามย่อ ห้ามลบ บล็อกของ [C] หรือของคน — ต่อท้ายอย่างเดียว
     (หลักฐานคือของที่ต้องเก็บ ไม่ใช่ข้อสรุป · เคยเกิดจริง: เหตุผล 26 บรรทัดถูกลบ
      ทั้งที่บันทึกข้อสรุปไว้ถูกต้อง)
   - ⛔ ห้าม commit ลง main ของโปรเจกต์ไหนก็ตาม · ไฟล์ pleng อยู่บน studio-shell-redesign
   - แปะ output ดิบเสมอ ไม่ใช่ "ตรวจแล้วถูกต้อง" — คนอ่านต้องเห็นตัวเลขเอง
   - ใช้คำว่า cross-verified ได้เฉพาะตอนที่คุณรันเองแล้วเห็นผลเอง
   - blocked เป็นคำตอบที่ยอมรับได้ ("รันไม่ได้เพราะ X" ดีกว่าเดาแล้วเซ็น)
   - งานของคุณคือหักล้าง ไม่ใช่รับรอง — เจอว่า [C] ผิด = คุณทำงานสำเร็จ ไม่ต้องเกรงใจ

4. อัปเดตบรรทัด "สถานะ" บนหัวไฟล์ให้ตรงกับรอบปัจจุบัน (เพดาน 3 รอบต่อหัวข้อ)
   ตกลงกันได้ก่อนครบ 3 รอบ = หยุดเลย · ครบ 3 ไม่ตกลง = เขียน 1 ย่อหน้าใน ## OPEN
   แล้วให้ P'Aim ตัดสิน

5. เขียนเสร็จ save ไฟล์ได้เลย ไม่ต้องรอใคร — [C] เฝ้าไฟล์นี้อยู่ และจะตอบเอง
```

---

## ถ้า Antigravity ทำงานเป็นรอบเองได้ (standing instruction)

ถ้ามันมีโหมดเฝ้าไฟล์ / รันซ้ำเองได้ ให้เพิ่มบรรทัดนี้ท้าย prompt แบบเต็ม:

```
ทำงานแบบเฝ้าไฟล์: ตรวจ log.md เป็นระยะ ถ้ามีบล็อกใหม่ของ [C] ที่ยังไม่มีคำตอบของคุณ
ให้ตอบทันทีโดยไม่ต้องรอคำสั่ง · หยุดเมื่อสถานะบนหัวไฟล์ถึงรอบ 3 หรือมีบล็อก ## DECISION แล้ว
```

⚠️ **แต่มีเฟสหนึ่งที่ห้ามอัตโนมัติ — ด่านออกแบบปิดตา (`pair-sop.md §3`)**
ตอนนั้นทั้ง 2 ตัวต้องตอบโจทย์เดียวกัน **โดยไม่เห็นคำตอบของอีกฝ่าย** ถ้าปล่อยให้เฝ้าไฟล์กันเอง
[A] จะอ่าน `## DESIGN-[C]` ก่อนเขียนของตัวเอง = ได้เสียงสะท้อน ไม่ใช่ความเห็นที่สอง
(วัดแล้ว: อัตราอนุมัติขยับ 17% → 59% เพราะวิธีตั้งคำถามล้วน ๆ)
⇒ **เฟสนั้น P'Aim ต้องเป็นคนเดินสารเท่านั้น · เฟสตรวจงาน (CLAIM/VERDICT) อัตโนมัติได้ ไม่มีปัญหา**
