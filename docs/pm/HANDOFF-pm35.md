# HANDOFF — pm35 → pl pm ใหม่ (2026-07-20)

อ่านไฟล์นี้ + `docs/pm/pm.md` + `docs/pm/board.md` (▶ RESUME) แล้วทำต่อได้ทันที
**ตั้งชื่อตัวเอง `pm36`** แล้วอัป `board.md §🎯 PM session ปัจจุบัน`

---

## 1. LIVE / GIT
- **LIVE = รอบ 35 `94199d6`** (`origin/main` · bundle `assets/index-0T7X4x_B.js` · CSS `index-AX6xWeFW.css`) — **ยืนยันจาก bundle จริงแล้ว**
- ⚠️ **hash ที่ GH Actions build ≠ hash ที่ build ในเครื่อง** (ของเราได้ `DdCOmry4`) — **อย่า verify ด้วยชื่อไฟล์** ให้ grep หา **commit stamp** ในตัว bundle แทน: `curl` หน้าแรก → หา `assets/index-*.js` → `grep 94199d6`
- **ของที่ขึ้นรอบ 35 (3):** B111 เส้นขเบ็ดถูกชั้น · B114 เลิกพึ่ง rAF (ประกัน) · B113 ชื่อเพลงเต็มบนการ์ด
- **⛔ ห้าม merge main / deploy จน P'Aim สั่ง** · deploy = merge เข้า main ผ่าน worktree ของ main (ห้าม commit main จาก dir นี้ — dir นี้อยู่ `studio-shell-redesign`)

## 2. ▶ งานถัดไปที่ P'Aim สั่งไว้แล้ว (ตามลำดับ)

### 🥇 B112 — เล่นต่อตอนปิดจอ + คุมจากจอล็อก (P'Aim ยืนยันเองตรงๆ + ส่งรูปมา)
- P'Aim: *"อยากได้คล้ายตามภาพ"* (รูป lock screen พระคำ: ชื่อ + รูปปก + แถบเวลา + ก่อนหน้า/เล่น/ถัดไป)
- **P'Aim สั่ง "ให้เสร็จทีละงาน"** → รอบ 35 จบแล้ว **อันนี้คือคิวถัดไป**
- **ยืม lib กลางพระคำ `assets/pk-audio.js`** (UMD `window.PKAudio` · ไม่ต้องแก้ build · option `media` = ชื่อบนจอล็อกของเราเอง ไม่ใช่ "พระคำ.ชีวิต") = **สลับข้างกับ DockKey ที่เราเป็นเจ้าของ**
- 🔴 **ไม่ใช่ drop-in — pk pm9 เตือนเอง ห้ามรับปาก P'Aim ว่าง่ายก่อนสำรวจ:** พระคำ = mp3 อัดไว้ล่วงหน้า · **เราสังเคราะห์สดตอนกด** (`src/lib/audioExport.js` OfflineAudioContext→lamejs) + ไฮไลต์ **index-based** (`onNote(note, idx)`) ไม่ใช่ time-based → ต้องเลือก: ป้อน blob + timing map เอง **หรือ** ตัดไฮไลต์ เอาแค่ background + lock screen
- **อ่านก่อน scope:** `pm/design-audio-mp3.md` + `pm/report-audio-player.md` (repo พระคำ)
- **ขอแก้ lib = ผ่าน pk pm9 เท่านั้น** ห้ามแก้ไฟล์เขาตรง (session id ของ pk pm9 อยู่ใน `list_sessions`)
- 💡 MediaSession **ไม่ต้อง https** — ทดสอบผ่าน LAN ได้ · ⚠️ ต้องเสิร์ฟด้วย server ที่รองรับ **Range** (`py -m http.server` ไม่รองรับ → แถบเลื่อนเด้งกลับ 0 เหมือนพัง · เขามี `tools/range_server.py`)
- 🤝 **เขาขอตอบแทน 2 อย่าง (ของเรา):** (1) แถบเลื่อนแบบเข้าถึงได้ (keyboard + `aria-valuetext`) ที่ `SingTransport.vue` ยังไม่มี (2) บั๊ก `aria-label` ปุ่ม ▶ ค้างเป็น `it.name` ตอนเล่น — อยู่ใน `DockKey.vue`

### 🥈 B117 — ถามพี่เปา 1 คำถาม (ไม่ต้องเขียนโค้ด แค่ถาม)
- **คำถาม:** *"โน้ตชุดเร็วอย่าง `4 3 2 1` ที่แต่ละตัวมีคำของตัวเอง อยากให้มัดติดกันเป็นกลุ่มไหม หรือแยกตัวแบบตอนนี้ดีแล้ว"*
- **ตัวเลข:** กฎ `issue8` มัดคานตามพยางค์ → เพลงไทย 1 โน้ต = 1 คำ → **ทั้งคลังมัดแค่ 10.9% (516 runs)** · ถ้ามัดตามจังหวะจะได้ **68.7% (3,238 runs) = 6.3 เท่า**
- **ไม่ใช่บั๊ก** — เป็นกฎที่ตั้งใจใส่ มี golden test 10 ตัวที่พี่เปาเคยขอ · **ห้ามเปลี่ยนเองโดยไม่ถาม**
- ทางเลือกหลังได้คำตอบ: **B** เปลี่ยนกฎทั้งระบบ (~ครึ่งวัน · ล้ม golden test เดิม) · **C** ทำเป็นตัวเลือกให้สลับ (~1-2 วัน · ขัด KISS) · (A เฉพาะตอนพิมพ์ = ❌ จอ≠กระดาษ · D รื้อวิธีวาด = ❌ ไม่จำเป็น)

### 🥉 ค้างอื่น (ไม่บล็อก)
- **B115** กด back จากหน้าเพลง เด้งกลับชั้นหนังสือ แทนกลับรายการในเล่ม (pre-existing · tester เจอ)
- **B108 quirk** เลือก "— ไม่ระบุธีม —" → เก็บ null → เปิดใหม่อ่านเป็น "ไม่รู้" → เผยแพร่แล้วธีมเก่าถูกส่งกลับ (ต้องทำ tri-state)
- **polish** ชิป fermata อาจทับแป้นตอนโน้ตอยู่ล่างสุดจอเตี้ย
- **`𝄐` glyph risk** บาง Android/iOS อาจเห็นเป็นกล่อง □ — P'Aim ยังไม่ได้ยืนยัน · กระทบของที่ live แล้ว
- **DockKey ใหม่ยังไม่นิ่ง** — branch `dock-resize` `f8d77a3` ค้าง **P'Aim ไม่อยากใช้จนกว่าจะนิ่ง** · DockKey = core แชร์พระคำ ถ้าจะ deploy ต้อง gate 2-host ประสาน pk pm
- **งานใหญ่ที่คุยกับ G ไว้** (ยังไม่ทำ · file เข้า backlog ก่อนแตกงาน): ลูกคู่ multi-voice (ไม่มีในโมเดลเลย) · D.C./Fine/Segno ให้เล่นกระโดดจริง · measure grid · layer/track — ดู `docs/reports/current-editor-audit.md`

## 3. 🙏 ค้างให้ P'Aim/พี่เปา ยืนยันบน live รอบ 35
1. **พี่เปา** เปิดเพลงที่เคยบ่น (แก้ไป **9 จุด · 4 เพลง: #7 #89 #101 #120**) ว่าเส้นถูกแล้ว
2. **สั่งพิมพ์ PDF จริง** อีกรอบบนโค้ดที่ merge แล้ว (ก่อน merge ยืนยันแล้วว่าถูก)
3. **ชื่อเพลงบนมือถือจริง** — ที่ 360px + ล็อกอิน รายการยาวขึ้น **+60.8%** (P'Aim เห็นตัวเลขแล้วสั่ง deploy = รับตามนั้น) ถ้าพี่เปาบ่นว่ายาวไป ทางแก้ = จำกัด 2 บรรทัดบนมือถือ
4. **เพลง 751** = ร่างของพี่เปา คนอื่นไม่เห็น (RLS `song_drafts`: เห็นได้เฉพาะเจ้าของ หรือคนที่เป็น approver) — **P'Aim ล็อกอินแล้วก็ไม่เห็น = บัญชียังไม่ใช่ approver** · การกวาดคลังครอบเฉพาะ **109 เพลงที่ anon อ่านได้** ไม่ครอบร่าง

## 4. 🔴 บทเรียนรอบนี้ (สำคัญ — เขียน memory แล้ว 2 อัน)

### (ก) agent browser ไม่ render → ห้ามสรุปบั๊กที่ขึ้นกับเวลา
memory **`pleng-agent-browser-not-rendering`**
Browser pane + claude-in-chrome ที่ agent เรียก รายงาน `visibilityState:"hidden"` และ **rAF ไม่ยิงเลย** (PM วัดเอง: 0 ครั้งใน 6 วินาที) · IntersectionObserver ก็ถูกพัก
เกิดจริง: dev รายงาน "คานหายหลัง 4 วิ" (B114) → tester ยืนยันซ้ำ → **ทั้งคู่วัด env พังอันเดียวกัน ไม่ใช่บั๊กจริง** · dev ถอนหลักฐานตัวเองอย่างซื่อสัตย์
→ **ผลวัด static geometry (`getBoundingClientRect`/`getComputedStyle`) ยังเชื่อได้** · ที่เชื่อไม่ได้คือสิ่งที่ต้อง "ยิงซ้ำตามเวลา" → ต้องให้คนเปิดจอจริงชี้ขาด

### (ข) เทียบกับโมเดล ไม่ใช่สัญชาตญาณ
memory **`pleng-compare-to-model-not-intuition`**
PM เปิด PDF นับเส้นได้ตัวเลขถูกทุกตัว แล้ว**สมมติเองว่า "ขีดใต้ติดกัน = ต้องมีคานเชื่อม"** → ประกาศ "คานหาย 92%" → **เกือบสั่งรื้อวิธีวาดคาน 3-5 วัน แก้บั๊กที่ไม่มีจริง**
ความจริง: กฎมัดคานตามพยางค์ตั้งใจไม่มัด · dev รันโมเดลจริงเทียบ PDF: **8 runs/16 bars ตรงกันเป๊ะ คลาด ≤0.1pt**
→ **ก่อนประกาศว่า output หาย ต้องรันฟังก์ชันที่ผลิตมันออกมาเป็นค่าคาดหวังก่อน** · วัดเลขถูก ≠ ข้อสรุปถูก

### (ค) เจอบั๊กในบ้านเดียวกัน = แก้เลย อย่าแยกรอบ
P'Aim ถามตรง *"เจอแล้วทำไมไม่แก้เลย"* — PM แยก B114 ออกไปด้วยเหตุผล "กันตรวจปนกัน" (หลักถูก **แต่ใช้ผิดที่**)
เกณฑ์: อยู่ไฟล์เดียวกับที่เพิ่งแก้ + คนเดิมยังมี context + **อาการเป็นเรื่องเดียวกับที่ผู้ใช้บ่น** → **แก้ในรอบเดียว** ไม่งั้น "แก้ให้แล้ว" แต่ผู้ใช้ยังเจอปัญหาเดิม = เสียความเชื่อถือ

### (ง) กระบวนการที่เวิร์กมาก รอบนี้
- **PM สร้าง agent session เองได้** · **1 agent = 1 ฟีเจอร์ full-stack** · **tester = agent แยกเสมอ** (คนสร้างห้าม gate งานตัวเอง)
- **M1 = ทำ "หน้าตา" บนโค้ดจริงแล้วหยุด → P'Aim เคาะ → M2 ต่อ logic** (ห้าม throwaway mockup) · resume agent เดิมด้วย `SendMessage`
- **tester รอบนี้พิสูจน์คุณค่าชัด:** ไม่เชื่อรายงาน dev ไปทำวิธีตัวเอง → เจอว่า base แย่กว่าที่ dev เล่า (`3•` เห็น 3 เส้น ไม่ใช่ 2) · เทียบ MIDI แบบ full note object แทน dump 7 ฟิลด์ · **ล็อกอินเข้า badge ได้ด้วย `import('/src/store.js')` แล้วพลิกธง session** (ไม่ปลอม token) ที่ dev ทำไม่ได้
- **PM ตัดสินเรื่องมาตรฐานเอง ห้ามถาม P'Aim ว่า "อะไรถูก"** — รอบนี้ตัดสิน 3 เรื่อง (ขอบเขตการกวาดคลัง · ความยาวขีดสั้นอ้าง Gould · ทิศขีดสั้น) · P'Aim ตอบแค่ "อยากได้อะไร"
- **ขนาน 2 สายได้** ถ้าคนละไฟล์ — สั่งทั้งคู่ว่า "ห้ามแตะไฟล์ของอีกสาย ถ้าจำเป็น = หยุด ping PM"

## 5. 🧹 ความสะอาด (ทำแล้วรอบนี้)
- **worktree 36 → 25** · ลบ 11 อัน (งานที่ merge + deploy แล้ว: beam · names · deploy35 · t-b111 · t-b113 · t-b111-base · t-b113-base · fermata · drawer · guide · category)
- **ปิด dev server ค้าง 7 ตัว** (5380 · 5390-5394 · 5398)
- ⚠️ **ก่อนลบ worktree ต้องเช็ก `git status --porcelain` เสมอ** — รอบนี้เจอ **รายงาน tester 2 ไฟล์ที่ไม่เคย commit** (fermata · tablet drawer) เกือบหายถาวร → ย้ายเข้า `docs/reports/` แล้ว
- **25 อันที่เหลือ ห้ามลบมั่ว** — มี unmerged work: 3 ที่นั่งประจำ (`-sa` `-uxui` `-music` ยังใช้อยู่) · `dock-resize` · mp3 · b107 · cello · b109 · dock-space
- 🔧 `C:/gl/krisada/pleng-deploy35` = โฟลเดอร์เปล่าค้าง (git ถอดออกแล้ว · ไฟล์ถูกล็อก) ลบทิ้งได้เมื่อรีสตาร์ท
- memory sync ขึ้น OneDrive แล้ว

---
*pm35 · 2026-07-20 · live รอบ 35 · B111 เส้นขเบ็ด + B113 ชื่อเพลงเต็ม + B114 ประกัน rAF ขึ้นครบ*
