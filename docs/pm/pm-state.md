# PM state — เพลง.พระคำ.ชีวิต

> **สมองอยู่บน disk** → PM ตายเกิดใหม่ได้ · อ่านไฟล์นี้ + `decisions-log.md` = rehydrate ทันที
> เก็บ **สั้น เฉพาะของสด** · จบแล้วตัดออกทันที

## ▶▶ PM คนต่อไปเริ่มที่นี่ (handoff 2026-07-29 ค่ำ · จาก pl pm52)

**สถานะ 1 บรรทัด:** ของ 3 ก้อนสำหรับพี่เปาอยู่บนสายเดียวแล้ว ผู้ตรวจผ่าน 20/20 · **ค้างชิ้นเดียว = งานแก้ชิปให้ส่องตารางถูก** พอเสร็จ → ผู้ตรวจซ้ำ → **P'Aim สั่งปล่อย** · **พรุ่งนี้ = ออกแบบ v3**

**⛔ วิธีทำงานที่ P'Aim สั่ง (เข้มกว่าปกติ · ใช้ต่อไป):**
- **เสนอ → หยุด → รอ ทุกก้าว รวมก้าวย่อย** · ⛔ ห้ามทำอะไรเองแม้เล็ก · **1 ข้อความ = 1 การตัดสิน**
- **ทุกข้อความที่ขออนุมัติ ต้องพิมพ์รหัส `OKgo` / `Nogo` ให้เขาเห็น** — ประตูโปรแกรมอ่านเฉพาะรหัส ไม่อ่านคำว่า "อนุมัติ"
- **ทุกข้อเท็จจริงต้องมี path ของไฟล์ที่เพิ่งเปิด** · ไม่มี path = เขียนว่า "ยังไม่ได้เปิด"
- ทุกข้อสรุป design/มาตรฐาน ต้องผ่าน N · **G กับ C: P'Aim สั่งเลื่อนไว้ก่อน**

---

## 🔴 ค้างที่ P'Aim (เรียงตามด่วน)
1. **สั่งปล่อยขึ้นเว็บ** — รอผลงานแก้ชิป + ผู้ตรวจ แล้ว PM สรุปให้เคาะครั้งเดียว
2. **เคาะลำดับประตูตรวจ 16 ประตู** ใน `docs/standards/รายการมาตรฐาน-v3-ฉบับร่าง.md`
3. ถามพี่เปาว่าแอปบน iPad ที่เปิดให้ดูคือแอปอะไร (แกชอบ *"กดจังหวะจริงแล้วมันบอกค่าให้"*)

## 🏃 งานที่กำลังวิ่ง (2 ตัว)
| งาน | สาย | รออะไร |
|---|---|---|
| **แก้ชิป** — นับร่าง `pending` + พาไปแผง "งานร่าง / รอตรวจ" + เปลี่ยนคำเป็น "รอตรวจ" | แตกจาก `rel-v1-all3 @ccf8cb2f` | ผลงาน · **ตัวเดียวที่ขวางการปล่อย** |
| **ผู้ตรวจ** (session ยังเปิด รู้บริบทหมดแล้ว) | ตรวจ `rel-v1-all3` ผ่าน 20/20 แล้ว | **ต้องส่งให้ตรวจซ้ำหลังแก้ชิป** |

## 🚀 สายที่พร้อมปล่อย (ยังไม่ปล่อย)
**`rel-v1-all3 @ccf8cb2f`** (ฐาน `rel-v1-b128-chip @c4a9843d` ← `origin/v1 297a7b65`) = **เรียงเพลง (B131) + ชิป + เตือนชื่อซ้ำ (B128)**
- ผู้ตรวจ **20/20** · **ทำอาการของพี่เปาซ้ำได้บนเว็บจริง** (โหลด 3 ครั้ง = 3 ลำดับ) แล้วยืนยันสายใหม่หายสนิท · วัดจอ 360/412 ผ่าน
- ⚠️ ผู้ตรวจยังไม่พิสูจน์: ล็อกอินเป็นพี่เปาตัวจริง · ลบ/กู้คืนของจริงในฐานข้อมูล · โทรศัพท์จริง
- ⚠️ **★ เพลงโปรดไม่ได้ขึ้น v1** (v1 ไม่มีฟีเจอร์นี้ · `git grep "โปรด"` = 0) — ของนั้นใช้ได้เฉพาะ v2/v3
- **deploy v1:** ย้าย tag `v1-frozen` + push -f **แล้วต้อง push เข้า `main` เป็นตัวจุด** · `gh workflow run` ใช้ไม่ได้ (PAT ไม่มีสิทธิ์ Actions) · **verify: curl bundle หา build stamp เสมอ**

## 🌐 LIVE (ไม่เปลี่ยนทั้งวัน)
`/` = `297a7b65` (tag `v1-frozen`) — **พี่เปาใช้ทุกวัน** · `/v2/` = จอดที่ `v2-release@f30767ca` (⚠️ ไม่ตรงกับสายใด ถ้าจะแตะต้องเคาะสายก่อน)
**ฐานงาน v3/v2 = `studio-shell-redesign`** ตอนนี้ `2dfc8af9`

## 🔴 หนี้ที่ต้องปิดใน v3 (รวมอยู่ใน B132)
1. **กด "อนุมัติเผยแพร่" ไม่ตั้งธง `verified`** — `db/004:177` insert 8 คอลัมน์ ไม่มี `verified` ⇒ เลข "ยังไม่ตรวจ" ไม่มีวันลด · มี 3 เพลงที่พี่เปาอนุมัติแล้วไม่เคยขึ้นเว็บ
2. **ช่องทางเข้าที่ 2** — ผู้อนุมัติใส่แถวเข้าตารางเพลงตรง ๆ ได้ (`db/002:72`) แล้วระบบปั๊ม "อนุมัติเผยแพร่" ให้เอง (`db/004:52`) = ต้นเหตุของค้าง
3. **ปุ่มย้อนเวอร์ชันเขียนทับด้วยสำเนาเก่า ไม่มีกันชน ไม่มี undo** (v1:1989 · main:1855)
4. **ค้างเก่า** — เพลงที่เผยแพร่อยู่โดยไม่เคยผ่านด่าน (นับได้ 139 จากเกณฑ์ "ไม่มีร่างผูก" · **เกณฑ์ยังไม่ทดสอบ** → `tools/probe-3-songs-20260729.sql` บล็อก 1)

## ⭐ มติ v3 ที่ล็อกแล้ว (เต็มใน `decisions-log.md` 29 ก.ค.)
| เรื่อง | มติ |
|---|---|
| Stack | **Vue 3 + Tailwind + Preline UI ทั้งหมด** · PWA · ⛔ ไม่ลงร้านแอป |
| มาตรฐาน | **⛔ ยกเลิก Apple HIG + Material Design** · ใช้รายการของเราเอง **111 ข้อ** ที่ `docs/standards/รายการมาตรฐาน-v3-ฉบับร่าง.md` (WCAG 2.2 · ARIA APG · NN/g · IA · Web Bloopers) · ⛔ ไม่มีคำว่า "Modern SaaS Patterns" · **ยังต้องแก้ `C:\gl\CLAUDE.md` §Design quality ที่ยังอ้าง 2 ค่ายนี้** |
| WCAG | **86 ข้อ · A 31 · AA 24 · AAA 31** · AA = ประตูบังคับ + AAA เฉพาะ 5 ข้อที่คุ้มกับผู้สูงอายุ · **"Lighthouse 100 = ผ่าน" ผิด** |
| เจียนพู่ | **หัวใจหลัก ย้ายมาทั้งก้อน ⛔ ไม่เขียนใหม่** |
| กระดาน | **เดียว ทุกคนที่ล็อกอินเห็นตัวเลขชุดเดียวกัน · บทบาทเปลี่ยนแค่ปุ่มที่กดได้** |
| ขั้นอนุมัติ | **ไม่มีขั้นให้คนกดเพิ่ม** แต่ **เก็บ "ตรวจแล้ว" แยกจาก "เผยแพร่แล้ว"** |
| หลักเหนือทุกข้อ | **ทุกเพลงต้องผ่านตาผู้ตรวจ ไม่มีข้อยกเว้น** |
| staging / production | `/v3/` บน GitHub Pages ไม่มีลิงก์จากหน้าแรก → `/` เมื่อ stable · ⛔ ไม่ force push ประวัติ |

## 🔧 สภาพโค้ดจริง (นับแล้ว ไม่ต้องนับซ้ำ)
`package.json` = `vue ^3.4` + `vue-router ^4.4` + **`axe-core ^4.12.1` (มีอยู่แล้ว)** · ⛔ ไม่มี Tailwind/Preline/vite-plugin-pwa/playwright
**CSS เขียนมือ 6,319 บรรทัด / 31 จุด** (นับ 29 ก.ค. จาก `git ls-files src`) · ไฟล์ทดสอบ 141 ไฟล์
**PWA มีแล้ว ทำเอง:** `public/sw.js` + `public/site.webmanifest` · ลงทะเบียน `src/main.js:36-39`
**`.github/workflows/` มีแค่ `deploy.yml` + `keepalive.yml`** ⇒ **ยังไม่มีประตูตรวจคุณภาพใน CI/CD เลย**
**`vite.config.js` รองรับ 2 แอปบนโดเมนเดียวแล้ว** — `assetManifest()` :40 · `v2Manifest()` :58 ⇒ `/v3/` เดินตามแพตเทิร์นนี้ได้
🔴 **สาย v1 กับ `studio-shell-redesign` ห่างกันมาก** — `EditorMode.vue` ต่าง +5437/−5761 · `Studio.vue` +877/−161 ⇒ ⛔ **ห้าม merge ข้ามสาย** ให้ **ยกเจตนาไปทำใหม่**

## 🔴 กฎถาวร
- ⛔ merge = PM · deploy/SQL = P'Aim สั่ง go · SQL จบด้วย `ROLLBACK;` · **รัน SQL prod ก่อน merge client**
- ⛔ ไม่แตะพอร์ต 9222 (ของ P'Aim) / 9335 (ai-bridge) / 5393 · ⛔ **ห้าม `taskkill /IM chrome.exe`** ปิดเฉพาะของตัวเองด้วย PID · ไม่กั้น UI ด้วย `@media(hover)`
- **PM = จ่ายงาน + อ่านสรุป + gate** · เปิดไฟล์เองได้เพื่อตอบ P'Aim · ⛔ ไม่ code เอง ⛔ ไม่ run Agent tool
- 🔴 **ผู้ใช้จริง 1 คนสับสน > ที่ปรึกษา 3 เสาเห็นตรงกัน** · ความเห็นที่ถอยเพราะถูกดัน ≠ ผิด
- 🔴 **ห้าม relay สิ่งที่ยังไม่เปิดดูเอง** · **ที่ปรึกษาอ้างที่มาปลอม 6 จุดในวันเดียว (29 ก.ค.)** ⇒ ชี้หน้าไหน ต้องเปิดหน้านั้นตรวจ · ไม่ตรง = ตัดข้อนั้นทิ้ง
- 🔴 **คีย์ anon ถูก RLS ซ่อนแถวที่ยังไม่เผยแพร่** (`db/005`) ⇒ ห้ามสรุป "ไม่มี" จาก anon ให้พูดว่า "มองไม่เห็น"
- **⭐ คำสั่งที่จะวางลงใบสั่งงาน ต้องรันเองก่อนจริง ๆ** ⛔ ห้ามพิมพ์จากความจำ (29 ก.ค. PM วาง `grep` ผิดให้ worker) · **ตัดขาดทุน: พลาด 2 ครั้ง = หยุด เขียน inbox**
- **⭐ สถานะ session เปลี่ยนทุกนาที** — ก่อนอ้างว่าใครตายใครรัน ต้องเช็ก `list_sessions` ใหม่ (29 ก.ค. PM บอก P'Aim ว่า worker ตายเงียบ แล้วมันตื่นมาส่งงาน)

## 📌 SSOT pointers
- **ค่าคงที่ทุก session:** `C:\gl\krisada\ceo\ai-desk.md` (push แล้ว `aa8efed`) — **รหัสโน้ตบุ๊กชุดใหม่:** `Pleng UX 1` `f814bbeb-845e-429d-a6b4-d24703287409` · `Pleng UX 2` `c5cff0ef-6407-443b-a74c-ef0560912603` · `Pleng Music 1` `043a0bef-3e86-4ac7-9fbb-304212fb3927` · `Pleng Music 2` `d0ba75dd-b22b-4bcd-8d1f-5ec408fc11f6` ⛔ **ชุด `Pleng resource 1-5` ถูกลบแล้ว รหัสเก่าใช้ไม่ได้**
- **แหล่งอ้างอิงในเครื่อง:** `OneDrive\4 Personal\claude\references\pleng-v3-ux\` (13 ไฟล์ `plengux-` + `INDEX.md`) · `OneDrive\4 Personal\pleng.phrakham.life\_resource\for_n\` (โน้ตดนตรี + `_IMPORT-MANIFEST.md`)
- **PM brain:** ไฟล์นี้ + `docs/pm/decisions-log.md` · **worker report:** `C:\gl\pm-inbox\pleng\`
- **คลังงาน:** `docs/backlog.md` — **B132 = ก้อนใหญ่ของ v3 สำหรับพี่เปา** (สถานะเพลง + ทำงานเป็นทีม)
- **อ่าน 3 memory ก่อนทำอะไร:** `feedback_follow_exactly_and_guard` · `feedback_definition_of_complete` · `feedback_dispatch_hand_locked_ssot`
