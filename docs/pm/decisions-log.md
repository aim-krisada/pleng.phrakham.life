# Decisions log — เพลง.พระคำ.ชีวิต (มติที่ยัง relevant · ใหม่สุดล่างสุด)

> มติ + เหตุผลทีละบรรทัด · แทน transcript · resolved/เก่า → `decisions-archive.md` · rehydrate อ่านไฟล์นี้ + `pm-state.md` พอ

## Milestones (สถานะสำคัญปัจจุบัน)
- 2026-07-23 (เย็น-ดึก) · **16 งานเข้า base · เทสต์ 804→1085 · ไม่ deploy อะไรเลย** — ป๊อปอัปมุดใต้เมนู(z-index token ladder) · คอร์ด port + พิมพ์คอร์ดเองใน inline · G1 order-free + ป้ายเตือนในตัวแก้ · เส้นเขบ็ตต่อกัน + B110/B114 · B108 หมวดหาย(โค้ด) · มือถือ viewport + `--dock-h` · **inline บันทึกได้ (working copy + เตือนก่อนออก)** · พิมพ์สัญลักษณ์ 12 ตัว · **ปุ่มสัญลักษณ์สอนคีย์ลัด (เรียนจาก keydown ไม่ hardcode)** · **undo/redo กลับมา** · fermata port จาก main · **จังหวะ: meter.js + ชดเชยห้องยก** · แถบบนยุบตามที่ว่างจริง(B123) · เอกสาร audit
- 2026-07-23 · **db/011 รันบน production แล้ว** (P'Aim กด) — ปิดรู `approve_and_publish` ล้าง theme/title_en/**number** ทั้ง insert+update · ยืนยัน **diff ต่อ id = ไม่มีแถวไหนถูกแตะ** · ⚠️ `db/006` (author_id) **ไม่เคยถูก deploy** = งานแยก รอ P'Aim เคาะ

## Milestones เพิ่ม (23-24 ก.ค.)
- 2026-07-23 (ดึก) · **สเปก `docs/ds/repeat-flow-override.md` ปิดสมบูรณ์ @`e73da9d`+§6-R6 @`544c80b`** (branch `claude/nostalgic-perlman-7b5f10`) — โครงเก็บ "การวนร้องรายข้อ" ครอบ 8 รูปแบบ ไม่มีข้อไหนทำไม่ได้ · **id ถาวร (ตรง MEI `<expansion @plist>`) · `time-only` นับ pass ต่อ stanza · fallback through-composed เคส 2 มิติ** · R1-R6 + broken reference (§2.1.1) · **R1 = ตัวเดียวแตะข้อมูลเดิม** (id งอกตอนเปิด+เซฟเอง ⛔ ห้าม bulk) · จ่าย dev แล้ว (เอนจิน R1-R5)
- 2026-07-24 · **เข้า base เพิ่ม 2 ชิ้น: `f8d9a47` แป้นสัญลักษณ์ตัดบรรทัด (ครบ 21 ปุ่ม ≥320px) · `c037c47` sparkle referee ฟังเสียง (โน้ตผีเพลง 33 หาย · 294→0 จาก stress 35,161 เคส)** · `e35b82a` notationLint เป็น vitest จริง (1109→1176 เคส · exit=1→0) · **base = `c037c47` · เทสต์ 1184 · ⛔ ยังไม่ deploy**
- 2026-07-23 · **บั๊กเพลง 33 = sparkle เลือกโน้ตจากคอร์ด ชนโน้ตทำนองที่กำลังจะร้อง** (คนละตัวกับ melody-เกิน 7.3%) · แก้ = referee ดูเสียงด้วย · 🔴 **หนี้: คืนคอร์ด 33 E7→E หลัง deploy** (`tools/restore-song33-chord-E.sql` มี guard+rollback)
- 2026-07-23 · **จังหวะรอยต่อท่อน = 3 บั๊กแยก** (ห้องท้ายไม่ครบ=ข้อมูล+โค้ด · rubato ~0.46วิ=รสนิยม รอหูพี่เปา · +0.25=ข้อมูลในใบแจ้ง) · patch จังหวะ base **deploy ก็ไม่หาย** (คิดค่าชดเชยค่าเดียวทั้งเพลง)

- 2026-07-24 · **เอนจินการวนร้องรายข้อ (R1-R5) เข้า base `b620c87`** — Tester PASS: repro จุดตัด 20/20 (marker id + holds + item แปลก บรรทัดเดียวรอดพร้อมกัน) · เพลง 105 = 10/5/5 attack ตรงค่าที่คำนวณจากโมเดล ไม่ก๊อปทำนอง · visual 1280/360/412 ≥24px ไม่มี `@media(hover)` · test:all **1246** · เหลืองานผิว 3 จุด (จ่ายแล้ว)

- **2026-07-24 (pm46) · 🚀 BATCH แก้ editor 7 ตัว LIVE บน /v2 (deploy สำเร็จ+verified live · P'Aim สั่ง "assemble+redeploy เลย"):** integration branch `integration/editor-fix-batch @717fb7a` (off `7afb038`) รวม BI-004/dc-ds/symbol/BI-005/BI-006+007/new-song/BI-010 · 3 conflict trivial UNION · **test:all 1577 pass/0 fail** · build root+/v2 ✓ · **live verified:** `/v2/` bundle `index-Dmx24X04.js` มี `717fb7a` · `/` bundle `index-D2HsqkzO.js` มี `393fe9e` (tree=3286a1f เป๊ะ v1 ปลอดภัย) · ทั้งคู่ 200 · 0 SQL
  - 🔑 **assembler จับ error บรีฟ PM:** `132a041` ≠ deploy.yml เดี่ยว = โครง /v2 ทั้ง 11 ไฟล์ (VersionSwitch.vue switcher · vite base=/v2/ · scope-aware sw.js · SW reg) → cherry-pick ทั้ง 132a041 + reconcile deploy.yml/sw.js byte-identical main = integration เป็น **superset จริง** ของ /v2 live + batch (ถ้าทำตามบรีฟ PM = /v2 พัง) · **บทเรียน: PM ยืนยัน mechanism จาก git จริง ไม่เชื่อ pm-state summary เก่า**
  - 🔧 **deploy mechanism จริง (durable · pm-state landmine เก่าเข้าใจผิด):** `origin/studio-shell-redesign` = 132a041 (=/v2 live · ไม่ใช่ c90c18f) · deploy.yml (identical main+ssr) trigger `push:[main,ssr]`+dispatch **build ทั้ง 2 เสมอ:** v1 checkout `main` (root) + v2 checkout `studio-shell-redesign` (/v2/) · **env-protection: เฉพาะ main deploy ได้ · ssr-run FAIL เสมอ (1s env reject)** → **redeploy /v2 = trigger บน main** (มัน checkout ssr สดมา build /v2) · PAT `GITHUB_TOKEN_PHRAKHAM` = contents-only (ไม่มี actions:write → workflow_dispatch/rerun 403) → **trigger = empty commit บน main** (`git commit-tree origin/main^{tree} -p origin/main` = tree เดิม v1 byte-identical) · land = push integration→origin/ssr (`--force-with-lease`) + empty-commit→main · rollback = force-push 132a041→ssr
  - **local -pm ssr (PM docs ea373d7) diverged จาก origin/ssr (717fb7a=app)** — ⛔ **ห้าม push -pm ssr ไป origin** (จะทับ deploy) · PM docs commit local อย่างเดียวพอ · reconcile ทีหลัง (housekeeping ไม่ด่วน)

- **2026-07-24 (pm46) · 🎯 เฟสใหม่ = POLISH/OPTIMIZE ทุกงาน (P'Aim สั่ง · durable):** batch bug ใหญ่ ship แล้ว → จากนี้ **ทุกงาน = polish + optimize ระดับ world-class สุดๆ** · **G consult บังคับทุกงาน** (ย้ำ feedback_sa_must_consult_g เดิม) ผ่านกลไก **ai-bridge Chrome :9335 (ตัวใหม่ · P'Aim อยากให้ลอง · ไม่ใช่ chromium/meeting-room)** `py -3.14 C:\gl\.aibridge\bridge.py ask G <slug>` · เซฟ transcript ใส่ EVIDENCE · เริ่ม 2 features:
  - **BI-011** ลบเส้นโค้งด้านบนโน้ต (tie/slur arc) — ทำให้ลบได้ + discoverable (session `567d43e1` running · เพิ่มเงื่อนไข G แล้ว)
  - **BI-012** ใส่คอร์ด **inline เหนือโน้ตตรงตำแหน่ง** (แทน popup ปุ่มล่าง) + **คีย์บอร์ดต่อเนื่อง** (พิมพ์คอร์ด→advance โน้ตถัดไป · ทำทั้งเพลงด้วยคีย์บอร์ด) · design+G→STOP gate→build · pattern อ้าง MuseScore chord-mode/iReal · popup อาจกลายเป็น mobile fallback (สอดคล้องมติ octave-keyboard: คีย์บอร์ด=power desktop · ปุ่ม=มือถือ)

- **2026-07-24 (pm46) · G-consult = บังคับทุกการเปลี่ยนที่เกี่ยว design (P'Aim ย้ำ · durable · กว้างกว่าเดิม):** ไม่ใช่แค่งาน SA design — **ทุก fix ที่แตะ UX/UI/behavior ต้องปรึกษา G มีเหตุผลฟันธงว่า world-class ควรเป็นยังไง** ก่อนเคาะ · ผ่าน ai-bridge Chrome :9335 · เซฟ transcript ใส่ EVIDENCE · = ต่อยอด memory feedback_sa_must_consult_g_every_design (ขยาย scope)
- **2026-07-24 (pm46) · ⚠️ infra risk (BI-011 session เจอ · must-fix ก่อน redeploy รอบหน้า):** lockfile drift — `smplr` + `qrcode-generator` อยู่ package.json แต่อาจไม่อยู่ package-lock → `npm ci` (ใน deploy.yml build /v2) พังได้ → **assemble batch polish รอบหน้าต้องรัน `npm install` sync lock + commit ก่อน · gate ที่ test:all/npm ci เขียว** (deploy 717fb7a ผ่านมาได้ = lock บน 717fb7a น่าจะ OK · drift อาจอยู่บน branch polish) · continue-on-error /v2 = ถ้าพังแค่ /v2 ไม่ update, / v1 ปลอดภัย

- **2026-07-24 (pm46) · 🔴 บทเรียนซ้ำ: PM relay claim ที่พิสูจน์ไม่ครบ → P'Aim จับได้** — BI-011 พิสูจน์ toggle-off เฉพาะ tie `~` (data 5~→5) แต่**เคลม slur `( )` ลบได้ด้วยโดยไม่ verify** · PM relay ทั้งก้อนให้ P'Aim → P'Aim ลองจริง (เพลง 141 · กด ( ที่ 6, ) ที่ 1) **ลบไม่ได้** · = ตรง lesson เดิม "ยก NOT PROVEN ทุกครั้ง · ห้าม relay claim ไม่มีหลักฐาน" (worker เขียน "~ tie / ( ) slur" รวมกันเหมือน verify ทั้งคู่ · จริงมีหลักฐานแค่ tie) → **PM ต้องอ่าน EVIDENCE ว่าพิสูจน์อะไรจริง ไม่เหมาว่า 'ลบได้' = ครอบทุกสัญลักษณ์** · แก้ = BI-011b (verify slur JSON จริง)

- **2026-07-24 (pm46) · ✅ ยืนยันบทเรียน "prove real drag": BI-004 "drag PASS" = CDP false positive** — BI-015 พิสูจน์ด้วยการลากจริง (Input.dispatchMouse/TouchEvent) ว่า native HTML5 DnD บน 717fb7a **ลากเมาส์จริงไม่ขยับ + touch ไม่ยิง event เลย** ทั้งที่ tester BI-004 เคยรายงาน "drag PASS" (ใช้ dispatchDragEvent synthetic) → **CDP synthetic drag = false positive · ต้องลากด้วย pointer/touch จริงเสมอ** (ตรง memory feedback_heard_bugs_prove_by_ear + agent-browser-not-rendering) · fix = pointerReorder.js pointer-based ครบ 3 ลิสต์

- **2026-07-25 (pm46) · 🚀 POLISH BATCH (9 fix) LIVE บน /v2 (`00aa719`) — deploy สำเร็จ+verified live · PM assemble เอง (chip ไม่ขึ้นให้ P'Aim กด):** รวม 9 สาขา off 717fb7a ใน worktree pleng-polish · merge clean 8 · conflict แค่ **BI-018/StructureDrawer.vue** (union: bi015 `ref="outlineEl"` + bi018 header/breadcrumb — disjoint จริง) · **2 integration test ตก แก้แล้ว:** (1) `styles.layering` z-index — bi015 drop-indicator ใส่ `z-index:2` ดิบ → `var(--z-raised,5)` (2) `songEdit.toggle` slur — BI-011 เปลี่ยน `( )`/`{ }` เป็น **ลบทั้งคู่จากโน้ตไหนก็ได้** (bracketSpanAt · "no dangling half-bracket") → เทสเก่า (ลบทีละครึ่ง) ล้าสมัย อัปเดตตาม contract ใหม่ · gate: test 1620/0-fail · build root+/v2 ✓ · lockfile in-sync (npm ci ผ่าน · smplr/qrcode ครบ) · **live: /v2=00aa719 · /(v1)=16906d8 tree เดิม byte-identical** · land = force ssr(guard 717fb7a)+empty-commit main · 0 SQL
  - 9 fix ที่ขึ้น: BI-011(ลบ slur any-note+chip) · BI-012(chord inline+keyboard+discoverability) · BI-013(ลบบรรทัด+undo) · BI-014(คลิกว่างพิมพ์ต่อ) · BI-015(drag pointer-based ทั้ง 3 ลิสต์+touch) · BI-016(เมนูปิด click-outside+composable) · BI-017(ปุ่มสร้างเพลง→inline 5 ทางเข้า) · BI-018(ทำนอง A/B master-detail) · issue9(lead-sheet header)

- **2026-07-25 (pm46) · 🔴 แก้บันทึกผิด: "ใส่ D.C./D.S. เองได้ (item1 dc/ds Tester PASS)" = ไม่จริง (verify โค้ด live 00aa719):** ของจริงบน /v2 —
  - ✅ **ใส่เองได้ (ในตัวแก้แบบเต็ม EditorMode ไม่ใช่ดินสอ ✏️):** ย้อนซ้ำ `‖: :‖` (ปุ่ม repeat บรรทัด/เมนู ⋯ ห้อง) · จบรอบ 1./2. volta (ปุ่ม volta/⋯ห้อง "ห้องจบ") · ร้องรับทุกข้อ (ติ๊กหัวท่อน + แผงวนซ้ำรายข้อ)
  - ❌ **ใส่จริงไม่ได้:** **D.C./D.S./Segno/Coda/Fine = พิมพ์ได้แค่ free-text `label` (cosmetic · โชว์บนแผ่นแต่ playback ไม่ย้อน)** — ไม่มีปุ่ม/เมนู/palette เขียน `{type:'jump'}` จริง · engine resolver อยู่แค่ spike branch `dc-ds-jumps` (ไม่ merge) · ย้อนมิดบาร์ + Ctrl+K palette = ไม่ได้ทำ
  - **ต้นเหตุจดผิด:** item1 dc/ds "Tester PASS 4/4" เทส **glyph RENDER** (มีจริง · SongSheet classifyJump) ไม่ได้เทส **ENTRY** → ตรงเป๊ะกับบทเรียน pm45 "render บังเงา entry · D.C./D.S. entry หลุด release" · spec ที่ pm-state อ้าง `marker-entry-ui.md` = **ไม่มีจริง** (ของจริง = dc-ds-jump-flow.md/repeat-jumps-midbar.md/command-palette.md) · = ช่องโหว่ requirement ที่ P'Aim เน้น "ใส่ D.C./D.S. เอง" ยัง**ไม่เสร็จจริง** → follow-up build (engine + entry UI) ถ้า P'Aim เอา

- **2026-07-25 (pm46→47) · 🎯 P'Aim เคาะ DEPLOY CONSOLIDATION (แก้ ssr-แดง ถูกทางเทคนิค · ไม่สลับ topology):** ปัญหา = deploy.yml trigger `[main, studio-shell-redesign]` แต่ github-pages env-protection ให้เฉพาะ main deploy → push ssr แดงทุกรอบ (3s) · deploy จริงมาจาก empty-commit บน main · **P'Aim ถาม "ทำไมไม่รวม main เดียว v2 ผ่าน /v2" → เคาะแนวนี้:** main=โค้ด v2 · v1 แช่แข็ง tag `v1-frozen` · deploy.yml trigger `[main]` build v1(tag)→root + v2(main PLENG_V2=1)→/v2 · **ไม่มี ssr/แดง/empty-commit · ผู้ใช้ไม่กระทบ (root ยัง v1, /v2 ยัง v2) · ปลอดภัยเท่าเดิม (v1 tag แตะไม่ได้)** · **dry-run ใน throwaway ก่อน (task_3a0e519c) · ห้ามแตะ prod จน P'Aim เห็นหลักฐาน+go** · pm47 execute land หลัง gate
- **2026-07-25 (pm47) · ✅ DEPLOY CONSOLIDATION LANDED + VERIFIED LIVE (P'Aim สั่ง "go deploy consolidation"):** ผลจริง — `origin/main` `16906d8..9c6730c` FF (ไม่ force) · new main = commit-tree(v2 tree cb474ee, parents=[16906d8 v1, 00aa719 ssr]) + commit deploy.yml `[main]`-only · tag `v1-frozen`→16906d8 · **verify ชั้นจริง (curl served-bytes + Actions API):** Actions **run เดียว เขียว** (30139760308 success · **ไม่มี ssr run โผล่ = ssr-แดงหายจริง**) · **root/ = v1** (มี 16906d8 · ไม่มี "รุ่นทดลอง" · 200) · **/v2/ = v2** (มี 9c6730c · มี "รุ่นทดลอง" · 200) · ไม่ปนกัน · **origin/ssr=00aa719 ไม่ถูกแตะ** (rollback `force-with-lease origin v1-frozen:main` พร้อม)
  - 🔑 **land ต่างจากคำสั่งใน inbox (บทเรียน):** `main` ถูก checkout อยู่ใน worktree `pleng-natural-tie` → `git branch -f main`/`checkout main` จะ fail → ปรับเป็น **pure plumbing**: `git commit-tree` + temp-index swap deploy.yml (ไม่แตะ working dir) แล้ว `git push origin <sha>:main` (ไม่ต้อง checkout main เลย) · = ตรวจ worktree ก่อนเสมอ ไม่เชื่อคำสั่งสำเร็จรูป
  - **ยังไม่พิสูจน์:** P'Aim eyeball เอง (ด่าน #5) · UI คลิกจริงใน browser (curl=served-artifact layer ไม่ใช่ interaction) · PWA SW cache transition · **deploy mechanism เดิม (empty-commit hack บน main + ssr dual-trigger) = SUPERSEDED ทั้งหมด** → จากนี้ redeploy /v2 = commit จริงบน main ธรรมดา (main=โค้ด v2 แล้ว)
- **2026-07-25 · 🔴🔴 P'Aim โมโหมาก (พลาดครั้งที่ 4 เรื่องเดียว) — บันทึกกฎถาวร `feedback_follow_exactly_and_guard` + CLAUDE.md §4 (⭐⭐⭐ เหนือทุกข้อ):** repeat/ย้อน (D.C./D.S.) pm45+46 relay "ใส่เองได้ Tester PASS" ทั้งที่ Tester เทสแค่ **render glyph** ไม่ใช่ **entry/ย้อนจริง** · + เห็น ssr แดงทุกรอบไม่เตือน · **หน้าที่ Claude 2 อย่างเท่ากัน: (1) ทำตามคำสั่งเป๊ะ เกทที่ผลจริงไม่ใช่ proxy · ยึดสเปกจริงของ P'Aim ไม่ซอยเอง · ไม่ relay done จนพิสูจน์ชั้นจริง (2) เตือนเมื่อเห็นผิด/เสี่ยง ไม่ทำตามเงียบ · ขาดข้อใด=ไร้ประโยชน์** · ใช้ทุกเรื่องทุกงานทุกโปรเจกต์
- **2026-07-25 (pm47) · 🔴🔴 P'Aim credibility ตกขีดสุด → กฎถาวรทุกงาน "Definition of Complete" (memory `feedback_definition_of_complete` + hook):** พลาดซ้ำ = **นิยาม "ครบ" ด้วยการเดารายการ (4 ข้อ) แล้วถาม P'Aim "แค่นี้พอไหม / Vamp เอาไหม"** ทั้งที่ intent ชัด → โยนการตัดสินความครบกลับ user = มือสมัครเล่น · **หลักการเดียว: "ครบ" ต้องสืบจากแหล่งจริง exhaustive + G/N ตรวจเชิงรุก "ขาดอะไร" · ห้าม AI ตัวเดียวเดา ห้ามโยนถาม user** · **G-VERIFY ≠ "G อนุมัติ"** = (1) ใช้ G เชิงรุก (2) ตรวจซ้ำทุก citation กับแหล่งจริง URL+element (3) seat/PM ตัดสินเอง — G ยกพื้น ไม่แทนวิจารณญาณ (G เคย hallucinate ถอนคำ 2 ครั้ง) · **hook ติดตั้งแล้ว** (`preflight_gate.py` GATE_CLAIM): เคลม design/build ครบ/ถูก ต้องมี evidence + `G-VERIFY: <path>` · infra/deploy = evidence พอ ไม่ต้อง G · unit-tested 7 เคสเด้งถูกจุด · **= วิธีทำงานทุกงานทุกโปรเจกต์ ไม่ใช่ปะผุ** · ควรเข้า CLAUDE.md §4.5
- **2026-07-25 (pm47) · 🎯 ทิศ SONG-MAKER FIRST (P'Aim reset "v2=ปะผุ · ยกเครื่องโดยไม่ทำภาพรวม"):** เลิกไล่แก้ BI-xxx ทีละอัน → ทำใหม่ทีละก้าว **(1) user story+journey คนทำเพลง → (2) จัดกลุ่ม function/IA → (3) mockup → (4) build)** · scope = **reuse ของเดิม re-layout · ⛔ ไม่ rebuild ไม่แตะ engine · แต่ครบทุก function** · north-star = ง่ายคนไม่รู้ดนตรี + ขั้นสูงคนรู้ดนตรี + world-class ดนตรี+ซอฟต์แวร์ · import/verify = flow first-class (พี่เปาใช้จริง) · **ก้าว 1 ✅ (`docs/us/songmaker.md`) + ก้าว 2 ✅ gated+G-verified (`docs/ds/songmaker-ia.md` · 62 fn→10 surface)** · รอ P'Aim เคาะ 4 §9 + ลำดับ build ก่อนก้าว 3 · งานย่อยเดิม (ย้อน D.C./D.S., /v2 nav, SB1/SB2, BI-xxx) ยุบเข้าใต้ journey · Material=ระบบหลัก · HIG=หลักการ cross-check · N corpus `_resource/for_n/` (18 md standards) พร้อม import
- **2026-07-25 (pm47) · ✅ deploy consolidation LANDED+LIVE + G-VERIFY hook v1.0.0 user-level** (ดูรายละเอียด 2 entry ล่าง/บน) · pm47 เก็บ PM ส่งต่อ pm48
- **2026-07-25 · G/N consult = ai-bridge `ceo/tools/aibridge` (กฎกลางใหม่ · แทน meeting-room :9222):** `cd C:\gl\krisada\ceo\tools\aibridge && py -3.14 bridge.py ask G <slug> "q"` · runtime/login `C:\gl\.aibridge` :9335 · CLAUDE.md §4.5 ข้อ 10 + §4 · memory `pleng-gn-consult-bridge` · **+ PM ห้าม run Agent/Explore tool เอง — จ่าย session เสมอ (ยกเลิก carve-out เดิม · §4.5 r2/r7)**

- **2026-07-25 (pm48) · §9 IA ปิด + build order เคาะ:** PM ตัดสิน 3 ข้อมาตรฐานเอง (ไม่โยนถาม P'Aim ตาม feedback_never_ask_user_what_is_correct): (1) D.C./Segno **split** — จุดปัก Segno/Coda = S5 inline (anchor ตำแหน่งเดียว) · คำสั่งกระโดด D.C./D.S./Fine = S7 drawer (อ้างข้ามท่อน) · Material side-sheet ไม่เหมาะปักจุด (2) โหมดแก้ **ตัด transport เต็ม** เหลือ scoped listen (ห้อง/ท่อน/caret) + หยุดได้เสมอ + ฟังทั้งเพลงลง ⚙ + คง Space=play/stop (แก้ BI-002 ที่ราก) (3) import = **หน้า/โหมดแยก S10** (HIG modes-for-distinct-tasks) · **G รอบ 2 = ไม่ต้อง** (adversarial 1 รอบจับ gap ครบ · PM reject 2 ข้อ G อ้างลอย/ชนมติแล้ว) · **P'Aim เคาะ build order (ก้าว 4) = "แก้ตรงจุด/พิมพ์ (S5) ก่อน"** (v1-parity หัวใจ "v1 ดีกว่า") → ตามด้วยโครง/ย้อน (S7) → import (S10)
- **2026-07-25 (pm48) · 🔴 กฎกระบวนการถาวร (P'Aim สั่ง · memory `feedback_3ai_each_step_then_paim_ui`): ก่อนปรับ code — (1) ทุกก้าวออกแบบผ่าน 3AI กลั่นกรอง (C ร่าง → N ตรวจเทียบมาตรฐาน → G หาช่องโหว่) ครบก่อนขยับ (2) P'Aim ดู UI (mockup) ก่อน dev.** เกทแต่ละก้าว (journey→IA→mockup→build) บนหลักฐาน 3AI จริง (transcript) + P'Aim เคาะ UI ชัด · กันปะผุ · ก้าว 1+2 ปัจจุบัน: C✅ G✅ N=กำลังตรวจ → ครบ 3AI พอ N กลับ → P'Aim ดู → ก้าว 3
- **2026-07-25 (pm48) · 🆕 ล็อกทำนอง (พี่เปาเสนอ · P'Aim เคาะ "เติมเข้า song-maker ตอนนี้"):** พี่เปา (user จริง v1): "ทำนองนิ่งแล้วน่าจะ lock ได้ · แก้เนื้อร้อง 2 บางทีทำนองเปลี่ยนตาม ไม่ควรเป็น" → 2 เรื่อง: (1) แก้เนื้อ→ทำนองเปลี่ยน = ถ้าเกิดใน v2 (ที่แยก stanza/verse แล้ว) = บั๊ก silent-melody-corruption (เกทเสมอ) หรือ shared-stanza M3.3 by-design · **จ่าย session ตรวจของจริง v2 + root cause ก่อน (task_224df994) · ยังไม่แก้โค้ด** (2) ฟีเจอร์ "ล็อกทำนอง" per-stanza = design+G(3AI)+fold US/IA (S7 toggle/S9 indicator/S5 enforce · แก้เนื้อได้ แก้โน้ตไม่ได้จนปลด) · ตรงหลัก melody=SSOT 100% · **ping mockup session เผื่อที่แล้ว** · ถ้าเป็นบั๊ก = จ่าย fix แยก · = ตัวอย่าง "ทุกงานย่อยยุบเข้า journey" (ไม่ทำ BI เดี่ยว)
- **2026-07-25 (pm48) · 🔵 P'Aim สั่ง: N ตรวจก้าว 1+2 ก่อนก้าว 3 (mockup) — "ไล่ทีละก้าว · N เพิ่งเข้ามา เผื่อเจอจุดหลุด จะได้เติมเลย":** N มี corpus มาตรฐานครบแล้ว (18 md · Material/MusicXML/SMuFL/MEI + HIG URLs · จาก resource-convert 25 ก.ค.) → ใช้ N adversarial ตรวจ `songmaker.md` (journey) + `songmaker-ia.md` (IA) เทียบมาตรฐาน หา function/notation ที่ตกหล่น + IA grouping ที่ผิดหลัก · **N notebooks = Pleng source 1/2/3** (P'Aim ให้ id: 7b8ac29d…/16421df5…/527cd15f…) · จ่าย session (ai-bridge · ห้าม PM run เอง) · ⚠️ N เคย approve ใต้แรงกด (memory feedback_symbol_reviewer_pressure) → session ถาม "ขาดอะไร" ไม่ใช่ "ดีไหม" + ตรวจ citation กับ source จริง · gap ที่ N ยกมา = session ตัดสินเองว่าจริงไหน (N ยกพื้น ไม่ตัดสินแทน · ตาม feedback_definition_of_complete)

## Milestones เพิ่ม (25 ก.ค. pm48)
- **2026-07-25 (pm48) · ✅ search phrase-first ranking = LIVE + VERIFIED บน root(v1) + /v2 (P'Aim สั่ง go · fix แรก pm48 ที่ขึ้น 2 รุ่น):** สาเหตุ = ผลค้นไม่จัดอันดับ (เพลงตรงจมล่างใต้ fuzzy) · แก้ไฟล์เดียว `songSearch.js` (filterSongs คืน sort-by-score + title-boost) · **v1/v2 blob เดียวเป๊ะ → แก้ครั้งเดียว deploy 2 ที่** · main @f187950 (FF จาก 9c6730c) · **tag v1-frozen → 9160a10** (16906d8 + same fix · 2 ไฟล์ · byte-identical blob b1726a3 ทั้ง 2 ฝั่ง) · Actions run 30162821385 success (รันเดียว · ไม่มี ssr ซ้ำ · ไม่มีแดง) · gate: /v2 test:all **1626 pass** + build · v1 build ✓ · **verify ชั้นจริง:** served bundle ทั้ง root+/v2 มี ranking signature · live "ยามพระ" → "ยามพระเจ้าอยู่ร่วมกับเรา" #1 ทั้ง 2 รุ่น (แซง 42 lyric-only) · topology intact (root ไม่มี "รุ่นทดลอง") · **rollback (tag-first 2 push) พร้อม** · NOT PROVEN: P'Aim eyeball + physical typing (session ยิง input event บนโค้ด+Supabase จริง · ไม่มี PNG เพราะ agent browser composite ไม่ได้) · **= พิสูจน์ mechanism v1+v2 co-deploy จริง** (ขยับ tag v1-frozen + FF main รอบเดียว)

## บทเรียนวันนี้ (durable · ใช้ต่อ)
- **2026-07-25 (pm48) · PM gate ต้องเช็ก "design-language fidelity กับดีไซน์ที่ล็อก" ไม่ใช่แค่ completeness+renders** — mockup ก้าว 3 rev1 ครบ 65 fn + ภาพสวย + G ผ่าน แต่ **ถอยเป็น v1** (แท็บโหมด + top-nav แนวนอน + ไม่ต่อยอด SongViewer.vue ตัวแก้ v2 จริง) · PM เกทผ่านเพราะเช็กแค่ "ครบ+ภาพมี" → **P'Aim จับได้ว่า "อ้างอิง v1"** · บทเรียน: งาน re-layout/mockup ต้องเกท **ว่าตรงโมเดล/ภาษาดีไซน์ที่ล็อกไว้ไหม** (v2 = แผ่นเดียว/ดินสอ/ไม่มีแท็บ/นำทางใน ☰ · ux-groundup-design.md LOCKED) + **ใบสั่งต้องชี้ component พื้นผิวจริงให้ชัด** (SongViewer.vue) ไม่ใช่แค่ "reuse components" (worker เลยสร้าง host ใหม่ + หยิบ layout v1) · ตรง memory `feedback_ground_up_integrate_locked_design` + `feedback_follow_exactly_and_guard`

- **Tester verify บน Chromium จริงช่วยกัน false alarm 2 เรื่องในงานเดียว** (24 ก.ค.) — browser pane ทำให้ดูเหมือนกล่องเตือนถูก dock ทับ + เหมือนปุ่มลบกิน flow ข้ออื่น · ของจริงถูกต้องทั้งคู่ (scrollIntoView ไม่ทำงาน / CDP serialize Vue proxy เป็น `{}`) ⇒ **ห้ามฟันบั๊ก UI/timing จาก browser pane** (ตรงกับ memory `pleng-agent-browser-not-rendering`)
- **`ICONS[name] || ''` = ไอคอนหายเงียบ** — เรียกชื่อที่ไม่มีในชุด ได้ svg ว่าง ไม่มี error · ตระกูลเดียวกับบั๊กเงียบอื่นๆ ที่ทีมไล่ปิดทั้งวัน
- **G hallucinate เรื่องมาตรฐานจริง (P'Aim เตือน · ยืนยันแล้ว 24 ก.ค.)** — G อ้าง "มาตรฐานไม่มีความต่างรายข้อ" ผิด (มี `time-only`/`<volta>`/`<expansion>`) · **ข้ออ้างมาตรฐานทุกข้อต้องมี URL+ชื่อ element ก่อนเขียนลงสเปก · แหล่งเดียวตอบตรงกันหลายรอบ ≠ ยืนยัน · "มาตรฐานไม่มี X" พิสูจน์ไม่ได้ เขียนได้แค่ "ไม่พบ" · การติดป้าย NOT PROVEN ไม่ทำให้ของผิดกลายเป็นไม่ผิด**
- **grep หา `field =` ไม่เจอ UI ที่เขียนค่าผ่าน `v-model` หรือซ่อนหลังเมนู ⋯** — SA เกือบสรุป "volta ใส่ไม่ได้" 2 รอบ (นับ ⚙ 8 ของจริง 5 ด้วย) · ต้องเปิดของจริงกดทุกเมนูก่อนพูด "ไม่มี"
- **เทียบกับโมเดลก่อนรายงานตัวเลข** — SA ได้ 61 เคส เปิดจริง 3 เพลงพบ over-count (จับ verse+refrain ปกติ) · **แยก "0 เพราะไม่มี" กับ "0 เพราะระบบยังใส่ไม่ได้" เสมอ**
- **"รีโปไม่ตรงกับของจริง"** เกิด 3 ครั้งใน 1 วัน (RPC เป็นรุ่นก่อน db/006 · คอลัมน์ song_drafts มีบน live แต่ไม่มีในรีโป · main มีของที่ base ไม่มี 3 ชุด) → **ทุกงานที่แตะ production ต้องอ่านของจริงก่อน ห้ามเชื่อไฟล์** · ทางออกที่ได้ผล = **ใส่ guard ในตัว SQL ให้หยุดเองถ้าไม่ตรง**
- **"ทำครึ่งเดียวแล้วขัดกันเอง" อันตรายกว่าไม่ทำ** — lint รู้กฎ accidental ทั้งห้อง แต่ playback ไม่รู้ · G1 ทำให้ห้องพัง parse ผ่านแต่ยังเล่นผิด → **ต้องปิดครบชั้น (เก็บ/แสดง/เล่น) ไม่งั้นรายงานว่า "ซ่อมแล้ว" ทั้งที่ยังผิด**
- **เกณฑ์ตรวจต้องมีตัวคุมฝั่ง "ต้องไม่เปลี่ยน" เสมอ** — 4/4 ต้องไม่ขยับ(patch จังหวะ) · 71 เพลงเปิดเต็มห้องต้องไม่ขยับ(patch ห้องยก) → จับได้ว่าเผลอทำของดีพังถึง 2 ครั้ง
- **dev จับข้อผิดพลาดเครื่องมือตัวเองแล้วรายงาน 3 ครั้ง** (วัดด้วย songToNotes แทน resolvePlayOrder · จับคู่ด้วยเลขเพลงทั้งที่ 29 เพลงไม่มีเลข · เกณฑ์ฟ้อง tie ปกติ ~90 เพลง) → **ถ้าไม่บอก PM จะเชื่อตัวเลขผิดแล้วตัดสินใจผิด** = พฤติกรรมที่ต้องรักษาไว้
- **PM ต้องยกข้อ "ยังไม่พิสูจน์" ของ dev ขึ้นบอก P'Aim ทุกครั้งที่มันแปลว่าของยังใช้จริงไม่ได้** (พลาดครั้งหนึ่ง: รายงานว่า "พิมพ์สัญลักษณ์ได้" ทั้งที่ยังไม่มีปุ่ม → P'Aim ตีกลับ)
- **ผู้ใช้จริงชี้จุดได้แม่นกว่าการวัดทั้งวัน** — "หน้าทิ่ม"/"คล่อมจังหวะ"/"melody เกิน" แยกออกเป็น 3 บั๊กคนละชั้น · และ "3/4 ก็แปลก" คือสิ่งที่ทำให้เจอบั๊กตัวที่ 2 ที่ทีมมองข้าม

- 2026-07-22 · **home/nav (สาย 2) → base** (ธีมอุ่น+สดใส · ★favorites · i18n th · แชร์/QR/playlist · brand marigold)
- 2026-07-23 · **editor redesign ครบ → base `3a3e618`** (Tester job D PASS: build+804 เทสต์ · slur 3 ชนิดอยู่ร่วมกัน ≤0.06px · ไม่ regress) — inline editor + คำใต้โน้ต + melisma + คอร์ด + slur geometry · **base = แอปใหม่ทั้งชุด (ยังไม่ deploy)**
- 2026-07-23 · **คอร์ด hotfix → LIVE production `5661068`** (พี่เปาพิมพ์คอร์ดมาตรฐานครบ + ENTER เลือกคอร์ด · verify live bundle) · ⚠️ **main(chord) กับ base(editor redesign) diverged → port chord เข้า base + reconcile ก่อน full deploy ครั้งหน้า**

## มติล่าสุด (24 ก.ค. บ่าย)
- **เมนู "เพลง ▾" ในหน้าเพลง → ย้ายเข้า ⋮ เพิ่มเติม (P'Aim เคาะ · ตามดีไซน์ล็อก)** — เอา "สร้างเพลงใหม่" ออก (ซ้ำหน้าแรก) · "เปิดเพลงอื่น" เข้า ⋮ + ต่อเข้าเครื่องค้นจริง `filterSongs` (เดิม substring เฉย ๆ · โฆษณาค้นโน้ต/เลขแต่ไม่ทำงาน) · เหตุ: Explore ยืนยันเมนูซ้ำหน้าแรกทั้งหมด ไม่มี unique · เมนูนี้ v-if mode!=edit อยู่แล้ว = คนละเรื่อง "ซ่อนแท็บตอนแก้"
- **พี่เปา 24 ก.ค.: (1) แทรกโน้ตหน้าตัวอื่นไม่ได้ ("ใส่ 5 ก่อน 2") (2) เสนอสลับ Del↔Backspace** → PM วินิจฉัย = **ต้นตอเดียว: cursor เป็นแบบ "เลือกทับตัวโน้ต" ไม่ใช่ "ระหว่างตัว" แบบ text** → แก้ราก = caret ระหว่างตัว แล้วแทรกหน้า + ทิศ Del(ขวา)/Backspace(ซ้าย) ตามมาตรฐาน text editor ออกมาถูกเอง · ⛔ ไม่สลับปุ่มดื้อ ๆ · จ่าย SA ออกแบบ caret/insert/delete model (G บังคับ)
- **พี่เปา 24 ก.ค.: พิมพ์ octave ด้วยคีย์บอร์ดล้วน** `.5`=ต่ำ · `5'`=สูง · "แบบที่เคยเป็น" → **RESTORE ของเดิม ไม่ออกแบบใหม่** (มี muscle memory) · คีย์บอร์ดล้วน=power path เดสก์ท็อป ไม่แทนปุ่ม (มือถือยังใช้ปุ่ม) · รวมเข้า caret keyboard model เดียวกัน · **SA สรุป: `.`=dead-key prefix · `5'`=คีย์เดิม · ตรง parser notation.js:4-8 · low octave วันนี้ไม่มีคีย์ (ปุ่มอย่างเดียว)**
- **24 ก.ค. · repeat/nav notation DESIGN เสร็จ merge `af5788d`** (docs · G Pro 2 รอบ · corpus 170 เพลง = "discoverability trap" ~0 structured เพราะใส่ไม่ได้ ไม่ใช่ไม่ต้องการ · G surface Vamp) · **P'Aim เคาะ scope:** Vamp = ออกแบบไว้ build ทีหลัง · **v1 = รวม mid-bar ด้วย (P'Aim ปรับทิศ 24 ก.ค.: "ย้อนมิดบาร์ก็มีจริง")** — เดิมจะเลื่อน mid-bar → เอาเข้า v1 · ⚠️ **engine spike เป็น line-level → ต้องต่อยอดรองรับ mid-bar (ไม่ใช่ reuse ตรงๆ)** · design ต้องปิด mid-bar entry UX ให้ครบก่อน build (เดิม doc คลุม mid-bar แบบ "ทีหลัง") · PM รับ al-target `to` field (directive กำหนดเป้า ไม่ใช่ "first marker met") · integration แก้โครง: marker=sheet item(⋮/Ctrl+K) · D.C./D.S.=structure directive
- **24 ก.ค. · caret spec merge `4922418`** — cursor 2 โหมด (Block ทับ default / Insert→Line Caret ระหว่างโน้ต แก้ "ใส่ 5 ก่อน 2") · G ค้าน caret-ตลอด (context-dependent พัง muscle memory) SA รับ · **🔄 P'Aim ย้อนมติ Q1: Delete = แบบ Word ลบแล้วดึงชิด (ไม่ใช่ "เหลือ rest") · rest = พิมพ์ "0"** — เหตุ: พี่เปาอยากได้ text-model + ทั้งชุด caret เป็น text-like อยู่แล้ว = สอดคล้อง · เมนู "เพลง▾"→⋮ + search engine จริง

- **24 ก.ค. · คุย G/N = ใช้ meeting-room tool ตัวเดียวร่วม (P'Aim เคาะ · แก้ friction ล็อกอินซ้ำ)** — `ceo/tools/meeting-room/` (`py meeting_cli.py launch` → Chromium ร่วม :9222 default profile · **ล็อกอินครั้งเดียว ค้างข้ามวัน**) · แต่ละ session สร้างแชท G เอง (ไม่ปน) · N ใช้ notebook แสวงหา 1-5 ไม่สร้างใหม่ · บรีฟยาว >3000 ตัว = อัปโหลดไฟล์ ไม่ paste · **verify/render UI = คงกฎ own-port §4 เดิม** (แยกตามจุดประสงค์) · ⛔ เลิก hand-roll หน้าต่าง Gemini ต่อ session · กฎเต็ม §4.5 ข้อ 10 · Explore ยืนยันเครื่องมือ automate G/N via Playwright/CDP + เคยใช้ pleng มาแล้ว (2026-07-22-pleng-i18n)

- **2026-07-24 · verified-gate ≠ คำถาม ON/OFF (P'Aim แก้กรอบ):** requirement คงที่ = ① คนทำเพลงที่ทำไว้แล้วต้องใช้ได้เหมือนเดิม (team ล็อกอิน เห็น+แก้ทั้งหมดรวม unverified) ② public ไม่เห็นแคตตาล็อกว่าง (120 เพลง live ต้องยังโผล่หลัง /v2) · **PM มีหน้าที่ออกแบบให้ถูกตามมาตรฐานเอง + จ่ายงานแก้ ไม่โยน choice เรื่องความถูกต้องกลับ** (ตรง feedback_never_ask_user_what_is_correct) · P'Aim authorize ให้จ่าย/เตรียม/แก้ตามจำเป็น + บริหารเวลาเอง · gate บนหลักฐาน live (RLS + สถานะ verified จริง + ผลถ้ารัน reset-verified-false)

- **2026-07-24 · 🎯 หลัก SSOT correctness (P'Aim · durable): เสียง≠100% · MusicScore/เมโลดี้ DATA=100%** — timbre/คุณภาพเสียงปล่อยได้ (ship-first-fix-later) · แต่ **ข้อมูล MusicScore เป็น SSOT ต้องถูก 100% อย่างน้อยเมโลดี้** เพราะ เล่น/พิมพ์/export งอกจากมัน → **ข้อมูลเมโลดี้เพี้ยน/หายเงียบ = กลุ่มเดียวที่ห้ามปล่อยแม้ ship-fast** · ผล: (1) holds-drop ยกเป็น gate-level + ขยายเป็น melody-round-trip integrity 170 เพลง (session `5bd5b849`) (2) spawn melody-correctness corpus audit (`task_4bb13e30`) หา/วัด/จัดอันดับเมโลดี้ผิด = worklist สู่ 100% (ไม่บล็อก code-deploy · เพลง live อยู่แล้ว DB เดียว) · ตรง memory feedback-audio-honest-to-sheet + pleng-ssot-scope-musicxml-not-staff
- **2026-07-24 ~19:40 · ✅✅ /v2 LIVE + VERIFIED บน PRODUCTION (deploy สมบูรณ์ · P'Aim เห็นเอง):** deploy-prep post-verify real browser: **`/` = PRISTINE v1** (170 เพลง · footer `3286a1f`=5661068 app byte-identical +2 CI ไฟล์ deploy.yml+sw.js · ไม่มี v2 pill · 0 error · คนใช้ทุกวันปลอดภัย) · **`/v2/` = ใหม่** (footer `132a041` · v2 pill · แท็บหาย · ‹back · ⋮ · 152 SVG render · play · SW scope /v2/ activated · 0 error) · coexistence SW ทำงาน · **0 loss · 0 SQL · FF ไม่ force** · ("failed" studio-shell-redesign run = Pages deploy-lock race ไม่เป็นไร · main run สำเร็จ deploy ทั้ง root+/v2) · **ก่อน 20:00 ตามสัญญาคนทำเพลง** · 🔴 **P'Aim ขอ topology swap ต่อ: v2→root · v1→/v1 · ถอดป้ายทดลอง** → deploy-prep กำลังทำ PLAN (เวลา/เสี่ยง/`/v2/`-redirect/SW-cache transition/rollback · plan-ก่อน-execute · P'Aim เคาะ) · preview :5410 เก็บไว้ (ยังไม่ teardown)
- **2026-07-24 ~20:00 · 🚀 GO LIVE (P'Aim สั่ง "อยากให้ go live เลย"):** push **`132a041`** (= `af15399` tabs-gone/import⋮/share-2 + cherry-pick 554f52a) → FF studio-shell-redesign + deploy.yml+sw.js→main (stage 3286a1f) · deploy-prep execute §6 (PM authorize · P'Aim go) · **/v2 live · / เดิม v1 ไม่กระทบ** · ทุก gate เขียว (test 1430 · builds · serve 200/200 · SW scope · audio P'Aim ได้ยินเอง · B104/B108 base safe · 0 loss · v2≥v1) · **af15399 verified: แท็บหาย + ⋮ มีตัวแก้เต็ม(4 caps)+import+export = 0 loss + `‹` back ถูก + no trap**
- **2026-07-24 · คิว FOLLOW-UP หลัง go-live (P'Aim สั่งทำต่อ · redeploy 2-3 นาที/รอบ · ไม่มีของหาย · เรียงตามที่ P'Aim เห็น):** (1) **SB1 แถบบนสะอาด (ลบ share/⋮ ซ้ำ 2 ชุด → แถบเดียว) + SB2 ▶ hero** (clever-einstein ทำอยู่ = redeploy แรก) (2) **create-new → inline+✏️** (ตอนนี้เปิดตัวแก้เต็ม · escapable via logo ไม่ตัน · ระวัง blank-song) (3) **port DISPLAY เข้า inline: lint(~1-2ชม) · paste-syllable(~1-2ชม) · repeat-markers UI(~ครึ่งวัน มี spec 02cde29+engine daa8c7f) · per-verse** — engine banked (test 1478) รอ hot-file SongViewer ว่าง (4) **header แก้ inline** (design task_08af63d8 · 2 synced surface + ⚙) (5) onboarding hints · ✓เสร็จ label · ลบ DownloadTool.vue ตาย · undo dup-bar (unconfirmed re-verify) · **bug-intake channel** (`bug collectors` session · `docs/pm/bug-intake.md` BI-NNN · P'Aim ส่ง bug ผ่านช่องนี้ · PM จ่ายแก้) · 🔴 **G tooling พัง → ai-bridge (task_efe00113) กำลังประเมินแทน CDP** · 🔴 **topology swap v2→หลัก, v1→/v1 (P'Aim อยากได้ปลายทาง · ต้อง rework SW v1)**
- **2026-07-24 ~19:00 · LIVE DEPLOY DECISION STATE:** ทุก gate เขียว (audio P'Aim ได้ยิน · B104/B108 base ปลอดภัยกว่า v1 · melody · SW · base) · **floor `integration-editor-cp @b4d841d` = MERGE-READY** (de216d0 + import→inline ⋮ + export relabel "ดาวน์โหลด JSON" + share-2 icon · **แท็บ 3 โหมดยังอยู่ = additive 0 loss** · test:all 1429 + build + Tester จอจริงผ่าน) · 🔴 **cut-tabs = รื้อ nav-contract (ผูก ~10 เทสต์ .sb-mode-btn + ต้องออกแบบทางกลับ) → ตัดปลอดภัยไม่ทัน 20:00** · **A/B รอ P'Aim:** A=push floor 20:00+cut-tabs fast-follow / B=เลื่อนรอแท็บหาย · **clever-einstein ให้ firm go/no-go ~19:20** (go=push tabs-gone 20:00 · no-go=push @b4d841d + fast-follow) · **GO push = FF studio-shell-redesign→(tip) + cherry-pick 554f52a (union Icon.vue+vite) + deploy.yml+sw.js→main(stage 3286a1f) + push · P'Aim สั่ง go** · port item1(repeat)+item4(lint) ENGINE banked (test 1478 · UI รอ hot-file หลัง cut-tabs) · header-inline design (2 synced surface `task_08af63d8`)+onboarding = follow-up · 🔴 **G automation พัง (CDP driver/Python3.14) → G consult ทุกสาย DEFER · ซ่อม tooling หลัง deploy** · undo dup-bar = UNCONFIRMED (synthetic click ข้าม model · re-verify หลัง cut-tabs · ไม่ fix)
- **2026-07-24 · ⭐ DEPLOY /v2 canonical (deploy-prep reconciled):** base ไม่แตก — origin `e28c60d` แค่ stale/un-pushed · **local `dac3970` = canonical (นำ 71 commit · origin ขาด 71 · local ขาด 0)** · **ship tip = `de216d0` (integration-editor-cp = dac3970 + CP-A holds/melody + CP-B structure + CP-C flow-polish) + cherry-pick 554f52a → assembled `6394a3d`** (test:all 1430 · both builds ✓ · /,/v2/=200) · ⛔ **ทุกสาย assemble บน de216d0 ไม่ใช่ e28c60d** (สร้างจาก origin = 71 behind) · **GO = FF studio-shell-redesign→de216d0 + cherry-pick 554f52a + deploy.yml+sw.js→main + push** (FF push ไม่ force · stage-main `3286a1f`)
- **2026-07-24 · P'Aim เคาะ topology: "ใช้ v2 ก่อน"** = ขึ้น **/v2 subpath คืนนี้** (proven·SW-safe) แล้ว **สลับ v2 เป็นโดเมนหลัก + v1→/v1 = รอบถัดไป** (ต้อง rework SW ของ v1 ให้ scope-aware ก่อน) · ไม่เอา topology swap มาเสี่ยงเดดไลน์ 20:00
- **2026-07-24 · 🔴 5 ความสามารถยังมีเฉพาะตัวแก้เก่า (parity clever-einstein):** JSON import (คนใช้เยอะ·เล่มใหญ่) · repeat-markers ‖::‖ · per-verse flow · paste-paragraph auto-syllable · lint → **ห้ามตัดตัวแก้เก่าจนกว่า port ครบ** · แผน: cut แท็บโหมด + ย้ายตัวเก่าเข้า ⋮ ("ตัวแก้แบบเต็ม") = chrome สะอาด + 0 loss · **P'Aim สั่ง port ทั้งหมด** → import=hand-2(competent-austin คืนนี้) · repeat/flow/paste/lint=สาย `task_dc1e7bd0` (repeat มี spec 02cde29+engine daa8c7f พร้อม · ทยอยหลัง 20:00) · **audio audible = ต้องหู P'Aim บน foreground :5321** (automation ยืนยันไม่ได้ · tab hidden suspend WebAudio · engine ไม่ regression)
- **2026-07-24 · melody-audit รอบ 1 เสร็จ (pass partial · `2026-07-24-melody-correctness-audit.md` · branch bold-robinson `d7c6545`):** ไม่มีเมโลดี้ผิดร้ายแรง · melody↔lyric สลับ=0 (แก้หมดแล้ว) · spot-check PDF อนุชน #84/#120 pitch ตรง 100% · **worklist ซ่อม:** Tier1 11 เพลง (จังหวะรวมไม่ครบห้อง=duration encode ผิด) · Tier2 16 เพลง (เนื้อกรอกไม่ครบ ≥5 ไม่ใช่โน้ตผิด) · **เส้นทาง 100% pitch จริง = `tools/pdf-melody-diff.mjs`** (prototype แล้ว · diff pitch 120 เพลงเทียบ PDF) = จ่ายต่อหลัง deploy · 🔴 G/N consult ล้มเหลว (Gemini selector พัง · meeting-room :9222 ไม่เปิด) = รอ P'Aim launch
- **2026-07-24 · ดีพลอย /v2 = 2 เสาคุณค่า (P'Aim):** #1 **ฟังเพลง/ฝึกร้องได้จริง** (audio playback ต้องทำงาน · timbre ไม่ต้องเป๊ะ) · #2 **คนทำเพลงแก้ได้สมบูรณ์ อุดช่องโหว่ทั้งหมด** (structure-edit/copy-paste = ชิ้นใหญ่สุด · running lane `492b8dc1` · + editor-flow-polish + marker-entry)
- **2026-07-24 · หลักบริหาร session (P'Aim สั่ง · durable):** **งานเดิมที่ไม่บล็อก = ปล่อยเดินต่อ อย่าไปหยุด/wind-down · งานใหม่ = spawn session เพิ่ม** (ไม่ repurpose/หยุดของเดิม) · "คนมีลูกมือมากมาย จ่ายงาน" = dispatch เชิงรุกเข้าเดดไลน์ · **override session-health rotate ชั่วคราวช่วง push เดดไลน์** (PM เคยจัดเกิน สั่ง midbar/deploy-prep wind down → P'Aim ทัก)
- **2026-07-24 · deploy /v2 พร้อม land (staged · รอ P'Aim go):** gate verified RESOLVED 0 SQL (170/170 verified · save ไม่พลิก) · deploy-prep prep 100% (`bd92821` /v2 mechanism 554f52a + `stage-main-deploy-yml c25991c` deploy.yml→main byte-identical + land sequence §6 line-by-line + db/010 ยืนยัน applied) · **land = PM execute §6 (cherry-pick 554f52a→studio-shell-redesign · test:all+build gate · deploy.yml→main · push) ~10 นาที** · deploy-prep กำลัง dry-run land ใน throwaway พิสูจน์ clean ก่อน go · **main แตะแค่ deploy.yml · go = P'Aim สั่งเท่านั้น** · midbar engine Phase2 done (`daa8c7f` test 1383) = **merge HELD หลัง deploy** (dormant ไม่มี entry UI · ship minimal)

- **2026-07-24 (pm45→46 handoff) · P'Aim 3 มติ:** (1) **"PM เก็บงาน + ไป PM session ใหม่"** → pm45 prune+commit+sync แล้วส่งไม้ pm46 · (2) **issue8 = ไม่ดึงมาตอนนี้** ("ทำไมต้องดึงมา" — ไม่มีเหตุจำเป็น · intake รอ P'Aim สั่งค่อยดึง) · (3) **BI-007 role editor/approver = P'Aim จะทดสอบเองหลัง deploy** (ไม่ต้องหา test creds · gate ปัจจุบัน = anon verified live + jsdom role tests พอ) · workers เดินต่อเอง ไม่กระทบ · pm46 rehydrate จาก pm-state+decisions-log แล้วทำต่อ
- **2026-07-24 (pm45) · P'Aim 2 กฎ broadcast (universal):** (ก) **G/N consult ทุก session ใช้ ai-bridge** `py -3.14 C:\gl\.aibridge\bridge.py ask G/N <session-id>` · code SSOT `ceo/tools/aibridge` · PM แจก chat-id/notebook · transcript auto-save · (ข) **⛔ ห้าม kill browser ด้วยชื่อ** — blanket `taskkill /IM chrome.exe` ฆ่าทั้ง Chrome+Chromium รวม :9222(P'Aim)+:9335(daemon) → ปิดเฉพาะของตัวเองด้วย PID/port (น่าจะเหตุ :9222 ตาย) · บันทึก memory `feedback_never_kill_browser_by_name` (sync OneDrive) · ควรเข้า CLAUDE.md §4 · MR tool: ceo!13 + ai-bridge!32 (PM สร้าง · P'Aim approve)
- **2026-07-24 (pm45) · G-consult ซ่อมแล้ว (ai-bridge) + G-review BI-007 ผ่าน:** root cause CDP พัง = เกาะ :9222 P'Aim busy (ไม่ใช่ Py3.14) · เครื่องมือใหม่ `C:\gl\.aibridge\` (Chrome เฉพาะ :9335 · `ask G/N` · transcript auto-save) พิสูจน์แล้ว → **G-defer ยกเลิก** · G-review BI-007 completion-flow (transcript เซฟ): ✅ ยืนยันปุ่มเดียวปรับ role + disabled-primary=anti-pattern · ปรับ: (1) label ภาษาหน้าที่ anon"ส่งข้อเสนอแนะให้ทีมงาน"/editor"ส่งให้ผู้อำนวยเพลงตรวจทาน"/approver"อนุมัติและเผยแพร่" (2) save indicator+"ระบบบันทึกร่างอัตโนมัติ"+แยก Saved/Submitted (3) มือถือ = pill+bottom-sheet ไม่ยก stepper เต็ม · Q5 anon JSON+email friction = follow-up (ทางเลือก G ชน constraint no-backend/PII) · feed Chip C แล้ว · repo-home tool รอ P'Aim (ceo/tools vs enteam/sandbox)
- **2026-07-24 (pm45) · 🚨 LANDMINE: studio-shell-redesign diverged ออกจาก editor (Explore git-verified):** ssr tip c90c18f = commit เอกสาร PM (bug-intake) ล้วน · common base กับ editor line = dac3970 (ก่อนรวม editor) → **ssr ไม่มี StructureDrawer/songStructure · SongViewer เก่า** · ⛔ deploy ssr = /v2 regress ตัวไม่มี editor · live /v2 = 132a041 (branch prewarm-probe) · **canonical editor base = editor-port-repeat-markers 7afb038** (superset live · SB1+BI-002+integration-cp+502 SongViewer) · ก่อน deploy: FF ssr→integrated tip + cherry-pick deploy.yml ของ 132a041 · **บทเรียน: PM สั่ง worker base off ssr ผิด (นึกว่า ssr=editor) → หลาย session build บน SongViewer เก่า (Chip A verify BI-005 บน editor ผิดตัว) → สั่ง rebase onto 7afb038 + re-verify · ต้องเช็ก base จริงก่อนสั่ง ไม่เดาว่า branch ชื่อคุ้น = ตัวล่าสุด** (ตรง feedback_shared_dir_branch + "รีโปไม่ตรงของจริง")
- **2026-07-24 (pm45) · SA BI-007 spec วิเคราะห์ผิดตัว editor (Chip C จับได้):** spec `docs/ds/edit-completion-flow.md` อ้าง `EditorMode.vue` (ตัวแก้ grid เก่า) ตลอด · §2 บอก "ไม่มีปุ่มเสร็จ" = ดู EditorMode · แต่พื้นผิวที่พี่เปาตันจริง = **inline editor `SongViewer.vue`** (มี "บันทึกร่าง/เสร็จ/สโคปเล่น" อยู่แล้ว) → **spec = แหล่งดีไซน์ · line-ref EditorMode = อ้างอิง ไม่ใช่ไฟล์เป้า · build จริงบน SongViewer inline** (ตรง migration old→inline) · Chip C เจ้าภาพ · แผน P1(foundation+stepper+auto-save)/P2(anon ส่งให้ทีม+ยืนยัน) · บทเรียน: design session ต้องยืนยัน "พื้นผิวจริงที่ user ใช้" ก่อน (grep EditorMode เจอ ≠ ตัวที่ user เห็น)
- **2026-07-24 (pm45) · BI-007 completion-flow = capability-based editorial workflow (P'Aim เคาะ · = มาตรฐาน WordPress/CMS):** SA เจาะโค้ด: publish→songs = **approver-only (RLS db/002:71-76)** · editor เพดาน "ส่งตรวจ" · "แย่กว่า v1" = v1 กด 1 ทีขึ้น · v2 เพิ่ม draft/review แต่ไม่มีจังหวะ "done" (5 dead-ends) · **P'Aim: "คนอนุมัติ=มีสิทธิ์อนุมัติ · ถ้าพี่เปามีทั้ง 2 สิทธิ์ก็เผยแพร่เองได้เลย · เลือกได้ว่าจะรอ approve หรือ publish เลย"** → PM ฟันธงมาตรฐาน = **สิทธิ์เป็น capability (แก้/เผยแพร่ แยกกัน) · ปุ่มจบงานปรับตาม capability สูงสุด** (มีเผยแพร่→"เผยแพร่เลย"+รอง"บันทึกร่าง" เลือกได้ · แก้อย่างเดียว→"ส่งตรวจ") · ร่าง→รอตรวจ→เผยแพร่ + audit · **ไม่แตะ RLS/schema** (ปุ่มใช้ capability ที่ user มีอยู่) · พี่เปาเผยแพร่เองได้ = grant approver (data 1 row แยก) · จ่าย build `4ed20d3d` (off spec branch @35a4ce3) · spec `docs/ds/edit-completion-flow.md`
- **2026-07-24 (pm45) · 🔴 บทเรียน: requirement ที่ P'Aim เน้นมาก (ใส่ D.C./D.S. เอง) หลุด release — PM ต้องแยก "render เสร็จ" ออกจาก "ใช้จริงได้"** — repeat/nav markers ออกแบบครบ (G Pro 2 รอบ) + engine สร้างแล้ว + **RENDER ship** แต่ **ENTRY UI (item1) ไม่เคย build ก่อน release** · หลุดเพราะ (1) ติด gate §8 G-consult ที่ G พัง = tooling พังกลายเป็น hard blocker (2) go-live 20:00 → ship เฉพาะที่พร้อม (lint/paste/render) item1-UI เป็นก้อนใหญ่+G-gated เลยถูก park เป็น follow-up (3) **render เสร็จ = บังเงาช่องโหว่** — เห็น glyph นึกว่า "ใช้ได้" แต่ครึ่งที่ขาด (ใส่เอง) มองไม่เห็นจนลองใส่ = silent-gap ตระกูลเดียวกับบั๊กเงียบอื่น · **PM-level miss: ตอน release ควร flag ชัด "ผู้ใช้ยังใส่ D.C./D.S. เองไม่ได้ — ดูได้อย่างเดียว" ไม่ปล่อยให้ 'render done' = 'done'** (ตรง feedback: PM ต้องยก "ยังใช้จริงไม่ได้" บอก P'Aim ทุกครั้ง) · แก้: pm45 จ่าย build ทันที (G-deferred)
- **2026-07-24 (pm45) · SB1 = HOLD (P'Aim ขอเวลาคิด):** "มีปัญหาเยอะที่อยากแก้" → ไม่ redeploy bundle (SB1+BI-002-fix+lint+paste) จนพี่เอมพร้อม · fix เสียงค้าง BI-002 รอ ship พ่วง · session PM scope = เคลียร์ 7 issues + จ่าย dc/ds (ไม่ขยายเรื่องอื่น)
- **2026-07-24 (pm45) · P'Aim เคาะ: จ่าย fix 3 บั๊ก editor เลย (BI-003/004/005 · พี่เปา user จริง · commit b9bd677)** — ธีมรวม "แก้เพลงใช้ยากกว่า v1" · BI-003 symbol ลบ/ใส่ซ้ำไม่ได้ (CP-0 registry) · BI-004 copy/paste ห้อง+บรรทัด drop-point ไม่ชัด (structure) · BI-005 พิมพ์เนื้อไม่ auto-advance + eng leak "v1 ดีกว่าเยอะ" (flow-polish) · จ่าย 3 session สด (ไม่รอ issue infra · bugs hot) · แต่ละตัว own worktree off studio-shell-redesign · **BI-005 = signal editor ยังไม่พร้อม replace v1 → ยืนยัน defer topology swap ถูกต้อง** · ⚠️ hot-file SongViewer.vue แชร์ musing-carson(BI-002) → merge ตามลำดับ PM · topology swap = พักจน bugs นิ่ง (P'Aim: "ยังไม่ย้าย bug ยังเยอะ")
- **2026-07-24 (pm45) · SB2 = KEEP dock (P'Aim เคาะ):** P'Aim: "ไม่เข้าใจ ปัญหาคืออะไร ตอนนี้ play dockey มันก็ดีอยู่แล้ว" → **ไม่ย้ายคีย์/เสียงดนตรีออกจากบาร์** (คง quick-transpose + one-tap sound ที่จูน 13 ก.ค.) · SB2 จบแค่ส่วนปลอดภัย `@0cc866f` (download→⚙) · clever-einstein ทำถูกที่หยุด+ถามก่อนทับดีไซน์ 13 ก.ค. (เทสต์ fail 10 = SingTransport "คีย์ ON THE BAR" + "⚙ NOT คีย์") ไม่ override เงียบ · ▶ hero accent = เฉพาะ visual เบาไม่ restructure · บทเรียน: ข้อในลิสต์เก็บกวาด ("▶ เด่น + secondary→⋮") ≠ อนุญาตรื้อ dock ที่ผู้ใช้จูนเอง — worker ตีความเลยเถิด PM ต้องเกท

- **2026-07-24 (pm45) · issue9 = lead-sheet metadata header (P'Aim เคาะ · เฟส 1 เท่านั้น):** source `OneDrive\4 Personal\pleng.phrakham.life\pleng2-pow-bug-report\issue9` (txt+png song141) · ปัญหา = ชื่อเพลงยัดในแถบบน ถูกตัด "..." → **ย้ายชื่อลงบรรทัดเต็มของตัวเอง + แถบ meta ใต้ชื่อ** ตามมาตรฐาน lead-sheet · **เฟส 1 (ข้อมูลมีครบ ไม่แตะ DB):** ชื่อเต็ม + `คีย์ · อัตราจังหวะ · ความเร็ว` (key/timeSignature/bpm มีในโมเดล) + โชว์ scripture (book_refs มีอยู่) · **⛔ ไม่เอา credits/ลิขสิทธิ์ (เฟส 2 · P'Aim เลื่อน · ต้องเพิ่ม field+กรอก 120 เพลง)** · **คีย์ที่โชว์ = P'Aim เคาะ "แสดงต้นฉบับได้ + shift ได้แล้วแต่คนใช้ · ต้นฉบับ=แสดงปกติ · shift=บอกว่า shift"** → SA เช็คมาตรฐานการแสดง transposed key (ผ่าน G/ai-bridge บังคับ) แล้ว design (displayKey มีอยู่แล้ว SongViewer:149 · original=content.key) · **bpm บาง null → ไม่มีให้ซ่อน ไม่โชว์ fallback 92 หลอก** · จ่าย SA ร่าง spec `docs/ds/` (ยังไม่ build)

## สถาปัตย์ + กฎ (durable)
- **2026-07-24 · 🔴 แอปมีตัวแก้เพลง 2 ตัวซ้อนกัน (audit ยืนยันด้วยการเปิดของจริง)** — **ดินสอ ✏️ (FAB) = ตัวแก้ inline ใหม่ ตรงดีไซน์ล็อกและทำได้ดีจริงแล้ว** (แก้บนแผ่น·พิมพ์ทับโน้ต·ทำนองร่วมเปลี่ยนทุกข้อ·สถานะบันทึก·กู้คืนอัตโนมัติ) · **แท็บ "แก้ไข" บนแถบบนสุด = โต๊ะแก้ตัวเก่าแบบกล่อง คนละตัวกันเลย** และยังถือความสามารถเกือบทั้งหมด
  - ⇒ **มติ PM: ยกของเข้าตัวแก้ใหม่ทีละชิ้น แล้วค่อยตัดตัวเก่า+แถบ** (ไม่ใช่ big-bang) · เหตุ: ปล่อยของได้เรื่อยๆ · ทำขนานได้ · ตัวเก่ายังใช้ได้ระหว่างทาง ผู้ใช้ไม่ติดขัด · ความเสี่ยงที่ยอมรับ = 2 ทางอยู่ร่วมกันชั่วคราว (= "ดินสอ 2 ทาง" ในคิวเดิม)
  - ลำดับที่จ่าย: **A) 3 จุดเจ็บทุกวัน** (แถบปุ่มบังคำร้อง 15/97 ช่อง · แท็บตายกดแล้วไม่เกิดอะไร · คำอธิบายกินจอ 40%) → **B) ฟังเพลงจากในตัวแก้** (loop หลักของคนทำเพลงที่ขาด) → **C) ตั้งค่าเพลง (B060 พี่เปาขอตั้งแต่ 9 ก.ค.)** → ที่เหลือ (โครงเพลง·วนซ้ำ·เพิ่มบรรทัด/ห้อง·พิมพ์) → **แล้วค่อยตัดแถบ**
  - ✅ **หน้าแรกตรงดีไซน์เกือบครบแล้ว** (ค้นหา·ชั้นหนังสือ·โปรด·เพลย์ลิสต์·＋เพลงใหม่·☰·สลับภาษา) → ไม่ต้องจ่ายสายหน้าแรกอีก ทุ่มหน้าแก้เพลงเต็มตัว
- **2026-07-23 · ทุกการตัดสินใจออกแบบของ SA ต้องคุยกับ G ทุกครั้ง (P'Aim สั่ง · บังคับ ไม่ใช่ทางเลือก)** — เหตุ: คุย G ไม่เกิน 3 รอบได้ไอเดียดีที่ทีมมองข้ามเสมอ ประหยัดเวลามาก · ต้องเซฟ transcript + ใส่ path ใน EVIDENCE (กฎกลาง §4.5 ข้อ 10) · PM ต้องใส่ข้อนี้ในทุกใบสั่งงานออกแบบ
- **2026-07-23 · "ทุกรูปแบบการวนร้องทำได้ ไม่ว่าซับซ้อนแค่ไหน" (P'Aim + C + G ตกลงกันแล้ว)** — ⛔ ห้าม SA/dev สรุปว่า "โมเดลปัจจุบันเขียนไม่ได้" แล้วโยนคำถามกลับ P'Aim · ต้องอ่าน `work/ปรับ pl edit ui/แปลงโน้ตเพลงเป็นอัลกอริทึม.md` + `บทวิเคราะห์-สถาปัตยกรรม.md` ก่อน แล้วเสนอ**วิธีทำตามมาตรฐาน** · เคสต้นเรื่อง = สัญลักษณ์ย้อนผูกกับ "ทำนอง" ไม่ใช่ "ข้อ" (เพลง 105 ข้อ 2-3 ไม่วนเนื้อ)
- **2026-07-23 · แท็บที่พี่เปาขอ = ป๊อปอัปแป้นสัญลักษณ์ (ความยาว/เสียง/กลุ่ม-ห้อง) ไม่ใช่หน้า ⚙ ตั้งค่า** — เหตุผลของ P'Aim: ป๊อปอัปนี้จะมี option เยอะกว่านี้อีกมาก → ออกแบบเผื่อโต ไม่ใช่แค่ 12 ตัวที่เห็นตอนนี้
- **2026-07-23 · หลักการปล่อยของ (P'Aim): "ship fast, fail fast, fix faster"** — deploy v2 ให้เร็วที่สุดด้วยบั๊กน้อยสุด + UX ระดับโลก · **ขึ้นไปผิด = ถือเป็นบั๊ก แก้ทีหลังได้** · ⛔ ไม่บล็อก deploy ด้วยการพิสูจน์ PDF/พิมพ์ (คนส่วนใหญ่ใช้ฟังออนไลน์) · P'Aim ตั้งใจ rely on Claude Code + G ในการ go-live ให้มากที่สุด ไม่ลงมือ verify เอง
  - **ข้อยกเว้นที่ PM ยังต้องเกท (PM เพิ่ม · ไม่ขัดหลักข้างบน):** บั๊กที่ทำ **ข้อมูลของพี่เปาหายเงียบ** (B108 หมวดหาย · item หายตอนเซฟ) ไม่เข้าข่าย "fix faster" เพราะกู้ไม่ได้และผู้ใช้ไม่รู้ตัว → เกทเฉพาะกลุ่มนี้ · บั๊กที่เห็นได้/ผู้ใช้รู้ตัว = ปล่อยแล้วแก้
- **สถาปัตย์ = SSOT สากล 1 ชุด (v2, MusicXML-like) → output ≥2 format** (มาตรฐานสากล / แบบโบสถ์ไทย) · alignment = core format-agnostic · masks = display layer · ที่มา `บทวิเคราะห์-สถาปัตยกรรม.md`
- **PM operating model = กฎกลาง `C:\gl\CLAUDE.md §4.5`** (universal ทุกโปรเจกต์) · **browser/render isolation = §4** · SSOT เดียว ไม่ dupe memory · live state ต่อโปรเจกต์อยู่ `docs/pm/`
- **backlog = `docs/backlog.md`** (กล่องกลาง shared) · **สมอง PM = `docs/pm/`** (pm-state + decisions-log)
- **บทเรียน (จำ):** seat แก้ให้ถูกตามมาตรฐานเอง — ไม่โยน choice เรื่องความถูกต้องให้ P'Aim · ไม่ตีความคำติ "ตำแหน่ง/ยาวเกิน" เป็น "เอาออก" · อย่าป้อน claim ตัวเลขไม่แม่นให้ Tester · (รายละเอียด → archive)

## Queued (ตัดสินแล้ว · รอ build)
- **songbook (B120) = หน้ากาก "แบบอนุชน" (มีโน้ต) — P'Aim เคาะ 23 ก.ค.** · สเปกเดิมที่บันทึกว่า "เนื้อล้วน" = **บันทึกผิด/คนละเล่ม** → ย้ายเป็น **B126 เนื้อล้วน (แบบเล่มบทเพลง)** ทำทีหลัง
  - อ้างอิงของจริง: `OneDrive/1 Devotional/hymm/อนุชน 01-120 THEN…pdf` (jianpu + คอร์ดเหนือโน้ต · คีย์+จังหวะ+ข้อพระคัมภีร์ใต้ชื่อ · เนื้อใต้โน้ตเฉพาะข้อ 1 · ข้อ 2-4 ข้อความล้วน · ท่อนรับใช้วงเล็บ · 1 เพลง=2 หน้า ไทย/อังกฤษแยกหน้า)
  - "เล่มใหญ่" (รูปถ่าย 24 ใบใน `work/ปรับ pl edit ui/`) = กำลังวิเคราะห์เป็น reference เพิ่ม
  - ที่ยังยึดได้: DEFAULT ไม่มี toggle · ข้อ=ย่อหน้าใหม่ · ตัดที่จบวลี · density=auto
- **เลือกขนาดกระดาษพิมพ์ (B121)** · follow-up bugs B122-B125
- **2026-07-23 · release นี้ target = พี่เปา (ผู้ใช้จริง) ไม่ใช่สาธารณะ** — "ครบ" วัดที่พี่เปาใช้งานจริงได้จบ flow · zh/en · dark · B121 · B126 = เลื่อนหลัง release
- **2026-07-23 · "เล่มใหญ่" (P'Aim เคาะ): ⛔ ไม่ทำบรรทัด 5 เส้น / แนวเบส** — ใช้รูปแบบเดียวกับอนุชน (jianpu + คอร์ด + เนื้อใต้โน้ต) · **แต่ต้องรองรับ "การวนร้อง" หลากหลายแบบ** (D.C./D.S./Fine · 1st-2nd ending · `|: :|` · (รับ) · เลขกำกับซ้ำท่อน) — ทำตาม **มาตรฐานสากล** แล้วจะครอบคลุมเอง → ผูกกับ **B102 symbol-SSOT play-order resolver** (มีของอยู่แล้ว ต่อยอดได้) · 🔴 **"(อ.NNN)" = cross-ref ข้ามเล่ม แต่ "อ." ชี้เล่มไหน ยังไม่ยืนยัน** — เดาแรกว่า "อนุชน" **แต่เลข 3 หลัก (933·1054·686) เกิน 120 ที่อนุชนมี → อ.≠อนุชน** (SA จับได้เอง 24 ก.ค. · โฟลเดอร์เดียวกันมี "เล่มบทเพลง.pdf" 53MB น่าจะเป็นตัวจริง) · ⛔ ไม่ใช่ "ใช้ทำนองร่วม" · **audit อนุชน hold ไว้จน P'Aim ยืนยันว่า "อ."=เล่มไหน (เขารู้เล่มดีกว่าเปิด PDF เดา) แล้ว spawn SA ใหม่** · **"(=NNN)" = ที่จริงคือ "NNN =" เขียนด้วยมือ (ปากกา) ที่หัวเพลง** (#432="238 =" · #515="233 =") = ผู้ใช้เขียน cross-ref เลขอีกเล่ม/ระบบด้วยมือ · 🔴 **ยังไม่รู้ 238/233 = เล่มไหน → คำถามเปิด รอ P'Aim** · **completeness critic เล่มใหญ่ 18/18 ผ่าน: 8 รูปแบบครอบพอ ไม่เจอแบบใหม่ · dev สร้างเอนจินได้ ไม่ต้องแก้ schema** · เพลงแบบ 105 = ≥3 (105·669·432)

## มติล็อกก่อนหน้า (ยกมาไว้อ่านครั้งเดียว)
- ⛔ **ห้าม re-import/bulk-write 120 เพลง** — ทีมแก้ live · migration ทำตอน app เสร็จ
- คง **Vue3+Vite** (ไม่ Nuxt/Tailwind) · คง bookshelf หน้าแรก · ripple default · backspace ลบชิด
- **แปล zh/en สุดท้าย** (หลัง string นิ่ง) · **ไม่ release จนเสร็จ** · **main = P'Aim สั่ง go เท่านั้น**
- font = **Hybrid Noto** · favorites/playlist **ไม่มี account** · **merge = PM เท่านั้น**
- **2026-07-26 (pm48) · ✅ 717 multi-lyric ปิดจ็อบครบวงจร (v1+v2 live + DB merged + QA ผ่าน):** โจทย์ = เลขเดียว ทำนองเดียว **เนื้อ 2 "แบบ" (versions) สลับด้วยแท็บ** ≠ หลายข้อ (verses) — worker+PM เคยสับสนจนหลงทาง P'Aim จับได้ · โมเดล `content.lyricSets[]` + `arrangement[].set` (map MusicXML `<lyric number>`) · **v1** `b8e7caa` (แท็บ · ＋เพิ่ม/🗑ลบชุด+guard ชุดสุดท้าย · ชื่อประจำชุด name+label · wrap 320/360 ไม่ล้น · 44px coarse · search index ชื่อทุกชุด · print=ชุดเลือก · 844 tests) · **v2** `71e24fa` (เคารพ set — กรอง**ข้างใน** resolveContent เพราะ inline editor ใช้ `_entryIndex` · 1671 tests · back-compat 144 รูปทรง 0 mismatch) · **DB merged** โดย P'Aim รัน `717-merge-COMMIT.sql` (guard 4 ชั้น · dry-run ก่อน · backup `717-before-20260726-1.json`) → 1 แถว number=717 · 2 sets มีชื่อ · 13 entries (set int) · แถว 99128d10 ลบแล้ว → **บั๊กขีดกลาง 717-1/-2 หายในตัว** · **QA live pass** (PDF จริง 2 ชุด · 360px วัดจริง · ค้นชื่อชุด ๒ เจอทั้ง 2 เว็บ · regress 3 เพลง x2 เว็บ 0 error)
  - **บทเรียน PM:** (1) ถือ SSOT ที่ล็อกแล้วต้อง**ยื่นให้ worker** ไม่ใช่สั่ง "ไปหาวิธีเอง" → หลงทางหลายรอบ (memory `feedback_dispatch_hand_locked_ssot`) (2) worker บอก "ขอ handoff" **ไม่เท่ากับหยุดจริง** — ยืนยันก่อนจ่ายทับ (เคยจ่ายซ้ำ) (3) relay ต้องครบ: "ทำนองเหมือนเป๊ะ" จริงเฉพาะ**โน้ต** คอร์ดต่าง 2 จุด
  - **ค้าง (ไม่บล็อก):** MP3 export ใช้ชุด 0 เสมอ · แชร์ลิงก์ไม่มี `?set=` · /v2 ไม่มี ＋/🗑 ชุด · harden v1 กับ string-set (v2 ทนแล้ว) · ลบไฟล์ preview 2 ไฟล์ · **2 ชุดยาวไม่เท่ากัน (5 vs 8 ท่อน) = ข้อมูลต้นฉบับมาแต่เดิม ไม่ใช่ผลจากรวม → งานพี่เปาเคาะ**
- **2026-07-27 (pm48 ปิด · session-health) · งานเพลงที่ยังรัน 2 ตัว = ปล่อยเดินต่อ (กลาง chunk ห้ามแตะ):** `local_55f0dc74` ออกแบบระบบทำงานหลายคน (task_8106f416) · `local_e9dff5bc` กันชื่อเพลงซ้ำ (task_29ebf41c) · **worker เพลงตัวอื่นจบ+ปิดหมดแล้ว** (ยืนยัน netstat/PID · `:9222` P'Aim + `:9335` ai-bridge ไม่ถูกแตะตลอดวัน) · session ที่ยัง running อื่นๆ = โปรเจกต์พระคำ/aibridge ไม่ใช่ของเรา
- **2026-07-27 (pm48) · PM housekeeping:** prune `pm-state.md` 369 → 75 บรรทัด (เหลือเฉพาะงานสด · รอเคาะ · หนี้ · กฎถาวร · pointers) · ประวัติ 717 ทั้งสายอยู่ในบันทึกมติแล้ว

- **2026-07-27 (pm49) · 🎯 P'Aim สั่ง: "ทำ v1 ให้จบก่อน · รื้อ v2 ค่อยดูหลัง v1 เสร็จจริง"** — v1 ได้เฉพาะ (1) ข้อมูลหายเงียบกู้ไม่ได้ (2) พี่เปาทำงานไม่จบ flow · นอกนั้นยกไป v2 ทั้งหมด (เลิกกฎ "fix ต้องลงทั้ง v1+v2" แบบเหมารวม เพราะทำให้ทุกงานเสียแรง 2 เท่ากับของที่จะรื้ออยู่แล้ว) · ⛔ ห้ามเริ่มวางแผนรื้อ v2 · **deploy = รอปล่อยทีเดียวตอน v1 จบครบ ห้ามทยอย**
- **2026-07-27 (pm49) · ✅ พี่เปา = ผู้อนุมัติ (P'Aim ยืนยัน)** — ตัดความกังวลว่าเขาจะติดที่ "ส่งตรวจแล้วรอคนอนุมัติ" · flow เขาจบเองได้ → v1 เหลือแค่ 2 ข้อจริง
- **2026-07-27 (pm49) · กวาด v1 @9bb1c8d: เหลือ 2 ข้อ + ตัด 4 เรื่องออก** — เหลือ: (1) `EditorMode.vue` (อยู่ `components/` ไม่ใช่ `views/`) มี `isDirty` ~:2211 คร่อมแค่ปิดแท็บ (:2219) + เปลี่ยนหน้า (:2222) **แต่ไม่คร่อม `watch(pickerId)` :1532 → สลับเพลง = งานที่ยังไม่บันทึกหายทันที ไม่ถาม** (PM เปิดโค้ดยืนยันเอง) `task_1967b1e9` (2) pre-echo referee มีแต่บน main → พอร์ตมา v1 `task_4ff0f3b8` · **ตัดออก:** ปุ่ม ⏪ ไม่ได้ทำข้อมูลหายถาวร (db/004:104-131 เก็บสำเนา before ทุก UPDATE — PM เคยเข้าใจผิดและ relay ผิด) · เพลง 33 คอร์ดหายเองแล้วบน live · orphan flow 0/199 เพลง · เซฟทับกัน = กู้ได้ ยกไป v2 · ไฟล์ demo 2 ตัว build ไม่เอาขึ้น = ขยะ
- **2026-07-27 (pm49) · C = ⛔ ไม่สร้างตัวนำเข้าเป็นชุดในเว็บ (P'Aim คุยกับ "เยี่ย" คนนำเข้าเพลงแล้ว)** — เยี่ยนำเข้า **ทีละเพลงผ่าน JSON และตรวจเองก่อน** เพื่อ train AI ให้ convert เป็น JSON ดีขึ้น → ความเสี่ยง "ทับทีเดียวหลายสิบเพลง" ไม่มีจริง · เหลือแค่ "พิมพ์เลขเพลงผิดไปชนเพลงที่พี่เปาแก้ไว้" → B = ใส่ยามในเทมเพลต `docs/importing-songs.md:129-135` (`on conflict (number) do update` = ทับเงียบ · PM เปิดอ่านยืนยันเอง) `task_3a275770`
- **2026-07-27 (pm49) · PM ตัดสินเอง 3 ข้อของสเปกระบบหลายคน (ตามกฎใหม่ §4.5 ข้อ 8.5 — ตัดสินเองแต่ชี้แจง)** — ปลดล็อก **15 นาที + เตือนที่นาทีที่ 14** (WCAG 2.2 SC 2.2.1 ห้ามหมดเวลาเงียบ) · ถังขยะ **30 วัน** (ค่ามาตรฐานสากล) · **เอา "ความรู้สึก checkout" ไม่เอาการบล็อกจริง** (ไอเดีย P'Aim เอง · N+G ตรวจแล้ว · ล็อกฝั่ง client มองไม่เห็นทาง SQL) · เหตุ: 3 ข้อนี้เป็นเรื่อง "อะไรถูกตามมาตรฐาน" = หน้าที่ PM ไม่ใช่หน้าที่ P'Aim
- **2026-07-27 (pm49) · กฎกลางใหม่ §4.5 ข้อ 8.5 (P'Aim สั่ง · ทุก PM ทุกโปรเจกต์ · PM เปิด `C:\gl\CLAUDE.md:271` ยืนยันเอง)** — ก่อนวางเรื่องหน้า P'Aim ต้องผ่าน N+G+C กลั่นกรองก่อน → เหลือถึงเขาแค่ **ของจริงให้ดู + คำถามเดียว + ข้อเสนอเดียว** ทีละเรื่อง · ตัดสินเองเรื่องมาตรฐาน/วัดได้ · ถามเขาเฉพาะรสนิยม/ทิศทาง/พันธกิจ · ⛔ **ตัดสินเองแล้วต้องชี้แจงเสมอ** — "กลั่นกรอง = ลดสิ่งที่เขาต้องตัดสิน ไม่ใช่ลดสิ่งที่เขารู้" · เหตุ: P'Aim ว่า *"ที่เหลือไม่เข้าใจเลย มันเยอะมาก"* (pm49 เคยกอง 5 ข้อให้เคาะทั้งที่ 3 ข้อเป็นเรื่องมาตรฐาน = ผิดกฎนี้)
- **2026-07-27 (pm49) · 🔊 ทิศทางเสียง P'Aim: "เหลือแค่ piano ตอนนี้ทำให้ดีที่สุด · อนาคต cello (เสียงทอง) + piano · แค่นี้พอ"** — โหมดเต็มวง *"ผมกะเอาออกด้วยซ้ำ … ถ้าไม่จำเป็นไม่ต้องทำ ปิด link เต็มวงไปเลยก็ได้"* **แต่ยังไม่สั่งปิด** — *"รอ tune piano, cello แยกให้ดีที่สุดก่อนดีกว่า เพราะไม่รู้จะเจออะไรอีก"* ⇒ งานเต็มวงพักลิ้นชักไม่ merge (`v1-preecho-ensemble`@f772428 กีตาร์ 913→0 · `v1-preecho-violin-discarded`@c96732c ไวโอลิน 1045→0 · blast radius 0 ทั้งคู่) · ⭐ **เปียโนนำไม่มี grace → pre-echo ไม่เกิดในเส้นทางเปียโนตั้งแต่แรก** (มีเทสยืนยัน) = ทิศทางนี้ปลอดภัยเอง
  - **บทเรียน PM (ผิดเอง):** PM เคาะเองว่า "police ไวโอลินด้วย" โดยอ้างกฎ *เสียงต้องซื่อสัตย์กับแผ่นโน้ต* — **แต่ไม่ได้ถามก่อนว่าโหมดนี้จะอยู่ต่อไหม** ซึ่งเป็นเรื่อง **ทิศทาง = ของ P'Aim** ไม่ใช่ของ PM · กฎ 8.5 ให้ตัดสินเรื่อง "อะไรถูกตามมาตรฐาน" **แต่ต้องเช็คก่อนว่าของชิ้นนั้นยังอยู่ในแผนไหม** ไม่งั้นตัดสินถูกบนของที่กำลังจะทิ้ง
- **2026-07-27 (pm49) · ✅ v1 เสร็จครบ 3 ชิ้น รอ merge:** `unsaved-guard-v1`@b5080f0 (+/v2 `claude/priceless-shtern-3ba8a1`@9bd7769) · `v1-preecho-port`@cfb95e0 · `claude/import-template-guard`@962c162 · **worker เจอของที่ใบสั่ง PM ไม่ครอบเอง 2 เรื่อง:** ปุ่ม "สร้างเพลงใหม่" = รูที่ 5 ของงานหาย · `import-ties.sql` ทับ content 48 เพลงรวดไม่มียามเลย · **แผน:** merge → tester ตรวจว่า 3 ชิ้นไม่ตีกัน → ลิงก์ให้ P'Aim+พี่เปาลองจริง **+ ฟังเพลง 33 ด้วยหู** (ยังไม่มีใครฟังสักคน) → ขอ go → ขึ้นเว็บทีเดียว (v1 = เลื่อน tag `v1-frozen` + จุด workflow เอง)
- **2026-07-27 (pm49) · 🚀 ปล่อย v1 ขึ้นเว็บแล้ว (P'Aim สั่ง go: "ยังไม่ได้ทดลองหมด แต่ไม่เป็นไร deploy ให้พี่เปาลองเลย")** — merge 4 ชิ้นเข้า `v1-release-try`@`1a55189` (ไม่มี conflict สักไฟล์ · เทส **1014 เขียว** · build เขียว) → push เข้า branch `v1` → `git tag -f v1-frozen 1a55189` + push -f → **จุด workflow ด้วย empty commit `cebf366` บน `origin/main` ผ่าน `commit-tree`** (ย้าย tag เฉย ๆ ไม่ trigger · `gh` ใช้ไม่ได้ PAT ไม่มีสิทธิ์ Actions) → workflow conclusion=success → **ยืนยันที่ bundle live จริง: `/` มีข้อความใหม่ "เปียโนเดี่ยวอย่างเดียว" = 1 · `/v2` = 0** (ไม่เปลี่ยนตามเจตนา)
  - ⚠️ **ปล่อยโดยยังไม่มีใครฟังด้วยหูสักคน** — ทั้งเพลง 33 (pre-echo) และเปียโน 3 โหมด · รู้ตัวและยอมรับตามหลัก ship-fast · ถ้าพี่เปาทักเรื่องเสียง = จุดนี้
  - **/v2 ยังไม่ได้ 2 fix นี้** — PM ตัดสินไม่พ่วง เพราะ merge เข้า main = ขึ้นเว็บทันที และ /v2 ไม่มีใครทดสอบรอบนี้ · branch จอดพร้อม merge: `claude/priceless-shtern-3ba8a1`@9bd7769 · `claude/quirky-grothendieck-75af84`@5e2d307
- **2026-07-27 (pm49) · 🔊 เหลือ "เปียโนเดี่ยว" อย่างเดียว (P'Aim: "ความเพราะตอนนี้เปียโนเดี่ยวเท่านั้น")** — เครื่องดนตรี + การบรรเลง = **ซ่อนทั้งกลุ่ม ไม่ลบ** (สวิตช์เดียว `PIANO_ONLY` ใน `src/lib/soundOptions.js` · flip = กลับมาทั้งหมด) · คงโหมด **บรรเลง/สงบ/ตรงโน้ต** + กลุ่ม **"เสียงที่เล่น"** (ทำนอง/คอร์ด/รวม — worker ทักว่า PM เขียนบรีฟผิดว่าเหลือ 3 ปุ่ม จริง ๆ มี 2 กลุ่ม · P'Aim ไม่เคยพูดถึงกลุ่มนี้ → คงไว้ ถูกแล้ว) · **กับดักที่ปิดแล้ว:** ค่าค้างเก่า (เต็มวง/กีตาร์ ใน localStorage 4 คีย์) ตกกลับเป็น piano+solo อัตโนมัติ พิสูจน์บนเบราว์เซอร์จริงว่ายังออกเสียง · แผนอนาคต P'Aim: หลังจูน cello เสร็จ → เครื่องดนตรี = เปียโน+เชลโล่ · การบรรเลง = เดี่ยว+เต็มวง
- **2026-07-27 (pm49) · ✅ /v2 ได้ของครบและขึ้นเว็บแล้ว (P'Aim สั่ง go ทีละข้อ)** — `main`@`f6c9f22e` (กันงานหาย + เปียโนเดี่ยว · เทส 1838) → `main`@`be159a3b` (+ กันชื่อซ้ำ · เทส **1872**) · ยืนยันที่ bundle live ทั้ง 2 รอบ · หน้าแรกถูก rebuild จาก tag ไปด้วย → ไฟล์ demo หายจากเว็บจริง (`demo717.html` → **404**) และ tag/live ตรงกันที่ `47e4287`
  - **merge ตัวกันชื่อซ้ำชน 4 จุด** (`Studio.vue` 3 · `EditorMode.vue` · `SongViewer.vue`) — PM ถอยแล้วจ่ายงานแทนการดันเอง ถูกทาง: **มีกับดักพังเงียบจริง** — branch เก่ายังถือ `watch(pickerId, id => loadSong(id))` ตัวเดิมติดมา ถ้าเก็บไว้คู่กับตัวใหม่ Vue จะรัน callback ทั้งคู่ → กด "ยกเลิก" แล้ว guard เด้ง picker กลับ **แต่ตัวเก่ายิง `loadSong()` ต่อทันที = กันงานหายถูกข้ามโดยไม่มี error** · worker จับได้ + G ยืนยันตรงกัน → ลบตัวเก่าทิ้ง · PM เปิดไฟล์ยืนยันเองหลัง merge: เหลือ watcher **ตัวเดียว** ที่บรรทัด 1381
- **2026-07-27 (pm49) · 🔇 ปิดเรื่องเสียงถาวร: P'Aim ฟังเอง "เพราะ" · สั่ง "ไม่ต้องจูนเพิ่มแล้ว"** — ความเสี่ยงเดียวที่ค้างทั้งวัน (ปล่อย v1 โดยไม่มีใครฟัง) ปิดแล้ว
- **2026-07-27 (pm49) · 🔚 v1 กำลังจะ obsolete (P'Aim: "เรื่อง key f v1 ไม่ต้องทำอะไร ลืมไปได้เลย")** — ⛔ **เลิกลงแรงกับ v1 ทุกกรณี แม้งานเล็ก** · v1 = ของให้พี่เปาใช้ไปพลาง · ป้าย Key F ตัดทิ้งถาวร · ⛔ ไม่ต้องเอาตัวกันชื่อซ้ำขึ้น v1 (`dup-title-guard-v1`@7589cb4 จอดไว้เฉย ๆ) · **แรงทั้งหมด → /v2 + งานรื้อ**
- **2026-07-27 (pm49) · 🗑 ลบเพลงซ้ำ "พระเยซูทรงรักเด็กๆ" 3 จาก 4 (พี่เปายืนยัน · P'Aim รัน COMMIT · PM ยืนยันที่ DB จริงว่าหายแล้ว)** — ต้นเหตุ: **นำเข้าซ้ำ 4 แถวรวดตอน 25 ก.ค. 13:00:52-53** (ห่างกันมิลลิวินาที) · ลบ `1970bac7` + `ecb7cd1e` (ดิบ · `updated_at = created_at` · `chord:""` ไม่มี bpm = ไม่มีงานคน) + `357cc3c5` (bpm 104 · พี่เปาสั่ง) · **เก็บ `bc7d1664` (bpm 117 · แก้ล่าสุด 26 ก.ค.)** · สำรองทั้ง 3 แถวเป็น JSON ใน `public.songs_deleted_backup` (ตารางใหม่ = ถังขยะชั่วคราวจนกว่าจะ build ของจริง) · SQL `tools/delete-dup-song-20260727.sql` ยาม 3 ชั้น + จบด้วย ROLLBACK ให้ลองก่อน
  - 🔴 **บทเรียนที่ต้องจำ: PM อ่านคลังด้วยคีย์สาธารณะ = เห็นไม่ครบ** — anon เห็น 2 จาก 4 แถว · PM รายงานผิดไป 3 ข้อจากตัวเลขที่ถูกกรอง ("คลังเหลือ 198" · "คู่พระดำรัสเหลือ 1 แล้ว" · "มีคนลบไประหว่างวัน" — **ผิดทั้งหมด**) · P'Aim จับได้เอง *"ความเป็นไปได้คือพี่เปาเข้าแบบ login แต่คุณดูแบบไม่ login"* ⇒ **ห้ามสรุปว่า "ไม่มี/หายไป/ถูกลบ" จากคีย์สาธารณะ — พูดได้แค่ "ผมมองไม่เห็น" แล้วขอ P'Aim รันดู**
  - **คำถาม vs คำตอบไม่ตรงมุมกัน** — PM ถาม "เก็บอันไหน" พี่เปาตอบ "ลบ 1" → PM เขียนการตีความไว้บนหัวไฟล์ SQL ให้เห็นก่อนกด commit แทนที่จะเดาเงียบ ๆ (P'Aim ยืนยันภายหลังว่า "ลบ 1 = เก็บ 2" ถูกต้อง)
  - **ยังเหลือ:** "พระดำรัสชิมหวานสักปานใด" ในเล่มเด็กเล็ก **ยังซ้ำ 2 อัน** (anon เห็นแค่ 1) — ต้องถามพี่เปาแบบเดียวกัน
