# ใบสั่ง (dev) — B084: ช่องโน้ต กด space ต้องตัดพยางค์ตรงเคอร์เซอร์ (ไม่ใช่แค่กระโดด)

**สั่งโดย:** pm7 · **ฐาน:** `studio-shell-redesign` · **branch ใหม่: `notebox-split`** · **ไฟล์เดียว `NoteBoxes.vue` (แยกจากสาย EditorMode → ไม่ชน)**
**ที่มา:** พี่เปา (repro ชัด 11 ก.ค.) — **พิมพ์ "345" ในช่องโน้ตเดียว แล้วอยากเอา "5" ไปอีกช่อง ทำไม่ได้**

## ต้นเหตุ (PM triage · ยืนยันในโค้ด)
`NoteBoxes.vue` `onKey` (~บรรทัด 80): กด **space/Enter** → `focusBox(i+1)` เฉยๆ = **กระโดดไปช่องถัดไป ไม่ตัดตรงเคอร์เซอร์** · ต่างจากช่องเนื้อ (`EditorMode.vue:350`) ที่ space = ตัดที่ caret แล้วดันส่วนหลังไปช่องใหม่ → ในช่องโน้ต "345" เลยแยก "5" ออกไม่ได้ (space แค่กระโดด "345" ค้างทั้งก้อน)

## ต้องได้ (AC)
1. ช่องโน้ตมี "345" · เคอร์เซอร์อยู่หน้า "5" (ระหว่าง 4 กับ 5) · กด **space** → ช่องปัจจุบันเหลือ **"34"** · เกิดช่องใหม่ถัดไป **"5"** · โฟกัสไปช่อง "5" (= เอา 5 ไปอีกช่องได้)
2. เคอร์เซอร์อยู่ท้ายสุด (ไม่มีตัวหลัง caret) + space → เหมือนเดิม (ไป/สร้างช่องถัดไปว่าง)
3. คง `fixAccidental` กับส่วนที่ตัด (เช่น "3#4" ตัดถูก) · คง paste-หลาย-โน้ต (`onInput` split ช่องว่าง) · arrow ซ้าย/ขวาข้ามช่อง · Backspace ช่องว่าง = ลบ+ถอย (เหมือนเดิม)
4. **ไม่ regress:** พิมพ์โน้ตปกติ · save/ดูผล เหมือนเดิม

**แนวทาง:** ใน `onKey` เคส `' '` (space) — ถ้ามีข้อความหลัง caret → ตัด: `boxes[i]=ก่อน caret` · แทรกช่องใหม่ `หลัง caret` ที่ i+1 · `focusBox(i+1)` (caret ต้น) · ถ้าไม่มีหลัง caret → พฤติกรรมเดิม · (Enter จะให้ตัดด้วยหรือคงเดิม = dev เลือกตามสมเหตุสมผล · space เป็นหลักตามที่พี่เปาใช้)

## ตรวจเอง + test
- **unit test บังคับ:** "345" caret@2 + space → ["34","5"] focus 1 · caret@end + space → ช่องใหม่ว่าง · accidental ตัดถูก
- Browser MCP: พิมพ์ 345 → แยก 5 ได้จริง · vitest เขียว + build · console 0
- แตะเฉพาะ `NoteBoxes.vue`(+test) · เช็ก `git branch --show-current` ก่อน commit

## รายงาน
session-agnostic: (1) `docs/reports/notebox-split.md` (2) board §📥 inbox (3) ping PM = pm7 · ⛔ ไม่ merge/deploy · เปิด `npx vite . --host --port 5415 --strictPort` (IP 10.215.141.98) ใส่ Network URL
