# Brief — cello bake-off (เชลโล + เปียโนทอง) · 3 คลิปให้ P'Aim ฟังเลือก

**สายนี้ = spike ฟังเทียบ ไม่ใช่ feature** · ฐาน `studio-shell-redesign` · worktree + port ของตัวเอง

## เป้าหมาย (ข้อเดียว)

ทำคลิปเสียงสั้น **3 คลิป · ท่อนเดียวกันเป๊ะ · เปียโนทองเดิมเป๊ะ · ต่างกันแค่ "ไฟล์เสียงเชลโล"**
แล้วให้ **P'Aim ฟังตัดสินด้วยหู** ว่าตัวไหนเพราะ / หรือไม่เอาเลย

**อย่าปรับจูนความเพราะเอง อย่าวนเดา** — SA ฟัง mix ไม่ออก (memory `pleng-aesthetic-audio-needs-ear`)
ไวโอลิน + เชลโลพักไปแล้ว 2 รอบเพราะวนเดาแบบนั้น **หน้าที่สายนี้ = ทำการทดลองที่ยุติธรรม แล้วส่งให้หูตัดสิน**

## ตัวแปรที่ต้องคุม (สำคัญที่สุด — นี่คือหัวใจของงาน)

ทุกอย่างเหมือนกันหมด **ยกเว้นไฟล์เสียงเชลโล**:

| ตัวแปร | ค่าที่ต้องล็อก |
|---|---|
| เพลง/ท่อน | ท่อนเดียวกัน ~15–20 วินาที (เพลงจริงในแอพ เช่น id 1) |
| เปียโน | **Grand `public/samples/splendid-grand` + วาทยกร `src/lib/arranger` (referee.js) preset "บรรเลง" — ของที่ deploy จริงรอบ 27** ⚠️ P'Aim สั่งเอง: "ต้องใช้วาทยกรเปียโนเสียงทองคุม" |
| หน้าที่เชลโล | **สีทำนอง (melody) เส้นเดียว ลากยาว ร้องนำ** · เปียโนคลอ = เสียงคู่เปียโน+เชลโลแบบที่ P'Aim ฟังใน YouTube |
| เรียบเรียง/ความดัง/reverb | เหมือนกันทั้ง 3 คลิป |

**3 ตัวเลือกเชลโล:**
- **A. Karoryfer x bigcat** — `C:\Users\aimkr\Downloads\karoryfer-bigcat.cello-master` (P'Aim โหลดมาให้แล้ว) · **CC0 ยืนยันแล้ว** · `Samples/sus` 136 ไฟล์ 4 ระดับ `p/mp/mf/f` × คันชัก `_d`/`_g` (down/up bow) · `Programs/*legato*.sfz` มีแมป legato ให้ดูเป็นแนว · ⚠️ **อัดมาแบบไม่สั่นนิ้ว (no vibrato)**
- **B. Sonatina (SSO)** — <https://github.com/peastman/sso> · solo cello สเตอริโอ + **มีสั่นนิ้วในตัว** + มี legato · ⚠️ license = CC Sampling Plus 1.0 (CC เลิกใช้แล้ว) → **ทดลองในเครื่องได้ แต่ห้าม commit ไฟล์ sample เข้า repo** จนกว่า P'Aim จะเลือกตัวนี้แล้วค่อยเคลียร์ลิขสิทธิ์
- **C. Iowa เดิม (ของเปรียบเทียบ)** — `public/samples/CC0/cello/*.ogg` บน branch `claude/charming-cori-25514b` · = ตัวที่เคยโดนตีว่า "หวูดเรือ" ต้องมีไว้เป็นหลักเทียบ

## กติกาเทคนิค (กันพลาดซ้ำของเก่า)

1. **smplr velRange gotcha** (memory `pleng-smplr-vellayer-relative`): อย่ายัด p/mp/mf/f เป็น velRange groups ในตัวเดียว → ความดังจะรีเซ็ตที่รอยต่อชั้น · ให้โหลด **แยกเป็น instrument ชั้นละตัว** (velRange เต็ม) แล้วคุมดังด้วย gain
2. **รอบนี้เอาแบบดิบ ๆ ตรงไปตรงมา** — ยังไม่ต้องประดิษฐ์ vibrato/swell/crossfade อัจฉริยะ (นั่นคือกับดักที่ทำให้ 2 รอบก่อนพัง) · เป้าคือให้ P'Aim ฟัง **เนื้อเสียงวัตถุดิบ** ว่ามีทางไปต่อไหม
3. **อย่าแตะทางที่ deploy แล้ว** — ห้ามแก้ `midi.js` / `audioExport.js` / SongView ที่ใช้จริง · โค้ด spike แยกไฟล์
4. **honest to the sheet** (memory `feedback-audio-honest-to-sheet`): เชลโลเล่นโน้ตตามแผ่นจริง · ถ้าโน้ตหลุดช่วงเสียงเชลโลจนต้องย้าย octave → **รายงาน อย่าเงียบ ๆ ย้ายเอง**
5. อย่า commit ไฟล์ wav ดิบ 155MB เข้า repo · แปลง/ตัดเป็น ogg/mp3 แบบ `tools/prepare-samples-cc0.mjs` ทำอยู่

## ส่งมอบ

1. **หน้าเว็บเทียบ 1 หน้า** — ปุ่มเล่น 3 คลิป (A/B/C) สลับฟังไปมาได้ทันที + ป้ายบอกว่าตัวไหนเป็นตัวไหน
2. **ไฟล์ MP3 3 ไฟล์ให้โหลดฟังในมือถือ/หูฟังได้**
3. เปิด server ด้วย `--host` + **ใส่ Network URL (`http://<IP>:<port>`) ในรายงาน** (P'Aim ฟังบนมือถือ)
4. รายงาน `docs/reports/cello-bakeoff.md` — ทำอะไร · ตัวไหนใช้ sample อะไร · ข้อจำกัดที่เจอ · **ถ้ามีอะไรที่หูต้องตัดสิน ให้ list ไว้ อย่าตัดสินใจแทน**
5. เพิ่มบรรทัดใน `docs/pm/board.md` §📥 PM inbox + ping PM session ปัจจุบัน (ดู `board.md` §🎯)

## นอก scope (อย่าทำ)

- ทำให้เชลโลเพราะ · เพิ่มลูกเล่น · เอาเข้า SongView · แตะ preset ที่ deploy แล้ว
- **ตัดสินเองว่าตัวไหนดี** ← หูของ P'Aim เท่านั้น
