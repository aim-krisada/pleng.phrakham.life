# Brief — กอง C: เส้นโยงวาดผิด (slur ผิดห้อง + เส้นใต้ควรต่อ/แยก)

**ฐาน `studio-shell-redesign`** · worktree + port ของตัวเอง · **⛔ refine ไม่ใช่ redesign**

## ทำไม 2 ใบนี้ต้องอยู่สายเดียวกัน

พี่เปาแจ้งคนละใบ **แต่มันคือระบบวาดเส้นโยงตัวเดียวกัน** (`notation.js` + `NoteRow`/`SongSheet`) → **แยกทำ = แก้ทับกันแน่นอน**
⛔ **ห้ามแตะ `EditorMode.vue`** — สาย `task_6a1a7e2a` ถืออยู่ (**PM เพิ่งพลาดเรื่องนี้กับสายเชลโล 2 สายทำ vibrato ทับกัน — จะไม่ให้เกิดซ้ำ**)

---

## C1 — issue 5: **slur โชว์ผิดห้อง (บั๊กจริง)**

> พี่เปา: *"พิมพ์เครื่องหมาย slur **ถูกต้อง** แต่อยู่คนละห้อง แต่**หน้าแผ่นเพลงโชว์เครื่องหมายผิด**"*

**ต้นฉบับ:** `OneDrive/4 Personal/pleng.phrakham.life/pleng2-pow-bug-report/issues5/` (มีภาพวงกลมจุดที่ผิด)
**= ข้อมูลถูก แต่การวาดผิด** → **ห้ามแก้ข้อมูล ให้แก้ตัววาด** (memory `pleng-b068-data-vs-b062-render-lane`: DA=ข้อมูล · render=วาด **อย่าข้ามเลน**)
**เริ่มที่:** `src/lib/notation.js` (`notation.slur.test.js` มีอยู่แล้ว) · `NoteRow.vue` / `SongSheet.vue`
**ต้องได้:** **เทสต์ที่ล้มก่อนแก้ แล้วผ่านหลังแก้** (เคสข้าม-ห้าม) — ไม่ใช่แค่ "ดูแล้วหาย"

---

## C2 — "issue 8": **พี่เปาไม่ได้แจ้งบั๊ก — เขาเสนอกฎแล้วขอให้เราตรวจกับมาตรฐาน**

> พี่เปา: *"อยากให้วิเคราะห์จากข้อมูลที่มีทั้งหมดว่า **ถ้าจะใช้กฎว่า ถ้ามีช่องว่างต่อ ให้ขีดเส้นเดียวต่อเนื่อง · ถ้ามีแยกคำ ให้ขีดแต่ละโน้ต จะมีปัญหาอะไรไหม ตรงกับมาตรฐานไหม**"*

**ต้นฉบับ + ภาพ 3 ใบที่เขาอธิบายเคสเอง:** `pleng2-pow-bug-report/issue 8/` (ชื่อไฟล์คือคำอธิบายของเขา — **อ่านชื่อไฟล์ด้วย**)
- *"ควรเป็นเส้นเดียวกันถูกแล้วเพราะข้างๆ ไม่มีคำ"*
- *"ควรแยกขีดเส้นใต้ - เส้นไม่ต่อกัน เพราะมี 2 คำ อา+ภรณ์"*
- *"ควรแยกขีดเส้นใต้ - เส้นไม่ต่อกัน เพราะมี 2 คำ ไม่+เคย"*

**⭐ เขาถามคำถาม → ต้องตอบเขา ไม่ใช่แก้เงียบ ๆ:**
1. **กฎที่เขาเสนอ ตรงมาตรฐานสากลไหม** (beaming ผูกกับพยางค์: 1 พยางค์/หลายโน้ต = คานเชื่อม · คนละพยางค์ = แยกคาน) — **verify กับมาตรฐานจริง อย่าตอบจากความจำ**
2. **"จะมีปัญหาอะไรไหม"** → **รันกับเพลงจริงทั้งคลัง นับว่าเพลงไหนเปลี่ยนบ้าง กี่จุด** (ห้ามเดา)
3. **ถ้ากฎเขาไม่ครบ/ชนเคสอื่น → บอกตรง ๆ พร้อมตัวอย่าง** · **ถ้าถูกแล้ว → บอกว่าถูก แล้วทำตาม**

**บริบทที่ต้องรู้ (memory `pleng-render-data-gaps`):** ระบบเรา **ไม่ได้ encode ties/slurs ไว้เลย (0/120 เพลง)** และ B062 (วาด slur) เคยถูกพักไว้รอ DA encode ก่อน → **เช็กก่อนว่าเรื่องนี้ยังจริงไหม ถ้าจริง = C2 อาจติดที่ข้อมูล ไม่ใช่การวาด → รายงาน PM อย่าดันต่อ**
**และ:** เอื้อน/beaming เคยแก้ไปแล้วรอบ 22 (`สถานะบั๊ก/3-เสร็จแล้ว/issue2 - เอื้อน beaming`) → **อ่านของเดิมก่อน อย่าแก้ทับ**

---

## กติกา
- **ตอบคำถามพี่เปาเป็นภาษาคน** (เขาอ่านผ่านตารางสถานะ) — **โดยเฉพาะ C2 เขาถามตรง ๆ ว่า "ตรงมาตรฐานไหม"**
- **มาตรฐานสากลเป็นค่าเริ่มต้น** — P'Aim: *"สุดท้ายทุกอย่างต้องถูกต้องตามทฤษฎีดนตรี จำเป็นต้องแก้ก็ต้องแก้"*
- ⛔ **ห้ามแตะ `EditorMode.vue`** (สายอื่นถืออยู่) · ⛔ ห้ามแก้ข้อมูลเพลงเพื่อให้ภาพออกถูก (แก้ตัววาด)
- **verify ผลลัพธ์ ไม่ใช่การกระทำ** — PM เดาผิด 6 ครั้งวันนี้เพราะเชื่อชื่อ/คอมเมนต์โดยไม่รันจริง → **เปิดของจริง นับจริง · ค้าน PM ได้และควรค้าน**
- **พิสูจน์ด้วยแผ่นจริง ไม่ใช่ DOM** (memory `feedback_verify_print_from_pdf`) · ทดสอบ 360px จริง

## ส่งมอบ
`docs/reports/notation-render-bugs.md` — **ต้นเหตุจริง · แก้ยังไง · เพลงที่ได้รับผลกระทบกี่เพลง (นับจริง) · คำตอบถึงพี่เปาเรื่องมาตรฐาน** + Network URL จริง (เช็ก vite line · IP เปลี่ยนบ่อย) · เพิ่มบรรทัดใน `docs/pm/board.md` §📥 PM inbox + ping PM session ปัจจุบัน (ดู `board.md` §🎯 · **อย่า hardcode ชื่อ PM session**)
