# HANDOFF — pleng.phrakham.life (2026-07-18 ค่ำ)

เอกสารส่งมอบงานแบบครบจบในตัว — อ่านไฟล์เดียวนี้แล้วทำต่อได้ทันที (คนหรือ AI ใดก็ได้)
บริบทเต็มเพิ่มเติม: `docs/pm/board.md` · `docs/pm/pm.md` · `docs/sop.md`

---

## 0. สถานะสำคัญที่สุด (อ่านก่อน)

**LIVE ตอนนี้ = เวอร์ชันนิ่ง "รอบ 30" (`2f4177e`, 17 ก.ค.)** — ถูก rollback แล้ววันนี้
- P'Aim สั่งทิ้งงาน editor ทั้งหมดของวันที่ 18 ก.ค. (dock-space redesign, contextual toolbox, free-form resize, B109 keyboard) เพราะสร้างปัญหาซ้ำ → deploy เวอร์ชันก่อนหน้าที่นิ่งขึ้นแทน
- **verify prod แล้ว:** live bundle `assets/index-C3C3fafV.js` มี build-stamp `2f4177e` · ไม่มี marker `dock-space`/`dk-resize`/`auto-hide` · index+bundle HTTP 200 · หน้าเพลงขึ้นครบ
- editor บน live = เวอร์ชันเรียบง่ายเดิม (ปุ่ม copy/ลบ/octave inline, ไม่มีแถบลอย, ไม่มี resize)

**งานวันนี้ที่ทิ้ง = ยังอยู่ใน git ครบ (recoverable)** — ไม่ได้หายถาวร

---

## 1. Git / Deploy state

- **`origin/main` = `2f4177e`** (round 30) → deploy อัตโนมัติผ่าน `.github/workflows/deploy.yml` (push main → GH Pages) ✅ live แล้ว
- **base branch `studio-shell-redesign` = `64ec8ef` + PM docs commits** (ยังมีงานวันนี้ทั้งหมด) — **งานที่ทิ้งกู้จากที่นี่ได้**
- วิธี rollback ที่ใช้: `git push --force origin 2f4177e:main` (จาก dir `pleng.phrakham.life-pm`)
- **⚠️ redesign รอบใหม่ควร branch จาก `2f4177e` (เวอร์ชันนิ่ง) ไม่ใช่จาก `64ec8ef`** (กันสร้างทับของที่ทิ้ง)
- งานวันนี้ที่กู้ได้: dock-space engine + contextual toolbox + resize (`e5c0a08`/`d1eeb8a`) + B109 keyboard (`bb18c08`,`54c3fed`,`499d8d3`) + 344-fix (`2e238f0`) + full song names (`ce1022e`) + phase-1 "ห้ามเลือดไหล" (`38cd50b`)

## 2. Supabase / DB state (สำคัญต่อความปลอดภัย)

- **รันแล้ว + verified live:** `db/005-verified-read-rls.sql` (RLS: คนนอก/anon เห็นเฉพาะเพลง verified=32 · ทีมล็อกอินเห็นหมด=126) — P'Aim รันเอง
- **ยังไม่รัน:** `db/008-notifications.sql` (ระฆังเตือน · รอ UI), `db/009-security-hardening.sql` (F1 editor self-approve audit-forge + F2/F3)
- **ทิ้งถาวร (P'Aim สั่ง · ไม่เคยรัน):** `db/006`/`db/007` author_id (ไม่โชว์เครดิตคนแต่ง)
- **round 30 frontend compatible กับ db/005:** โค้ด DB (`supabase.js`/`store.js`/`songSearch.js`) diff รอบ30..ตอนนี้ = 0 บรรทัด · RLS แค่กรองแถวไม่แตะ schema → ถอยโค้ดไม่กระทบ

## 3. Sessions / ทีม (roster 3 ที่นั่งถาวร + dev/tester ต่องาน)

| บทบาท | session id | dir | สถานะ 18 ก.ค. ค่ำ |
|---|---|---|---|
| PM (นี่) | pm28 | `pleng.phrakham.life-pm` | ส่งมอบ |
| SA | `local_6904d4be` | `pleng.phrakham.life-sa` (`sa-standing`) | **PAUSE** ขั้น1 editor-redesign-problem · งาน design จบหมด · security-audit+system-map เสร็จ |
| UX/UI | `local_284b3dbe` | `pleng.phrakham.life-uxui` (`uxui-standing`) | idle |
| dev | `local_e20ebbbd` | worktree `laughing-hugle` | **STAND DOWN** (ทิ้ง `editor-rollback`) |
| tester | `local_9f001e0f` | worktree `compassionate-poincare` | resize GATE เสร็จ (moot) |
| 🎼 music | `local_278e3228` | `pleng.phrakham.life-music` (`music-standing`) | cello spike ค้าง · รอหู P'Aim · **แยกสาย ไม่กระทบ deploy** |
| 📖 pk-PM (พระคำ) | `local_43cdd55a` | `phrakham.life2-pm` | คนละโปรเจกต์ · DockKey 2-host |

## 4. บทเรียนวันนี้ (ต้องรู้ก่อนทำต่อ — กันพลาดซ้ำ)

1. **ออกแบบให้จบก่อนสร้าง · ใช้ทีม SA/UX/UI ตามขั้น — อย่าสั่ง dev ปะทีละจุด** (นี่คือรากของหายนะวันนี้ · P'Aim ตำหนิตรง)
2. **`@media(hover:hover)` = กับดัก:** เครื่อง P'Aim (Surface จอสัมผัส) รายงาน `hover:none · pointer:coarse · any-hover:false · any-pointer:fine:false` แม้ต่อเมาส์ → element ใน `@media(hover)` = `display:none` มองไม่เห็นบนเครื่องเขา · **ห้าม gate ฟีเจอร์ pointer ด้วย hover/pointer-fine** (memory `feedback_verify_hover_on_real_browser`)
3. **verify บนเครื่อง/สถานการณ์จริงของผู้ใช้ก่อนส่ง** — pane/headless ทุกตัวรายงาน hover:none → พิสูจน์ discoverability ไม่ได้ · เช็ก computed `display`/`matchMedia` บนเบราว์เซอร์จริง (claude-in-chrome) แต่แฟลกกี้
4. **อย่าเอาของครึ่งๆ ให้ P'Aim ลอง** — เขาไม่ใช่ tester · ส่งเฉพาะของเสร็จ+ยืนยันแล้ว ครั้งเดียว
5. **แถบลอย/overlay ห้ามทับพื้นที่แก้ไข** (toolbox บังคอร์ด = P'Aim สั่งเอาออก)

## 5. งานค้าง / ทิศต่อไป

**A. Redesign editor (งานหลัก · รอ P'Aim พร้อม · เริ่มใหม่แบบวางแผนก่อน)**
- โจทย์ราก: แถบเครื่องมือ/dock กินพื้นที่แย่งเนื้อเพลง + แก้ไขบนจอสัมผัสยาก
- แผน 4 ขั้น: (0✅ deploy รอบ30) → (1) SA สรุปโจทย์+ข้อจำกัด (device matrix: Surface touch/มือถือ/Fold) → P'Aim รีวิว → (2) UX/UI ออกแบบภาพเดียวบนโค้ดจริง + SA feasibility → P'Aim เคาะ → (3) build ครั้งเดียว → tester → verify บนเครื่อง P'Aim จริง → deploy
- constraint: touch≥44px · Fold 344 · ห้าม overlay ทับเนื้อหา · 2-host (DockKey แชร์พระคำ)

**B. เฟอร์มาต้า (feature ที่ P'Aim กำลังจะคุยกับ Gemini — ดู §6)**

**C. ค้างอื่น (secondary):** B108 ระฆังเตือน (design+backend พร้อม · db/008 รอรัน) · security db/009 · verify-batch 94 เพลง unverified

## 6. เฟอร์มาต้า → Gemini (task ที่ค้างอยู่ · P'Aim จะไปใช้ Gemini)

**ที่มา:** บทถอดเสียง `C:\Users\aimkr\Downloads\1.เฟอร์มาต้า_original.txt` (UTF-16 · P'Aim ↔ พี่เปา)

**สรุปโจทย์ 4 ข้อ:**
1. ปัญหา: สัญลักษณ์เฟอร์มาต้าใส่ได้ แต่กดเล่นแล้วจังหวะเพี้ยน ห้องถัดไปหลุด ("น่าทิ่ม")
2. เฟอร์มาต้า = ลากยาวตามใจคนเล่น ไม่มีจังหวะตายตัว
3. ทางออก: ใส่สัญลักษณ์ → auto-แนะนำค่าลากยาว (จากช่องว่างในห้อง) → แก้เองได้ → playback ผูกค่านี้ ลงห้องตรง
4. แสดงผล: หน้าแก้ไข = โชว์ค่า · แผ่นพิมพ์ = โชว์แค่สัญลักษณ์ (ไม่โชว์ตัวเลข)

**เจอในโค้ด:** ปัจจุบันเฟอร์มาต้า = boolean · playback คูณ `FERMATA_FACTOR=1.75` คงที่ (`src/lib/midi.js`) = ต้นเหตุที่เพี้ยน · ต้องเปลี่ยนเป็นเก็บค่า hold รายตัว (auto+แก้ได้) = แก้ data model

**บท Gemini (paste ได้ · โฟกัส: ตรวจความถูกต้องดนตรี + ออกแบบ UI ให้ง่ายสำหรับคนไม่เป็นดนตรี):**
> บริบท: เว็บโน้ตเพลงนมัสการไทย (โน้ตตัวเลข+คอร์ด) คนใช้=อาสาสมัครโบสถ์ ไม่ใช่นักดนตรี ใช้มือถือ มีปุ่มเล่นเสียง · เฟอร์มาต้าตอนนี้เก็บเป็น boolean คูณ 1.75 คงที่ → เสียงลากไม่ตรงใจ+ห้องถัดไปหลุด
> (A) ตรวจถูกต้อง: 1) โปรแกรมมาตรฐาน (MuseScore/Sibelius/Finale) จัดการระยะเวลาเล่นเฟอร์มาต้าอย่างไร คงที่/แก้ได้? 2) ควรเก็บค่า hold เป็นอะไร (จังหวะ/ตัวคูณ/เติมจนจบห้อง)? 3) ถูกไหมที่แผ่นพิมพ์โชว์แค่สัญลักษณ์ ไม่โชว์ตัวเลข?
> (B) ออกแบบ UI ให้คนไม่เป็นดนตรีใช้ง่ายสุด (มือถือ): ประเมิน+จัดอันดับ 4 แนวทาง — ก.ลากยืดเห็นภาพ ข.สั้น/กลาง/ยาว ค.ค่าแนะนำ+▲▼ ง.ฟังแล้วปรับ · ความเห็นเริ่มต้น: ผสม ก+ง (ลากยืด+ฟังทันที) น่าจะเป็นธรรมชาติสุด — ช่วยตรวจ+หาช่องโหว่

**meeting-room (ให้ Claude/successor ขับ Gemini เอง):** `C:\gl\krisada\ceo\tools\meeting-room` · ต้องรัน `py meeting_cli.py launch` (Chromium CDP 9222) + เปิดแท็บ Gemini ก่อน · แล้ว `py meeting_cli.py push G "..."` / `pull G` · G-URL เฟอร์มาต้า = `https://gemini.google.com/u/1/app/e492655ece30f5d2` (config ปัจจุบันชี้แชทเก่า ต้องแก้)

## 7. กติกา/บริบทที่ต้องรักษา

- คุยกับ P'Aim = ภาษาคน ม.ต้น · เรียก "พี่เอม"
- ห้าม deploy/merge main จน P'Aim สั่ง · world-class by default (Material/HIG/WCAG)
- board `docs/pm/board.md` = ไม้ต่อสด (อัปเดตทุกครั้งที่สถานะเปลี่ยน)
- memory: `C:\Users\aimkr\.claude\projects\C--gl-krisada-pleng-phrakham-life\memory\` (sync → OneDrive)
- 1 ไฟล์ = 1 สาย · PM จัดคิว · 2-host: `DockKey.vue` แชร์กับพระคำ (แก้ engine = test 2 host)
