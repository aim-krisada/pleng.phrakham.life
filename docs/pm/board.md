# PM board — pleng (ไม้ต่อ · refreshed 2026-07-18 · หลัง DEPLOY รอบ 31)

## 🎉 DEPLOY รอบ 31 LIVE `938497a` (18 ก.ค. · P'Aim "deploy 3 เลย")
- **ของที่ขึ้น (3):** 🎛️ **dock-space** (แถบ editor สลิม+หลบตอนเลื่อน+contextual toolbox ◀▶/copy/del/octave+continuity · ผ่าน tester GATE2 · A anchoring+B 44px แก้แล้ว) · 🛡️ **เตือนก่อนทับงาน** (phase-1 ห้ามเลือดไหล `13f0f52`) · 📝 **ชื่อเพลงเต็ม** (2c `ce1022e`)
- **✅ verify prod (ไม่ใช่ push=ขึ้น):** live bundle `index-CgSKljyx.js` มี `938497a` · เก่า `2f4177e` หาย · **เจอ marker dock-space จริง: `auto-hide`·`dk-shift`·`slot-tools`×4** · เทสต์ 736 ผ่าน (notationLint process.exit = pre-existing) · build ✓
- **git:** base `studio-shell-redesign` = origin/main = `938497a`
- **🎯 พี่เปาตัดสินจากการใช้จริง** บนมือถือ (editor ที่ใช้ทุกวัน · ไม่ต้อง LAN) — 3 จุดยืนยัน: keyboard-aware · narrow 344/390 · ◀▶ syllable
- **🔴 2-host phrakham:** DockKey engine เปลี่ยน → พระคำ island ต้อง rebuild `pk-dock-island.js` ตอนพระคำ deploy รอบหน้า (autoHide default false = ตอนนี้ไม่ regress · แจ้ง pk-PM แล้ว)
- **🔴🔴 ESCALATION 18 ก.ค. (หลัง deploy): พี่เปา "ไม่ชอบ dock-space" — อยากได้ free-form resize ที่ PM pivot ทิ้งไป** · P'Aim: "PM ต้องคุยกับพี่เปา · ทำ shortcut ด่วน กู้หน้า" · **บทเรียน (memory `feedback_deliver_user_concrete_want`): user จริงขอของรูปธรรม = ทำให้ · อย่าเอา expert-ว่าดีกว่า ไปแทน · resize "ทำได้" (mockup เคยใช้ได้ + engine reflow deploy แล้ว) ไม่ใช่ "ทำไม่ได้" — relay ให้ตรง**
  - **PM recovery — P'Aim "แก้ทุกจุด ระมัดระวัง · shortcut+resize+ล้นจอ คู่ขนาน" → 3 สาย (คนละไฟล์ ไม่ชน):**
    - **สาย 1 · B109 shortcut** (`EditorMode.vue`) = dev เดิม · ✅ **logic เสร็จ (`bb18c08` branch `b109-keyboard` · 743 tests · live smoke Ctrl+→ ✅):** Ctrl+←/→=ห้อง(wrap) · Ctrl+↑/↓=บรรทัด · Tab=โน้ต/พยางค์ · Home/End+Ctrl+Home/End · **Enter=คอร์ด(allow-custom) · Esc=wrapper** · nav preventDefault · Tab-out ที่ขอบ(a11y) · Mac=Tab หลัก · fn=`jumpNote/jumpBar/jumpLine` · **branch free → UX ปุ่มบนจอ+คู่มือ (baton สะอาด)** · ✅ Home/End = UX ฟันธง (ตัด plain · เหลือ Ctrl+Home/End · native caret · US §2 แก้) → dev apply
    - ✅ **UX เฟส A เสร็จ (`54c3fed`):** ปุ่ม nav (◀▶⏮⏭▲▼) เรียก jump* + @mousedown.prevent · **ใช้ `kind:'slot'`+`#cell-nav` (ไม่ใช่ btn ที่ PM บอก — ดีกว่า: ไม่แตะ DockKey เลย+group/aria+ไม่ล้น 344 · git diff=EditorMode+Guide) = PM รับ** · คู่มือ Guide §② · verify device-matrix (344/375 nav แถวตัวเอง·44×44·no h-scroll · desktop merge row1)
    - 🟡 **tester GATE2 B109 = CONDITIONAL (`95a4cba` · เทสเพลงหลายห้อง/บรรทัดเอง):** ✅ PASS: ปุ่ม nav 44×44·aria·no-hscroll · **jumpBar wrap ข้ามบรรทัด (0-1→1-0)** · jumpLine edge-guard · jumpNote · desktop keys (Tab/Ctrl+arrows/Ctrl+Home-End) · **ลูกศรเปล่า=native ไม่ hijack** · **toolbox re-anchor gap 0 หลัง jump** · คู่มือ §② ครบ · 🟡 **ค้าง 1: Home/End override caret** (`54c3fed` ยังไม่มี dev fix — branch ถูก lock) → **เคาะ: apply dev batch (native caret · ctrl-only) ไม่รับ override** → dev commit → re-verify Home/End → clean · **3 ข้อรอพี่เปามือถือ (Enter/Esc คอร์ด · nav เหนือ keyboard · narrow 344/390)** ไม่บล็อก GATE
    - ✅ **dev commit Home/End fix (`499d8d3`):** Ctrl-only (plain=native caret คืน · Ctrl+Home/End=song-edge) + test + anchor-follow · 744 tests · **→ tester re-verify Home/End (จ่ายแล้ว) → clean GATE → merge → พี่เปา** · 3 ข้อ mobile (Enter/Esc·nav-เหนือ-keyboard·narrow) = พี่เปามือถือ · **โบนัส: toolbox anchor ตาม jump เอง (jsdom-verified)** · **🔴 กันชน 2 สาย: ปุ่ม nav = `editItems` (EditorMode/B109 lane · kind btn · pleng-only) ⛔ ไม่แตะ DockKey.vue (สาย2) · PM verify integration ตอนรวม (ทั้งคู่กระทบ dock render)**
    - **สาย 2 · 344-overflow fix + free-form resize** (`DockKey.vue` · 2-host) = dev ใหม่ (`task_7533e1a5`) · 🔴 **344-fix ก่อน (LIVE regress · block พระคำ · pk PM วัด @344 dock 367 overflow +23 · กระทบ pleng@344 ด้วย)** → free-form resize (แบบ 2 · สิ่งพี่เปาอยากได้)
    - **pk PM:** เลือกรอ DockKey cap fix (แก้ราก · P'Aim สั่งตรง) · block พระคำ deploy ไว้ · PM ping ตอน fix พร้อม → pk rebuild island+gate 344/690/768 tree จริง → deploy Acts 26
    - (3) ข้อความขอโทษ→พี่เปา (P'Aim relay) · **บทเรียน `feedback_deliver_user_concrete_want`**
- **▶ B109 shortcut + คู่มือ editor = BUILD (P'Aim สั่ง · เร่งด่วนหลัง escalation):** branch จาก base `938497a` · **ไม่ 2-host (nav = editor ไม่ใช่ DockKey)** · **baton: dev logic (jumpBar/jumpLine + Enter=allow-custom + Esc wrapper + keydown preventDefault · reuse slotStarts/onUndoKeys) → free → UX ปุ่มบนจอ(editor-side ◀▶⏮⏭▲▼)+คู่มือ (เฟส A พี่เปาได้ก่อน)** → tester GATE2 → พี่เปา/deploy · สเปก `docs/us/editor-keyboard-nav.md` · music เชลโล คู่ขนาน

---
_(ประวัติ HAND-OFF + งานก่อน deploy รอบ 31 ด้านล่าง)_

# (เดิม) PM board — refreshed หลัง DEPLOY รอบ 30 + ตั้งโครง 3 ที่นั่งถาวร

กระดานนี้ = สถานะสด + งานค้าง + routing เท่านั้น · **รายละเอียดเทคนิค → git log + `docs/reports/<branch>.md`**
**⛔ เปิด PM session ใน worktree `C:\gl\krisada\pleng.phrakham.life-pm` เท่านั้น** · **ตั้งชื่อ session ถัดไป = `pm27`**
**เปิด PM session ใหม่:** `docs/pm/pm.md` → **`docs/sop.md` (อ่าน §1.1-1.3 roster + §6 anti-patterns ก่อน)** → memory `pleng-pm-role` + `pleng-roster-3-seats` → ไฟล์นี้ · IP เปลี่ยนบ่อย (เช็ก vite Network line ก่อนส่ง URL · ปัจจุบัน `192.168.1.124`)

## 🔑 HAND-OFF (18 ก.ค. · session เต็ม → pm27) — อ่านก่อนทำต่อ
**โครงทีมใหม่ (P'Aim 17 ก.ค. · `docs/sop.md §1.1`):** 3 ที่นั่งคิดถาวร + Dev/Tester ต่องาน + **Music Composer (สาย 4 คู่ขนาน · P'Aim 18 ก.ค.)**
- **PM** = `pleng.phrakham.life-pm` (นี่) · **SA** = `pleng.phrakham.life-sa` (`sa.md` · session `pl sa`) · **UX/UI** = `pleng.phrakham.life-uxui` (`uxui.md` · session `pl uxui`)
- **🎼 Music Composer** = `pleng.phrakham.life-music` · branch `music-standing` (fork `cello-vib-arc`) · docs `docs/music/composer.md`+`board.md` · session `🎼 pl music` (`local_278e3228`) · **เสียงล้วน · spike `src/spikes/` · ⛔ ไม่แตะ deploy-code/ไม่ merge/ไม่ deploy จน P'Aim สั่ง = ไม่ชน dev/UX/SA** · ping PM ตอนมี milestone ให้ P'Aim ฟัง · สถานะ: 🎹 เปียโนทองจบ(prod) · 🎻 เชลโลค้าง 5.3 (marcato+normalize+vibrato · รอหู P'Aim · sound source ย้าย OneDrive `sfz/` rebuild ก่อนฟัง)
- **P'Aim เปิด window เองในแต่ละ dir** (spawn_task สร้าง worktree ทิ้ง ไม่เกาะ dir — อย่า spawn ที่นั่งถาวร) · **1 ไฟล์ 1 สาย PM จัดคิว** · ยกรายงานเข้าฐานทันที

**🔴 P'Aim decision ค้าง (session หน้าถาม/รอ):**
1. **RLS leak → ✅✅ ปิดแล้ว + verified LIVE (18 ก.ค. · P'Aim รันเอง)** — pre-flight `public_after=32 / hidden=94 / total=126` · ก้าว 2 "Success" · **ก้าว 3 `set role anon` = 32** ตรง public_after เป๊ะ = คนนอกเห็นเฉพาะ verified จริง · **ช่องโหว่ปิดสนิท** · policy สด: "Public reads verified songs"(anon) + "Authenticated reads all songs"(team) · SQL `db/005-verified-read-rls.sql` อยู่ในฐาน · rollback อยู่ท้ายไฟล์ · SA batch-readiness (clean 22/tiny 11) = backlog พี่เปาทยอยตรวจเอง (เพลงโผล่เมื่อกด ✓ · ไม่ block)
   - 🔔 **B108 ระฆัง = ศูนย์แจ้งเตือนตามบทบาท (design+arch เสร็จ · banked หลัง dock-config):** UX flow+mockup (`a0b6c22`) + SA ฟันธง notifications table v1 (`fa5a329`) เข้า inbox แล้ว · **PM ยังไม่ GATE0-verify mockup · ยังไม่ส่ง P'Aim** · `docs/backlog.md B108`
2. **dock-config GATE 1:** P'Aim ลอง mockup https://claude.ai/code/artifact/555c023e-4bdb-46b4-a894-05509fbd559d → เคาะทิศ → UX ทำ DS ส่ง dev · **DockKey แชร์ 2 เว็บจริง (แก้ engine=2 เว็บ · regression พระคำต้อง test 2 host)**
3. **toolbox (hover/tap contextual) = banked** หลัง dock-config · mockup https://claude.ai/code/artifact/234933b6... · เฟส A/B รอ P'Aim

**🎯 PM session ปัจจุบัน = `pm27` (18 ก.ค.)** — สายอื่น ping/รายงานกลับที่นี่ · ครั้งหน้า `pm28`

**🔔 จ่ายแล้ว 18 ก.ค.: B108 ระฆังเตือน → UX นำ + SA feasibility คู่** (send_message ทั้ง 2 สาย · design ก่อน · build ต่อคิวหลัง dock-config · ระวังชนไฟล์ shell/IdentityStrip กับ orientation 2a)

**📐 มาตรฐานใหม่ผูกแล้ว 18 ก.ค.: `docs/ux-platform-patterns.md`** (UX เขียน `3f94ce9` · P'Aim สั่งเพราะเบื่อพูดซ้ำ) — touch≠pointer (ตารางแปลง desktop→mobile) + เช็คของเดิมก่อนออกแบบ (DELTA only) · **PM ผูกเข้าฐาน:** `ui-standards.md §1(ข้อ6)+§2` · `sop.md §1.3+roster` = **binding ทุก US/DS/mockup ของ UX**

**🔴🔴 ESCALATION 18 ก.ค. (P'Aim "จะไล่ออก/เลิกซื้อคอส") → SOP §3.0 HYBRID:** เลิก throwaway HTML mockup · **งาน component = แยกหน้าตา/ตรรกะ บนโค้ดจริง branch เดียว:** **หน้าตา (template/CSS/layout/responsive) = UX ทำเองบนโค้ดจริงฝั่งแอปไม่แชร์** (`EditorMode` template · `editItems`) · **ตรรกะ+engine แชร์ (`DockKey.vue` 2-host) = dev** · UX+dev = ทีมเดียว 1 คิว (PM สลับ template↔script) · สเปก = "สัญญาพฤติกรรม" · → tester gate → P'Aim ลองแอปจริงครบชิ้นเดียว · +DoR §3.0.1 + เคาะทิศ 1 หน้า §3.0.2 · memory `feedback_no_throwaway_mockups_spec_on_real_code` (hybrid) · **UX ไม่ docs-only แล้ว** · ✅ **UX รับโมเดลเต็ม · ส่งสเปก `docs/ds/dock-space-spec.md` (`cd4efe9`) map โค้ดจริง 1:1 · git-verify: UX ถือ docs-only จริง (src ไม่แตะ)** · hold build
   - 🚩 **PM git-verify เจอ: `uxui-standing` ล้าหลังฐาน** — base นำ `38cd50b`(phase-1 แก้ EditorMode.vue) + `ce1022e`(2c SongList) → **สเปกอ้าง EditorMode เวอร์ชันเก่า (เลขบรรทัด :2994/:1724/:139/404/157 อาจไม่ตรง base)** → สั่ง UX re-verify code-ref vs base ปัจจุบัน (read-only) · **dev build ต้อง branch จาก `studio-shell-redesign` ปัจจุบัน ไม่ใช่ uxui-standing** (กัน revert phase-1+2c · SOP §5)
   - 🏗️ **สถาปัตย์ (UX ยืนยันจากโค้ดจริง):** slim=แก้ `editItems.default` (data-only ไม่แตะ engine ไม่กระทบพระคำ) · hide-scroll/width-cap/keyboard/SR=engine (2-host) · contextual toolbox=EditorMode · pin/reorder/collapse มีใน DockKey แล้ว
   - ✅ **UX แก้ดริฟต์แล้ว (`6c52760`) — re-map ทุกเลขบรรทัด vs base:** editItems 1824 · `<DockKey>` 3094 · slot-tools 2909 · togglePin 138 · `--touch-min` 516 · **เจอเพิ่ม: อย่าแตะ `save` (phase-1 D2 reword) · width-cap ต้อง reflow ไม่ย่อ target (`:616` cascade) · dev branch จาก base**
   - ✅ **PM DoR gate (§3.0.1) = ผ่าน:** `docs/ds/dock-space-spec.md` ครบทุกปุ่ม(§1)/สถานะ/device-matrix(§7)/a11y(§6)/continuity + map ชื่อจริง 1:1 base + ไฟล์+2-host+เฟส(§8)+exists-vs-new(§0 "5/7 มีเชื้อแล้ว") · **เหลือก่อน build = SA ตอบ 5 คำถาม feasibility (§9)** — hold จนกว่า P'Aim เคาะทิศ (ไม่ลงแรงก่อน)
   - 🟢 **P'Aim เคาะ "เดิน dock-space" (18 ก.ค.) — เดินเครื่องแล้ว** · direction summary `docs/pm/summary-dock-space-direction.md` · **P'Aim: PM สั่งเปิด dev เองได้ ไม่ต้องให้ P'Aim เปิด window**
     - **SA:** ✅ feasibility ตอบครบ (`7f28aa1` · ดู inbox) = dev unblock
     - **UX:** เริ่มฝั่งหน้าตา — branch ใหม่จาก base `dock-space-ux` · `editItems` slim + toolbox template/CSS บน `EditorMode.vue` (ไม่แตะ DockKey/save)
     - **dev:** 🟢 launched (`local_e20ebbbd` · worktree `laughing-hugle` · branch `claude/gifted-payne-d17da3`) — scope = logic + engine `DockKey.vue` (2-host) · **⚠️ เตือนแล้ว: ยืนยัน base=studio-shell-redesign ไม่ใช่ main (worktree auto)** · relay SA feasibility + go phase-A แล้ว
     - **sequencing:** UX presentation pass (EditorMode) ก่อน → dev script pass (กันชน) · DockKey.vue = dev คู่ขนาน (คนละไฟล์) · PM คุมคิว
     - **UX presentation pass #1 ✅ (`10a3207` · branch `dock-space-ux` จากฐาน):** playAll→⚙ slim · verify แอปจริง (715 tests · Fold 344 · hOverflow 0 · URL `http://10.189.195.98:5390/#/studio`) · **ชนขอบเขต (ตามคาด · dev-heavy):** soundctl/export=`kind:slot` → dk-panel(:404) ไม่ render slot → **ต้อง dev เพิ่ม slot-in-panel ก่อน (engine 2-host)** · contextual toolbox + hide-scroll = dev
     - **🔀 คิวคู่ขนาน (คนละไฟล์ ไม่ชน · PM จัด):** dev=`DockKey.vue` (🥇 slot-in-panel unblock UX → hide-scroll/cap/keyboard) · UX=`EditorMode.vue` toolbox template/CSS (ยังไม่ wire) · **dev rebase ต่อ `dock-space-ux`** · EditorMode script wiring = dev หลัง UX template (PM สลับกันชน)
     - **dev prep เสร็จ + phase-A plan เคาะแล้ว (`3ff46bd` · branch `dock-space-dev` จาก base `fdd233f` ✓):** prop `auto-hide` default false(พระคำปลอดภัย) · hide-on-scroll ผ่าน `.dk-shift` wrapper(เลี่ยง drag transform :168) · peek handle · a11y toggle engine-owned · Tier-A test · **width-cap=phase C ไม่ใช่ A** · PM relay SA Q1(scroll=window injectable)+Q3(reduced-motion→auto-hide off·⚙ toggle override) → **dev build phase A ได้** (DockKey only · ขนาน UX ไม่ชน)
     - **UX pass #2 ✅ (`4dd99c5` · amended · render-verified จริง):** fit-344 clamp บน `.slot-tools` · **P'Aim ดัน "try again" → UX ขับ editor เข้าสถานะจริง (add note→verse lens→syl-box→focus) วัด max-width 320px@Fold344 · hOverflow 0** · 715 tests · บทเรียน: ห้าม hand-wave "verify ไม่ได้" → ขับแอปเข้าสถานะจริงเสมอ (`feedback_verify_mobile_real_width`)
     - **🔴 contextual toolbox = joint UX+dev pass (ไม่ solo · UX เคาะ):** รวม `slot-tools`(:2911) + `seg-tools`(:2934 · always-visible มือถือ=clutter :4631) → **anchored on-selection toolbox เดียว** (ตัด clutter) · wire `pullSlot/pushSlot/duplicateSegment/removeSegment`+octave(NoteBoxes) · positioning bar/line/section · **verify ด้วย editor data จริง** · **คิว: หลัง dev ปิด phase-A** (dev busy DockKey) · UX เตรียม mini-plan ระหว่างรอ
     - **dev #1 slot-in-⚙-panel ✅ (`1cd032c` · rebase บน dock-space-ux):** dk-panel render `kind:'slot'` + `panelOpenId` (popover ใน ⚙ โดย ⚙ ไม่ปิด) = **ปลดล็อก UX ย้าย soundctl/export เข้า ⚙** · 717 tests · URL `:5313` · ⚠️ edge (tester): slot pinned+in-⚙ = render 2 ที่ (เหมือน pinnable btn เดิม) · dev → #2 hide-on-scroll
     - **integration model:** `dock-space-dev`(DockKey engine) + `dock-space-ux`(EditorMode presentation) = คนละไฟล์ = **PM merge รวม clean** (ไม่ต้อง pull branch กัน · แต่ละ lane ทำของตัว) · render-verify ผสม (soundctl-in-⚙ · toolbox) = ตอน integration/tester มี data จริง
     - **UX lane เสร็จ 3 commits (dock-space-ux · ทั้งหมด verify ซื่อสัตย์):** `10a3207` playAll→⚙(full render-verified) · `4dd99c5` toolbox fit-344 clamp(320px@344) · `f88e189` soundctl/export→⚙(structure+715 tests · **control-cell ว่างจนกว่า merge dev slot-in-panel = popover render-verify ตอน integration**) · UX กำลังเตรียม **toolbox joint-plan** (merge slot-tools+seg-tools · map functions · positioning) ส่ง PM review ก่อน joint pass
     - ✅ **dev engine set ครบ 4 (`dock-space-dev`) — PM gate รับ (git-verify scope = DockKey.vue+test เท่านั้น · ไม่แตะ EditorMode):** `1cd032c` slot-in-panel · `3955a1f` hide-on-scroll (prop auto-hide default false · window rAF+delta · `.dk-shift`) · `27f5d9b` cap=f(width) clamp(floor(w/50),3,14) observe documentElement · `e406e29` keyboard visualViewport>150px · **728 tests · 0 warning** · URL `192.168.1.124:5313` · dev held: (a) `:auto-hide` wiring=UX template lane (b) 2-host gate=PM ประสาน pk PM (c) Tier-B=tester GATE2
     - ✅ **toolbox joint-plan อนุมัติ (`§10` `627a83b`):** รวม slot-tools+seg-tools → anchored toolbox เดียว (ตัด clutter seg-tools always-visible มือถือ) · UX=template/CSS · dev=wire(pullSlot/pushSlot/dup/remove+octave NoteBoxes)+positioning(bar/line/section) · verify repro documented
     - ✅ **P'Aim เคาะ (18 ก.ค. · applying "ไม่แน่ใจห้ามเดา"): seg-tools copy/delete → โชว์ตอนเลือก (จอโล่ง)** = ยืนยัน declutter/on-selection ที่ทีมวางไว้ (เปลี่ยนพฤติกรรมเดิม always-visible → ถาม P'Aim ก่อน ไม่เดา) · relay ทีมตอน dev ตอบ mapping (กันส่งซ้ำ)
     - **▶ JOINT PASS (ชิ้นสุดท้าย core · lane เดียว EditorMode = sequence):** **PM เคาะลำดับ: note scope ก่อน** (มี @focus + clamp) → bar/line/section ทีหลัง(dev positioning) · **2 feasibility → dev ตอบ (data model · ไม่ให้ UX เดา):** Q1 slot↔note mapping (focusedSlot→parent note เพื่อ copy/delete · grouping จริง) · Q2 trigger (note=@focus now · bar/line/section=dev เฟสถัดไป) · **UX hold template จน dev ยืนยัน grouping** (กันทำ button-set แล้วรื้อ)
       - ✅ **dev 2-host gate PASS (`99c9002`):** rebuild island ใน scratch (ไม่แตะ phrakham tree) · smoke 344/690/768/1200 · auto-hide inert = พระคำไม่ regress · source-safe · **commit island เข้า phrakham = post-merge + pk-PM** (hold ถูก)
       - ✅ **UX template pass เสร็จ + freeze EditorMode (`63f546c`):** `:auto-hide="true"` (:3098) + toolbox CSS foundation (clamp) · **UX finding: `.slot-tools` 2 instance (`:2913` cell.slot มี note context · `:3043` `i` ไม่มี) → note-tool buttons ต้อง scope ถูก = dev wire (UX ไม่วาง markup เดี่ยว กัน guess ผิด)**
       - ✅ **dev ตอบ grouping/mapping (verify `:2884-2937`):** slot+seg อยู่ `.seg-col` เดียว = ได้โน้ตแม่ตรง ไม่ reverse-lookup · **layout `[◀▶ พยางค์ · lens-only] ┊ [⧉ ✕ โน้ต · ทุกโหมด]`** + octave=NoteBoxes แยก · **trigger `focusedSlot`(มี) ‖ `focusedSeg`(dev เพิ่ม @focusin `.seg-col`)** · continuity: เก็บ selection ref อิสระจาก blur (จุดเดียวคุม 2)
       - ✅ **UX template เสร็จ + freeze (`ba18718`):** merged box `[◀▶]┊[⧉✕]` ใน `.slot-tools` :2913 (note context) · :3043 เว้นให้ dev · 715 tests · verify ซื่อสัตย์ (render-capture ไม่สำเร็จ · v-if เดิม render-verified แล้ว)
       - ⚠️ **scroll-container alarm = FALSE (UX retract เอง · corrected ก่อน dev แก้ผิด):** "inner container" สรุปผิดจาก **MCP pane scroll ไม่ได้** (env · `documentElement.canScroll` แต่ `scrollTop` คง 0 · ไม่เจอ inner scroller) → **SA "window" อาจถูก · dev คง window listener + verify scroll-target บนเบราว์เซอร์/มือถือจริง** (ไม่ใช่ MCP) · **บทเรียน: MCP pane scroll ไม่ได้ = scroll-behavior verify บนของจริงเท่านั้น** (เหมือน worktree 0-width) · หลักฐาน 1 จุดไม่พอ flag critical
       - ✅ **dev joint-pass เสร็จ (`bfdcee1` · 732 tests +4 · jsdom mount แทน browser-pane · build ✓):** merged `.slot-tools` (◀▶+⧉copy+✕del โน้ตแม่ `dup/removeSegment(bar,si)`) · `.seg-tools` = octave▼▲+copy+del (`v-if focusedSlot===-1` ไม่ซ้ำ) · `octaveShift` via activeInput (jianpu `.`/`'`) · **ตัด seg always-visible → on-selection (P'Aim ✅)** · :3043=◀▶-only
       - **PM ตัดสิน flags:** octave activeInput=คงไว้(works) · **beam/slur=defer backlog** (ไม่มี function · ห้ามปุ่มตาย) · **🔴 continuity(SA §7)=ทำต่อเลย required** (ref อิสระจาก blur · ระวัง loop กับ keyboard-aware · verify พับ/หมุน บนเบราว์เซอร์จริง) · bar/line/section=phase 2 · scroll-target=verify hide-on-scroll ยิงจริงบนเบราว์เซอร์จริง(คง window listener)
       - ✅ **P'Aim เคาะ scope (18 ก.ค. · หลังทัก "ทำไมโตไปทั้งหน้า"):** เข้าใจ 2 ส่วน (dock เอง vs แถบลอยที่แตะ editor) แล้ว **เลือกส่งทั้งชุด (dock+toolbox) ลองทีเดียว** · ⚠️ P'Aim: "ดีชัด=อาเมน · ไม่ดี=โดนดุ" → **tester GATE2 ต้องตรวจ UX จริงบนมือถือ/จอพับ (feel: หลบจริง·โผล่ลื่น) ไม่ใช่แค่ renders**
       - ✅ **joint-pass v2 (`b4dad62` · 733 tests):** hoisted merged toolbox บน UX `ba18718` (drop parallel bfdcee1) · **continuity แก้สวย: `focusedSeg` sticky (ไม่ล้าง blur → พับ/หมุน toolbox อยู่ + แก้ keyboard-loop ไปในตัว)** · scroll=window ยืนยัน · ลบ slot-tools/seg-tools ซ้ำ+dead CSS · :3043=◀▶-only (dev+UX ถูก · PM message แรกผิด "เพิ่ม note :3043" = แก้แล้ว)
       - **PM ตัดสิน: 🔴 octave หลุดตอน reconcile → re-add (activeInput ที่ทำงาน · รายงานพี่เอมไปแล้วว่ามี · ห้ามทิ้ง)** · NoteBoxes-child=refactor อนาคต · UX เคาะ placement (default: ในกล่อง toolbox)
       - ✅ **dev octave re-add + continuity เสร็จ (`47119e3` · 735 tests):** octave ▼▲ ในกล่อง toolbox (activeInput) · `selSlot` sticky (ไม่ล้าง blur · ล้างเฉพาะ outside pointer-down · **ไม่ loop keyboard-aware**) · note=`[▼▲]┊[⧉✕]` syllable=`[◀▶]┊[⧉✕]` · UX ยืนยัน octave-in-toolbox ตรงกัน
       - **🔴 PM เคาะ branch (UX จับ conflict ถูก):** **`dock-space-dev` @ `47119e3` = FROZEN · branch รวมสมบูรณ์** (ux ba18718+engine+toolbox+octave+continuity) · **UX ทำ positioning CSS ต่อยอดบน dev tip (โครงจริง) ไม่ใช่ `dock-space-ux` เก่า (stale)** · dev stand by · = กัน 1-ไฟล์-1-lane conflict
       - ✅ **UX positioning CSS เสร็จ (`6c3b225` · `dock-space-dev` FF · verified บนแอปจริง measured):** toolbox เกาะเหนือ NoteBoxes (`bottom:calc(100%-32px)`) · Fold 344 gapToNote 35→**3px** ไม่ทับ · fit 344(195px)+desktop · hOverflow 0 · 735 tests · **`dock-space-dev @ 6c3b225` = core ครบชุด (engine+toolbox+octave+continuity+positioning)**
       - 🚩 **flag (UX · optional · P'Aim ตัดสิน):** static CSS ทำ gap ดีขึ้นแล้ว (syllable 83→51) · ถ้าอยากให้กล่องเกาะ**พยางค์ที่เลือกจริง**ตอน lens = dynamic offset ตาม selSlot (dev JS · follow-up ถ้า P'Aim ยังรู้สึกห่าง)
       - **P'Aim ดู URL แล้ว (หน้าเปล่า+desktop → เห็นเหมือนเดิม: slim subtle · hide/toolbox ไม่ trigger เพราะไม่มีโน้ต) → PM สรุป 3 การแก้ + ทำไมไม่เห็น · P'Aim เคาะ: "เช็ค+ลองดู · ให้พี่เปาตัดสิน · เก็บงานให้เรียบร้อย ปิดงานนี้ก่อน"**
       - **🎯 การตัดสิน = พี่เปา (คนใช้ editor จริง) ไม่ใช่ P'Aim · P'Aim = wrap up+close**
       - 🟡 **tester GATE2 = CONDITIONAL (`4c57489` · `docs/reports/dock-space-tester.md`):** ✅ PASS: dock slim · hide-on-scroll (real Chrome เลื่อนจริง+peek) · continuity · 2-host (autoHide default false=พระคำ inert · อาศัยหลักฐาน island dev) · octave/copy/del · **🔴 2 concern แก้ก่อน merge:** (A) `.slot-tools left:50%`=กลาง segment ไม่ใช่โน้ตที่โฟกัส → ห่าง 335px + หลุดขอบขวา (segment กว้าง=ป้อนทำนอง) → **clamp(บังคับ)+เกาะ x โน้ตจริง (=dynamic anchor ที่เคยว่า phase2 · tester ยืนยันจำเป็น v1)** (B) ปุ่ม toolbox 30×26→≥40 (ลบ 26px) · ⚠️ 3 ข้อรอพี่เปามือถือ (keyboard-aware · narrow 344/390/690 fit · ◀▶ syllable+draft) · **h-scroll@768=ของเดิม `.note-boxes{flex-wrap:nowrap}` ไม่ใช่ dock-space (tester ยืนยัน · backlog แยก)** · **จ่าย UX+dev แก้ 2 concern (dev un-freeze pair · ≤3 loop) → tester re-verify → พี่เปา → deploy**
       - ⚠️ **A ทำซ้อน 2 branch (baton หลุด · branch lock 2-worktree · PM จัดคิวพลาด):** dev `anchorToolbox` (`6a4cec2` dock-space-dev · verify gap 0 + clamp 8px) · UX `positionSlotTools` (`2b43435` dock-space-css · verify gap 0 + clamp 12px + B 44×44) — **ทั้งคู่วัดตรงกัน = approach ยืนยัน 2 รอบ** · **บทเรียน: paired fix ไฟล์เดียว = serialize concern (ไม่ปล่อย 2 คนทำ concern เดียวกันขนาน · 1-ไฟล์-1-lane ต้องคุมระดับ concern ไม่ใช่แค่ไฟล์)**
       - ✅ **A+B รวมครบบน `dock-space-dev @ 13d482f`** (6c3b225 → 6a4cec2 dev-A anchor+clamp → 13d482f UX-B `.slot-btn` 44×44) · **verify สด (real Chrome · segment 16 กล่อง): ปุ่มทุกตัว 44×44 incl ✕ลบ · gap 0 ตามโฟกัส · clamp 8px ไม่หลุด · 195px fit 344 · 16 tests เขียว** · dock-space-css (UX A ซ้ำ) ทิ้ง · **branch free**
       - **▶ dispatch tester re-verify 2 concern แล้ว (`local_9f001e0f` · real Chrome pixel):** A เกาะโน้ตจริง+clamp 344/390 · B ปุ่ม ≥40 · 3 ข้อ mobile คงไว้ให้พี่เปา · pass → clear GATE2 → พี่เปา → merge → deploy · 🎧 `http://192.168.1.124:5346/#/studio`
       - _(ประวัติ tester spawn `task_c743ab55`) — มือถือจริง_ (โหลดเพลงมีโน้ต · hide-on-scroll ยิงจริง · toolbox+octave/copy/del · continuity พับ/หมุน · device-matrix 344-desktop · **2-host phrakham rebuild+smoke**) · dev+UX stand by แก้ ≤3 loop
       - **▶ หลัง tester ผ่าน:** PM merge → base → ประสาน pk-PM commit `pk-dock-island.js` → **พี่เปาลอง+ตัดสิน (URL มือถือ · เพลงมีโน้ต)** → wrap up ปิดงาน · deploy=P'Aim สั่ง · **bar/line/section+beam/slur+dynamic-anchor=phase 2**
     - **▶ หลัง joint:** PM integration → 🔴 **2-host: ประสาน pk PM (`local_43cdd55a`) rebuild `pk-dock-island.js`+gate 344/690/768** (cap=f(width) always-on) → tester GATE2 device-matrix (+edge slot pinned+in-⚙) → P'Aim ลองแอปจริงครบจบ

**📱 มาตรฐาน all-device ผูกแล้ว 18 ก.ค. (P'Aim: "รองรับ desktop·tablet·Samsung Fold·Android+iOS ทั้งหมด · ใส่ SOP ให้ทุกคน"):** `ux-platform-patterns §5.5` (device matrix: Fold พับ~344+กาง~690–768·tablet·phones · continuity คง state ข้ามพับ/หมุน · safe-area · touch+pointer) + `ui-standards §2` (binding SA/dev/tester) + `sop.md GATE2` (tester ทดสอบ matrix) · แจ้ง UX/SA ให้ใส่ในงาน dock-space แล้ว (Fold พับ = เคสตึงสุด)
- 🔴 **dock-config mockup v2 ถูก P'Aim ปฏิเสธ "ห่วยมาก" (18 ก.ค.) → ตีกลับ UX แล้ว:** UX ออกแบบหน้าตั้งค่าใหม่ทับ **ของเดิม (ภาพ 2: ⚙ วนซ้ำ/คอร์ด/ความเร็ว/แสดงผล/โปร่งใส+ปัก) ที่ P'Aim ว่า "ดีอยู่แล้ว"** + mockup **ปุ่ม ⚙ หาย+เพี้ยน** · **PM verify โค้ดจริง `DockKey.vue` แล้ว: ของเดิมครบ (⚙:320 · setting form:404 · ปัก/▲▼:139/450 · slider โปร่งใส:386 · drag-move:184)** · **DELTA จริงมีแค่ 2: (1) ลากปรับ*ขนาด* free-form (ตอนนี้มีแค่ move) — ทิ้ง size-preset (2) มือถือ ⚙ เปิดหน้า*เต็มจอ*** · PM เสนอ UX: มือถือใช้ *สไลเดอร์ขนาด* (free-form+touch-safe reuse โปร่งใส) desktop=ลากขอบ · **บรีฟใหม่ส่งแล้ว · PM จะเปิดเทียบแอปจริงก่อนส่ง P'Aim (GATE0 ใหม่)**
  - 🔴 **บทเรียนใหญ่ (SOP §3 GATE0 + §6 #8/#9 · memory ใหม่):** PM relay mockup ดิบไม่เปิดดู = ปล่อยของห่วยถึง P'Aim · + P'Aim = เสียงคนใช้ ไม่ใช่ผู้ตัดสินมาตรฐาน → seats ต้องฟันธง ไม่ถาม "เอาไหม"
  - ✅ **v3 `2cc051a` — PM GATE0 ผ่าน (อ่านซอร์ส mockup ทั้งไฟล์เทียบ `DockKey.vue` จริง · claude.ai artifact เปิดใน browser pane ไม่ได้=ไม่ login → verify จากซอร์ส):** ⚙ present · settings เดิมครบ 5 (วนซ้ำ/คอร์ด/ความเร็ว/แสดงผล/โปร่งใส)+ปัก▲▼📌 · preset ลบหมด(free-form แทน) · delta 1 desktop drag-handle + mobile size-slider(ต่อเนื่อง reuse โปร่งใส) · delta 2 mobile full-screen sheet+"เสร็จ"กลับที่ · a11y role=dialog/switch/Esc/click-outside ครบ · **refine ไม่ redesign ✓** → **relay P'Aim ลองได้**
  - 📝 **2 note สำหรับ DS (ไม่บล็อก P'Aim · UX รับแล้ว `7398af6` เก็บ `dockkey-config.md §NOTES`):** (1) **resize floor:** mockup SMIN=0.86→~38px ต่ำกว่า floor จริง (44/42/40px) → DS clamp min = `--touch-min` จริง · footprint เล็กกว่านั้น = ซ่อนปุ่ม/ลด gap ไม่ย่อ target (SOP §3) (2) **grip ลากย้าย** (`dk-grip`) คงไว้ครบใน DS/build กัน "ปุ่มหาย" · +2-host DoD
  - 🔄 **REFRAME (18 ก.ค. · P'Aim): root cause = "dock กินพื้นที่แย่งเนื้อเพลง → ใช้ยาก" + เปิดให้ expert เสนอทางดีกว่า** ("นี่คือสิ่งที่ผมต้องการ แต่ expert แนะนำให้ดีกว่าได้") → โจทย์เปลี่ยนจาก "ทำ resize" เป็น **"แก้ dock แย่งพื้นที่ editor แบบ world-class"** · resize (แบบ 2 window reflow) = แค่ 1 ทางเลือก ไม่ใช่คำตอบสุดท้าย
    - _(ประวัติ: v3=uniform zoom ผิด · P'Aim เคยเคาะ "แบบ 2"=window resize+reflow ก่อน reframe · engine `buildRows`/`cap` รองรับ reflow)_
  - **PM expert POV (ตั้งต้น · UX ต่อยอด+ฟันธง):** resize = แก้ปลายเหตุ · world-class = ให้ dock **หลบเอง** ไม่ใช่ให้คนย่อ → (1) auto-collapse เมื่อไม่ใช้ (มี collapse-in-place `:151`) (2) essential-only+overflow (SOP §3 · มี pin) (3) **contextual toolbox** (`selection-driven-editor.md §10` · SA feasibility `93d05b8` · อาจไม่ต้องมีแถบใหญ่ถาวร = แก้ root สุด) (4) โปร่งใส+resize เสริม
  - **SA pre-verify (stand by · ตอบเร็วตอน flow มา):** กลไกมีครบบน engine (`cap` mobile7/desktop14 · `collapse-in-place`+grip `transition()` · pin/reorder) → **งานใหม่ = trigger เท่านั้น** (auto-collapse = reuse `collapsed` ref) · contextual toolbox `93d05b8` reuse ได้ · 🚩 **flag regression: auto-collapse ที่ engine → dock อ่านออกเสียงพระคำ collapse ด้วย → ต้องเช็กพระคำอยากได้ไหม** (หรือวาง trigger ที่ host = แยกได้แต่ไม่ share) · แตะ `collapsed`/shell = คิว 1-ไฟล์-1-สาย (2a/EditorMode)
  - ✅ **v4 `740b1be` (resize แบบ 2) — PM GATE0 DYNAMIC ผ่าน (วัดจริงในเบราว์เซอร์ · serve localhost + measure ไม่ใช่เชื่อซอร์ส):** `--dk-w` คุมความกว้าง · **reflow จริง:** dock 336px=3แถว · 180px=**4แถว** · 120px=**5แถว** (แคบ→ปุ่มขึ้นแถวใหม่) · **ปุ่มไอคอนคง 44×44 เป๊ะทุกความกว้าง ไม่ย่อ** ✓ = footprint ลดจาก reflow ไม่ใช่ zoom · `--scale`/preset ลบหมด · gear/grip/ปัก/โปร่งใส/เมนูครบ · mobile ⚙ เต็มจอ + สไลเดอร์ความกว้าง
  - ✅ **UX synthesis "dock-space-reclaim" (`2f7e34e`) — PM GATE0 dynamic ผ่าน (serve+ขับจริง viewport 1280):** thesis "conditional not smaller" (dock หลบเอง ไม่ใช่ให้คนย่อ) · behavior ครบ: hide-on-scroll+peek ✅ · tap→contextual toolbox ✅ · keyboard→dock ซ่อน+toolbox เหนือแป้น ✅ · SR mode ไม่ auto-hide ✅ · 0 error · รวม dock-config+toolbox(§10 banked กลับมามีบ้าน)+resize เป็นภาพเดียว · ส่งมอบ `docs/us/dock-space-reclaim.md`+`summary-dock-space-paim.md` · mockup https://claude.ai/code/artifact/1483051e-a506-4384-8d97-64a1d42c3d2c
    - ⚠️ artifact ที่หลอกตอน verify: browser pane เรนเดอร์ viewport จิ๋ว (ต้อง `resize_window`) + `scroll-behavior:smooth` ทำ scrollTop sync อ่านไม่ทัน (ต้อง isolate)
  - ✅ **เวอร์ชันใหม่ (`2b5a004`) UX ฟันธง "resize = personalization ไม่ใช่ตัวแก้ root" + 2 ชั้น (auto ต่อจอ / nudge ใน ⚙) + device presets 344/390/690/834/desktop · PM GATE0 re-verify:** continuity ผ่าน (สลับ Fold พับ↔กาง = selection คงอยู่ ไม่ reset) · ไม่มี body h-scroll ทุกความกว้าง · scroll-hide ทำงาน 344/834 · 0 error
  - ✅ **toolbox fix (`3ca279d`/`5d42467`/`eb6ebc2`) — UX self-verify:** icon-only+aria 6+overflow ⋯ · 291px fit @Fold344 · **insight: ความกว้าง toolbox แปรตาม emoji glyph ข้าม OS/font (อย่านับ px) → DS guarantee `max-width:calc(100%-12px)`+ปุ่มเกินเข้า ⋯ = 0 overflow ทุก font/จอ** · 0 error
  - 🔴 **บทบาท PM ปรับ (P'Aim 18 ก.ค. รอบ 2): "PM ไม่ควรทดสอบเอง · จ่าย tester เชี่ยวชาญกว่า · PM มีหน้าที่บริหาร"** → SOP GATE0 แก้: **ผู้ผลิต self-verify (device-matrix) + tester QA เชิงลึก · PM อ่านหลักฐาน+เช็ก exists-vs-new+คุมคิว+gate · ⛔ PM ไม่ serve/วัด pixel เอง** (memory `feedback_pm_verify_deliverable_before_relay` · SOP §6 #8) · ✅ SA breakpoint fix: `cap=f(width)` ต่อเนื่อง (DS `5d42467`)
  - 🏛️ **SA flag build/DS:** DockKey breakpoint เดียว `max-width:760` → cap 7/14 · **Fold กาง 690–768 คร่อมเส้น → dock กระโดด** · ต้อง breakpoint ละเอียด/ cap แปรตามกว้างจริง · SA จะวัด engine 344/690/768 + continuity ตอนตรวจคู่
  - ✅ **GATE0 = PM gate-accept จาก UX self-verify evidence (โมเดลใหม่ทำงานจริง · PM ไม่ re-measure):** ตาราง device-matrix 344/390/690/834/920 = toolbox fit·44px·0 h-overflow ทุกจอ + behaviors ครบ (scroll-hide/tap-toolbox/keyboard/SR/continuity) + 0 error — หลักฐานครบ = ผ่าน
  - **▶ สถานะ: dock-space concept handoff สะอาด · รอ P'Aim เคาะทิศ (GATE1 direction).** โอเค → UX ลง DS 4 หัวข้อ · SA feasibility คู่ → **build → tester GATE2** (deep QA = tester ไม่ใช่ PM) · build เข้าคิวหนัก PM sequence (DockKey engine 2-host + EditorMode ร้อน)

**สถานะที่นั่ง (18 ก.ค.):**
- **SA:** งานออกแบบจบหมด · ✅ RLS ปิดจบแล้ว · stand by รอ UX flow (dock-config + B108) ตรวจ feasibility คู่ · SQL ที่เหลือ: `db/006`(author_id fix) `db/007`(back-fill·latent ไม่ urgent) พร้อมไม่รัน
- **UX: ✅ ส่งมอบครบ (`45ecc39` uxui-standing · design ล้วน)** — `docs/us/dockkey-config.md`(+ตารางชื่ออ้างอิง §4) · `docs/pm/summary-dockkey-paim.md`(1 หน้า ม.ต้น · hedge "รอ SA" ลบแล้ว) · `docs/ds/dockkey-config-mockup.html` · **mockup มือถือ** https://claude.ai/code/artifact/555c023e-4bdb-46b4-a894-05509fbd559d · เฟส A(44px+ชื่อ+ตาราง)/B(เลือกปุ่ม)/C(preset ขนาด)/D(→shared พระคำ) · **รอ P'Aim GATE 1 ลอง mockup→เคาะทิศ→UX ทำ DS ส่ง dev** · toolbox banked
- **สาย orientation `91b05cf3`:** 2a (แถบตัวตน+คิวอนุมัติขึ้น shell) กำลังทำ — **เช็กว่าเสร็จยัง (ไล่ดูเอง อย่ารอ ping)**

**เข้าฐานแล้ว รอ deploy รอบ 31:** `2c`(การ์ดชื่อเต็ม) · เฟส1(เตือนก่อนทับงานโม `13f0f52`) — **รอ P'Aim ลองรวม + สั่ง go**
**🐞 backlog dev:** pk-drawer.js header เคลม "แชร์พระคำ" ทั้งที่ import ตรง (แก้คอมเมนต์) · null byte `EditorMode.vue:1569`
**design banked (รอ P'Aim หยิบ):** `#`ยกทั้งห้อง · ถังขยะ · slur/เส้นเอื้อน(ทำสัญลักษณ์แยก) · selection-driven toolbox

---
_(ประวัติ DEPLOY รอบ 30 + วันนี้ด้านล่าง)_

---

## 🎉 DEPLOY รอบ 30 LIVE `2f4177e` (17 ก.ค. · P'Aim "deploy ให้พี่เปาทดสอบจริงเลย")
- **ของที่ขึ้น (3 merge):** 🔴 MP3 เล่นครบท่อน (เดิม #16 หาย 116 วิ · บั๊กบน live มาตั้งแต่รอบ 19) · 🎼 เฟอร์มาตาลากเสียงจริง (#120/#118) · 🔧 หน้าแก้ไข: ปุ่มขยายในหน้าต่าง+จำขนาด+เต็มจอ · แถบติดจอ(issue20) · เซฟร่างกดได้จริง · ลบร่างตัวเอง
- **✅ verify prod (พิสูจน์ด้วยโค้ดจริง ไม่ใช่ push=ขึ้น):** GH Actions #118 เขียว 30s · **เจอ `exportPlayNotes` ใน `audioExport-DgJGtSut.js` บน live = โค้ด MP3 ใหม่ขึ้นจริง** · **บทเรียน: อย่าเฝ้า hash ของ `index-*.js` (bundle หลักไม่เปลี่ยนเพราะงานอยู่ใน code-split chunk แยก) → เฝ้า chunk ที่เปลี่ยน หรือหา marker โค้ดใหม่**
- **699 tests · build ผ่าน** · `notationLint.test.mjs` fail = pre-existing (`process.exit` · ยืนยัน 3 สาย + main เหมือนกัน)
- **git:** base `studio-shell-redesign` = origin/main = `2f4177e`
- **📌 พี่เปาทดสอบบน live จริงได้แล้ว** — ไม่ต้องใช้ LAN IP/Wi-Fi เดียวกันอีก (แก้ปัญหา login ผ่าน IP + รหัสเบราว์เซอร์ไม่มา)

## 🔎 VERIFY หลัง P'Aim restart เครื่อง (14:23) — ไม่มีงานหาย
- ✅ **resize (dev สาย2) เสร็จจริง+verify** (`d1eeb8a` DockKey · idle เพราะทำจบ ไม่ค้าง): free-form width→reflow · prop `resizable` default false (2-host inert) · desktop drag handle+dblclick→auto · mobile slider · auto-hide coexist · 743 tests · **⚠️ ต้อง wire `:resizable="true"` ใน EditorMode 1 บรรทัด (editor lane) เพื่อเปิดใน pleng**
- ✅ **344-fix (`2e238f0`) + B109 (`499d8d3` tester CLEAN) เสร็จหมด** · dockkey-fix-resize tip=`314b2de`
- **สถานะ session (restart):** รัน=SA·dev1·music·pk-PM · หยุด(idle/restart)=UX·tester·dev2(ทำจบแล้ว) → re-launch UX/tester ตอน integrate resize-wire
- **dev1 = เช็ก B108 backend ว่าเริ่มยัง** (ยังไม่เห็น branch push)
- **author_id = P'Aim ตอบ "ยังไม่โชว์เครดิต" → ทิ้ง db/006/007** (ไม่ queue · ปิดเคส)

## 🔔 P'Aim เคาะตรง SA (18 ก.ค. ค่ำ)
- **B108 ระฆัง = build อนุมัติ** → **backend (db/008 + store.js counter) จ่าย dev คู่ขนานแล้ว** (`b108-backend` · SA เตรียม `db/008`) · **shell UI ระฆัง = คิวหลัง** (ชน shell/dock/orientation)
- **author_id (db/006/007) = hold** — P'Aim ถาม "ทำไมต้องทำ" · SA ตอบ latent (ไม่มีใครอ่าน author_id) · **รอ P'Aim ตอบ "จะโชว์เครดิตคนแต่งไหม"** → ไม่โชว์=ทิ้ง · โชว์=รัน db/006 · ไม่จ่ายจนตอบ
- ⚠️ **B109 ไม่ใช่ docs แล้ว = build เสร็จ+tester CLEAN (`499d8d3`)** — sync P'Aim (เคยเข้าใจว่ายังกระดาษ) · ไม่ต้องดึง Enter=คอร์ดมาก่อน ทั้งชุดเสร็จ รอ deploy

## 📥 PM inbox
- ✅✅ **B109 keyboard-nav = ANALYSIS FINAL + PM gated (UX `5638e07` fold 2 tweak · SA feasibility ผ่าน `5e3f011`) — จอดรอเสนอ P'Aim/พี่เปา หลัง dock-space deploy settle:** jump (Tab หลัก + Ctrl+Arrow Win/Linux · Mac ใช้ Tab/ปุ่มจอ) · cross-device ปุ่มบนจอ (เฟส A = พี่เปาได้ก่อน) · Enter คอร์ด=`allow-custom` · Esc=`.chord-cell` wrapper · WCAG-safe · **build = สายเดียวกับ dock-space (EditorMode · continuity fix ร่วม) หลัง dock-space ปิด**
- 🏛️ **SA B109 keyboard-nav feasibility คู่ UX ✅ (`5e3f011`)** — scheme ทำได้จริงทั้งหมด · **fold 2 tweak ก่อน sign-off:** 🔴 **Mac Ctrl+←/→ = OS Spaces แย่ง → preventDefault ไม่ได้ → Tab เป็นหลัก (safe) · Ctrl+Arrow=Win/Linux only** · **Esc คอร์ด = `@keydown.esc` ที่ `.chord-cell` wrapper ไม่ใช่ ComboSelect** (share 3 ที่) · Enter คอร์ด=`allow-custom` (root cause `ffbf54f`) · data model/integration(`onUndoKeys` :1710)/dock-keyboard-guard/focus = ✅ · focus+nav-band ร่วมสาย dock-space (continuity fix เดียว) · **build หลัง dock-space** · **B109 design ready หลัง UX fold 2 tweak → PM gate → เสนอ P'Aim/พี่เปา หลัง dock-space deploy**
- 🎨 **UX B109 keyboard-nav design ✅ (`32f1d1d` · `docs/us/editor-keyboard-nav.md` · analysis read-only)** — scheme ครบชุด: **desktop** Tab=โน้ต · **Ctrl+←/→=ห้อง · Ctrl+↑/↓=บรรทัด** (MuseScore/Flat.io) · Home/End · **Enter=ยืนยันคอร์ด+Esc=ยกเลิก (reuse `commitRename` :2591)** · **มือถือ=ปุ่มบนจอ** (◀▶โน้ต ⏮⏭ห้อง ▲▼บรรทัด ใน keypad) = **1 nav model 2 trigger** · เช็คคีย์เดิมครบ (Space/Enter/Backspace/Ctrl+Z/Y จอง) → ใช้ modifier/non-printable = **นอก WCAG 2.1.4** ✓ · **เฟส A=ปุ่มบนจอทุกอุปกรณ์ (พี่เปาได้ก่อน)** /B=desktop keys /C=cheatsheet · **6 คำถาม→SA** (ยืนยันคอร์ด mechanism · Ctrl+Arrow override · ปุ่มเหนือแป้น) · **build หลัง dock-space (EditorMode lane)**
- 🏛️ **SA B109 shortcut feasibility ✅ (`8b392e6` · read-only)** — jump bar/line ทำได้ (`slotStarts["li-bi-si"]`+`data-bar`+`focusSlot` มีครบ · เพิ่ม `jumpBar/jumpLine(±1)` reuse) · **cross-device: มือถือไม่มี Tab/Ctrl → ปุ่มบนจอเรียกฟังก์ชันเดียวกับ physical key (1 ฟังก์ชันกลาง 2 trigger)** · jump ระหว่าง input = คีย์บอร์ดเปิด = ไม่ trigger hide ผิด ✅ · **🔴 nav key ทุกตัว preventDefault กัน native scroll→hide-on-scroll flicker** · ไม่กระทบ 2-host (editor nav ไม่ใช่ DockKey) · **build หลัง dock-space ปิด (EditorMode lane)** · รอ UX เลือกคีย์+ออกแบบปุ่มจอ + Enter=ยืนยันคอร์ด (`editingChord`)
- 🏛️ **SA dock-space feasibility ✅ (`7f28aa1`)** `docs/reports/dock-space-feasibility-sa.md` — 5 คำถาม §9 ครบ: scroll=`window`(rAF+delta~8px·guard popover/keyboard) · `visualViewport`>150px (⛔ห้าม `interactive-widget=resizes-content`) · AT ไม่มี API(toggle+reduced-motion=ถูก·อย่า sniff) · cap=clamp(floor(w/50),3,14) observe **full-width ref** ไม่ใช่ dock(feedback loop)·`ro` slot ยังไม่ assign wire ใหม่ · scope 2 lane แยก(dock hide vs inline toolbox)ปลอดภัย · **🔴 continuity: `focusedSlot` ผูก DOM focus(:2920)→blur ตอน fold/หมุน/ปิดแป้น=selection หาย→เก็บ ref อิสระ** · 2-host: island 4-5 ปุ่ม low-risk แต่ rebuild+gate 344/690/768 · **dev unblock · PM relay dev แล้ว**
- 🎨 **UX B108 = notification center (role/event) ✅ (`64ca32b` · supersede a0b6c22)** `docs/us/notification-bell.md`+`summary-notification-bell-paim.md` · mockup https://claude.ai/code/artifact/46569a53-99ee-44cd-8501-cff261503e95 — **list ไม่ใช่ตัวนับ** item `{id,type,icon,text,link,read,ts}` (เพิ่ม role/broadcast = เพิ่ม type ไม่รื้อ UI) · **badge = unread** (Apple) · **"N รอตรวจ" = สรุปหัว panel + ลิงก์ ไม่ใช่ badge · 94 backlog แยกหน้า tracker** · v1 `review_incoming` · v2 `draft_approved/returned` · v3 broadcast · desktop popover/mobile เต็มจอ · เหลือ RLS ยืนยันตอน DS · **⚠️ PM GATE0 ยังไม่ verify vs แอปจริง — ยังไม่ส่ง P'Aim**
- 🏛️ **SA B108 arch verdict (`fa5a329`·§6)** — ฟันธง **มี `notifications` table ตั้งแต่ v1 (ไม่ derive):** broadcast derive ไม่ได้ + submitter unread ต่อชิ้นทำไม่ได้ถ้า derive · schema `notifications(recipient_id·type·ref_id·payload·created_at·read_at)` RLS `recipient_id=auth.uid()` · insert by trigger เท่านั้น (reuse `db/004 log_song_event`) · v1 เล็ก (event `draft_submitted→approvers` เดียว) · v2 submitter · v3 broadcast · 0 migrate · **ไม่ pre-create SQL จน GATE1 ผ่าน** (`db/008` รอ)
  - ✅ **badge-semantics reconcile แล้ว (UX+SA ตรงกัน):** badge = unread notification (ไม่ใช่ pending-work count) · "N รอตรวจ" = สรุปแยก + ลิงก์ไป tracker · ตรงกับ `notifications.read_at`
- 🏛️ **SA B108 review-bell feasibility (`10c8479`)** `docs/reports/b108-review-bell-feasibility.md` — ทำได้·refine · **ประเด็นหลัก = "ระฆังนับอะไร" 2 แหล่ง (UX/P'Aim เคาะ):** (ก) `song_drafts.status='pending'` = ทีมส่งร่างให้อนุมัติ ("ส่งเข้ามา" · โหลดแล้ว · เล็ก · SA แนะใช้อันนี้) vs (ข) `songs.verified=false`=94 backlog import (แยกไว้ review tracker ไม่เด้งระฆัง) · **ไม่มี realtime→poll** (พอ) · `created_at` มี แต่ไม่มี seen-state (unread ต้อง last-seen marker) · **extend orientation 2a แต่แตะ shell ไฟล์เดียว → คิว lane เดียว + ยกตัวนับ pending ขึ้น `store.js`** (กัน 2 source of truth) · เฟส P1 count pending · P2 ใหม่/ค้าง · P3 backlog 94 แยก
- 📊 **SA verify-batch readiness (`78e93fe`·§4.5)** — 94 unverified: clean 22 · tiny 11 · moderate 28 · heavy 33 · เนื้อว่าง 0 · ✓clean+tiny→public 65 · เร่งตรวจ batch แรก=เร็ว ไม่ block · ⚠️ เกณฑ์=โครงสร้างพยางค์↔โน้ต ไม่ใช่ตรวจเนื้อหา
- 🏛️ **SA DockKey-config feasibility — CORRECTED (`2c98c2c`)** `docs/reports/dockkey-shared-core-feasibility.md` — **P'Aim ถูก 100%: DockKey แชร์ 2 เว็บจริง** (phrakham `IslandApp.vue` import `@pleng/components/DockKey.vue` = single source ไม่ก๊อป · phrakham ใช้เป็น island dock อ่านออกเสียง) · **แก้ engine DockKey ครั้งเดียว = 2 เว็บ (1 edit + 2 deploy · phrakham ต้อง rebuild island)** · 🔴 **regression พระคำมีจริง → DoD ต้อง test 2 host** (48/44px·aria·no-scroll·print) · ⚠️ เช็กต่อ: integration นี้ live พระคำแล้ว หรืออยู่ branch read-aloud
- 🎨 **UX toolbox flow+mockup (คลิกได้) ✅** `selection-driven-editor.md §10.7` (`d08c4bf`) — ฟันธง **ขนาดกลาง** (Canva/Apple callout · tap นำ hover เสริม) · artifact จิ้มบนมือถือได้ · **banked หลัง dock-config** · เฟส B = GATE 1 รอ P'Aim
- 🏛️ **SA dock refine (`0940e9b`):** resize=ปรับ*ขนาด*(ลด default · UX ฟันธง ≥44px) · **ข้อ5 ชื่ออ้างอิง 80% มีแล้ว** — 13 ปุ่ม id+ชื่อไทยครบ (keys/grip/undo/redo/play/stop/soundctl/setting/save/playAll/export/draft/preview) ส่ง UX ทำตาราง · id+ชื่อไทย+data-cell มีครบแล้ว = test hook+aria ฟรี · **summary-dockkey-paim = UX เขียนคนเดียว (ดึงจาก SA report) กันชน** · 🐞 backlog: pk-drawer header เคลม "แชร์พระคำ" เท็จ (phrakham ไม่ track) → dev แก้คอมเมนต์
- 🔴 **SA verified-RLS-leak DS+SQL ✅ พร้อม (P1 · รอ P'Aim เคาะจังหวะ)** `docs/reports/verified-rls-leak.md` · `db/005`(P1)+`db/006`(queue author_id) (`fcf15ff` · sa-standing) — พิสูจน์ anon เห็น 94 เพลงยังไม่ตรวจจริง (gate อยู่ client เท่านั้น) · **fix 0 บรรทัดใน src (RLS โปร่งใส) · leak-hunt ครบ (PWA/print/MP3/JSON ไม่รั่ว · จุดเดียว=`songs` SELECT)** · ⛔ **PO run SQL เอง · รอ P'Aim เลือกจังหวะ: ก.เปิดเลย(public เหลือ 32) ข.เร่งตรวจ batch ก่อน ค.เปิดพร้อม deploy สื่อสารทีม** · count นับสดตอนรัน ไม่ hardcode
- 🎨 **UX editor-toolbox (hover/tap) design ✅** `docs/us/selection-driven-editor.md §10` + `summary-editor-toolbox-paim.md` (`4b36760` · uxui-standing) — ❌ ไม่ลอก Photoshop (แถบลอย desktop-only) ✅ Canva/Apple callout เกาะ element + tap (มือถือได้จริง) · pleng มี `slot-tools` เกาะ+keyboard-safe อยู่แล้ว = refine · **banked รอ dock-config เสร็จก่อน (P'Pao priority)** · เฟส A(ยุบ+ไฮไลต์) / B(รื้อ · รอ P'Aim) · 3 คำถาม feasibility → parked กับ toolbox
- 🏛️ **SA feasibility contextual toolbox ✅** `docs/reports/contextual-toolbox-feasibility.md` (`93d05b8` · `sa-standing`) — **โครงรองรับ (verify จริง: hover=0 ต้องเพิ่ม · selection 5 ระดับมีครบ · positioning 3 primitive มี ขาดรวมเป็น helper เดียว)** · ยัง refine (⚠️ SA ค้าน: verdict "refine" เดิมเป็นของทิศ *ยุบปุ่ม* ที่ P'Aim ปฏิเสธ ห้ามยกมาดื้อ ๆ · ขนาดผันตาม scope UX เลือก) · **3 ข้อต้องเติม: hover-preview · anchored toolbox ครบ line+stanza · trigger consistent 5 ระดับ** · ⚠️ มือถือ: scope ที่พิมพ์ (โน้ต/พยางค์) toolbox ต้องลอย**เหนือ** (คีย์บอร์ดบัง) + คีย์ลัดห้ามแย่ง Space/Enter/Backspace · **ทั้งงานใน `EditorMode.vue` = 1 ไฟล์ 1 สาย** · ต่อ UX ออกแบบ flow → SA ตรวจ feasibility คู่ → P'Aim GATE 1

## 🎯 สถานะสด 17 ก.ค. — **P'Aim สั่งเหลือ 2 สายหลัก** (เพลง 30% · อีก 70% ผ่านพี่เปาคนเดียว = คอขวด)
- **สาย A · SA UX design (`local_91b05cf3`):** ออกแบบเสร็จ `2a`(IdentityStrip ขึ้น shell +ชิปคิวอนุมัติ `📨 n รอตรวจ`) · `2b`(รอเฟส1 merge) · `2c`(SongList ชื่อเต็ม ~6 บรรทัด CSS · `docs/ds/songlist-full-names.md`) · เฟส3(Aa+สลับปุ่มเซฟ/เผยแพร่ `DS §5-6`) · **⛔ SA ล้วน ยังไม่ commit โค้ด**
- **สาย B · dev เฟส1 (`local_996eafda`) ✅ MERGED เข้าฐาน (`13f0f52`):** "ห้ามเลือดไหล" — reviewingDraft = computed derive (เปิดทาง list/URL/refresh/กล่องร่าง ได้ผลเดียวกัน) + `.pending-alert` เตือนก่อนทับ + ปุ่มพูดความจริง · **rebase ลงฐาน deploy ก่อน merge (ของ live อยู่ครบ) · dev แก้บั๊ก auto-merge deleteDraft(computed readonly) เอง · 293 เทสต์เขียว** · ⚠️ เบี่ยง DS จุดเดียว: ชื่อเจ้าของอยู่แถบเตือน+aria ไม่ใช่บนปุ่ม (360px ล้น AC-9) → ถ้าอยากได้บนปุ่ม = เฟส 2 (DockKey ellipsis)
- **⏹️ ปิดแล้ว (ของอยู่ใน git ครบ):** เชลโล · เฟอร์มาตา(deployed) · MP3(deployed) · slur/เส้นเอื้อน · `#`ทั้งห้อง(design) · ถังขยะ(design `task_29bd8550`) · UX baseline(`local_ca5e6663` ส่ง doc แล้วปิด)
- **🚩 ค้าง P'Aim เคาะ:** (1) `2c` ให้ SA commit เอง(6 บรรทัด) หรือ dev ทำ (2) เฟส2 `2a` แตะ `Studio.vue` = ปลดล็อกแล้ว (`mission.md 8fd3b0a`) จ่าย dev ได้เมื่อพร้อม
- **📥 issue ใหม่รอ triage:** 17(พรีวิว·ซ้ำ16) · 19(เส้นเอื้อนไม่ต่อ·รู 330/331 เดาจากช่องว่าง→รอทำสัญลักษณ์เอื้อนแยก) · 20(แถบหาย=deployed แล้ว) · 21(พิมพ์ 2 คอร์ด/ห้องไม่ได้·สาย editor)
- **🐞 backlog:** null byte ใน `EditorMode.vue:1569` (ทำ ripgrep หยุดอ่าน 34%) · `FontTool.vue` ตายทั้งไฟล์ · ShellBar test ผี · `publish_draft` บันทึก author_id ผิดคน(=คนอนุมัติ ไม่ใช่คนเขียน) · verified GATE ไม่มี RLS (anon เห็น 104 เพลงยังไม่ตรวจ) · GITHUB token หมดอายุ(เช็ก Actions เองไม่ได้)
- **📖 บทเรียน PM วันนี้ (memory `feedback_never_ask_user_what_is_correct` + จะเขียนเพิ่ม):** ปะทีละปุ่ม 6 ใบ = พี่เปาบ่นเรื่องเดิมซ้ำ = แก้ผิดจุดตั้งแต่ใบแรก · เวลาผู้ใช้บ่นซ้ำครั้งที่ 2 = สัญญาณ ไม่ใช่บั๊กใบใหม่ · "ห้าม merge เอง" ทำรายงานติดในสาขา = สายอื่นทำซ้ำ → **PM ยกรายงานเข้าฐานทันทีที่เสร็จ**

---

## ▶ RESUME (HAND-OFF จาก pm26 · 16 ก.ค. ค่ำ · PM session ถัดไป = **pm27**)

**สถานะ: สะอาด · ไม่มีงานค้าง/บล็อก · ไม่มีสายย่อยเปิดค้าง (archived หมด)**
- 🎉 **รอบ 29 LIVE `f573d2d` = origin/main** (deploy ล่าสุด · 16 ก.ค.) — ชื่อแอพไทย "เพลง.พระคำ" + คู่มือทำเพลง (`/#/notation`) + เมนู "คู่มือ ▾" 2 อัน + ไอคอนพื้นขาวกุญแจทอง(เฉด E) + shell overflow fix + นัชวิลล์→คอร์ดโรมัน + a11y ตาราง + accent knob ออก(เสียง=เดิม) · verify prod ครบ · _(ประวัติรอบ 22-29 + DEPLOYED blocks + รายละเอียดงานด้านล่าง)_
- 🎹 **golden-piano (เปียโนทอง) = จบ · P'Aim พอใจ** · สรุปเทคนิคส่งที่ปรึกษา = `docs/reports/golden-piano-tech-summary.md`
- **git:** base `studio-shell-redesign` = origin/main = `f573d2d` · สะอาด · memory synced OneDrive
- **cleanup:** archived สายย่อยทั้งหมด · ❌ Remix cancelled · 🎻 violin archived (parked · เปิดใหม่เมื่อมี sample ดีกว่า · branch `charming-cori`+Iowa samples ใน git) · 📖 phrakham = คนละโปรเจกต์ (ไม่แตะ)

**🎼 NEW (16 ก.ค. ค่ำ): reference-track analysis — `task_cd47ede8` (รอ P'Aim กดชิป · แทน `task_1bffa814` ที่ยกเลิก)**
- **โจทย์:** P'Aim ให้ 2 เพลงที่ฟังแล้ว "เพราะมาก" = เป้าหมายเสียงของ pleng → **ถอดเทคนิคที่ลอกได้** (Canon cello&piano wedding · A Thousand Years piano-cello cover · ใน `OneDrive/1 Devotional/hymm/mp3/บรรเลง/`)
- **brief = `docs/pm/brief-reference-analysis.md` v2 (`7882016`)** · **แก้ตรง root cause ที่ Canon spike สรุปเอง:** เชลโลเราเป็น static conditional logic ต้องการ **human-authored phrase-map** → งานนี้ถอด map จากคนเล่นจริง
- **🧠 PM triage คำแนะนำที่ปรึกษา (P'Aim สั่งให้ค้านได้ · อยู่ในไฟล์ §"PM คัดกรอง"):** ✅ เอา "ลมหายใจร่วม" เป็น P1 **แต่ต้องวัดความหนาแน่นโน้ตด้วย ไม่ใช่แค่ dB** (ลอกแค่ dB = เปียโนเบาลงแต่ยังรัว = ฟังออกว่าโดนหรี่) · 🔄 "ตัดเบส <200Hz" → เปลี่ยนเป็น **voicing** (ไม่เล่นทับแต่แรก) ไม่ใช่หรี่ทับแบบช่างมิกซ์ · ❌ **"หน่วงแบบสุ่ม"** (ensemble offset มีทิศทาง+สม่ำเสมอ · หัวเสียงเชลโล 30-80ms ปน onset · **MP3==live → ห้ามสุ่มสด ใช้ `rng.js` seeded**) · ❌❌ **"ผลลัพธ์เป็น if-else"** = สิ่งเดียวกับที่ทำเชลโลพัง → ค่าคงที่เอาได้ **แต่โครงต้องเป็น phrase-level role plan** · ⬇️ "ถอดสูตรรอยต่อคันชัก" ลดเป็น P3 (sample หัวโน้ตแก้ไม่ได้ · ดัด gain = `celloEngine` = "หวูดเรือ" → ทางแก้จริงคือ legato maps ของ Karoryfer = งานสาย bake-off) · ⚠️ **n=2 ห้ามเฉลี่ยรวม** (Canon=เชลโลเดินเบส · ATY=เชลโลร้องนำ → แยกรายงาน)
- **มาตรฐาน:** วัดได้เท่านั้น (Claude ฟังไม่ได้ · ห้าม "ฟังแล้วรู้สึกว่า") · ทุกข้อต้องแมปเป็นพารามิเตอร์จริงใน `src/lib/arranger/` · ⚖️ ห้าม commit MP3/stems · ห้ามลอก A Thousand Years โน้ตต่อโน้ต (มีลิขสิทธิ์)
- **ของที่ PM ต้องการ:** ตาราง "เทคนิค → แก้ไฟล์ไหน" + แยก 3 กอง (ลอกได้เลย / ต้องเขียนใหม่ / เพดาน sample ฟรี)

**🎻 สถานะสด (17 ก.ค. เช้า) — เชลโลผ่านหู P'Aim แล้ว · รอ 5.2+5.3:**
- ✅ **ค่าที่ P'Aim เคาะ (ล็อกแล้ว · ถอยกลับได้เสมอ):** หัวโน้ต **5%** · เลื่อนตัวโน้ต **10ms** · ตัวลาก **`p`** · หัว **`mp`** — **เป้าหมาย "ไม่แสบแก้วหู ฟังลื่น" บรรลุแล้ว**
- ✅ **vibrato 22 cents = P'Aim ชอบ** ("มิติชัดขึ้นจริง ๆ โหยหวนดีกว่า") · เหลือแก้ 2 ข้อ → 5.2
- ✅ **กังวานเปียโน สร้างแล้ว (+0 MB)** · รอ P'Aim ฟัง
- ❌ **RR (คันชักคู่) พักถาวร** — วัดแล้วสลับไม่ได้เชิงโครงสร้าง (t50 ต่าง 200ms) · **ประหยัด 1.09 MB** · ปุ่มปิด default อยู่แล้ว
- 🔄 **5.2 + 5.3 = `task_5968ddc8`** (สายใหม่ · สายเดิม context เต็ม · ต่อบน `cello-bakeoff` `50f4532`) — vibrato polish + **⭐ ปุ่มความกว้างดัง-ค่อย** (แก้ "ออร์แกน" · กฎเดียวครอบ 124 เพลง)
- **🎧 URL: `http://10.189.195.98:5451/docs/spikes/cello-marcato.html`** (IP เปลี่ยนบ่อย — เช็กก่อนส่ง P'Aim ทุกครั้ง · PM เคยส่ง IP เก่าให้)
- **⛔ ยังไม่แตะแอพจริงเลย** — ฐานล้ำ `main` ด้วย **เอกสารล้วน ไม่มีโค้ดสักบรรทัด** · เชลโลยัง `disabled:true` · เปียโนที่ deploy ไม่ถูกแตะ
- **🚩 รอ P'Aim เคาะ:** เอา **เส้นความเข้ม** เข้าจริงไหม (แตะ `dynamics.js` = ไฟล์ที่เขาสั่งทิ้งงานไป) — **ฟังปุ่มก่อน แล้วค่อยตัดสิน**

**🎻🎻 cello-soften จ่ายแล้ว (16 ก.ค. ค่ำ) — P'Aim ตัดสินด้วยหูแล้ว · bake-off ปิดเคส:**
- ❌ **Iowa ตัดทิ้ง** "ไม่ดีเลย" (ตรงกับ t50 245ms + เพี้ยน 26-38 cents) · ❌ **SSO ตัดทิ้ง** ลิขสิทธิ์สุ่มเสี่ยง · ✅ **Karoryfer ผ่าน** + **P'Aim: ไม่มี vibrato ไม่เป็นไร**
- **🔴 โจทย์ใหม่ = "ยังแสบแก้วหู"** · **DoD (P'Aim เอง):** "ไม่แสบหู ฟังลื่น เพราะระดับหนึ่ง พอแล้ว" — **ไม่ใช่ "เหมือนคนเล่นจริง"** (P'Aim ยอมรับเองว่า 100% เป็นไปไม่ได้ · ขอ 80% จาก 20% effort)
- **brief `docs/pm/brief-cello-soften.md` (`eb07373`)** · ส่ง send_message กลับสาย bake-off เดิม (harness+เครื่องมือวัดมีครบ = refine ไม่ใช่ redesign)
- **⭐ ขั้น 1 (ทำก่อน แล้วหยุด):** เราใช้ Karoryfer แค่ **1 ใน 8** — `Samples/sus` = 136 ไฟล์ (17 pitch x **4 ระดับ p/mp/mf/f** x คันชัก `_d`/`_g`) แต่ `prepare-samples-cc0.mjs:33` โหลดแค่ `*_mf_d.wav` → **ทุกโน้ต = น้ำหนักกลางค่อนข้างหนักเท่ากันหมด = สมมติฐานหลักของ "แสบ"** · ชั้น p/mp = **ความนุ่มของจริง** (เนื้อเสียงเปลี่ยน ไม่ใช่แค่ค่อยลง) **ไฟล์มีอยู่แล้ว รอโหลด** → ส่งมอบ = ปุ่มสลับ p/mp/mf ให้ P'Aim ฟัง
- **🅿️ ขั้น 2-4 ห้ามทำ:** ห้องเสียง/ระยะห่าง (Karoryfer close-miked unprocessed) · EQ วัดเป้าจากแผ่นจริง (ไอเดีย P'Aim: อ้างอิงความนุ่มจาก mp3 ที่มี = เปลี่ยน "จูนให้เพราะ" → "ทำตัวเลขให้ตรงเป้า") · brightness ตาม dynamics · vibrato/legato/pan/crossfade
- **เหตุผลที่ต้องทำทีละขั้น:** ใส่รวดเดียว = P'Aim แยกไม่ออกว่าอะไรช่วย อะไรทำพัง = **กับดักที่ทำไวโอลิน+เชลโลพักไปแล้ว 2 รอบ**

**🎻 cello bake-off ✅ เสร็จ · 🔴 รอ P'Aim ฟังตัดสิน (16 ก.ค.):** branch `cello-bakeoff` `31adff8` · report `docs/reports/cello-bakeoff.md` · **URL: `http://10.189.195.98:5451/docs/spikes/cello-bakeoff.html`** (:5450 เปิดค้าง · 4 ปุ่ม + MP3 · มือถือได้)
- **4 คลิป** เพลง #1 ท่อนแรก 20.3s · เปียโนทอง `arrange()`+splendid-grand รอบ 27 · เชลโลร้องนำ: **A Karoryfer · B SSO · C Iowa · D เปียโนอย่างเดียว (ตัวควบคุม · dev เพิ่มเอง = initiative ถูก)**
- **PM git-verify PASS:** scope สะอาด (spike files + report เท่านั้น · `git diff` vs base ฝั่ง `src/lib/` = ว่าง) · ไม่มี .ogg/.wav/.mp3 ใน git (SSO license ปลอดภัย) · 673 tests (fail 1 = `notationLint` pre-existing)
- **ยุติธรรมพิสูจน์แล้ว:** เปียโน A/B/C เหมือนกันเชิงตัวเลข (ต่าง 3e-8 = float rounding) · เชลโลดังเท่ากัน · ทุกคลิป RMS −24.0dB ไม่คลิป · **near-miss ที่ dev จับเอง:** normalize ทั้งไลบรารี → B ดังกว่า A 6.5dB (Karoryfer เสียงสูงเบากว่าต่ำ 14.7dB) = P'Aim จะเลือก B เพราะดังกว่า → แก้เป็นวัดจากท่อนที่ใช้จริง
- ⭐ **negative delay ยืนยันสมมติฐาน PM เป๊ะ (t50 วัดเอง ไม่ยกเลขที่ปรึกษา):** Grand **0ms** · Karoryfer **40ms** · SSO **55ms** · **Iowa 250ms** → **"หวูดเรือ" = ตัวเลขแล้ว** (Iowa ตามหลัง ~1/3 บีต · สายวิเคราะห์วัดคนจริง mean≈0 = มาตรงบีต) · **PM อนุมัติ t50 แทน time-to-peak** (สเปก PM เขียนศัพท์ผิด · time-to-peak จริง 320–730ms = เลื่อนแล้วมาก่อนบีต)
- **🔑 dev จับได้ว่า PM ผิด: shipped cello = Karoryfer มาตลอด** (`tools/prepare-samples-cc0.mjs:33`) → brief ตั้ง A vs C เป็นคนละตัวทั้งที่เป็นตัวเดียวกัน · dev สลับไปใช้ Iowa จริงจาก `dreamy-lumiere` = การทดลองรอด · **PM ผิดเพราะไม่อ่าน `tools/` ก่อนค้นเว็บ** → memory `pleng-two-cellos-karoryfer-vs-iowa`
- **PM เคาะ: memory เดิม *ไม่ผิด* ไม่ต้องแก้** — `slice-iowa-cello.sh` + `cello-iowa/` **มีจริง** (commit `4d8175c` · อยู่บน spike branch ไม่ใช่ base) → `pleng-smplr-vellayer-relative` + `pleng-cello-piano-spike` พูดถึง Iowa ฝั่ง spike = ถูกต้อง · **เชลโล 2 ชุดอยู่คู่กัน ไม่ใช่ความขัดแย้ง** (ไวโอลิน shipped = VSCO-2-CE)
- **🐞 bug ค้าง (ไม่กระทบผู้ใช้ · ไม่แก้ตอนนี้):** `CC0/cello/preset.json` pitch **ต่ำไป 1 octave ครบ 17/17** (ประกาศ 24 · วัดจริง 36 · เชลโลเล่น MIDI 24 ไม่ได้ = ฟิสิกส์ยืนยัน) · cello/violin `disabled:true` ใน `soundOptions.js` → ปลอดภัย · ⚠️ ถ้าเปิดใช้ ต้องแก้ preset **+ ไล่ที่เรียกใช้** (spike `render-real-audio-spike.html` เล่น `b-12` = อาจชดเชยมือไว้)
- **✅ dev ตอบกลับ PM ครบ (`2c19715`):** negative delay อยู่ในคลิปแล้ว + **มี checkbox ปิด/เปิดให้ P'Aim A/B ว่าช่วยจริงไหม** · **dev พยายามหาหลักฐานค้าน PM เองแล้วไม่เจอ:** เช็กว่า Iowa 245ms เป็น "ช่วงเงียบหน้าไฟล์" หรือไม่ (ถ้าใช่ = ต้องตัดหัวไฟล์ ไม่ใช่เลื่อน onset + คำอธิบาย "หวูดเรือ" จะผิด) → **เสียงเริ่มมาที่ 5ms แต่ไต่จริง 240ms = การบวมของคันชักจริง** · dev ถอนคำ over-claim เรื่อง memory เองแล้ว (สาเหตุ: หาแค่ใน working tree ตัวเอง = พลาดแบบเดียวกับ PM เป๊ะ) · เติมตัวเลขวัดเข้า memory `pleng-two-cellos-karoryfer-vs-iowa` แล้ว
- **▶ ถ้าไม่เพราะทั้ง 3 = พักถาวรได้อย่างสบายใจ** — รอบนี้ตัดตัวแปรกวนหมดแล้ว (ดัง/จูน/ออกเทฟ/เวลา/เรียบเรียง) เหลือเพดาน sample ฟรีจริง ๆ

**🎧 P'Aim ear ground-truth (Canon · 16 ก.ค.) — พลิกข้อสรุปสายวิเคราะห์ · เก็บไว้ใช้ยาว:**
- P'Aim: "1:30 โดยเฉพาะ **2:00–2:25 cello นำ** · **2:25–2:35 เปียโนนำ** · แล้วผลัดกัน **เหมือนคุยตอบกัน ถึง 3:30** · **3:45 cello นำ**" · **"นำ = เด่น"**
- ⚡ **presence ≠ prominence** — สายวิเคราะห์วัด "ใครส่งเสียง" (เชลโล 93%) → สรุปผิดว่า "ไม่มีการผลัดกันเล่น" · P'Aim ได้ยิน "ใครเด่น" = **สลับกันชัด** · **ทั้งคู่เล่นตลอด + ผลัดกันนำ ไม่ขัดกัน**
- 🔍 **หลักฐานอยู่ในรายงานแล้วแต่ไม่มีใครตีความ:** `balance swing p10→p90 = 9.0 dB (Canon) / 17.9 dB (ATY)` = **การสลับนำนั่นเอง** (median +0.7 dB ปกปิดไว้เพราะเป็นค่าเฉลี่ยของการผลัดกัน)
- ✅ **นาที P'Aim = label แก้ confound ของรอบแรก** (รอบแรกเทียบ "เชลโลเล่น vs เงียบ" = เทียบกลางเพลงกับอินโทร ซึ่งเบาอยู่แล้ว) · 2 ช่วงใหม่อยู่กลางเพลงทั้งคู่ ต่างแค่ใครนำ → **นี่คือ human-authored phrase-map ที่ตามหา · คนเขียน = P'Aim**
- **จ่ายรอบ 2 แล้ว** (send_message → สายวิเคราะห์): วัด balance/density/register ที่ 120–145s (cello นำ) vs 145–155s (piano นำ) + จังหวะสลับช่วง 155–210s

**🅿️ round-2 audio techniques = พักไว้ (`docs/pm/audio-round2-techniques.md` · `c2a9f89`) — ใช้หลัง P'Aim เลือก sample เท่านั้น**
- ที่มา = คำแนะนำที่ปรึกษาชุด 2 (16 ก.ค.) · **PM จับได้ว่าที่ปรึกษาขัดกันเองในเอกสารเดียว:** "Attack Time Breathing" (หน่วงเชลโล**ช้าลง**) vs "Negative Delay" (เลื่อนเชลโล**เร็วขึ้น**) = ตรงข้าม 100% → **Negative Delay ชนะ** (เชลโลช้าตามธรรมชาติอยู่แล้ว หน่วงเพิ่ม = ช้าซ้อนช้า) · ทิ้ง Attack Time Breathing
- **ส่งเข้า bake-off แล้ว 1 ข้อ = negative delay** (เหตุผล: วัดได้ ไม่ใช่รสนิยม · กระทบ 3 คลิปเท่ากัน ไม่บิดผล · **กัน false negative** ที่ P'Aim จะเทหมด 3 ตัวเพราะเชลโลตามหลัง ทั้งที่เป็นความผิดเรา)
- **พักไว้:** pan (ขยับเฉพาะเชลโล ไม่ใช่เปียโน · มือถือ mono ไม่ได้ยิน = value ต่ำ) · layer crossfade (ต้องผ่าน workaround `pleng-smplr-vellayer-relative` · เสี่ยง "เชลโล 2 ตัว" + CPU 2 เท่า) · sustain-tail (เรามี shared reverb bus อยู่แล้ว · เหตุผล sympathetic resonance ของที่ปรึกษาผิด)
- **ตรงกับ PM อยู่แล้ว:** phrase-map 2 โหมด · <200Hz→voicing · ไม่ใช้ if-else ต่อโน้ต

**🎻 NEW (16 ก.ค. ค่ำ · P'Aim สั่ง): cello bake-off — `task_4ebc6bc9` 🔵 RUNNING (P'Aim กดแล้ว · +negative delay ส่งเข้าแล้ว)**
- **โจทย์:** P'Aim ฟัง YouTube แล้วชอบ **เปียโน+เชลโลคู่กัน** (เชลโลเดี่ยว = น่าเบื่อ) → ลองอีกรอบ · **ต้องใช้วาทยกรเปียโนทอง (`src/lib/arranger` + splendid-grand) คุม** (P'Aim สั่งเอง)
- **brief = `docs/pm/brief-cello-bakeoff.md`** (`b4d9e22`) · ส่งมอบ = 3 คลิป A/B/C ท่อนเดียวกัน ต่างแค่ sample + หน้าเทียบ + MP3 · **หูของ P'Aim ตัดสิน · ห้าม SA วนเดาความเพราะ** ([[pleng-aesthetic-audio-needs-ear]])
- **research แล้ว (16 ก.ค.):** ✅ **Karoryfer x bigcat = CC0 จริง** (P'Aim โหลดไว้ `C:\Users\aimkr\Downloads\karoryfer-bigcat.cello-master`) 4 ระดับ p/mp/mf/f × คันชัก d/g · **SFZ มี legato maps** · ⚠️ no vibrato · ⚠️ **SSO = CC Sampling Plus 1.0 (retired) → ทดลองในเครื่องเท่านั้น ห้าม commit sample** · ❌ **Pianobook ตัดทิ้ง (EULA ห้ามแจกต่อ = PWA ใช้ไม่ได้)** · ❌ VSCO 2 CE 2 ระดับ (แย่กว่า Iowa 3) — ที่ปรึกษาแนะนำผิด
- **ผลลัพธ์ที่เป็นไปได้:** เพราะ → ต่อยอด preset · ไม่เพราะ → **พักถาวร** (เพดาน sample ฟรี ไม่ใช่จำนวนชั้นความดัง)

**▶ งานถัดไป (ไม่บล็อก · รอ P'Aim หยิบ):**
1. 🔴 **GATE (สำคัญสุดระยะยาว):** ทีมทยอยกด "✓ ตรวจแล้ว" ให้เพลง (public เห็นเฉพาะ verified · ~8/124) → เพลงถึงจะโชว์ครบ · tracker `docs/pm/review-anuchon.md`
2. **post-deploy verify มือถือ (P'Aim/พี่เปา · optional):** iOS ลบไอคอนเก่า+ติดตั้งใหม่ (ชื่อ "เพลง.พระคำ"+ไอคอนทองพื้นขาว) · Android install PWA · เปิดคู่มือทำเพลง
3. **to-do พี่เปา:** issues4(โครงเพลง▾)/6(คลิก preview→แก้)/7 · print โน้ต (§🐞/🐞 to-do)
4. **backlog:** เนื้อ 3 ภาษา · สิทธิ์ลบเพลง · import เล่มใหญ่(พัก) · shared-core token (§📌)
5. **parked:** 🎻 violin (รอ sample หลาย-dynamic) · phrakham parity (ทีมพระคำ)

**--- ประวัติ pm26 (รายละเอียดรอบ 27-29) ด้านล่าง ---**

**สถานะจริงตอนนี้ (pm26 · sync กระดานล่าง):**
- 🎉 **รอบ 29 LIVE `f573d2d`** (16 ก.ค. · P'Aim "go") — **ชุดใหญ่:** ชื่อแอพไทย "เพลง.พระคำ" + คู่มือทำเพลง (มาตรฐานการเขียนโน้ต /#/notation) + เมนู "คู่มือ ▾" 2 อัน + ไอคอนใหม่พื้นขาวกุญแจทอง(เฉด E) + shell overflow fix + นัชวิลล์→คอร์ดโรมัน + a11y ตาราง + เอา accent knob ออก(เสียง=เดิม) · **verify prod:** Actions success · Last-Modified 12:12 · app-name meta "เพลง.พระคำ" · bundle มี "คู่มือทำเพลง"/"มาตรฐานการเขียนโน้ต" · ไอคอน 512 พื้นขาว(ไฟล์เล็กลง) · **post-deploy:** archive 6 สายรอบ 29 + verify มือถือ (ไอคอน/ชื่อแอพ iOS re-install · install PWA Android)
- 🎉 **รอบ 28 LIVE `09b9712`** — **ชุด 3 งาน:** (1) คู่มืออัปเดต + การ์ด "ขอบคุณ & แหล่งที่มา" ใน About (แหล่งเพลง+sample+FluidR3_GM CC-BY+เทคนิค→GPL · P'Aim เคาะถ้อยคำ) (2) ปุ่ม "ติดตั้งแอพ" PWA ในเมนู ☰ (3) ป้ายคอร์ด→"คอร์ดโรมัน (I IV V)" · verify prod: Actions success · live bundle มี 3 marker ครบ · Last-Modified 01:42 · **post-deploy:** 🤖 verify ปุ่มติดตั้ง Android บน **live https** ได้แล้ว (dev/LAN ทำไม่ได้) + iOS · archive 3 สาย (chord/pwa/guide) · tidy: guide-② "คอร์ดโรมัน" + นัชวิลล์-in-docs
- 🎉 **รอบ 27 `ad63021`** — golden-piano เปียโนเดี่ยวร่างทอง (referee+MP3 Grand จริง) · accent ear-check ค้าง
- ✅ **รอบ 26 `bd53742`** — whole-site parity เพลง↔พระคำ จบครบ 2 เว็บ · เหลือ P'Aim/พี่เปา verify Android 2 จุด (§✅✅ PARITY จบ)
- ✅ **Canon spike ปิด** — เปียโนสำเร็จ · เชลโล/ไวโอลินพัก (§🎻🎹 Canon)
- 📝 **guide-update ✅ เสร็จ · รอ P'Aim ดูถ้อยคำ + PM gate (15 ก.ค.):** `task_ee559d4f` · branch `guide-update` (fork ถูกฐาน · `1393eed`) · report `docs/reports/guide-update.md` · **PM git-verify: scope สะอาด 2 ไฟล์ (Guide.vue+About.vue · ใช้ Icon เดิมไม่แตะ Icon.vue) · 649 tests + build ผ่าน** · Guide.vue ② ตรง UI จริง (เสียงดนตรี 5 แกน/golden-piano/คาราโอเกะ/export PDF-JSON-MP3/ป้ายตรวจแล้ว team-only · ไม่พูดถึงปุ่ม A/B ที่ถอด) + ไอคอน 1/หัวข้อ · About การ์ด "ขอบคุณ&แหล่งที่มา" (9 เล่ม+hymnal · sample · FluidR3_GM CC-BY · เทคนิค→ชี้ GPL)
  - **🎧 URL ให้ P'Aim ดูของจริง (โดยเฉพาะถ้อยคำ "ขอบคุณ" = ให้เกียรติคน):** `http://10.189.195.98:5310/#/guide` และ `/#/about` → เคาะถ้อยคำ → PM gate เต็ม + merge → deploy รอบ 28
  - **🚩 2 เรื่องนอก scope (ตั้งงานแยก):** (1) **toggle คอร์ดตัวเลข mismatch** — เมนู label "เลขนัชวิลล์ (1 4 5)" แต่ `chordToRoman()` เรนเดอร์เลขโรมัน I/IV/V + คู่มือ ① เรียก "คอร์ดโรมัน" → SA เคาะแล้ว sync label/badge/output/① ให้ตรง (dev เขียน ② กลางๆ เลี่ยงไว้) (2) ป้าย ✓ตรวจแล้ว team-only — เผื่อ P'Aim ปรับถ้อยคำ
- 📱 **pwa-install ✅ เสร็จ + PM merge เข้า base แล้ว (`86faea5` · รอ batch deploy 28):** ปุ่ม "ติดตั้งแอพ" ในเมนู ☰ (beforeinstallprompt Android/desktop + iOS คำแนะนำ Share→หน้าจอหลัก · standalone แล้วไม่โชว์) · ไฟล์ใหม่ InstallAppTool+pwaInstall.js + ShellBar/Icon/main.js · git-verify สะอาด ไม่แตะ manifest/sw/สายอื่น · 658 tests (+10) + build ผ่าน
  - **⚠️ verify Android = post-deploy:** ปุ่ม beforeinstallprompt ยิงเฉพาะ **secure context (HTTPS)** → LAN http บนมือถือ ปุ่มไม่โผล่ (โค้ดถูก · verify แล้วบน localhost-secure + tests) → **ตรวจ Android จริงบน live https หลัง deploy** · iOS ทดสอบผ่าน LAN ได้เลย (`http://10.189.195.98:5387/`) · PM แนะนำ **ไม่ตั้ง tunnel** (บริการภายนอก · overkill สำหรับแพทเทิร์นมาตรฐาน)
- ✅ **verified-badge team-only = ทำถูกอยู่แล้ว (ไม่ต้องแก้):** `showVerifiedBadge(s, loggedIn)` บังคับ loggedIn · anon ไม่เห็น (git-verified `bookshelf.js`) — P'Aim ขอ "เห็นเฉพาะทีม" = เป็นอยู่แล้ว
- 🎼 **chord-roman-label ✅ เสร็จ + PM merge เข้า base แล้ว (`83de3c5` · รอ batch deploy 28):** P'Aim เคาะ "โรมัน I IV V" · label "เลขนัชวิลล์ (1 4 5)"→"คอร์ดโรมัน (I IV V)" (Studio:150+SongViewer:48) + badge '145'→'I·V' (SongViewer:504) · git-verify scope สะอาด (2 code + report · ไม่แตะ chords.js/Guide/About/ShellBar) · 649 tests + build ผ่านบน base
  - **📌 follow-up (ไม่บล็อก):** คำ "นัชวิลล์" ยังค้างใน docs (docs/us ps3-dock:51/ps3-viewer:9 · `docs/ds/dockkey-print-edit.md:26` badge เดิม "145" ควร sync "I·V" · design prototypes 3 ไฟล์ · chords.js:1 comment) — เก็บกวาดรอบว่าง · ตอน merge guide-update: ให้ ② พูด "คอร์ดโรมัน" สอดคล้อง
- 🏷️ **app-name-thai ✅ เสร็จ + PM merge เข้า base แล้ว (`f6641b8` · รอ deploy รอบ 29):** `task_cf8c062a` · เติม 4 meta ใน index.html (`apple-mobile-web-app-title "เพลง.พระคำ"` + application-name + capable×2) → iOS home-screen โชว์ไทยแทน URL อังกฤษ · git-verify: แตะ index.html ไฟล์เดียว (+7) · build ผ่าน · meta อยู่ใน dist ครบ · 658 tests · **verify iOS = post-deploy (ต้องลบไอคอนเก่า+ติดตั้งใหม่)** · dev report `docs/reports/app-name-thai.md` (ไม่ได้ commit บน branch · เนื้อหาใน git log) · **รอ P'Aim สั่ง deploy รอบ 29**
- **git:** base `studio-shell-redesign` = origin/main = `f573d2d` (รอบ 29 synced · FF) · สะอาด · session ต่อไป = `pm27`
- **🧹 post-deploy cleanup รอบ 29 (16 ก.ค.):** archived 8 สาย (notation-build/icon-v2/cleanup-round/a11y-tblwrap/accent-soften/app-name-thai/notation-standard-SA/icon-refresh-superseded) · ปิด preview :4180
- **❌ Remix CANCELLED (P'Aim 16 ก.ค.):** "เสียงปัจจุบันดีแล้ว ไม่ทำ Remix" · เป็นแค่ดีไซน์ ไม่เคย build/live (ไม่มีโค้ดต้องแก้) · design docs (`docs/us/remix.md`+`ds/remix.md`+wireframe) เก็บใน git · archived สาย `hopeful-cori`
- **roster เหลือ parked สายเดียว:** 🎻 violin (`charming-cori` · รอ sample ดีกว่า) · phrakham ไม่แตะ (คนละโปรเจกต์)
- **📌 post-deploy verify (P'Aim/พี่เปา · ไม่บล็อก):** iOS ลบไอคอนเก่า+ติดตั้งใหม่ → ชื่อ "เพลง.พระคำ"+ไอคอนทองพื้นขาว · Android install PWA · เปิดคู่มือทำเพลงบนมือถือ
- 📐 **notation-standard ✅ P'Aim เคาะ GATE 1 แล้ว (16 ก.ค.) · SA อัปเดต IA อยู่ → จ่าย dev:** branch `claude/friendly-nash-594af5` · design ผ่าน (US+DS · route `/#/notation` · 7 หัวข้อ · ⭐ ตาราง §7 แมป arranger · a11y/test) · 4 คำถามเอาตาม default
  - **🔄 P'Aim ปรับ IA (สั่ง SA อัปเดต DS):** เมนู "คู่มือ" = **ดรอปดาวน์ 2 เมนูย่อย** (คู่มือใช้งาน→/#/guide · คู่มือทำเพลง→/#/notation) · **เห็นทุกคน ไม่ผูกล็อกอิน** (ทำเพลงเปิดให้ทุกคน · anon โหลด JSON) · เหตุ: คู่มือทำเพลงไม่ควรซ่อน · ⚠️ dev แตะ **ShellBar.vue** (ไฟล์ร่วม · rebase ก่อน) · Studio link = ทางลัดเสริม
  - **จ่าย dev build:** `task_1862d5ed` สาย `notation-build` · brief `docs/pm/brief-notation-standard-dev.md` · 5 ไฟล์ (NotationStandard.vue ใหม่ + router + Guide ตัด① + ShellBar dropdown + EditorMode help link · fork ฐานล่าสุด ต่อยอด ShellBar ไม่รื้อ)
  - **✅ notation-build เสร็จ (ชิปใหม่ `task_41ac88e5` ติด · branch `notation-build` `8a24c57` fork ฐานล่าสุด bf62fae) · รอ P'Aim ดู + PM gate:** 11 ไฟล์ (NotationStandard.vue ใหม่ 528 + test · Guide −314 ย้าย① ออกจริง · ShellBar dropdown+test · router · styles · EditorMode help) · **672 tests (+14) · axe Tier-A+B 0 violations · git-verify: fork ถูก · ไม่ชน icon-refresh** · _(⚠️ merge ไม่เอา `.claude/launch.json` ที่พ่วง)_ · **URL P'Aim ดู: `http://192.168.1.124:5420/#/notation` (หรือ localhost:5420)**
  - **📌 dev flag (follow-up ไม่บล็อก):** latent shell bug `main.container flex-shrink:0` → ตาราง 4 คอลัมน์ล้นแนวนอนมือถือ · dev cap `max-width` เฉพาะหน้านี้ (ไม่แตะ shell ร่วม · refine) · root fix ทั้งเว็บ = งานแยก
- 🎨 **icon-refresh ✅ เสร็จ · รอ P'Aim ดู + PM gate:** `task_ff1bfdcd` · branch `icon-refresh` `c25ab4e` (fork a11e1c6) · ชุดไอคอนใหม่ กุญแจซอล+กางเขน full-bleed #363636 (ตัดมุมโปร่ง/baked · ไม่มีเศษ) + maskable-512 · git-verify: แตะเฉพาะ public/icons+site.webmanifest (ไม่แตะ index.html=คง app-name meta · ไม่ชน notation-build) · verify 4 มุมทึบ #363636 · ทองในวง maskable 80% ครบ · build+dist ครบ · ไฟล์เล็กลง (optimized) · **URL: `http://192.168.1.124:5316/` (รูป 512: /android-chrome-512x512.png)**
  - **✅ PM merge notation-build + icon-refresh เข้า base แล้ว (`eee84cd` · 672 tests+build · code-only ไม่เอา launch.json)** — base = app-name + notation + icon พร้อมรอบ 29
- 🧹 **cleanup-round ✅ เสร็จ + PM merge เข้า base แล้ว (`8a596e9`):** dev revert ไอคอนออกทัน (diff ไม่มี public/icons/manifest จริง) → merge: shell overflow root fix (styles.css min-width:0+max-width · 0 h-scroll ทุกหน้า) + นัชวิลล์→คอร์ดโรมัน (chords.js comment+US/DS+prototypes+badge 145→I·V) + guide② · 672 tests+build · **flag:** (a) "นัชวิลล์" ที่ `NotationStandard.vue:339` = เจตนา (อธิบายกฎบ้านเรา) คงไว้ (b) a11y `.tbl-wrap` scrollable-region-focusable → จ่าย a11y-tblwrap
- 🔧 **a11y-tblwrap ✅ เสร็จ + PM merge เข้า base แล้ว (`79d4b1f`):** branch `claude/adoring-benz-0d5526` · `.tbl-wrap` +tabindex/role/aria-label (axe scrollable-region-focusable serious หาย) · git-verify แตะแค่ NotationStandard.vue · build ผ่าน · **พร้อมรอบ 29**
- 🎨 **icon-v2 ✅ เสร็จ · รอ P'Aim เลือกเฉดทอง (A-E) + PM gate:** branch `claude/suspicious-jennings-d3d24e` `8a3f03f` (fork ฐานล่าสุด) · พื้นขาว #fff + กุญแจทองอุ่น · แยก any(84%)/maskable-512(72% ในวง safe-zone 80%) · git-verify: **public icons 7 ไฟล์เท่านั้น** (ไม่แตะ manifest/index.html=คง app-name meta · ไม่ชน) · build ผ่าน · deterministic
  - **5 เฉด (proof `docs/pm/icon-v2-proofs/shades-any.png`):** A#C9982F เข้ม · B#CBA04A goldenrod(default build) · **C#D0A54F อุ่น = PM แนะนำ** · D#D8B063 honey(นวลสุด) · E#C79A3E กลาง · caveat: favicon16 จางทุกเฉด (32/high-DPI ชัด)
  - **✅ P'Aim เคาะเฉด E `#C79A3E` (16 ก.ค.) · PM merge เข้า base แล้ว (`48abf99`):** dev rebuild dc5c77a → 7 public icons เฉด E · verify มุม #FFFFFF ขาวจริง (แทนเทา) · build+manifest ok · **พร้อมรอบ 29**
  - **🎯 รอบ 29 = app-name + notation + icon + cleanup · deploy เมื่อ cleanup เสร็จ + P'Aim ดูรอบสุดท้าย + สั่ง go**
- 🎹 **accent-soften 🔄 PIVOT (P'Aim iterate เสียงตรง 16 ก.ค.) · รอ P'Aim ฟันธงด้วยหู:** `task_81ca20c0` · branch `claude/zealous-chatelet-7cc43a` `c333aff`+`881f566`
  - **เข้าใจผิด:** "เน้นจังหวะแรก"≠ดัง/ค่อย (มันคือความต่างน้ำหนักบีต) · ลด accent→mix แบน→P'Aim ว่า "ออกออร์แกน" · **ที่ต้องการจริง:** accent เต็ม 100% (นุ่ม เป็นเปียโน) + **สไลเดอร์ "ระดับเสียงบรรเลง" (ดัง-ค่อย) ใหม่ · default ดังกว่าเดิมนิด**
  - **final:** (A) technique `volume` slider → master output gain ใน `midi.js playSong`+`audioExport renderSongToBuffer` (หลัง velocity+reverb · ค่าเดียว 2 path=MP3==live) `DEFAULT_VOLUME=1.1` (B) accent default กลับ 1.0 เต็ม (slider ลดได้ถ้าต้องการ) · **scope ขยาย: +midi.js+audioExport (audio lane · ไม่ชน cleanup)** · 681 tests+build · **⚠️ ยังไม่ verify หู** → **P'Aim ฟัง `http://192.168.1.124:5320/` (ปรับละเอียด→"ระดับเสียงบรรเลง" ตัวแรก) เคาะ default volume**
  - **✅ CLOSED = revert (P'Aim "เสียงเดิมดีอยู่แล้ว") · PM merge เข้า base (`20e92f7`):** ทิ้งงาน slider ทั้งหมด → กลับ base + **เอา entry accent ออกจากเมนู "ปรับละเอียด"** (กันสับสน) · accent ยังเปิดเต็มหลัง preset = เสียง=base เป๊ะ · diff แค่ techniques.js+test · 673 tests · **บทเรียน:** aesthetic-audio ต้อง P'Aim ฟันธงทิศก่อนลงมือ ไม่ทำ multi-slider เผื่อ ([[pleng-aesthetic-audio-needs-ear]])
- **🎯🎯 รอบ 29 = ครบทุกชิ้นในฐานแล้ว** (app-name + notation guide+dropdown + icon E + shell fix + คำค้าง + guide② + a11y + accent-menu-clean) · **รอ P'Aim ดู preview รวมรอบสุดท้าย + สั่ง go → deploy**
- **⏳ ค้าง:** (1) **app-name-thai merged เข้าฐาน (`f6641b8`) แล้ว — 🚫 P'Aim สั่ง "รอขึ้นทีเดียวพร้อมกัน" (16 ก.ค.) → HOLD ไม่ deploy เดี่ยว · รวมขึ้นรอบ 29 พร้อม notation-standard (+งานอื่น)** (2) accent ear-check (รอบ 27) (3) verify PWA install Android/iOS บน live

**🎉 DEPLOYED รอบ 27 `ad63021` LIVE (15 ก.ค. · P'Aim "ขึ้นได้") — golden-piano เปียโนเดี่ยวร่างทอง:** `task_033450b9` · branch `golden-piano` (fork ถูกฐาน) · merge code+report only (ข้าม board.md) · **verify prod:** GH Actions run `ad63021` = success · live Last-Modified 15 Jul 23:02 GMT · bundle ใหม่ `index-O2x1Kn-h.js` · FF push `bd53742..ad63021` studio-shell-redesign→main · **PM gate PASS:** scope 12 ไฟล์สะอาด · 649 tests + build ผ่านบน base · fork FF ไม่ diverged
- **📌 post-deploy (pm26):** (1) ⏳ P'Aim ear-check **accent** (เปิดตาม "เปิดหมด" · เคยปิดเพราะกระแทก) — ฟังบน live ได้เลย · เพี้ยน→รอบเก็บ (worktree ใหม่ · branch `golden-piano` เปิดกลับได้) (2) ✅ **archived สาย golden-piano** (`local_d26ef64d`) + ลบ worktree (branch คงไว้ใน git)
- _(ประวัติ dev · commits `9fd1c55`→`0779bd5`→`5a88b73`(legato)→`9765fef`(MP3 arrange)→`6af528d`(MP3 Grand timbre) · report `docs/reports/golden-piano.md`)_
- **PM git-verify (รอบแรก):** scope สะอาดตรง brief · อ่าน referee.js เอง = ตรรกะ no-clash (emb เล่นเฉพาะ gap ≥0.4บีต) + balanceFloor (≤ mel×0.8 + AUDIBLE_FLOOR 0.045) ทำจริง · 645 tests + build ผ่าน
- **🔄 design เปลี่ยน (P'Aim เคาะตรงกับ SA · `0779bd5`):** (1) **ถอดปุ่ม A/B "ก่อน/หลัง" ทิ้ง — วาทยกร = intrinsic เปิดตลอด** (มันคือกฎกันบั๊ก/คุมวินัย ไม่ใช่รสนิยม) ตัด cfg.strictReferee+store+เมนู+icon (2) **default preset "บรรเลง" = เปิดลูกเล่นหมด** (กลับทิศ minimalist 14 ก.ค. — วาทยกรคุม clash+balance ให้แล้ว เปิดหมดได้ไม่เละ · "สงบ" ยัง minimal)
- **🎧 URL:** `http://192.168.1.124:5430/` (P'Aim กำลังฟัง)
- **🚩 ear-check ค้าง:** accent (เน้นจังหวะแรก) เคยปิดเพราะ "กระแทกหนักไป" (14 ก.ค.) ตอนนี้เปิดตาม "เปิดหมด" → P'Aim เช็กว่ากระแทกไหม (ปิดเฉพาะตัวง่าย)
- **✅ MP3 route ผ่าน arrange() แล้ว (P'Aim สั่งทำต่อ · `9765fef` + docs `68ca7a8`):** `renderSongToBuffer` โหมด `arranger:true` → MP3 ที่โหลดได้ arrangement ครบ (referee+legato+ลูกเล่น+humanize+rubato+section) ผ่าน reverb+chord-bus เดียวกับ live · `arranger:false` = ทางเดิม (print/editor ไม่กระทบ) · buffer ยาวตาม perf end จริง (ไม่ clip หาง) · verify MP3 109KB เรนเดอร์สำเร็จ 0 error
- **🎹 MP3 timbre = Grand จริง — ✅ เสร็จ (`6af528d` + docs `341be4c`):** เรนเดอร์ Grand 5-layer ลง MP3 แทน synth → live↔MP3 ตรง **รวม timbre** · **กับดัก verify จริง (memory `pleng-smplr-offline-render` ถูกเป๊ะ):** smplr default scheduler คิวโน้ต >200ms ไปรอ setInterval ที่ไม่เดินใน OfflineAudioContext → RMS=0 · แก้ = inject `Scheduler(lookaheadMs:1e7)` เฉพาะ offline ctx (realtime คงเดิม) + โหลด Grand บน offline route reverb เดียวกับ live + **fallback→synth ถ้าโหลดพลาด (export ไม่ hard-fail)** · verify: Grand ได้ยินตลอด 14.3s (5/5 window RMS>0.001 · โน้ตยาว/late ไม่เงียบ) + fallback peak 0.57 · **เหลือ P'Aim ดาวน์โหลด MP3 จริงฟังยืนยันเสียงเปียโน**
- **สถานะรวม branch `golden-piano` = FEATURE-COMPLETE:** 4 ข้อ brief + G-audit legato + MP3-through-arrange + MP3 Grand timbre = ครบ · **649 tests เขียว + build ผ่าน** · report ครบ · commits ล่าสุด `6af528d`
- **🚩 รอ P'Aim ear ก่อน PM gate (2 จุด):** (1) ดาวน์โหลด MP3 จริงฟังยืนยันเสียงเปียโน (2) accent (เปิดตาม "เปิดหมด" · เคยปิดเพราะกระแทก) กระแทกไหม
- **✅ merged+deployed รอบ 27 (ดูหัวบล็อกด้านบน)** · เหลือ post-deploy: accent ear-check + archive สาย

**--- ประวัติ pm25 ด้านล่าง (รอบ 25 GATE leak#2 · parity in-progress) ---**

**🔴 P'Aim เจอ 15 ก.ค. เช้า (2 ภาพ Chrome+Samsung · `Downloads/Screenshot_20260715_06*`):**
1. **🔒 GATE leak #2 — 🎉 DEPLOYED รอบ 25 `bede377` LIVE (15 ก.ค. · P'Aim สั่ง deploy เฉพาะบั๊กก่อน · งานเพลงค่อยๆ ทำ):** push FF `3638484..bede377` studio-shell-redesign→main · **verify prod:** GH Actions success · live bundle `content, verified` ×2 (EditorMode+Studio = shell picker กรอง verified แล้ว) · HTML Last-Modified 15 Jul 00:33 GMT · **code delta รอบ 25 = เฉพาะ Studio.vue+test (งาน violin/Canon/drawer อยู่ branch แยก ไม่หลุด)** · _(รายละเอียดเดิม)_ ✅✅ Dev เสร็จ + PM gate PASS + merge base `b16f5f9`:** picker "เปิดเพลงที่มีอยู่" บน **Studio.vue** (shell "เพลง ▾") เป็นคนละ code path จาก EditorMode (รอบ 24 อุดแค่ EditorMode) → ยังรั่ว · fix = `pickerOptions` กรองผ่าน `visibleSongs(songList, tier!=='anon')` + เพิ่ม `verified` ใน select (1 ไฟล์ · แพตเทิร์นเดียว SongList/EditorMode) · จาก branch `claude/distracted-lederberg-4953c1` `fe45dfc` (fork ถูก · merge code-only Studio.vue+test) · **gate PASS:** scope สะอาด · gate tests 28/28 · full 631 pass (1 "failed" = `notationLint.test.mjs` standalone self-exit · pre-existing บน base · ไม่เกี่ยว) · build ✓ · anon เห็น 8 verified ครบ 3 surface (home/EditorMode/shell) · **รอ deploy รอบ 25 (รอ P'Aim go)** · dev worktree serve `:5327 --host` เผื่อ P'Aim verify มือถือ (session `local_997bfdfc` archive ได้)
2. **🎨 WHOLE-SITE look&feel parity — ⭐ P'Aim ตามงานนี้อยู่ (15 ก.ค.): "ทำให้พระคำกับเพลงเหมือนกันบน production จริง" · ขอ PM คุยพระคำ+รายงานเขา** · goal = 2 เว็บ match บน server จริง (pleng รอบ 26 + phrakham deploy ↗/toggle) · **ping pk pm5 ถาม: สถานะ ↗/toggle · timeline deploy · พระคำ live นิ่งไหม (เพลงวัดเทียบ live master) · จะ deploy ตัวล่าสุดไหม (live `e6fa73b*` < local `79e4f1a`)** — รอตอบ→รวม timeline รายงาน P'Aim
   - _(scope: "เปิดมาต้องเหมือนกันหมด — hamburger/footer/search/สี/font/ทุกอย่าง เช็กทุกรายละเอียดแม้ปุ่มปิด drawer")_
   - **🚦 GATE (P'Aim 15 ก.ค.): ตรวจทั้ง 2 เว็บบน LOCAL เทียบกันก่อน → deploy พร้อมกันทั้งคู่** (P'Aim เคาะ "รอขึ้นพร้อมกัน" · ไม่ทยอย) · **pk pm5: DoD + serve `feat/drawer-parity` `--host` + ส่ง URL + HOLD deploy** (Greek-✨ reader ขึ้นแยกได้) · เพลงเปิด `--host` เตรียม · PM รวม 2 URL ส่ง P'Aim เทียบทีเดียว → โอเค → สั่งขึ้นพร้อมกัน
   - **✅ pk pm5 confirm (15 ก.ค.):** ↗+toggle เสร็จ dev-verified (`feat/drawer-parity` · fence สะอาด ไม่แตะ core) · **พระคำ live `e6fa73b` = UI master นิ่ง** (commit ใหม่กว่า = pm/docs ล้วน 0 site-file diff) · ที่จะเปลี่ยน = +2 drawer tools เท่านั้น → เพลง audit เทียบ `e6fa73b` live ได้เลย
   - **🖥️ พระคำ serve local (ถือไว้ ยังไม่ deploy): `http://192.168.1.124:8931/`** (drawer-parity build · ☰→เครื่องมือ เห็นปุ่มมีหัว/ไม่มีหัว + ลิงก์เพลงมี ↗) · **PM ถือ URL รอเพลง serve → รวม 2 URL ส่ง P'Aim**
   - **✅ FULL-site audit เสร็จ (`musing-taussig-e9a834` `1178f81` · report เต็ม):** round22 ทำ ~70% แล้ว (สี/ฟอนต์/body18/การ์ด radius14/footer ตรง) · เหลือ + 7 flag · **⚠️ audit แก้ fact: desktop nav = 18.02 ตรงเพลง 18 อยู่แล้ว · 17.34 = เฉพาะในลิ้นชัก** (clever-kirch แก้ desktop ผิด → superseded ไม่ merge)
   - **🟩 P'Aim เคาะ flag ครบ (15 ก.ค.):** brand = **เพลงทำตามพระคำ (เทา/w400/กลาง/โชว์ชื่อ mobile · "เหมือนหมด")** · ↗+toggle = พระคำเพิ่ม · เส้นขอบแถบหัว = เพลงตัดออก · แถบน้ำตาลข้างการ์ด = คงไว้ · ช่องค้นหา = ครีม · root 16 = คง (body 18 เท่าแล้ว)
   - **✅✅ Dev whole-site เสร็จ + serve local: เพลง `http://192.168.1.124:5344/` ↔ พระคำ `:8931`** (`sad-moser-c66f59` `b07944f` · fork base 2ff2532 · styles.css+SongList) · แก้ครบ 6 กลุ่ม (brand เทา#545555/w400/22.5px · radius 8→10 · card 14+#e7dcca+คงแถบซ้าย · search cream 16px · ตัดเส้นขอบแถบ · drawer 4: nav 17.34 เฉพาะลิ้นชัก/padding-top55/pad/label น้ำตาล · desktop nav คง 18 ไม่ทำ bug clever-kirch) · verify computed เพลง==พระคำ desktop+mobile · 631 tests+build · **dev จับ brief error: พระคำ brand จริง = ชิดซ้าย+mobile icon-only (ไม่ใช่ 'กลางจอ' ที่ PM พิมพ์) → ทำ literal parity ตามพระคำจริงถูก** · ✅ ปิด gap icon แล้ว (`018f638`: icon มือถือ 40px/r11 เท่าพระคำ · แถบหัว 64px ≤ พระคำ 65 · 631 tests) · **ทุกจุดตรงหมด · ส่ง P'Aim เทียบ 2 URL (`:5344`↔`:8931`) แล้ว · รอ P'Aim OK local → PM merge b07944f/018f638 (code-only ไม่เอา launch.json) → deploy พร้อมพระคำ (pk pm5 hold รออยู่)**
   - _(เดิม: จ่าย dev รวบทั้งเว็บ `task_8dcba8ac`)_ (supersede drawer-only clever-kirch) · แก้ styles.css: brand·radius8→10·card radius14+border·search cream 16px·ตัดเส้นขอบแถบ·drawer 4(nav 17.34 เฉพาะลิ้นชัก/padding-top/padding/label น้ำตาล) · verify computed เพลง==พระคำ · **เปิด --host ค้างให้ P'Aim เทียบกับ :8931** พระคำ=master · **🔵 จ่าย SA (`local_9622522e`) full-site audit** ทุก surface (แถบหัว/hamburger/search/login/tools · drawer+ปุ่มปิด · footer · global tokens สี/font/ปุ่ม/radius/shadow · ข้ามหน้า chrome) → ตารางเดียวครบ · reuse รอบ 22 (~70% แล้ว) เก็บที่ตกหล่น · flag brand (P'Aim เคย "คงแบรนด์" แต่ตอนนี้ "เหมือนหมด" → เคาะบนลิสต์รวม) · **⚠️ sequencing กันชน styles.css: drawer 4 จุด (`task_17996433`) land base ก่อน → whole-site dev pass fork ต่อ (ไม่ให้ 2 dev แก้ styles.css ขนาน)** · มาตรฐาน: P'Aim ไม่เจอ gap สักจุด
  - **--- drawer (subset · กำลังทำ) ด้านล่าง ---**
  - **🚪 drawer PIVOT เดิม:** **พระคำ = ตัวตั้ง (master) · เพลงปรับตามให้เหมือน** (เลิก align-up ทั้งคู่ที่ต้องถามทีละจุด) · P'Aim ยังเห็นต่างชัด: font · การวางโครง · ปุ่มมีหัว/ไม่มีหัว · footer · ชื่อเว็บเข้มในเมนู (เพลงมี พระคำไม่มี)
  - **⚠️ บทเรียน PM (process fail · รับผิด):** รอบก่อนผมวางโจทย์ SA **แคบไป** (วัดแค่ font-size 4 จุด เพื่อตอบ "invert ไหม") → SA ตอบเป๊ะเฉพาะที่ถาม → เรื่องเห็นชัด (โครง/toggle/footer/ชื่อเว็บ) ไม่ถูกดูเพราะไม่ได้สั่ง = **PM ผิด ไม่ใช่ SA** · fix process → memory `feedback_parity_full_visual_audit`
  - **✅ FULL audit เสร็จ (`musing-taussig-e9a834` `cf9365f` · `docs/reports/musing-taussig-e9a834.md`):** โครง/เปลือกตรงแล้ว (core เดียวกัน) · drift = typography/content · **SA แก้คำ:** รอบก่อน "18=18" วัดผิด element — จริง เพลง 18px > พระคำ 17.34px (P'Aim ถูก)
  - **🟦 เพลงแก้ 4 จุด (จ่าย dev `task_17996433`):** ① font 18→17.34px (px ไม่ rem) ② **บั๊ก: แถวแรก "รายการเพลง" ซ้อนใต้ปุ่มปิด (padding-top:0)** → +55px ③ padding nav 11/10→10/21 ④ label เครื่องมือ 12.5px เทา→13.3px น้ำตาล · **ห้ามแตะ:** แบรนด์แถบหัวเพลง(หนา/น้ำตาล คงไว้) · ↗ · toggle · แผ่นเพลง
  - **🟩 P'Aim เคาะ 4 flag (15 ก.ค.):** (1) ↗ = **พระคำเพิ่ม** (ทั้ง 2 มี) (2) toggle มีหัว/ไม่มีหัว = **พระคำเพิ่ม** (`feat/thai-font-toggle`) (3+4) แบรนด์แถบหัว = **คงเอกลักษณ์แต่ละเว็บ** (เพลงหนา/น้ำตาล · พระคำเทา/กลาง — ไม่ใช่ menu-parity) · "ชื่อเว็บเข้ม" ที่ P'Aim เห็น = brand แถบบน ไม่ใช่ในลิ้นชัก
  - **🟪 พระคำทำ 2 (ส่ง pk pm5 แล้ว):** เพิ่ม ↗ + วาง toggle ในลิ้นชัก · ไม่แตะแบรนด์
  - **token/core:** P'Aim คงแบรนด์แต่ละเว็บ + เพลงตาม typography เป็นตัวๆ → **ไม่เร่งดัน `--pk-drawer-navfs` เข้า core** รอบนี้ (เพลง hardcode 17.34px จบ) · token กัน drift = อนาคต ตอน pk pm5 ร่างสเปกเต็ม แชร์แค่เปลือก → เนื้อ+ขนาด drift · **4 จุด (align UP · world-class):** (2.1) หัวลิ้นชักโชว์ชื่อเว็บ → พระคำเติมให้มี (เพลงมีแล้ว) (2.2) ↗ ท้ายลิงก์ข้ามเว็บ → พระคำเติม (เพลง→พระคำ มีแล้ว · พระคำ→เพลง ไม่มี · pk-ext ↗ มีสไตล์อยู่) (2.3) ปุ่ม "มีหัว/ไม่มีหัว" → **P'Aim เคาะให้พระคำมีด้วย** (2.4/ข้อ4) ขนาดฟอนต์เมนู inverted (เพลง desktop ใหญ่/mobile เล็ก · พระคำ desktop เล็ก/mobile ใหญ่) → สเกลเดียว **mobile ≥ desktop** (Material/HIG touch) · **SA วัด live 4 จุด (2 เว็บ × desktop/mobile) ก่อนเคาะเลข — ไม่เดาจาก source** (`feedback_verify_parity_live_computed`)
  - **✅ pk pm5 (`local_6217a03b`) รับทั้งหมด (15 ก.ค.):** (ก) พระคำเจ้าของสเปกร่วม — ต่อยอด `spec-pk-drawer.md` ครอบ 4 จุด + ระบุ core-vs-per-site · จุด 1/2/3 พระคำทำฝั่งตัวเอง (มี `feat/thai-font-toggle` build แล้ว เอามาวาง drawer) (ข) ดัน title-slot + nav font-scale เข้า core ผ่าน **CSS token `--pk-drawer-navfs`** (default เท่ากัน · site override ได้ = ไม่ drift + ไม่ hard-lock)
  - **ลำดับ 5 ขั้น (ไม่รีบ · พระคำมี Greek-✨ dev + deploy 2 รอบวันนี้):** (1) พระคำร่างส่วนขยายสเปก ‖ (2) **✅ SA เพลงวัด 4 จุดเสร็จ (`task_3c8a9cb7` · `musing-taussig-e9a834` · report `docs/reports/musing-taussig-e9a834.md`)** → (3) 🔵 pk pm5 ร่างสเปกเต็มหลัง Greek-✨ จบ (verify selector+text-size-adjust แล้ว · 2 PM ตกลง core-vs-per-site) → (4) **pk pm5 ส่ง review → เอาเข้า P'Aim เคาะ (แตะ drawer ที่ live)** → (5) จ่าย dev 2 ฝั่งตามสเปกเดียว · core update = ทั้งคู่ re-consume · lesson `pleng-shared-core-shell-deep`
  - **⭐ ผลวัด (reframe ข้อ 4):** font-size **~18px เท่ากันหมด 4 จุด** (เพลง18@root16 · พระคำ18.02@root17 · desktop=mobile) — **ไม่ได้ invert ที่เลข** · อาการ "สลับหัวท้าย" มาจาก (1) พระคำ `text-size-adjust:auto` → Chrome Android boost mobile ใหญ่จริง (เพลง pin 100% ไม่โดน · board §192) = **ตัวแก้ mobile จริง** (2) brand พระคำ 22.5px vs เพลง 18px (desktop) (3) zoom 125% เครื่อง P'Aim · **token ร่วม (2 PM ตกลง): core = `--pk-drawer-navfs:18px` (px ไม่ใช่ rem! root ต่าง 16/17) + touch 48px + title-slot · per-site = text-size-adjust:100% scoped `.pk-drawer`+navbar (ไม่ pin `html` = ไม่ย่อเนื้ออ่านทั้งเว็บ · แก้ตรงเมนูที่ P'Aim ติง) + thai-font toggle placement** · ⚠️ verify Android จริงว่า Chrome ยอม un-boost แบบ subtree (ถ้าไม่ยอม fallback per-site: pin navbar/body ระดับสูงขึ้น ยังไม่ถึง html)

**🎻🎹 NEW spike (P'Aim สั่ง 15 ก.ค. · แยก · ไม่อยู่ใน deploy):** **Canon in D เชลโล+เปียโน คุณภาพ "งานแต่ง"** — P'Aim ชอบมาก Pachelbel Canon in D (cello & piano wedding version · เชลโลเดินทำนอง + เปียโน ground bass/arpeggio) · อยากพิสูจน์ว่าทำถึงระดับนั้นไหม (ทดลอง) · **จ่าย SA `task_860b4412`** — reuse pipeline Iowa (สไปก์ไวโอลิน · Iowa มีเชลโลด้วย) + Grand STEP 0 เป็นเปียโน · หาเชลโลฟรี multi-dynamic self-host offline · seed เพลง v2 instrumental เล่ม "เพลงอื่นๆ" (verified=false = คนนอกไม่เห็น ปลอดภัย) · เดโมให้ P'Aim ฟัง · **P'Aim iterate ตรง SA** (`feedback_paim_direct_sa_creative`) · PM gate ตอนจบ · ถ้าฟรีไม่ถึง → SA flag → P'Aim ตัดสิน (เหมือนไวโอลิน) · แยกจากไวโอลิน `charming-cori`

**--- pm24 hand-off เดิม ด้านล่าง ---**

**🎉 DEPLOY รอบ 24 `2c580a8` LIVE** (https://pleng.phrakham.life · verify bundle: `lyric-words`+`PKDrawer`+`flowing`+`easeUnder` พบ · `violin-iowa`=0 ไม่หลุด · GH Actions success) = audio round 2 + STEP 0 + GATE-leak fix + karaoke พยางค์ + drawer เพลง(ซ้าย เหมือนพระคำ · pk-drawer core) · **กู้เสียงเงียบ live แล้ว**
- **post-deploy verify (live · มือคน):** พี่เปา verify karaoke พยางค์ + drawer slide บน device จริง (ถ้าเพี้ยนแก้รอบ 25 · เสี่ยงต่ำ)
- **ยังทำต่อ (แยก · ไม่อยู่ใน deploy):** 🎻 violin `task_15a3f347` (spike เสร็จ · Iowa PD · **รอ P'Aim ฟัง A/B** `:5344/docs/spikes/violin-iowa-demo.html`) · 🎨 Remix ⏸️พัก (design เสร็จ รอ P'Aim เคาะ GATE1)

**▶ pm25 เริ่มตรงนี้ (next actions · ไม่มีอะไรบล็อก · รอ P'Aim/พี่เปา):**
1. **violin:** ❌ **P'Aim ฟัง A/B แล้ว verdict: ยังไม่ใช่ — "ออกแนวออร์แกน"** (นิ่ง ตายตัว) · ไวโอลินจริงที่ชอบ = **"แหลม โหยหวน · หนักเบาชัดกว่ามาก"** → Iowa multi-dynamic ไม่พอ · ที่ขาด = **expression ภายในตัวโน้ต** (vibrato · dynamic swell คันชัก · brightness ตาม intensity · portamento) · **จ่าย SA เดิม (`local_c457a6cb`/charming-cori) ต่อ (15 ก.ค.):** วิเคราะห์เชิงตัวเลข (librosa) 3 ไฟล์ ref ที่ P'Aim ชอบ (Amazing Grace 박지혜/石川綾子 + Canon · ช่วงกลาง · ⚠️ ลิขสิทธิ์ ห้าม commit MP3) เทียบเดโม Iowa → หาว่า "ออร์แกน" มาจากอะไร → prototype เติม expression → เดโมใหม่ · ถ้าฟรี+processing ชนเพดาน → flag ทางเลือก (lib true-legato/vibrato · hybrid synth · รับข้อจำกัด) ให้ P'Aim ตัดสิน · P'Aim iterate ตรง SA
2. **Remix:** P'Aim เคาะ GATE1 (design พร้อม `docs/ds/remix.md`+wireframe) → จ่าย dev → รอบถัดไป
3. **post-deploy verify (พี่เปา · live):** karaoke พยางค์ + drawer slide (เพี้ยน→รอบ 25)
4. **shared-core token layer** (deferred · §📌) · notation print (พี่เปา) · to-do พี่เปา issues4/6/7
- **git:** base `studio-shell-redesign` = origin/main = `cafaa3b` (deploy synced) · สะอาด · memory synced OneDrive
- **🧹 cleanup (pm26 · 15 ก.ค.):** archived golden-piano + ลบ worktree สะอาด 4 อัน (gallant-maxwell/determined-panini/fervent-nash/nostalgic-hellman) · **เหลือ ~17 worktree ค้าง** — ลบเพิ่มได้แต่ต้อง `--force` (build junk) · **⚠️ 3 อันมีไฟล์ยังไม่ commit อย่า force ทิ้ง:** `agent-a617e20c`(SingTransport.vue/test แก้ค้าง) · `recursing-bartik`(docs/ds/menu-drawer-parity-fix.md untracked) · `youthful-payne`(docs/reports/answer-melisma-underline.md untracked) → ตรวจว่ามีค่าไหมก่อนลบ · **ห้ามลบ `dreamy-lumiere`(ถือ handoff+Canon spike) · `charming-cori`(ไวโอลิน parked)**

**--- ประวัติ pm24 ด้านล่าง ---**

## ▶ RESUME เดิม (pm23 → pm24 · 14 ก.ค. ค่ำ)

**⏯️ RESUME SNAPSHOT (ถ้า credit limit → เปิดใหม่หลัง reset อ่านตรงนี้ · P'Aim สั่ง "resume when reset active"):** ทุกอย่าง save ครบ (git commit + board + memory) · **ห้าม deploy อัตโนมัติ — รอ P'Aim go เสมอ** · สายที่วิ่ง (ปลุกต่อได้ไร้รอยต่อ · worktree อยู่ครบ):
- **🎹 รอบ 24 (จะ deploy)** — base มี: audio r2(ear-approved)+STEP0+GATE-fix+karaoke-fix พร้อม · **รอ 2:** (1) พี่เปา verify karaoke device (โหมดเนื้อล้วน) (2) drawer เพลง dev เสร็จ `task_b512fd83` → merge → push · **spec+exclude ล็อกไว้ §⏭️ รอบ 24**
- **🚪 drawer เพลง** `task_b512fd83` — consume `pk-drawer.js` (phrakham main · left) · 🔵 dev ทำ
- **🎻 violin** `task_15a3f347` — SA ใหม่ · P'Aim iterate · **ไม่เข้ารอบ 24**
- **🎨 Remix** ⏸️ พัก (design เสร็จ · P'Aim หยุดชั่วคราว) · **ไม่เข้ารอบ 24**
- **GATE-fix แยก** = worktree `../pleng-r24` (`r24-gate-fix` commit 14b996e · unpushed · เผื่อ deploy เดี่ยว)

**🎉 pm23 = DEPLOY รอบ 23 `bb0c8d3` LIVE** (role-gate+audit+badge+notation+menu parity+expressive audio) — รายละเอียด+verify ใน §🎉 DEPLOYED รอบ 23 ด้านล่าง
_(ประวัติ pm22 = รอบ 22 `6cb8e68` phrakham look&feel — เก็บใน §DEPLOYED รอบ 22)_

**⏭️ รอบ 24 (pm24 สานต่อ · 14 ก.ค. ค่ำ · P'Aim จะแก้ karaoke ใน session นี้):**
- **พร้อมใน base ครบ:** audio round 2 (P'Aim ear-approve · 626 tests) + STEP 0 (Grand 5 layer 12.9MB) + GATE-leak fix — **แต่ HOLD push** เพราะ:
- **✅ karaoke bug เจอ+แก้แล้ว (`c034de4` merge base):** ต้นเหตุ = **"โหมดแสดงผลเนื้อล้วน"** (ซ่อนโน้ต โชว์เนื้อ · ไม่ใช่เพลงเนื้อล้วน!) → เพลง v2 render เนื้อเป็น text ไม่มี .syl span + seg-playing gate ไว้แค่ v1 → ไม่ highlight · fix 1 บรรทัด (เปิด seg-playing เมื่อ lineLyricsOnly) · **ไม่ใช่ regression รอบ 2** (logic SongSheet เดิม) · 33 tests · **SA verify advancement (poll สด)** · **🔄 P'Aim ขอไฮไลต์ราย "พยางค์" ไม่ใช่ท่อน → SA แก้เป็น per-syllable แล้ว (`aca4a74`: render inline syllable spans โหมดเนื้อล้วน · syl-playing เดิน 35 พยางค์เหมือนโหมดเต็ม · เว้นวรรคถูก · 623 tests)** · ⚠️ **base มี c034de4 (ทั้งท่อน · เก่า) → PM re-sync เป็น `aca4a74` (พยางค์) ตอนใกล้ push** (ไม่ churn ระหว่าง P'Aim iterate) · **เหลือ พี่เปา verify device (per-syllable · โหมดเนื้อล้วน) → re-sync + ปลด hold รอบ 24**
- **GATE-fix แยก:** cherry-pick สะอาดใน worktree `../pleng-r24` (branch `r24-gate-fix` · commit b8b2a3b บน origin/main) **ยังไม่ push** (P'Aim สั่ง "รอ") — พร้อม push standalone ถ้าจะปล่อยเฉพาะ GATE fix
- **🚀 DEPLOY รอบ 24 — spec ล็อกโดย P'Aim (14 ก.ค. ค่ำ):**
  - **Trigger:** deploy เมื่อ **(1) karaoke + (2) drawer** ผ่าน · **drawer ✅ merge base แล้ว** · **เหลือ พี่เปา verify device 2 จุด: (a) karaoke ไฮไลต์ทีละพยางค์ (โหมดเนื้อล้วน) (b) drawer slide-from-left** → ครบ → PM re-sync karaoke `c034de4→aca4a74` + push
  - **✅ รวม:** audio round 2 (ear-approved · กู้ hollow) + STEP 0 (5 layer) + GATE-leak fix + karaoke fix (`c034de4`) + **drawer เพลง (consume pk-drawer · `task_b512fd83`)**
  - **❌ ห้ามรวม/ห้าม merge เข้ารอบนี้: 🎻 violin (`task_15a3f347`) · 🎨 Remix** (ยังทำ/พัก · ไปรอบหลัง)
  - วิธี: merge drawer เข้า base → verify test+build → align main=base → push (violin/Remix อยู่ branch แยก ไม่ใน base = ปลอดภัย)

**🚀 pm23 ยิงขนาน 3 dev แล้ว (13 ก.ค. เย็น · P'Aim "เอาตามที่คุณเสนอ Go") — chip รอ/กดเริ่ม:**
- **[เลน 3] audit log B028 — ✅✅ Tester PASS 7/7 · PM merge เข้า base แล้ว** (`docs/reports/tester-audit-log-qa.md` · ลบ profile แล้วชื่อยังอยู่=actor_name snapshot จริง · integrity แก้ log ไม่ได้) · cherry-pick code-only จาก `distracted-shtern-255d85` `b6d7e2a` → base (3-way merge สะอาด · role-fix เลน4 `isApprover` คงอยู่ + audit RPC เข้ามาครบ) · verify บน base: db/004 6/6 (pglite Postgres) + verify 4/4 + approve-rpc/auditLog/RevisionHistory เขียว · +pglite devDep (`npm install` แล้ว) · **✅ db/004 รันบน prod แล้ว (13 ก.ค. ค่ำ · verify คอลัมน์ event/actor_name/op_group ขึ้น prod HTTP 200) — deploy-blocker เคลียร์** · เหลือ: วัด mobile 360/412 หลังมี data (post-deploy)
- **[เลน 5] เมนูเพลง — ✅✅ Tester re-check PASS 5/5 · PM merge เข้า base แล้ว** (boundary เป๊ะ 991=drawer/992=inline ตรง DS §4 · 768/900 drawer ไม่ regress desktop · report `docs/reports/tester-menu-breakpoint-qa.md`) · merge code-only `ShellBar.vue`+`styles.css` จาก `menu-breakpoint-992` (base ไม่แตะ 2 ไฟล์นี้ตั้งแต่ deploy 22 = checkout สะอาด) · verify base: ShellBar 7/7 · **รอ deploy รอบ 23**
- **[เลน 4] ปุ่ม "✓ ตรวจแล้ว" โชว์เฉพาะ approver — ✅ เสร็จ + PM merge เข้า base แล้ว** (`EditorMode.vue` `v-if loggedIn→isApprover` · จาก branch `sad-jennings-08f0ad` `fd314c9` · เอาเฉพาะโค้ด+test+report ไม่เอา board dev พ่วง) · git-verify diff สะอาด · 4/4 verify tests ผ่านบน base · report `docs/reports/verify-button-approver-gate.md` · **รอ deploy รอบหน้า (batch)**
- **[เลน 2] notation (issue8 beam + issues5 slur) — ✅✅ Tester PASS · PM merge เข้า base แล้ว (code-only 7 ไฟล์)** `task_4a0e274c` · branch `jovial-roentgen` `b287e84` · tester: golden 10/10 + Tier B บนจอ (beam ตามคำ · slur ข้ามห้อง 1arc/wrap 2ส่วน) · 545 tests · report `docs/reports/tester-notation-qa.md` · **⚠️ branch fork ฐานเก่า → merge เฉพาะ 7 ไฟล์ notation (ไม่ merge branch เต็ม = จะ revert เลน 3/4/6)** ✓ ทำแล้ว · verify base: notation/NoteRow/SongSheet 57/57 · **P'Aim เคาะ (ข) ขึ้นเลยเชื่อ QA บนจอ+golden — พี่เปา print/verify live หลัง deploy (ถ้าเพี้ยนแก้รอบหน้า)** · ไม่ regress issues2/B076/B069/B099

**🎉 DEPLOYED รอบ 23 — `bb0c8d3` LIVE (14 ก.ค. · P'Aim go · https://pleng.phrakham.life)** — 6 เลน: role-gate + audit-log(+db/004 prod) + review-badge + notation(beam ตามพยางค์+slur ข้ามห้อง) + menu parity + expressive audio(+popover พอดีจอ) · **verify prod จริง: bundle มี `easeUnderHold`/`approve_and_publish`/`ประวัติการแก้ไข`/`ประกายเสียง` + HTML Last-Modified หลัง push + GH Actions success** · push FF `git push origin studio-shell-redesign:main`
**📌 post-deploy follow-ups (pm24):**
- **🚪 drawer-parity (P'Aim 14 ก.ค. · เลือก ก):** drawer เพลงเปิดจาก**ขวา** ≠ พระคำ (top-right toolbar dropdown + nav กางจากซ้าย) + ขัด "ชิดซ้าย" → **✅ SA verify เสร็จ + 2 PM เห็นตรงกัน (14 ก.ค.):** พระคำ = Bootstrap collapse ไม่มี drawer · **ข้อเสนอร่วม = left off-canvas drawer + scrim ทั้ง 2 เว็บ (align-UP world-class)** · แก้ SSOT `menu-drawer-spec.md` แล้ว (`docs/ds/menu-drawer-parity-fix.md`) · สัญญาร่วม = เปลือก drawer (ไม่กำหนดเครื่องมือในเมนู · per-site) · **🏛️ P'Aim: drawer = core lib ตัวเดียวที่พระคำ · เพลงเรียกใช้ (แบบ pk-scrollnav/DockKey · แก้ที่เดียว) → พระคำ author  vanilla core (ไม่ใช่ Bootstrap offcanvas native · เพลง Vue แชร์ไม่ได้) · ส่ง pk pm5 แล้ว** · **🟢 P'Aim GO (14 ก.ค.): "พระคำสร้าง core" → พระคำ author `assets/pk-drawer.js` (จ่าย dev แล้ว) · decisions: ทิศ=left · ☰ ทุกหน้า + tools contextual (nav persistent มาตรฐานสากล) · API SSOT = phrakham `pm/spec-pk-drawer.md` (focus-trap re-query + refresh()) · เพลงรอ consume เมื่อ core เสร็จ (import+mount Vue panel+☰ ซ้ายบน)** · **✅ core เสร็จแล้ว: `assets/pk-drawer.js` บน phrakham `feat/pk-drawer` (61542cd left off-canvas)** · **P'Aim เคาะ "ปรับเมนูเพลงเหมือนพระคำ = (ก) consume core" (14 ก.ค. · จากภาพพระคำ live drawer ใหม่)** → **✅ pk pm5 ยืนยันครบ 3 (SSOT = phrakham `main:assets/pk-drawer.js` 61542cd left · live/นิ่ง · API+focus-trap re-query ครบ · ⛔ ไม่ใช่ feat/pk-drawer-right) → ✅✅ **Dev เสร็จ + PM merge เข้า base แล้ว** (`peaceful-clarke-c4f54a` `26ca8ba`) — vendored `src/lib/pk-drawer.js` **verbatim ตรง phrakham main:61542cd (พิสูจน์ byte-identical = แก้ที่เดียวจริง)** + ShellBar `PKDrawer.create side:'left'` (ซ้าย+ครีม+scrim) · backdrop regression กันไว้ (Studio menu click-away คงอยู่) · verify: ShellBar 7/7 + build + dev real-browser AC1-4 + 627 tests · **เหลือ พี่เปา verify slide-from-left บน device** (preview tab hidden = animation freeze · endpoint ถูก)** · **PM บทเรียน: อย่าเดา parity — เปิดดูจริง (feedback_verify_parity_live_computed)**
- **🎵 notation:** พี่เปา print #8/#12/เพลงเอื้อน เทียบหนังสือ (live) · เพี้ยน→แก้รอบหน้า
- **🎹 ROUND 2 audio (P'Aim 14 ก.ค. · ทิศ: โหลด sample ครบก่อน → ออกแบบจากของจริง · ไม่ band-aid):**
  - **✅ STEP 0 เสร็จ + PM merge เข้า base แล้ว** (`gallant-maxwell-ad4aa5` `51abe63` · code+assets-only · base ไม่แตะไฟล์นี้ = สะอาด · sampler 13/13) — Grand ครบ 5 layer (PPP/PP/MP/MF/FF · PD · self-host · Grand 2.4→12.9MB · samples รวม 21MB) · velocity-in-layer guard (mute-bug ปิดตายเชิงโครงสร้าง) · **comp ลง PPP ได้แล้ว = มือซ้ายเบาลงตามที่ P'Aim อยากได้** · offline-verified · **ยังไม่ deploy** (round 2 ก่อน) · report `docs/reports/grand-velocity-layers-step0.md` · **🔵 SA round 2 กำลัง iterate `task_f4bc785a`** (branch `vigilant-montalcini-c4c54d`) — P'Aim ฟัง+feedback แล้ว 4 รอบ: (1) hollow-gap fix half-note pulse (2) per-role mix ทำนองนำ/คลอ PPP (3) คลอไล่ 4 ระดับ (ลดพื้น GAIN_MIN + chordGain · ไม่แบน) (4) **ลูกรับส่งซ้าย-ขวา `fills.js`** (มือซ้ายตอบ chord-tone นำเข้าวรรค · fillLevel 0.4) (5) slash chord (6) passing bass (7) flowing legato comp default (8) slash root-first (9) sus4 voicing-swap · **(10) ⭐ เมนู "ปรับละเอียด" 12 เทคนิค** (toggle+slider · live-apply · persist · reset · 2-ชั้น preset/fine · SSOT `techniques.js`) — เครื่องมือให้ P'Aim ปิดทีละตัวหาตัวที่ไม่เพราะ · tests 622 เขียว · **round 2 ครบฟีเจอร์แล้ว** + ตามด้วย: คีย์ A→โซน A4 (พี่เปา · playback pitch เท่านั้น) · ซ่อนเมนูปรับละเอียดตอนกีตาร์ · **🎤 fix karaoke เพลงเนื้อล้วน (พี่เปาเจอ · หน้าฝึกร้องไม่วิ่ง) — ⚠️ verify sandbox ไม่ครบ (SA flag ตรงๆ) → เงื่อนไข gate: พี่เปาต้องลอง real device กับเพลงที่เจอปัญหาก่อน · ถ้าไม่วิ่ง ขอ id เพลง** · P'Aim iterate ตรง SA @ :5420 · **เหลือ P'Aim เคาะเสียง final + พี่เปา verify karaoke → PM gate → deploy รอบ 24 (เสียง + GATE fix + drawer · กู้ live hollow)**
  - **🔴 GATE จับได้ก่อน push (14 ก.ค.): karaoke highlight ไม่ขึ้น (real device incognito :5420)** — P'Aim/พี่เปาทดสอบ **เพลง 1 พระเจ้าเป็นความรัก** (`f266baf9`) = **มีโน้ต 47 ตัว + arrangement ครบ (ไม่ใช่เนื้อล้วน!)** → highlight พังบนเพลงมีโน้ตด้วย = กว้างกว่าที่คิด · **สงสัย regression จาก round 2 (songToNotes/midi.js)** · ✅ round 2 merge base แล้ว (626 tests) แต่ **HOLD push จนแก้** · ส่ง SA ตรวจด่วนแล้ว (`local_8d2bfd37`) · **P'Aim ตอบ: เพลงเล่นได้ (เสียง+เวลาเดิน) แต่ highlight ไม่ขยับ = บั๊กที่ตัวไฮไลต์ (li/si index ไม่เดินตาม playback) ไม่ใช่ playback** → ส่ง SA เจาะ onNote/tick→highlight sync · **⚠️ SA เดาผิดรอบแรก (ว่าเป็น Grand 12MB โหลดไม่ขึ้น/ไม่เล่น) — P'Aim ยืนยันเพลงเล่น = ตัดทฤษฎีโหลด · PM แก้ความเข้าใจ SA แล้ว: อาการ = เล่นได้แต่ highlight ค้าง/ไม่ advance (SA เห็น '.syl-playing 1 ตัว' = อาการค้าง ไม่ใช่ทำงาน)** · SA เจาะใหม่ทำไม playingSyl ไม่เลื่อนตาม onNote(ยิง 35 ครั้งพร้อม syk) · **→ ✅ RESOLVED (ดู §⏭️ รอบ 24): ต้นเหตุจริง = โหมดแสดงผลเนื้อล้วน (ไม่ใช่เพลงเนื้อล้วน/ไม่ใช่ load/ไม่ใช่ regression) · fix `c034de4` merge base แล้ว · รอ พี่เปา verify device**
  - **🎻 Violin เดี่ยว — SA `task_15a3f347` (branch `charming-cori-25514b`) · 🔵 research spike** (P'Aim สั่ง 14 ก.ค. · **ไม่เข้ารอบ 24**) — P'Aim ฟังเดโมแล้ว **ยังไม่เพราะพอ** · root cause = sample VSCO-2 **single-dynamic** (หรี่/เร่ง = แค่ volume · แบน · เหมือน Grand ก่อน 5 layer) → ต้องหา **multi-dynamic (pp/mf/ff)+vibrato** · **⚠️ ข้อจริง: solo violin คุณภาพสูงส่วนใหญ่ commercial · ฟรี PD/CC0 จำกัด → เต็ง University of Iowa MIS (PD หลาย dynamic)** · spike = audition 2-3 คลังฟรี→วัด→เลือก→self-host offline→เดโมใหม่ · **ถ้าฟรีดีสุดยังไม่ถึง Grand → SA flag → PM เอาให้ P'Aim ตัดสิน** · **✅ spike เสร็จ:** Iowa MIS (PD · pp/mf/ff) = ตัวเดียวที่ผ่านเกณฑ์ฟรี · หั่น self-host `public/samples/CC0/violin-iowa/{pp,mf,ff}/` (8พิตช์×3 · 644KB · offline · `tools/slice-iowa-violin.sh`) + **เดโม A/B `docs/spikes/violin-iowa-demo.html`** (Iowa↔VSCO) · **รอ P'Aim ฟัง A/B เคาะ** (Iowa ผ่าน→SA wire multi-dynamic bowed+เปิดปุ่ม→tester→gate · ไม่ถึง→SA flag→PM เอาให้ P'Aim ตัดสิน) · commits 7002418/cb22d41 · ยังไม่แตะ src/ · report `docs/reports/violin-solo.md`
  - ลำดับหลัง STEP 0 (P'Aim iterate ตรง SA · PM gate): (1) ✅ per-role volume + tune Grand (2) ✅ เมนูเทคนิคละเอียด 12 (3) ⏸️ **Remix — P'Aim หยุดชั่วคราว (14 ก.ค.)** · design เสร็จแล้ว (stepper ◀ [แบบที่N] ▶ + derived-lock · `docs/us/remix.md`+`docs/ds/remix.md`+wireframe · P'Aim iterate design แล้ว) · **on-hold รอ P'Aim กลับมาเคาะ GATE1 → dev** · ยังไม่จ่าย dev · handoff `docs/reports/handoff-audio-round2.md`
  - **⚠️ KNOWN ISSUE (live รอบ 23 · P'Aim รับไว้ 14 ก.ค.):** easeUnderHold หลบแรงไป → บางท่อนเงียบ ~2 บีต ("ท่อนเงียบ") คนโบสถ์ได้ยิน · **P'Aim เลือกไม่ redeploy band-aid → กู้ทีเดียวตอน round 2** (PM ท้วงแล้ว · P'Aim ตัดสิน ข) · SA มี fix "half-note pulse" พร้อมถ้าเปลี่ยนใจ
- **📱 audit mobile:** วัด RevisionHistory 360/412 หลังมี data · **🧹 cleanup hero.png/.webp 6.5MB**

**🟡 รอ P'Aim (action ฝั่งพี่เอม):**
- **1.2 เช็ก Supabase dashboard: ติว = role `editor` จริงไหม** (จุดตาย — ถ้าติว legacy/approver จะเผยแพร่เองได้ ผิด use case) · P'Aim รับไปทำ
- 1.3 = รับความเสี่ยง client-side filter ไว้ก่อน (ไม่ทำ) ✅ เคาะแล้ว

- **[เลน 6] ป้ายสถานะ ✓ตรวจแล้ว/ยัง ในรายการเพลง — ✅✅ Tester PASS 5/5 · PM merge เข้า base แล้ว** (`docs/reports/tester-review-status-badge-qa.md` · contrast 7.22:1 วัดสด AA+AAA · 360/412 ไม่ตัด/ไม่ h-scroll · ป้ายตรง verified 121/121 แถว 0 mismatch · ตัวนับ 8/124) · merge code-only (`bookshelf.js`+test · `SongList.vue` · ไม่เอา launch.json/board dev พ่วง) · verify บน base: bookshelf 20/20 · ป้ายเขียว/เทา + ตัวนับ X/Y team-only · **รอ deploy รอบหน้า (batch)**
- **[เลน 7] 🎹 ทำเพลงให้เพราะ (เปียโนเดี่ยว Grand-only) — ✅✅ Tester PASS + P'Aim "ดีขึ้น GO" · PM merge + re-sync tuning ล่าสุด เข้า base แล้ว** (5 เทคนิค: BPM-auto/sparkle+สไลเดอร์/rubato/ท่อนรับแตกคอร์ด 1.93×/humanize + เบส pedal + **final tuning: easeUnderHold มือซ้ายหลบ/touch 0.31/gapFill 0.18**) · re-checkout arranger ล่าสุดจาก branch (base แตะแค่ merge ผมเอง = สะอาด) · 54 arranger tests · **ROUND 2 handoff `docs/reports/handoff-audio-round2.md`** (P'Aim: จัดเต็ม tune Grand ให้เลอเลิศ + เมนูเลือกเทคนิคละเอียด + Remix(ต้องมี UX designer·แก้ MP3) + timecode · caveat L/R→slider · variability↔MP3 determinism)

**📋 to-do พี่เปา (root · ยังไม่จ่าย · §🐞):** issues4 (โครงเพลง▾) · ~~issues5~~ → เข้า spec notation แล้ว · issues6 (คลิก preview→แก้ไข) · issues7 (ใหม่ · ยังไม่ triage)

**🔴 เรื่องใหญ่สุด (สำคัญกว่าบั๊ก):** GATE — **คืบหน้าแล้ว 8/124 verified** (เปาทยอย approve · เช็กสด 13 ก.ค. เย็น: total 124 · verified 8 · false 116) · public เห็น 8 เพลงที่ approve แล้วถูกต้อง (ยืนยันตรงภาพ P'Aim: อนุชน 1/4/7/9/11/13/14/16) · กลไกทำงานถูก · เหลือทีมทยอยตรวจต่อ
- **🐞🔒 GATE leak (P'Aim เจอ 14 ก.ค.) — 🔵 จ่าย dev `task_6cb6764f`:** ตัวเลือกเพลง "เปิดเพลงที่มีอยู่" ในหน้าแก้ไข (`EditorMode.loadSongList`) ดึงเพลง**ทั้งหมด ไม่กรอง verified** → anon เห็นเพลงยังไม่ตรวจ (SongList กรองถูกแต่ picker ไม่) · fix = ใช้ `visibleSongs(list, loggedIn)` ใน pickerOptions · scope EditorMode.vue · **ควร deploy รอบหน้า (correctness)** · รอ dev → gate

**🧹 cleanup:** `public/pleng-hero.png/.webp` (6.5MB · ไม่ถูกอ้าง ลบได้) · pk pm4 = สาย 📖 (running · reconnected)

**✅ DEPLOYED รอบ 22 — `6cb8e68` LIVE (13 ก.ค. เย็น · P'Aim go · https://pleng.phrakham.life):**
- **merge 6 อันเข้า base แล้ว** (`studio-shell-redesign`): (1) parity [ae576b2 · styles/Guide/About] (2) tie+triplet [NoteRow/SongSheet] (3) beaming [NoteRow] (4) editor-preview [EditorMode] (5) repeat-seek [midi.js] (6) **hero/favicon** [cc84c7b · index.html/public ชุด favicon+hero/ShellBar/SongList/styles.css `.sb-app-ico` merge ไม่ทับ parity] · เอาเฉพาะไฟล์โค้ด/asset ไม่เอา launch.json/board.md ที่พ่วง
- **522 tests + build ok · serve :5350** (bg `bg281b8zj`)
- **✅ หัวเว็บ phrakham-style เสร็จ + P'Aim iterate โดยตรง + integrate เข้า base แล้ว** (menu Dev `local_e359bb54` `dreamy-dijkstra` → ShellBar/styles/SongList/Icon/2 test · verify parity คงครบ `--fs-base 1.125`) · desktop=ชื่ออย่างเดียว · mobile=icon+🔍/☰ drawer · login ขวาสุด · hero ลบแล้ว · 523 tests+build ✓
- **✅ 7 อัน LIVE production แล้ว** (verify live bundle: sb-drawer/burger/app-ico/brand-text + favicon links + android-chrome 200): parity · tie+เลข3(issue1) · beaming(issue2) · ดูผลทั้งเพลง(issue3) · repeat · favicon · phrakham-header · kanban พี่เปา → 🟢 เสร็จ (รอบ22) · **cleanup ทีหลัง:** `public/pleng-hero.png/.webp` (6.5MB) ไม่ถูกอ้างแล้ว ลบได้
- **🆕 ป้ายสถานะตรวจเพลง (team-only · GATE unlock) — P'Aim เลือก (ก) deploy 7 ก่อน · ป้ายรอบหน้า** → **Dev `task_5d4f34d6` กำลังทำ** (branch ใหม่จาก base ที่ deploy แล้ว) · รอ report → gate → deploy รอบ 23
- **🤝 ประสาน pk pm4 (menu design ร่วม · P'Aim สั่งให้ 2 PM คุยกันเอง):** drawer เพลงต่างพระคำ 3 จุด (ชิดขวา→ซ้าย · เอาไอคอน nav ออก · ตัวอักษรไทยปุ่มใหญ่→แถวเรียบ) · หลักสากล = พระคำถูกทั้ง 3 · **send_message → pk pm4 `local_9db3b697`** ถามว่าพระคำแก้อะไรอยู่ + เสนอทำ **สเปกเมนูร่วม 1 หน้า** ให้ dev 2 ฝั่งทำตามอันเดียว · **รอ pk pm4 ตอบ → เคาะสเปกร่วม → จ่าย Dev ปรับทั้ง 2 เว็บ**
- **issue2 = P'Aim ถามซ้ำ = อยู่ใน batch แล้ว (ยืนยัน)** · เผื่อหมาย issues5 (cross-bar slur · to-do) — ถาม P'Aim
- **notation (tie issue1 + beaming issue2) = P'Aim เคาะให้ขึ้น deploy ด้วย** (13 ก.ค. · issue2 = แก้เจ๋ง zero-data-migration · อยู่ใน batch NoteRow.vue แล้ว) → ไม่ hold รอพี่เปาแล้ว (พี่เปา verify live ทีหลังได้) · **ยังไม่ push main จน P'Aim go (รอหัวเว็บใหม่เสร็จก่อน)**

**⏸️ PAUSE/HANDOFF (pm22 · quota ส่วนตัว 97% weekly → ย้ายบัญชีบริษัท):**
- **ทุกสาย idle (isRunning=false) — ไม่มีการเผา token · worktree อยู่บนดิสก์ + context สายเก่าคงอยู่ → ไม่ต้องสั่งหยุด/ปลุก** (ปลุกต่อทีหลังได้ไร้รอยต่อ)
- **pm23 resume เรียงเล็ก→ใหญ่:** (1) gate+merge 4 สายเสร็จ [issues3 1ไฟล์ · repeat 1ไฟล์ · issues1 2ไฟล์ · parity ผ่าน Tester 3ไฟล์] — merge เฉพาะไฟล์โค้ด (ไม่เอา launch.json/board.md ที่พ่วง) → (2) hero/favicon `local_cfd288df` **เงียบตั้งแต่ 10:01 · เช็กก่อนว่าติด login ไหม** → (3) issues2 beaming ✅ **เสร็จแล้ว (NoteRow.vue · zero data migration)** รอแค่พี่เปา verify look+print → merge รวม tie Dev branch → (4) menu Dev (หลัง hero/favicon land) · **หมายเหตุ: tie Dev branch `brave-pasteur` มี issues1(fe8991b)+issues2(d90acbb) 2 commit — merge ได้ทั้งคู่ (NoteRow/SongSheet)**
- **verify ค้าง (มือคน):** repeat ear-verify `:5309` · issues1 พี่เปา print PDF "ของขวัญ"
- **ยังไม่ deploy จน P'Aim สั่ง** (นโยบาย batch: parity+hero/favicon+bug รวมรอบเดียว)

**--- ประวัติ pm21 (LAUNCH รอบ 21) ด้านล่าง ---**

## ▶ RESUME เดิม (hand-off pm21 → pm22 · 13 ก.ค.)
**🎉 LAUNCH สำเร็จ — deploy รอบ 21 `01765cb` (LIVE): B107 ระบบเสียงเครื่องดนตรีจริง 3 โหมด** (เปียโนเดี่ยว · กีตาร์เดี่ยว nylon · รวมวงเสียงจริง Option 1) + ปุ่ม "เสียงดนตรี" popover + self-host samples PWA offline. P'Aim confirm ครบ (desktop+มือถือ Chrome/Samsung) · ยืนยัน live (bundle มี SoundControl/audio-lines · `/samples/manifest.json`=200).

**🔴 GATE ยังอยู่ = priority จริง (ถ้า P'Aim ถามงานต่อ):** public เห็น **0 เพลง** · ปลดล็อก = ทีม review **อนุชน 122 เพลง** ในหน้าแก้ไข → กด "✓ ตรวจแล้ว" (verified=true) · **ยัง 0/122** (กลไก live พร้อม · ยังไม่มีคนเริ่ม) · tracker `docs/pm/review-anuchon.md` · เสียง = ของเสริม เสร็จแล้ว

**งานต่อยอด B107 (ไม่บล็อก · หลัง launch):**
- **🎨 icon violin/cello/ensemble** (branch `icons-violin-cello` `324d938` · asset ไม่ merge · **session archived 13 ก.ค.**) — ensemble = "วาทยกรมองจากหลัง แขน-V" (default D) + violin/cello (3 variant A/B/C ต่อเครื่อง) · **รอ P'Aim เลือก variant** → PM re-serve preview `docs/spikes/violin-cello-icons.html` (`node` server) ได้เมื่อ P'Aim พร้อมเลือก → dev เสียบ Icon.vue แทน `music`(violin/cello จาง)/`users`(ensemble placeholder)
- **🎵 ดาวน์โหลดเสียงจริง (P3)** — spike พิสูจน์แล้ว (`download-real-audio-spike.md` · offline+big-lookahead Scheduler + grand→Sampler pianoToPreset · 20-60× realtime) → **PM จ่าย dev integrate `renderSongToBuffer`** (≈1 module · reuse arrange/sampler/encoder) · ตอนนี้ดาวน์โหลด = synth เก่า
- **เครื่องเดี่ยว felt/violin/cello** (ยังจาง "เร็วๆนี้") + **ensemble balance** = P'Aim↔SA จูนต่อ (มี `window.__peaks()` · ค่าเป็น named const)
- **B102 รับข้อ4 มือถือ** — resolver ถูก (desktop) · ถ้า P'Aim ยืนยันมือถือป้ายถึง 4/4 แต่เงียบ = AudioContext suspend → เปิดงาน wake-lock แยก
- **B103 `.gitattributes` LF** (`b103-gitattributes` · dev เสร็จ) — renormalize · merge ได้แล้ว (EditorMode.vue land หมด) · **ควรทำ (กัน EOL noise ตอน cherry-pick)**

**บทเรียน launch (เข้า §กติกา):** (1) audio bug = วัด real output (AnalyserNode/OfflineAudioContext peak) ไม่ใช่ fire-ไม่-error (2) **UI มือถือ verify บน device emulation จริง 360/412 + per-button right≤viewport · Chrome font-boosting (text-size-adjust) เจอเฉพาะ Chrome มือถือจริง** (3) tester gate = เทียบ spec เต็มทุกข้อ · P'Aim ไม่ควรเป็นคนจับ spec-gap/บั๊กเอง

---

## 🟢 LIVE — รอบ 21 `01765cb` (13 ก.ค. · LAUNCH 3 โหมดเสียง)
main = `01765cb` (base=main · FF สะอาด) · **นโยบาย: PM คุม deploy เอง · deploy fix ที่ผ่าน tester** (§กติกา deploy)
- **รอบ 21 (LAUNCH):** B107 3 โหมด (arranger `src/lib/arranger/*` + `playEnsemble` + `SoundControl.vue`/`soundOptions.js` + self-host `public/samples/` + `public/sw.js` offline) · merge `b107-step9-instruments` → base (`01765cb`) → main FF
- ประวัติ (7-20): DockKey · slur/tie · B095 ล็อกหมวด · B097 undo · B098 tools · B099 ไท · B100 leave-warn · B101 copy-paste · B102 ร้องรับ · B104 3-mode chord · B105 real-time · B107P1 grand · GATE (public 0 เพลง) — **ทั้งหมด live + อยู่ใน base (superset · verify แล้วตอน merge launch)**

## 🆕 งานใหม่ (pm22 · 13 ก.ค. บ่าย)
- **🖼️ hero + favicon + ไอคอนมุมซ้ายบน** — P'Aim ทำ asset เสร็จเอง (`C:\Users\aimkr\Downloads\เพลง hero-favicon\`: `pleng-hero.webp/.png` + `favicon_io/` ชุดครบ · ไอคอน = หนังสือเรืองแสงแบบ (ก) เหมือนพระคำ) · **จ่าย Dev แล้ว** spawn_task `task_260b6d03` · brief `docs/pm/brief-hero-favicon.md` · integrate เท่านั้น · **⚠️ merge manifest ไม่ทับ (PWA offline ไม่พัง)** · รอ Dev → gate → P'Aim
- **🐞 บั๊กพี่เปาชุดใหม่ (survey ทั้งโฟลเดอร์ 13 ก.ค.):**
  - **issues1 triplet+tie ✅ Dev เสร็จ** (`fe8991b` branch `claude/brave-pasteur-07bbed`) · root cause 2 ชั้น (cold-load re-measure หลัง fonts.ready + arc anchor ข้าม dash หาเลขต้นทาง) + เลข 3 → 12.3px bold · **layer render ล้วน (data v2 encode tie ครบ · ไม่เด้ง lane)** · 278 tests + no-regression · **git-verify scope = NoteRow+SongSheet+test สะอาด** (⚠️ merge เอาเฉพาะโค้ด · ไม่เอา `.claude/launch.json`+`board.md` ที่ Dev พ่วง) · **ค้าง: visual verify + พี่เปา print PDF "ของขวัญ" ยืนยันบนกระดาษ** · Live http://192.168.1.124:5418/#/song/74ebe8b8...
  - **issues2 เอื้อน vs slur ✅ วิเคราะห์เสร็จ (`c675cd8` · report PART 2)** → **ไม่ใช่บั๊กเล็ก = decision convention โน้ต:** เอื้อนสั้นในบีท (`6 5`) หนังสือ = **beam เส้นใต้เชื่อม ไม่มี arc** · app = slur arc + เส้นใต้แยก (data ใส่ `(6_ 5_)`) · **Option A:** beat-based beaming ใน render + DA ล้าง `( )` คู่เขบ็ตในบีท (เอื้อนยาวข้ามบีทคง arc) · **blast 120 เพลง + ต้อง DA + กลับ design เดิม** → **✅✅ Option A prototype เสร็จ (`d90acbb` · NoteRow.vue เดียว 164บรรทัด · 522 tests)** · **⭐ zero data migration — ไม่ต้องแตะ 120 เพลง!** (render ตรวจ same-beat + suppress arc เอง · `( )` เดิม harmless) → บั๊กใหญ่กลายเป็นเล็ก ความเสี่ยง rollout ต่ำ · verify ข้ามเพลงเทียบหนังสือ (ในโลกนี้/เพลง5/ของขวัญ) ผ่าน · **ค้าง: พี่เปา verify look** (`:5418` 3 เพลง) **+ print PDF** → ok แล้ว rollout (รวม issues1 หรือแยก) · commit d90acbb แยกจาก issues1 fe8991b ได้
  - **issues3 "ดูผลทั้งเพลง" ≠ แผ่นเพลง ✅ Dev เสร็จ** (`2031962` branch `claude/trusting-joliot-780da7`) · root cause = preview ล็อก 42.05em (A4 print) vs sheet จอ 68.4em → ตัดห้องเร็ว · fix = วัดคอลัมน์จริง ตัดห้อง em เดียวกับหน้าแผ่นเพลง · **แตะแค่ EditorMode.vue → ไม่ชน SongSheet (ไม่ต้อง rebase)** · 94 tests · PM เคาะคง 720px (adjustable ทีหลัง) · **merge เอาเฉพาะ EditorMode.vue** · รอ gate + พี่เปา verify
  - kanban พี่เปา **จัดระเบียบแล้ว (P'Aim สั่ง):** issue1/2/3 + repeat → `2-กำลังทำ` (ลบ root · repeat เปลี่ยนชื่อเลิกผูก issue4) · **root = to-do:** issues4/5/6
  - **⚠️ to-do ใหม่ (พี่เปาเติมไฟล์ · ยังไม่จ่าย · หลัง deploy review):**
    - **issues4 ≠ repeat!** (พี่เปาเติมข้อความ) = **โครงเพลง/ทำนอง: ▾ ย่อขยาย "โครงเพลง" ให้เหมือน "ทำนอง"** (editor UI)
    - **issues5** = **slur ข้ามห้อง render ผิด** (พิมพ์ถูก อยู่คนละห้อง แต่แผ่นเพลงโชว์ผิด) — notation lane (เกี่ยว NoteRow/SongSheet)
    - **issues6** = **feature:** ใน"ดูผลทั้งเพลง" คลิกโน้ต → เด้งไปบรรทัดนั้นในหน้าแก้ไขเลย (ไม่ต้องเลื่อนหา)
- **🎨 เมนูสไตล์พระคำ (desktop+mobile · P'Aim 13 ก.ค. · ภาพ `Screenshot_20260713_164458_Chrome.jpg`)** — แฮมเบอร์เกอร์+ไอคอนซ้ายบน+ค้นหา+หมวด "เครื่องมือ" (Aa/ตั้งค่า/ดาวน์โหลด · = phrakham pk-navbar) → **✅ SA DS เสร็จ** `docs/ds/menu-parity.md` (`41708f0`) · แมปตามเครื่องมือจริง pleng (ShellBar เดียว · nav ซ่อนใน ▾ · Aa/⬇ contextual หน้าเพลง · ยังไม่มี ☰) → desktop nav inline + mobile ☰ drawer + หมวด "เครื่องมือ" · **PARKED (บล็อก: Dev เมนูต้องรอ hero/favicon merge — ชน shell-bar/teleport)**
  - **4 Q รอ P'Aim (§8) — PM เสนอ default:** (1) 🔍 = ใส่เป็นทางลัดค้นหาหน้า list ✓ (2) ย้ายตัวอักษรไทย→หมวดเครื่องมือ ✓ (3) desktop หน้าเพลงยุบ nav เป็น ▾ เว้นที่ Studio ✓ (4) login mobile = ปุ่มแยกขวาบน (ไม่ยัดในเครื่องมือ) · **เอามาให้ P'Aim เคาะตอนใกล้ทำ (หลัง hero/favicon) ไม่รีบ**
- **🐞 บั๊ก repeat (= issues4) ✅ Dev เสร็จ** (`7d54a77` branch `claude/sad-davinci-518b6e`) · root cause = playback engine slice โน้ตทิ้งหัวแล้ว do…while วน array ที่ slice → คลิก=นิยามลูปใหม่ · **fix `src/lib/midi.js` ไฟล์เดียว** (seek เฉพาะ pass 0 · loop วนกลับต้น order/B102 SSOT) · **🟢 ไม่ชน SongSheet/NoteRow (ไม่ต้องจัดลำดับ merge)** · พิสูจน์ playhead: ก่อน `[2,2,2,2]`→หลัง `[2,0,1,2]` · 517 tests · **merge เอาเฉพาะ midi.js** · **รอ P'Aim/พี่เปา ear-verify** http://192.168.1.124:5309/

## 🔴 GATE ACTIVE (สำคัญสุด) — คืบ 8/124 (13 ก.ค. เย็น)
- เช็กสด: total **124** · verified **8** · false **116** → public เห็น 8 เพลงที่ approve แล้ว (ทำงานถูก · client filter `bookshelf.visibleSongs`)
- **▶ งานหลัก:** ทีมทยอย review → กด "✓ ตรวจแล้ว" → โชว์ public · กลไก live (`EditorMode.markVerified`) · **เปาเริ่มแล้ว (0→8)** · tracker `docs/pm/review-anuchon.md` · PM re-run query นับ progress (curl `songs?verified=eq.true` count)
- **users จริง: มีแค่ 2 (P'Aim + เปา · ทั้งคู่ approver)** · ติวยังไม่สร้าง account (P'Aim 13 ก.ค.) → เรื่องเช็ก role ติว=editor ยังไม่ต้องทำ (จะ auto=editor ตอนสมัคร · ปุ่มตรวจ gate isApprover พร้อมแล้ว)

## 🎯 รอ P'Aim ตัดสิน (ไม่บล็อก)
- SA interlinear ≥3 ภาษา (`task_aea51f3c` mockup) · B080 expert standards · B028 audit log (3 Qs) · i18n · สิทธิ์ลบเพลง
- import เล่มใหญ่ "บทเพลงสรรเสริญ" (⏸️ พัก · เพลง 32 ใน DB รอพี่เปาตรวจ · report `hymnal-import.md`)

## 📌 follow-up เล็ก
- **🔗 shared-core token layer (P'Aim อยาก "แก้ที่เดียว" · deferred 13 ก.ค.)** — แผน SSOT = DS `docs/ds/phrakham-parity.md` §🔗: พระคำ author `assets/pk-tokens.css` (CSS-var SSOT: สี/radius/shadow/line-height) → เพลง copy+alias `--brand:var(--pk-brand)` เหมือน pk-scrollnav · **font-size เก็บ local** (root 16≠17 · แชร์แล้วรั่วแผ่นเพลง) · ไม่เอา submodule/monorepo · **ต้องประสานทีมพระคำ (pk pm4)** · phase: เพลงก่อน→พระคำ theme.scss ตาม
- B094 in-app confirm dialog (แทน window.confirm) · B087 FLAG_LABEL ไทย + `lem-yai`→"เล่มใหญ่" picker
- data/review: พี่เปา review 41 เพลงติดธง · Amazing Grace `insert-amazing-grace.sql` รอ run · detail `docs/backlog.md`

## 🔬 วิเคราะห์เสร็จ — รอ P'Aim เคาะ implement (pm22 · 13 ก.ค.)
- **ปรับ pleng → สไตล์ phrakham + zoom 125%** · report `docs/reports/analyze-phrakham-style.md` (branch `analyze-phrakham-style` `5de5dc2`) · SA เสร็จ analysis-only
- **ข้อสรุป A (zoom):** 125% = ค่า "ซูมเริ่มต้น" ของ Chrome Android ที่เครื่อง P'Aim (โดนทุกเว็บ · incognito ก็เป็น) **ไม่ใช่บั๊กโค้ด** · เห็นพังแค่เพลงเพราะ dock ไม่ทน zoom (จอเหลือ ~288px, DockKey.vue:78,604-609 เผื่อแค่ ≥360px) · รอบก่อน pin `text-size-adjust` = แก้ผิดกลไก (font-boosting ≠ page-zoom) · **แก้ 2 ชั้น:** A-1 เครื่อง P'Aim ตั้งซูมกลับ 100% (หายทันที) · A-2 ทำ dock ทน ~288-320px (=WCAG Reflow · M · ต่ำเสี่ยง · **verify มือถือจริงที่ 125%**)
- **ข้อสรุป B (design):** parity ~70% แล้ว (สี/ฟอนต์/เงา/footer ตรง) · quick-win: **B-1 ตัวหนังสือ 18px (แก้ zoom ต้นเหตุด้วย) · B-2 header ครีมอุ่น** → B-4 มุมโค้ง → B-3 บรรทัดโปร่ง(หน้าอ่าน) → B-5/B-6 (M) · ห้ามแตะ line-height แผ่นเพลง/รื้อ dock logic
- **P'Aim เคาะ (13 ก.ค.):** zoom แก้ที่เครื่องแล้ว (default zoom 100%) · เดินหน้าแค่ **design parity look&feel** · แบ่ง SA/Dev/Tester เหมือนเดิม · **SA = สายเดิม** (`analyze-phrakham-style` `local_cd5fddfc`)
- **⭐ ข้อกำหนดใหม่ P'Aim:** เพลง+พระคำ **ใช้ core เดียวกันเท่าที่ share ได้ (แก้ที่เดียว)** · **ทิศทาง P'Aim: ยึดแพตเทิร์นเดิม** (พระคำ=SSOT · เพลงดึงมาใช้ เหมือน `pk-scrollnav.js` · alias `--pk-*`) → ขยายไป design tokens · **KISS ห้าม submodule/monorepo** เว้นทำไม่ได้จริง · SA verify wiring ปัจจุบันก่อน · brief `docs/pm/brief-phrakham-parity.md`
- **✅ DS เสร็จ + PM review แล้ว** (`d0a29ff` · `docs/ds/phrakham-parity.md`)
- **shared-core: P'Aim เคาะ = ทำทีหลัง** (13 ก.ค.) · ค่าตรงกันอยู่แล้ว ทำตอนนี้ยังไม่เห็นภาพเปลี่ยน + ต้องประสานทีมพระคำ → **ปล่อย 5 tweaks เสร็จก่อน** · แผนเก็บไว้ครบใน DS §🔗 (`1ec3cfd`) → ดู §📌 follow-up
- **🚩 B-2 (สีหัวเว็บ) — PM verify ยืนยัน premise เดิมผิด:** `theme.scss` ไม่ override navbar bg → พระคำ = `#f8f9fa` = เท่าเพลงเป๊ะ · "เหมือนพระคำ" = **ตัด B-2 ทิ้ง** (PM รับผิด: เคยบอก P'Aim ว่าครีม — ผิด) · **รอ P'Aim เคาะ: ตัดทิ้ง(ก แนะนำ) / ครีมอุ่นต่างจากพระคำ(ข)**
- **P'Aim เคาะ B-2 = (ก) ตัดทิ้ง** (13 ก.ค.) · scope final = **B-1,3,4,5,6** · แจ้ง SA ล็อก DS แล้ว · DS+report ย้ายเข้า base แล้ว (`git checkout` docs only)
- **จ่าย Dev แล้ว** — Dev session `local_72f2a19b` (worktree `keen-montalcini-f0f21a`, spawn_task ตั้งชื่อ branch เอง) · ลำดับ B-1→B-4→B-3→B-6→B-5 · self-verify AC + มือถือ 360/412 + regression แผ่นเพลง/dock · **รอ Dev report → Tester gate → P'Aim ดู → deploy**
- **DS re-sync (แก้ sync gap):** SA ล็อก B-2=DROPPED (`a85132e`) *หลัง* PM copy DS เข้า base → PM re-sync `git checkout` DS+report ล่าสุดเข้า base แล้ว (verify B-2=DROPPED บน base) · ส่ง heads-up Dev: verify fork จาก studio-shell-redesign (ไม่ใช่ main) + DS final B-2 dropped
- **บทเรียน:** spawn_task fork worktree/branch เอง (ชื่อ `claude/*` ไม่ตามที่ prompt ขอ) + อาจ fork จาก HEAD primary clone → **ทุก brief Dev ต้องสั่ง verify base branch เอง** (git merge-base) ก่อนลงมือ
- **✅ Dev เสร็จ** (`b70e53c` branch `phrakham-parity` · fork base ถูกต้อง) · PM git-verify: diff = styles.css(13)+About/Guide.vue เท่านั้น scope สะอาด · self-verify AC ผ่าน + มือถือ 360/412 ไม่ overflow · **ข้อจำกัด: Dev worktree Supabase ว่าง → chip20/regression แผ่นเพลง verify ผ่าน bundle+git diff ไม่ใช่คลิกสด**
- **✅ Tester PASS ทุก AC** (`tester-phrakham-parity-qa` `d022aee` fork tip `c127578`) · report `docs/reports/phrakham-parity-qa.md` · หลักฐาน live 360/412/desktop: body 18px ไม่รั่วแผ่นเพลง(verified #1) · card14 · reading-page lh1.8/max820 · ปุ่มหลักทึบ · header คง #f8f9fa · 0 error · **Network URL live: http://192.168.1.124:5420/**
- **2 note ไม่บล็อก:** (1) `.section-chip` 20px ถูกแต่ยังไม่มี component render (selector dormant) → PM เก็บไว้ (เผื่ออนาคต/ตรงพระคำ ไม่ใช่ dead จริง) (2) B-5 hover ขอบน้ำตาล = `@media(hover:hover)` เดสก์ท็อปเท่านั้น (by design กัน hover ค้าง touch) — มือถือไม่เห็น = ปกติ
- **สถานะ: ผ่าน gate แล้ว · รอ P'Aim ดูของจริง (URL ข้างบน) + เคาะ deploy** (PM แนะนำ batch รวม hero/favicon) · ยังไม่ merge base/deploy
- 📎 เจอ core lib `assets/pk-navbar.js` + reader font-scale (`--pk-fs`) ใน phrakham (theme.scss:123,152) — เผื่ออนาคต shared toolbar (ไม่ใช่รอบนี้)

## 📥 inbox → PM
- **🧪🟡 tester GATE2 (dock-space) · CONDITIONAL — engine ผ่านแกนหลัก · 2 concern toolbox ให้ UX/dev แก้ + 3 ข้อรอพี่เปามือถือ** (branch `dock-space-dev` `6c3b225` · report `docs/reports/dock-space-tester.md`) · **🎧 http://192.168.1.124:5344/#/studio** (curl 200 · footer stamp `6c3b225*` ตรง) · **วัดสดจริง** (Browser pane ≥755px + **real Chrome (claude-in-chrome)** สำหรับ scroll/focus — pane เลื่อน/โฟกัสจริงไม่ได้ พิสูจน์ซ้ำ) · **✅ PASS:** (1) dock slim (soundctl/playAll/export ใน ⚙ ปักได้ · essentials 44×44) (2) **hide-on-scroll เลื่อนจริง** (ลง→ซ่อน+peek 56×44 · ขึ้น→คืน · handler peek คืนจริง) (5) continuity (blur/resize/inside เก็บ · outside `mousedown` เคลียร์) (7) 2-host (autoHide prop default false = พระคำ inert · อาศัย dev island evidence · ผมไม่ได้ rebuild เอง) · (4-note) octave▲ `5→5'` จริง+copy+del · **🔴 2 CONCERN (ให้ UX/dev แก้ ≤3 loop):** (A) **anchoring:** `.slot-tools left:50%` = กึ่งกลาง **segment** ไม่ใช่โน้ต → โฟกัสโน้ตซ้ายสุด toolbox ห่างขวา **335px** (sameToolboxX=true ไม่ตามโน้ต) · หนักตอนป้อนทำนอง segment กว้าง (เคสหลัก octave) · เสนอเกาะ x ของ activeInput+clamp (B) **touch target:** ปุ่ม toolbox **30×26px** < เป้า 44/40 (✕ลบ ทำลายที่ 26px) · **⚠️ รอพี่เปามือถือจริง (env ตั้ง viewport <755 ไม่ได้ — pane clamp 755 · real Chrome ค้าง max 2560):** (3) keyboard-aware · (6) narrow 344/390/690 keypad/toolbox fit+no-hscroll · (4) ◀▶ syllable + draft บนแถบตอน login · **note:** h-scroll ที่ 768 = `.note-boxes nowrap` **ของเดิม (diff ยืนยัน dock-space แตะ .seg-col แค่ +position:relative)** + test data ผมยัด 17 โน้ต/segment ไม่ใช่ dock · desktop 1280 = 0 h-scroll · **⛔ ไม่ merge · รอ UX/dev แก้ 2 concern → verify loop → พี่เปามือถือ**
- **🧩✅✅✅ dev (dock-space) · PM 4-flag decisions applied — continuity + octave เสร็จ · 735 tests** (commit `47119e3` · report §8) · **(1) octave = คง activeInput pattern (re-add ▼▲ เข้า toolbox · v2 เคย drop ตาม UX comment · PM override)** (2) beam/slur = defer (ไม่ใส่ปุ่มตาย) (3) **🔴 continuity SA§7 = ทำแล้ว:** `selSlot` sticky (◀▶ selection แยกจาก focusedSlot live) ไม่ล้างตอน blur → พับ/หมุน/ปิดแป้น toolbox+◀▶ อยู่ · `onSegFocus` note→octave▼▲ · syllable→◀▶ · ล้างเฉพาะ outside pointer · **ไม่ forced-refocus = ไม่ loop กับ keyboard-aware** (4) bar/line/section=phase2 · **verify jsdom +2 (octave "5"→"5'" · ◀▶ survive blur) · 735 tests · build ✓** · **⚠️ พับ/หมุนจริง + hide-on-scroll ยิงจริง = tester GATE2 device (pane scroll/fold ไม่ได้)** · **รอ PM merge dev+ux → base → 2-host island (pk-PM) → tester** · ⛔ ไม่ merge เอง
- **🧩✅✅ dev (dock-space joint-pass v2) · hoisted merged toolbox เสร็จ บน UX `ba18718` · 733 tests** (branch `dock-space-dev` · commit `b4dad62` · report `docs/reports/dock-space-dev.md §7`) · **🎧 http://192.168.1.124:5313/#/studio** · **scroll-container เคลียร์:** UX เดา inner-container แต่ = **MCP pane scroll ไม่ได้ (env limit)** → **คง window listener ตาม SA Q1** verify จริงบนมือถือ = tester · **reconcile:** ผม drop `bfdcee1` (parallel) → wire บน UX markup ตาม DEV comment 4 ข้อ: (1) `focusedSeg` @focusin `.seg-col` → **hoist toolbox เดียวโผล่ทุกโหมด** (note-entry copy/del · lens ◀▶+copy/del) (2) ลบ per-syl slot-tools+seg-tools แยก = ไม่ซ้ำ · **copy/del on-selection (P'Aim declutter)** (3) **octave=NoteBoxes child → แยก ยังไม่ทำ (flag)** (4) **continuity SA§7: focusedSeg sticky ไม่ล้างตอน blur** (พับ/หมุน/keyboard toolbox อยู่ · ล้างเฉพาะ outside pointer = แก้ keyboard-loop ที่ flag) · overflow-strip=◀▶-only (ไม่มีโน้ตแม่) · **verify jsdom** (pane ขับ focus ไม่ได้): contextual-toolbox +5 (note→copy/del · syl→◀▶+copy/del · continuity blur survive/outside clear) + note-bar-tools อัปเดต on-selection · **733 tests · build ✓** · **flag:** octave (NoteBoxes · ขอ spec/งานถัดไป) · positioning toolbox เหนือ seg-col (UX จูน CSS) · bar/line/section=pass ถัดไป · device-matrix=tester GATE2 · ⛔ ไม่ merge เอง
- **🧩🟢 dev (dock-space engine lane) · engine ครบ 4 ชิ้น + test เขียว — รอ wire+sequence+2host gate** (branch `dock-space-dev` rebased บน `dock-space-ux` `10a3207` · 4 commit `1cd032c`/`3955a1f`/`27f5d9b`/`e406e29` · report `docs/reports/dock-space-dev.md`) · **🎧 http://192.168.1.124:5313/#/studio** (curl 200 · IP `10.189.195.98` ตายแล้ว) · **แตะ `DockKey.vue`+test เท่านั้น** (`git diff 10a3207..HEAD` ยืนยัน · ไม่แตะ EditorMode) · **(1) slot-in-⚙-panel = unblock UX แล้ว** (2) hide-on-scroll (prop `auto-hide` default false · window · rAF+delta · peek · a11y toggle+reduced-motion+focus-guard) (3) cap=f(width) `clamp(floor(w/50),3,14)` observe documentElement แก้ Fold-jump 760 (4) keyboard-aware visualViewport>150px · **728 tests +13 DockKey · build ✓ · 0 warning · live smoke `.dk-shift` render+0 error** · **ต้องทำต่อ (นอก lane):** (a) **UX/wiring: `:auto-hide="true"` ที่ `<DockKey>` EditorMode:2994** (1 บรรทัด · PM sequence) (b) UX set soundctl/export `inSetting` (ปลดล็อกแล้ว) (c) **✅ 2-host gate PASS (ทำแล้ว):** rebuild `pk-dock-island.js` จาก DockKey ผม (PLENG_SRC override → scratch · ไม่แตะ phrakham tree) = **15 mod bundle สะอาด** · **live smoke Chromium 344/690/768/1200: mount ✅ · 1 row · ไม่มี h-scroll · auto-hide inert ทุกจอ (พระคำไม่ regress) · 0 console error · ไม่มี Fold-jump** · source-safe (⚙ พระคำไม่มี slot-kind · Aa=bar item) · **⛔ ยังไม่ commit island เข้า phrakham = post-merge + pk-PM** · **device-matrix editor (auto-hide จริง) = tester gate หลัง wire+serve** · ⛔ ไม่ merge เอง
- **🧩 dev (dock-space engine lane) · read-only prep เสร็จ — phase-A build plan รอเคาะ** (branch `dock-space-dev` แตกจาก `studio-shell-redesign` `fdd233f` **ไม่ใช่ `uxui-standing`** · report `docs/reports/dock-space-dev.md`) · **ยังไม่เขียน engine code** (HOLD ตาม brief) · **lane สะอาด:** ผมแตะ `DockKey.vue` ไฟล์เดียว (engine · 2-host) · **ไม่แตะ `EditorMode.vue`/`editItems`/toolbox** = lane UX → ขนานได้ · **phase A = prop `auto-hide` (default false กันพระคำเจ็บ) + hide-on-scroll (`.dk-shift` translateY แยกจาก drag inline-transform บน `.dk-dock`) + peek handle + a11y toggle "ปิดหลบอัตโนมัติ" ใน ⚙ (engine-owned persist · ไม่ยัดใน editItems) + `prefers-reduced-motion`→default off + Tier-A test (pure reducer `nextHidden`)** · **verify แล้ว:** DockKey `defineProps:23` · `.dk-host:260/468` · drag inline `d.style.transform:168,203` (ต้องเลี่ยง) · ⚙ panel `:404` · width-cap `:75/78/235` = **phase C ไม่ใช่ A** · **บล็อกแค่ 2 จุดจาก SA:** Q1 (scroll container window vs wrapper) · Q3 (AT-detect → เสนอ reduced-motion=default off) · Q2/Q4/Q5 = phase B/C ไม่บล็อก · **ขอ PM:** (1) เคาะ phase-A + sequence UX/dev บน EditorMode (2) relay SA Q1+Q3 → เริ่ม build ได้ทันที · ⛔ ไม่ merge เอง
- **🔴 สาย E · MP3 ตัดท่อนรับซ้ำ (บั๊กบน live) ✅ แก้เสร็จ · รอ PM gate** (branch `claude/cool-curie-fe8e6d` `94738cc` · fork ฐาน · report `docs/reports/mp3-order-fix.md`) · **🎧 <http://10.189.195.98:5311/>** · **verify เอง ไม่เชื่อ brief:** เปิดไฟล์เอง → `:37`+`:57` ไม่ส่ง `order` จริง · reproduce เลข SA **ตรงทุกตัว** (#16 −116.3 · #4 −79.4 · #141 −72.0 …) = harness เชื่อได้ · **ผลหลังแก้: 124/124 เพลง MP3 = ฟังสด เป๊ะทุกโน้ต · delta = 0 ทุกเพลง · ไม่มีเพลงไหน regress** · **⚠️ 12 เพลง ไม่ใช่ 10 — และ SA ไม่ได้พลาด ข้อมูลมันขยับ:** #118 ถูกติด `afterEachVerse` **09:03 UTC** (brief เขียน 09:25 → ตอน SA วัดยังไม่มี) · **#117 ถูกติด 09:34 = หลัง brief · ระหว่างผมทำงานอยู่** → **ทุกครั้งที่ DA ติดป้าย "ร้องรับทุกข้อ" เพลงพังเพิ่ม +1 เงียบ ๆ · เลขในเอกสารทุกฉบับเก่าทันที** (เพราะงี้ถึง derive จากข้อมูล ไม่ hardcode + มี audit รันสด) · **⚠️ ค้าน brief 1 ข้อ (สำคัญ):** brief สั่ง "ส่ง `order` เข้าทั้ง 2 จุด" แต่ `order` ใน viewer = `selectionOrder ?? strophicOrder` → **ถ้าทำตามตรง ๆ คนเลือกดูแค่ท่อน "รับ" แล้วกดโหลด จะได้ไฟล์แค่ท่อนรับ** → ผมใช้ **strophic (ทั้งเพลงตามที่ร้องจริง)** = ตรงกับเลขเป้าใน brief ที่วัดจาก `fullNotes` · **ไม่ใช่แค่เติม 2 จุด:** รวมเป็น seam เดียว `exportPlayNotes` (2 จุดเคยต่างคนต่างจำ แล้วลืมพร้อมกันทั้งคู่) + derive ในไฟล์เอง = `DownloadTool`/`ExportTool` **ไม่ต้องแก้ = ไม่ออกนอกเลน** · **⭐ test กันซ้ำ (พิสูจน์แล้วว่าจับได้จริง ไม่ใช่ "แก้แล้วผ่าน"): ใส่บั๊กกลับ → fail 3 เทสต์ทันที** (เทียบทีละโน้ต ไม่ใช่แค่ `.length`) + `tools/audit-mp3-order.mjs` รันกับ 124 เพลงจริง exit 1 ถ้าไม่ตรง · **verify ถึงไฟล์จริง:** เรนเดอร์จริงบน browser #16 = **297.9s** (เดิม 180.6) · **ถอดไฟล์ MP3 จริงกลับมาวัด #19 = 84.3s** (เดิม 62.6) · 678 tests (+5) + build ผ่าน · **คอมเมนต์โกหก `:50` ("MP3 == ฟัง in every musical detail") เอาออกแล้ว** — มันคือสิ่งที่ทำให้บั๊กรอด 19 รอบ deploy · **📌 ตอบสาย F:** MP3 วิ่งผ่าน `arrange()` เหมือนฟังสด → **เฟอร์มาตาพังตรงไหน MP3 พังตรงนั้น · ไม่ต้องแก้ `audioExport.js` แยก** พอสาย F ต่อเข้าวาทยกรได้ MP3 ได้ตามอัตโนมัติ (invariant test คุมให้เท่ากัน) · **ℹ️ `notationLint.test.mjs` fail = pre-existing ไม่เกี่ยวผม** (`git stash` แล้วยัง fail · script เรียก `process.exit` · ข้างในมันเอง 72/72 ผ่าน) · ไม่ merge เอง รอ PM
- **🎼 ✅ สาย F เฟอร์มาตา เสร็จ — P'Aim ได้ยินถูก แต่ ❌ brief verify ผิดทั้ง 3 ข้อ** (branch `claude/zealous-feistel-cc8cbd` · report `docs/reports/fermata-fix.md` · **🎧 <http://10.189.195.98:5312/>** · แก้ `src/lib/midi.js` ไฟล์เดียว **ไม่แตะ `audioExport.js`**) · **❌ ค้าน brief 3 ข้อ:** (1) **`d *= 1.75` ไม่ได้อยู่ในทางตัน — มันทำงานอยู่จริง** · `tokenBeats` อบ ×1.75 ลงใน `n.beats` **ก่อน** `arrange()` เห็น แล้ว `melodyEvents` copy `beats: n.beats` ตรง ๆ → **`grep -c fermata` = 0 เป็นเรื่องปกติ ไม่ใช่หลักฐานบั๊ก** (2) **สมมติฐาน "หายตอน `arrange()`" ผิด — วัดแล้วไม่หาย:** `1 5^ 1` ผ่าน `arrange()` ออกมา `1, **1.75**, 1.12` + โน้ตถัดไปเลื่อน `2 → 2.75` ถูกต้อง (3) **คู่มือมีเฟอร์มาตาอยู่แล้ว** `NotationStandard.vue:43` · ⚠️ **ถ้าทำตาม brief ("ต่อเฟอร์มาตาเข้าทางวาทยกร") = คูณ 1.75 ซ้ำ 2 รอบ (3.06×) ในเคสที่ถูกอยู่แล้ว และบั๊กจริงยังอยู่ครบ** · **🔴 ต้นเหตุจริง = ×1.75 คูณแค่กล่องแรก · กล่อง `-` ถูกบวกทีหลังโดยไม่คูณ** → **บั๊กกลับหัวกับการใช้จริง: ยิ่งโน้ตยาว เฟอร์มาตายิ่งอ่อน** (`1^`=1.750× ถูก · **`1^ -`=1.375×** ← เพลง #120 · `1^ - -`=1.250× · `1^ - - -`=**1.188× แทบไม่ได้ยิน**) — **แต่เฟอร์มาตาเขียนบนโน้ตยาวท้ายวรรคเป็นหลัก = เคสที่โค้ดทำงานแย่สุดพอดี** = เสียงที่ P'Aim บอก "ไม่ลากเสียงจริง" (#120 ได้ 1.375× = **ต่ำกว่าขั้นต่ำมาตรฐาน 1.5×**) · **⛔ ไม่แตะเลข 1.75 ตามสั่ง** (แก้ที่ "ไม่ทำงาน") · **หลังแก้ทุกความยาวได้ 1.750× เป๊ะ** (`1^ -`→3.5 · `1^ - - -`→7.0) · **เพลงจริงตรงทฤษฎี:** **#120** 390→**399 beats (+9.0** = 6×(2×0.75) ✅ **) · #118 +0.75 ไม่ขยับ** (เฟอร์มาตาบนโน้ต 1 จังหวะ = ถูกอยู่แล้ว → **ยืนยันไม่มี regression**) · **📊 นับคลังจริง (ไม่เดา): 124 เพลง → ใช้เฟอร์มาตาแค่ 2 เพลง** (#118, #120 · v2+anuchon ทั้งคู่) · #120 ซอร์สมี `^` ตัวเดียวแต่ stanza ใช้ซ้ำ 6 ข้อ → **ได้ยินจริง 6 จุด = เพลงที่ควรให้ P'Aim ฟังยืนยัน** · **✅ MP3 ได้เฟอร์มาตาด้วย ไม่ต้องแก้ `audioExport.js`** (วัดผ่าน `estimateMp3()` ไฟล์เดิม bpm92: #120 254.60→**260.47s = +5.87s** = 9×60/92 ✅) · **🧪 test ใหม่ `midi.fermata.test.js` (11 tests) พิสูจน์แล้วว่าจับบั๊กได้จริง** — revert เฉพาะบรรทัด ext แล้วล้มด้วยเลขจริง (`expected 2.75 to be close to 3.5`) · **ยึด "อัตราส่วน" ไม่ใช่เลขตายตัว** = สิ่งที่จับคลาสนี้ได้ (**โค้ดเดิมผ่านทุกเคสกล่องเดียว แต่ผิดทุกเคสที่ใช้จริง → test ที่เช็กแค่ `1^`=1.75 ไม่มีวันจับได้**) + ล็อก factor ในช่วง 1.5–2× + เช็กว่าโน้ตถัดไปถูกเลื่อน + เพลงไม่มีเฟอร์มาตาต้องไม่ขยับ · **684 tests pass** (`notationLint` fail = pre-existing `process.exit(0)` · ยืนยันโดยรันบน base ได้ผลเท่ากันเป๊ะ `72 passed, 0 failed`) · **❓ รอ PM เคาะ 2 ข้อ:** (1) **คู่มือ** — มีอยู่แล้วแต่ขาด "มันคืออะไรในความรู้สึก" → **เสนอกล่องใช้ถ้อยคำพี่เปา (สโลว์โมชั่นในหนัง) + ให้เครดิตเขา** (ร่างอยู่ในรายงาน) · **ไม่แก้เอง** เพราะ `NotationStandard.vue` ค่อนไปทางเลน render + brief ใช้คำว่า "เสนอ" → PM ชี้ว่าให้ผมทำต่อ หรือจ่ายสายที่ถือไฟล์ (2) **follow-up ไม่ใช่ scope นี้:** tie `~`/slur ที่มีเฟอร์มาตา เป็นอาการเดียวกันเป๊ะ (คูณไม่ครบทั้งโน้ต) แต่ **0/124 เพลงเขียนแบบนี้** → ถ้า DA encode ties (B068) ต้องกลับมาดู · **⛔ ไม่ merge เข้าฐานเอง**
- **🔧 ✅ issues7 รอบ 2 เสร็จ (P'Aim ลองแล้วยังไม่ผ่าน → แก้ต้นเหตุจริง) — รอ P'Aim ลองอีกรอบ** (branch `claude/interesting-bun-49b4ec` `31f8720` · report `docs/reports/editor-friction.md`) · **🎧 <http://192.168.1.124:5471/#/studio>** · **⚠️ IP: `10.189.195.98` ตายแล้ว (curl 000) · `192.168.1.124` = 200 · banner vite ยังพิมพ์ IP เก่าเพราะพิมพ์ตอน start ก่อนย้ายเน็ต → curl แข็งกว่า banner** · **683 tests** · **🎯 ต้นเหตุจริง = "ตัวเล็ก" กับ "เหลือที่ทางขวา" คืออาการเดียวกัน:** วัดหน้าแผ่นเพลงจริง เพลง #66 → บรรทัดกว้างสุด **397px ในคอลัมน์ 1160px = เนื้อเพลงใช้แค่ 34% · ว่าง 66%** → **preview ย่อตัวหนังสือเพื่อให้พอดีกับความว่างเปล่า** _(กับดัก: 24/34 บรรทัดมี width=0 ซ่อนอยู่ → ตัวเลขแรกเชื่อไม่ได้ เกือบเอาไปออกแบบ)_ · **แก้:** (1) **ปุ่ม −/%/+ ในหัวหน้าต่างเอง · จำค่า** — **ตอบ PM: แยก scale ไม่ใช้ Aa ตัวเดียวกัน** เพราะ "ดูตรงไหนแก้ตรงนั้น" ไม่ควรแปลว่ายื่นมือไปจัดหน้าแก้ไขข้างหลัง · **verify สด: หน้าแก้ไขไม่ขยับ (16px คงเดิม · readingFontScale ยัง 1)** · Aa เดิมยังอยู่ครบ · **กลไก: หาร em-width ไม่ใช่คูณ font** (คูณ font = แผ่นล้นหน้าต่าง = h-scroll ผิด ui-standards §2) · zoom 1 = parity เดิมไม่ถูกแตะ (2) **หน้าต่างจำขนาด/ตำแหน่ง** — เดิมลืมทุกครั้งที่ปิด = ครึ่งหนึ่งของ "ไม่ใช้พื้นที่เต็ม" · clamp ตอนเปิดกันหลุดจอ (3) **เลิกเลข 980 ตายตัว → 65vw** ตามกฎ P'Aim · **ผลวัดจริง:** 1536 เปิดมา 14px · กด + → **200%=28px** · reload → **จำได้ 1528px/150%/32.62px เหลือขอบขวา 4px** · 412 → **19.4→48.5px ที่ 400%** · **ไม่มี h-scroll ทุก zoom (360/412/1536)** · **🔴 ผมทำบั๊กเองอีกตัว เจอเพราะทดสอบ 412 จริง:** เอาเพดาน 20em (ที่มีไว้กันค่าวัดพัง) ไปใส่**หลัง**หาร zoom → มือถือคอลัมน์ ~21em → **ทุก zoom ชนเพดาน ปุ่ม + ตายสนิท** → แยก 20em (ก่อนหาร) / 8em (หลังหาร กัน h-scroll) · **🪤 test fake-pass 2 รอบ:** (1) jsdom ไม่มี layout → ตกไปใช้ fallback 42em = เคสที่ไม่เคยพัง (2) assertion อ่อน — กดครั้งเดียว 21.2→20 ก็ "ลดลง" แม้ติดเพดาน → **รอบ 3 กด 2 ครั้งติดต้องลดทุกครั้ง = ย้อนโค้ดแล้วแดงจริง** · **⚠️ ยังไม่ทำ (ตั้งใจ):** ย่อพอดี "เนื้อเพลง" แทน "คอลัมน์" (ตัวจะใหญ่เอง ~38px) — **เสี่ยง parity ที่พี่เปาขอเองใน issues3** → ปุ่ม + ให้ผลเดียวกันโดย P'Aim คุมเอง · **P'Aim สั่งได้ถ้าอยากให้เต็มอัตโนมัติ** · **issues6 ไม่แตะ + ไม่ทำให้ยากขึ้น** (zoom = font-size ไม่ใช่ transform → พิกัดคลิกยังตรง)
- **🔧 ✅ editor-friction รอบ 1 (issues 7/9/11 + ลบร่าง)** (branch `claude/interesting-bun-49b4ec` `d2b1f0f` · report `docs/reports/editor-friction.md`) · **🎧 <http://10.189.195.98:5471/#/studio>** (เช็ก vite Network line จริง + curl 200) · **679 tests เขียว** (`notationLint` แดง = pre-existing · พิสูจน์ด้วย `git stash`) · **⛔ ไม่แตะช่องป้าย** (`curLine().label`) ตามที่กำชับ — สาย `task_c3c169a7` ยังรันอยู่จริง · **issues7 = 3 ชั้นซ้อน:** (1) 🔴 `parseFloat("min(1160px, 100%)")` = **NaN** → `|| Infinity` → วัดคอลัมน์ 1455px แทน 1160px จริง → **จอยิ่งกว้างตัวยิ่งเล็ก** (พิสูจน์สด 1280→9.06px · 1536→**7.47px**) (2) ตัวลากขยาย = ทางตัน **ขยายได้ 27px** (หน้าต่างอยู่มุมขวาบน โตได้แต่ทางขวา) = **"ไม่ได้อีก" พี่เปาพูดถูกทุกตัวอักษร** (3) หน้าต่าง 720px เล็กเกินกว่าจะอ่านออก (แผ่นย่อให้พอดีหน้าต่าง = ผลของ parity ที่พี่เปาขอเองใน issues3) · **แก้:** parse px จากสูตร + **ชนขอบขวาแล้วเลื่อนซ้ายแทนหยุด** + default 980px → **7.47px → 13.73px (+84%) · ลากได้ถึง 21.75px** · **⚠️ ค้าน PM 4 ข้อ (ของจริง):** (a) `EditorMode.vue` อยู่ **`src/components/`** ไม่ใช่ `src/views/` (b) **`readingFontScale` ใช้กับหน้าต่างนี้อยู่แล้วและได้ผลจริง** (Aa 100%→9.06px · **220%→19.94px**) = **พี่เปามีปุ่มขยายในมือมาตลอด แต่ไม่มีทางรู้** → ผมจึงไม่แปะปุ่มซูม (ตัวลากขยาย=ซูม อยู่แล้ว + เป็นท่าที่เขาลองเอง) (c) **issues9 หนักกว่าที่ตั้งโจทย์:** ไม่ใช่แค่ "default ควรอยู่แถบหลักไหม" — **`btn` ในหน้า ⚙ ไม่มี branch render เลย** = กดใช้ไม่ได้จริง **ปักหมุดคือทางเดียว** (ภาพพี่เปายืนยัน: บันทึกร่างมีแต่หมุด · ดูผลทั้งเพลง(toggle)มีสวิตช์) → บันทึกร่างได้ที่นั่งถาวรบนแถบ + `btn` ใน ⚙ กดได้ + **กันปุ่มซ้ำ 2 ที่** (พี่เปาปักหมุดไว้แล้ว · ล้างให้เอง) · **ระบบปักหมุดไม่ถูกรื้อ** (d) **RLS มี delete policy อยู่แล้ว** (`db/002` "Delete own or as approver") → **ไม่ต้องแตะฐานข้อมูล ไม่ต้องหยุด** · audit `classifyChange` map DELETE ร่าง = `null` (discard) รองรับไว้แล้ว · **ลบร่าง:** ปุ่มบนแถวร่างเอง + confirm บอกชื่อ + 44px + ล้าง id ที่ตาย · ⛔ ไม่แตะลบเพลงเผยแพร่ (มี test ล็อกว่าไม่ยิง `songs.delete`) · **issues11:** `.edhead` = `position:static` → sticky ใต้หัวเว็บ · **วัดความสูงหัวเว็บจริง ไม่ hardcode** (64px desktop / 114px @360) · **🔴 SA เตือนถูก — ผมทำพังเอง แล้วแก้แล้ว:** `.cshead` (B085) sticky อยู่แล้วที่ `top:58px` hardcode → พอ edhead ปักด้วย **cshead มุดใต้ 62px จนปุ่มกดไม่ได้เลย** (โดน `ed-chip` บัง) = **แลกแถบที่กดไม่ถึงอันหนึ่ง ด้วยการทำอีกอันกดไม่ได้** (ลบล้าง B085 ทั้งดุ้น) · **ผมไม่ได้ตรวจในรอบแรก** → แก้: cshead ปักที่ `shellH + edheadH` (วัด DOM จริง + ResizeObserver) · **กับดักซ้อน: ใช้ template ref แล้ว ref เป็น null ตอน mount → `edheadH` ค้าง 0 → ซ้อนกลับมาเงียบๆ** จับได้เพราะอ่านค่าที่ render จริง (`top:64px` ทั้งที่ควร 158) · **verify ใหม่ scroll 900/1600/2300: ซ้อน 0px · ทั้ง 2 แถบผ่าน hit-test · chrome 25% ของจอ (< เพดาน ~30% ที่ SA ตั้ง → ไม่ต้องใช้ plan B ยุบแถบ)** · มือถือไม่ซ้อน (edhead ไม่ sticky · cshead ใช้ CSS เดิม 58px = ไม่เปลี่ยนจากเดิม) · **680 tests** (+test ล็อกการซ้อน · พิสูจน์ด้วยการย้อนโค้ดแล้วแดงจริง ไม่ fake-pass) · **⛔ ไม่แตะช่องคอร์ด/`chords.js`** (เรื่อง `FINE`→`GINE`) ตามที่กำชับ · **🔴 sticky เฉพาะเดสก์ท็อป — ตัดสินใจเอง:** วัดที่ 360px จริง แถบ **wrap 6 แถว = 256px = 32% ของจอ** → ปักบนมือถือ = **กินจอพี่เปาถาวร 1/3** → ไม่ทำ (มือถือเหมือนเดิม ไม่มีอะไรแย่ลง · 360/412 ไม่มี h-scroll · ปุ่มไม่หลุดขอบ · ไม่มีปุ่ม <24px) · **ขอ PM เคาะ:** (1) จะให้ sticky บนมือถือต้องทำแถบเตี้ยลงก่อน = **ทับสาย ⋯** (2) **บอกพี่เปาว่า "Aa" ขยายหน้าต่างดูผลได้** (3) **ลบเพลงที่เผยแพร่แล้ว** — พี่เปาถามใน issues10 ด้วย ยังรอ P'Aim (4) เจอบั๊กเดิม (ไม่แตะ): `NAV.rail` `top:58px` hardcode ทั้งที่หัวเว็บ 64–114px → มุดใต้หัวเว็บ
- **🎻 5.1 วัดเสร็จ — "กระตุก" = บั๊กจริง · PM ถูก · แต่คนละสาเหตุ → เสนอ "พัก RR" ประหยัด 1.09 MB** (branch `cello-bakeoff` `50f4532` · report `docs/reports/cello-marcato.md`) · **หูของ P'Aim ถูก ไม่ใช่รู้สึกไปเอง** · **สาเหตุที่ PM เดา (ความดัง/ความยาว) วัดแล้วไม่ใช่:** ความดังต่างกัน median **1.43 dB** · **0/17 pitch ที่เกิน 3 dB** (เครื่องมืออบ makeup แยกให้อยู่แล้ว `_d` +13.07 vs `_g` +12.94 = ห่าง 0.13 dB) · จูน median 2.1¢ (ตัวเพี้ยน 23¢ = โน้ต 84 ทำนองไม่เคยแตะ) · **🔴 ต้นเหตุจริง = หัวเสียง: t50 ต่างกัน median 200 ms · สูงสุด 580 ms** · **รูปคลื่นจริง (C3 · ช่องละ 50ms · % ของ peak):** `_d` = `22 26 33 41 51 63 69 75...` **ค่อย ๆ บวม ~400ms** vs `_g` = `81 100 75 69...` **เริ่มที่ 81% เต็มใน 100ms** → **คนละท่าเล่น ไม่ใช่ take เดียวกัน 2 ครั้ง** → สลับโน้ตเว้นโน้ต = **โน้ตนึงฟูขึ้นช้า โน้ตถัดมาโผล่เต็มทันที** = **"กระตุก เป็นช่วง ๆ" (เป็นคาบ) เป๊ะตามที่ P'Aim ได้ยิน** · **❌ normalize ไม่ได้และไม่ควร:** ทำ `_g` ให้บวมช้าเท่า `_d` = **ยัด fade-in ปลอม 400ms = swell สังเคราะห์ตัวเดียวกับที่ทำ Iowa เป็น "หวูดเรือ"** แล้วโดนตัดทิ้ง · ทำ `_d` ให้เต็มเท่า `_g` = **ตัดการบวมทิ้ง = ทิ้งเหตุผลที่เลือก `p`** → **2 take ใช้สลับกันไม่ได้โดยโครงสร้าง** · **✅ เสนอพักตามที่ P'Aim เสนอเอง ("ถ้ายุ่งก็พักได้") — ปุ่มปิดเป็น default อยู่แล้ว ไม่ต้องแก้อะไร ไม่ต้องจ่าย 1.09 MB** · **5.2/5.3 ยังไม่เริ่ม**
- **🎻🎹 ✅ resonance สร้างแล้ว — ครบ 3 ปุ่ม รอ P'Aim ฟังทีเดียว** (branch `cello-bakeoff` `08983f2` · report `docs/reports/cello-marcato.md`) · **🎧 <http://10.189.195.98:5451/docs/spikes/cello-marcato.html>** · **3 ปุ่มใหม่ เริ่มที่ปิดทั้งหมด:** สั่นนิ้ว · คันชักคู่ · **เสียงกังวานเปียโน** · ค่า P'Aim (5%/10ms) ครบ · **resonance:** preset จาก `Res.txt` (57 region) + Sampler ตัวที่ 2 ซ้อนใต้เปียโน (แบบเดียวกับที่ sfz ซ้อน Res group) · **⛔ ไม่แตะ preset เปียโนจริง** · **+0 MB ยืนยันแล้ว** (ทุก region ชี้ไฟล์ `PP` ที่ ship อยู่แล้ว · `-11dB` · attack 0.1) · **🔍 แต่เช็กเพิ่มไม่ได้เชื่อ "+0 MB" ลอย ๆ: `Res.txt` ต้องใช้ PP 57 โน้ต เรา mirror แค่ 30** (`fetch-grand-layers` ตัดเปียโนเหลือ MIDI 40–84) → **ใช้ได้ 30 region ครอบคลุม played-key 28–71** · **แต่ไม่กระทบจริง — วัดกับเพลง #1: ยิง resonance 74 เสียง · `notesAboveRange = 0`** = **ทุกโน้ตที่เปียโนเล่นอยู่ในช่วงที่ครอบคลุมหมด** (27 ไฟล์ที่ขาด = ช่วงสุดขั้วที่เราไม่เคยแตะ) · ตรวจ: เพิ่ม RMS **+0.76 dB** (บาง ๆ ตามที่กังวานควรเป็น) · ไม่คลิป · repeat delta **3.73e-8 = float noise → MP3 == live ยังจริง** · ค่า P'Aim ครบ · มือถือ 360px สะอาด · **ไม่ตัดสินว่าเพราะขึ้น — หู P'Aim**
- **🎻 ปุ่มคันชักคู่ ✅ ทำแล้ว · resonance 🟢 สมมติฐาน PM ถูก = ปลดล็อกแล้ว รอ go** (branch `cello-bakeoff` `1db0d3c` · report `docs/reports/cello-marcato.md`) · **🎧 <http://10.189.195.98:5451/docs/spikes/cello-marcato.html>** (ค่า P'Aim 5%/10ms ครบ · ปุ่มใหม่ 2 อัน **เริ่มที่ปิดทั้งคู่**) · **RR:** `karoryfer-p-g` สลับตาม index (i%2) · **ตัวลากเท่านั้น** (หัวไม่ต้อง = PM ถูก) · **ราคายืนยัน 1.09 MB** · **บอก P'Aim ตรง ๆ ในหน้าเว็บแล้วว่า 2 คันชักลดซ้ำได้แค่ ~31%→~15% ไม่หายหมด + ราคา 1.09 MB → ถ้าฟังไม่ต่าง ไม่ต้องจ่าย** · **✅ deterministic พิสูจน์ด้วยเลขจริง:** ปิด RR เรนเดอร์ 2 ครั้ง = **0.00e+0 (เหมือนทุกบิต)** · เปิด RR 2 ครั้ง = **1.49e-8** (ปัดเศษ float32 ~−156 dB · ต่ำกว่า noise floor 16-bit · เพราะ 2 sampler รวมเข้า bus แทน 1) · **ผลจริงของ RR = 9.40e-2 = ใหญ่กว่า noise 6 ล้านเท่า** → **MP3 == live ยังจริง** · ไม่มีสุ่ม ไม่ต้องใช้ `rng.js` · **🟢 resonance — PM คิดถูก วัดยืนยันแล้ว:** golden-piano **เหยียบค้างโดยพฤตินัยจริง** — **85% ของโน้ตเปียโนดังข้ามหัวโน้ตถัดไป · เฉลี่ย 2.37 เสียงซ้อน (สูงสุด 4) · เงียบสนิท 0 วินาที ตลอด 18.8s** → **เปิด resonance ทิ้งไว้ตลอดได้เลย ไม่ต้องนิยาม pedal = หมดข้อที่ทำให้ไม่ใช่ 80:20** (+0 MB) · ⚠️ หมายเหตุ: `bass:'pedal'` ใน `arrange()` = **pedal tone (โน้ตเบสค้าง) ไม่ใช่ CC64** — ข้อสรุปมาจากการวัดการกังวานจริง ไม่ได้มาจากชื่อ · **ยังไม่สร้าง รอ PM สั่ง go** (คำสั่งหยุดเดิมยังอยู่) · ตรวจ: ค่า P'Aim ครบ · ไม่แตะ `dynamics.js`/deploy/preset เปียโน/`soundOptions.js` · ไม่มีไฟล์เสียงใน git
- **🎻 ขั้น 4: vibrato ✅ ทำแล้ว · resonance 🛑 หยุดรอ PM เคาะ** (branch `cello-bakeoff` `7568bda` · report `docs/reports/cello-marcato.md`) · **🎧 <http://10.189.195.98:5451/docs/spikes/cello-marcato.html>** (ค่า P'Aim 5%/10ms ยังอยู่ครบ · **+ปุ่มสั่นนิ้วใหม่ เริ่มที่ปิด**) · **4.2 vibrato:** เอาค่าคนอัดมาทั้งชุด (**4.02 Hz** · **0–22 cents** เริ่มที่ 0 ตามที่เขาตั้ง · หน่วง 177ms · ไล่ขึ้น 177ms) · **smplr ทำไม่ได้** (เซ็ต `detune.value` ครั้งเดียว ไม่คืน node) → ดักจับ voice ตอนถูกสร้างแล้วต่อ LFO เราเองเข้า `detune` (อยู่ใน spike ล้วน ไม่ fork smplr) · **✅ พิสูจน์ว่าสั่นจริงตามค่า ไม่ใช่แค่ "เสียงเปลี่ยน":** วัดพิตช์โน้ตยาว (66 · 2.64s) **ล็อกช่วงค้นหา ±1 เสียง** → ปิด 14.5 cents (drift ธรรมชาติ) → **เปิด 43.5 cents · ทฤษฎีบอก 44 (±22) = ตรง** · ⏭️ **ข้าม EQ 1800Hz ตาม brief** ("ไม่ง่าย = ข้าม รายงาน" · ต้อง biquad ต่อ voice) · LFO ตัวเดียว = deterministic ไม่ต้องใช้ `rng.js` · **4.1 resonance 🛑 หยุดตามสั่ง:** **smplr = 0 support (verify แล้ว)** · **💡 แต่พลิกสมมติฐาน: ไม่ต้องโหลดไฟล์ใหม่เลย = +0 MB** — `Res.txt` ชี้กลับไปที่ **ไฟล์ PP ที่ ship อยู่แล้ว** (`sample=PP A0.$EXT` เล่นที่พิตช์อื่น เบา `-11dB` หัวช้า) · **ตัวบล็อกจริงไม่ใช่ไฟล์ แต่คือ "เหยียบตอนไหน"** — sfz เปิดเฉพาะ `locc64=65` (pedal ค้าง) **แต่เราไม่มีสถานะ pedal เลย** (`bass:'pedal'` = สไตล์เบส ไม่ใช่ CC64) → **ต้องนิยามเอง = การตัดสินใจเชิงดนตรี · นิยามผิด = ไปทับเสียงที่ P'Aim เคาะว่า "เพราะดี"** → **ไม่ใช่ 80:20 · รอ PM เคาะ** · ✅ ที่ปรึกษาผิดจริงตามที่ PM ว่า (ยืด release ≠ resonance · ของจริงคือ region แยกเล่นไฟล์ PP) · ตรวจ: ค่า P'Aim ยังอยู่ · ไม่แตะ `dynamics.js`/โค้ด deploy/preset เปียโน · ไม่มีไฟล์เสียงใน git
- **🎻 ✅ ขั้น 3 เสร็จ (ล็อกค่า + นับโน้ตซ้ำ) — รอ PM/P'Aim เคาะว่าซื้อ RR ไหม** (branch `cello-bakeoff` `8e297fb` · report `docs/reports/cello-marcato.md`) · **🎧 <http://10.189.195.98:5451/docs/spikes/cello-marcato.html>** (เปิดมาได้ค่า P'Aim เลย) · **3.1 ล็อกแล้ว:** `PAIM_MARCATO` = หัว mp **5%** · เลื่อน **10ms** · ตัว `p` → เป็น default ของหน้า spike (ปุ่มยังหมุนได้ · **ไม่แตะ `soundOptions.js` เชลโลยัง disabled**) · **3.2 นับแล้ว — PM สงสัยถูก:** 5 เพลงจริง · **ซ้ำไฟล์เดิมติดกัน 20–45% (กลาง ~31%)** · ซ้ำใน 2 วิ 29–54% · ⭐ **"ซ้ำแบบมองไม่เห็น" (คนละโน้ต แต่ไฟล์เดียวกัน) 4–27 ครั้ง/เพลง** — ชัดสุด #11 ขอบพระคุณ: ซ้ำ 23 ครั้ง **เป็นโน้ตเดียวกันแค่ 5 → อีก 18 มองจากแผ่นโน้ตไม่เห็น** · เหตุ: ทำนองนมัสการช่วงแคบ → **ใช้จริงแค่ 5–7 ไฟล์จาก 17** · **💰 แต่ราคาที่ PM ลดให้ผิด 3.6 เท่า: `_g` ของ `p` วัดจริง = 1.09 MB ไม่ใช่ ~0.3 MB** (ไฟล์คันชักขึ้นยาวกว่าคันชักลง) → รวม 4.09 → **5.18 MB · คลัง +20% ไม่ใช่ +15%** · ⚠️ **`_d`+`_g` = 2 take → ลดซ้ำ 31% เหลือ ~15% ไม่ได้หายหมด** (คนอัดใส่ staccato 4 take เพราะ 2 ไม่พอ) · ✅ PM ถูกที่ว่าหัวโน้ตไม่ต้อง RR (5% แทบไม่ได้ยิน) · **ตัวเลขบอกว่า "บ่อย" แต่ "คุ้มไหม" = PM/P'Aim เคาะ ผมไม่ตัดสิน** · ตรวจ: หน้าเว็บโหลดค่า 5%/10ms จริง · 4 คลิปเรนเดอร์ครบ ไม่คลิป · RMS −24.0dB
- **🎻 ✅ marcato (2.2 ทิศใหม่) เสร็จ — รอ P'Aim หมุนปุ่ม** (branch `cello-bakeoff` `d0e0eed` · report `docs/reports/cello-marcato.md`) · **🎧 <http://10.189.195.98:5451/docs/spikes/cello-marcato.html>** · **⭐ 2 ปุ่มพร้อมให้หมุน:** ความแรงหัวโน้ต (0–200% · 0 = p ล้วนเป๊ะ) + เลื่อนเวลาตัวโน้ต (0–200ms · **ขอให้ลอง 0**) → **หมุนจนพอดีแล้วบอกค่ามา ผมจำค่าให้** · 3 คลิป: `p+หัว mp` · `p+หัว mf` · `p ล้วน` + D · **✅ ดีไซน์ P'Aim = ตรงกับคนอัดเป๊ะ** (readme: staccato ทับ sustain · `vc_arco_marcato_map.sfz` hold 200ms + sustain 0 + `amplitude_oncc110` = **ปุ่มความแรงหัวโน้ต** → แม้แต่ "ต้องเป็นปุ่ม" เขาก็คิดเหมือนกัน) · **หัวแยกจากตัวด้วยเวลา ≠ crossfade 2 ชั้นค้างคู่กัน** (PM แก้คำพูดถูก) · **🔬 สมมติฐาน PM ยังตอบไม่ได้ ต้องหู:** วัดได้ว่าหัวมาถึงก่อนตัว **145–180ms** (หัว mf 20ms / mp 55ms vs p 200ms) = **มีของให้หูยึดตรงบีตจริง** แต่ "หูจะยอมปล่อยตัวช้าไหม" พิสูจน์ไม่ได้ → **ถ้า P'Aim ตั้ง 0 แล้วไม่รู้สึกช้า = PM ถูก และไอเดียเดียวแก้ 3 เรื่อง** · ✅ **เลี่ยงกับดัก "ดัง=ชั้นแรง" แล้ว** (ตัวโน้ต = p เสมอ · ไม่มี layer-selection ตามความดัง) · **📦 หัวโน้ตเพิ่มแค่ +0.39 MB** (ไฟล์สั้น) · รวม 102 ไฟล์ 4.09 MB = +3.14 MB จากเดิม · คลัง 21.20 → **24.34 MB (+15%)** · RR ยังพักตามสั่ง (+1.17 MB ถ้าเอา) — ⚠️ **ผลข้างเคียงที่ต้องรู้: ใช้ take เดียว → โน้ตซ้ำอาจได้ยินเป็นจังหวะกล (machine-gun)** คนอัดใส่ RR 4 take มาเพราะเหตุนี้ · **🔴 เจอบั๊กคลาสเดียวกับ "เสียงบอด" อีกตัว:** ไฟล์ staccato ตั้งชื่อไม่เหมือนกัน (11 ตัว `A1_mp_1.wav` · 6 ตัว `Eb2_mp1.wav`) → glob เดียวได้แค่ **11/17 pitch** → 6 โน้ตหายจะถูกปลอมด้วยการยืดโน้ตข้างเคียง = โน้ตผิดซ้ำรอย · **จับได้เพราะเช็กจำนวน** → แก้ + **ใส่ตัวกันถาวร: pitch ไม่ครบ = หยุดสร้างทันที** · ตรวจ: เปียโนเหมือนกัน 3×10⁻⁸ · ตัวโน้ตเหมือนกัน 1×10⁻⁸ · **หัว 0% = p ล้วนทุกบิต** · 2 ปุ่มเปลี่ยนเสียงจริง · ไม่คลิป · ไม่แตะ `dynamics.js`/โค้ด deploy · ไม่มีไฟล์เสียงใน git
- **🎻 ✅ cello-soften ขั้น 1 เสร็จ — รอ P'Aim ฟัง** (branch `cello-bakeoff` `479aa3d` · report `docs/reports/cello-soften.md`) · **🎧 <http://10.189.195.98:5451/docs/spikes/cello-soften.html>** (p / mp / mf + D · MP3 โหลดได้ · มือถือ 360px ผ่าน) · **✅ PM คิดถูก — ผมทดสอบก่อนลงมือทำ หักล้างไม่ลง:** ที่ความดัง**เท่ากัน** `p` ทึบกว่า `mf` **167 Hz** + ย่านแสบ >2kHz **น้อยกว่า 5.68 dB** · ไล่ระดับ p<mp<mf<f สม่ำเสมอครบ 17 โน้ต → **`p`/`mp` เป็นคนละเนื้อเสียงจริง ไม่ใช่ mf ที่หรี่ลง** (หรี่ = ลดทุกย่านเท่ากัน centroid ไม่ขยับ) · **PM เดาถูกอีกข้อ: `p` บวมช้ากว่า `mf` 10 เท่า** (200ms vs 20ms) · 🔴 **ความเสี่ยงที่ต้องบอก: `p` 200ms ใกล้ Iowa 245ms ที่ P'Aim เพิ่งบอก "ไม่ดีเลย"** — negative delay แก้*ตำแหน่ง*ได้ แต่แก้*อาการค่อยๆบวม*ไม่ได้ → อาจได้ "ไม่แสบแล้วแต่อืด" → **นี่คือเหตุผลที่ต้องมี `mp` (35ms) ให้ฟังด้วย ไม่ใช่แค่ p กับ mf** · **📦 ขนาด (วัดจริงตามสั่ง): 68 ไฟล์ = 3.70 MB · ตอนนี้ 17 ไฟล์ = 0.95 MB → โต 3.9 เท่า แต่เนื้อจริง +2.75 MB บนคลัง 21.2 MB = ~+13%** · เชลโล lazy-load อยู่แล้ว (คนไม่เล่นไม่โดน) · ที่ต้องระวัง = SW precache รอบแรก · (`_g` ด้วยจะเป็น 136 ไฟล์ ~7.4 MB) · **สร้างครบ 4 ชั้น 68 ไฟล์แล้ว** (เติม `f`) ตามที่ brief สั่งว่าโครงข้อมูลต้องรองรับหลายชั้นแต่แรก = ไม่ใช่ของใช้แล้วทิ้ง · ✅ แยก instrument ชั้นละตัว velRange เต็ม · ✅ t50 ต่อชั้น (200/35/20/15ms) ลง `plengMeta.attackMs` · ✅ match ความดังจากท่อนจริง · ✅ **ไม่แตะ `dynamics.js`** · ✅ ไม่เขียน logic เลือกชั้น · ✅ ไม่ทำขั้น 2-4 · **🟡 ถอนตัวเลขทิ้ง 1 ตัว:** noise floor วัดไม่ได้จริง (ไฟล์ Karoryfer ไม่มีช่วงเงียบ → ที่วัดได้คือหางเสียง · ค่าออกมากลับหัว) → **ไม่รายงานเลขปลอม** · พูดได้แค่ `p` โดนดัน +13dB (มากกว่า mf ~10dB) → ถ้ามี hiss จะดังตาม = **หู P'Aim ต้องฟันธง** · ตรวจ: เปียโนเหมือนกันเป๊ะทั้ง 3 ชั้น (2–3×10⁻⁸) · ทุกคลิป RMS −24.0dB ไม่คลิป · ไม่แตะโค้ด deploy · ไม่มีไฟล์เสียงใน git
- **🎻 ✅ cello bake-off เสร็จ — รอ P'Aim ฟังตัดสิน** (`task_4ebc6bc9` · branch `cello-bakeoff` `31adff8` · report `docs/reports/cello-bakeoff.md`) · **🎧 <http://10.189.195.98:5451/docs/spikes/cello-bakeoff.html>** (4 คลิป A/B/C/D · MP3 โหลดได้ · มือถือ 360px ผ่าน) · เพลง #1 ท่อนแรก 20.3s · เปียโนทองเดิม (`arrange()`+splendid-grand รอบ 27) · **ไม่แตะโค้ดที่ deploy** (diff vs base = ว่าง) · **ยุติธรรมพิสูจน์แล้ว:** ฝั่งเปียโน A/B/C เหมือนกันเชิงตัวเลข (ต่าง 3×10⁻⁸ = float32) · เชลโล 3 ตัวดังเท่ากัน (วัดจากท่อนจริง ไม่ใช่ค่าเฉลี่ยไลบรารี — เกือบพลาด: B เคยดังกว่า A 6.5dB) · แก้จูนทั้ง 3 · ทุกคลิป RMS −24.0dB · **✅ negative delay รับเข้าแล้ว** (วัดเอง ไม่ใช้เลขที่ปรึกษา · เฟรม 5ms: เปียโน **0ms** · A **20** · B **58** · **C Iowa 245ms = ช้ากว่า A 12 เท่า = อธิบาย "หวูดเรือ" เป็นตัวเลข** · เช็กแล้วว่าเป็นการบวมของคันชักจริง **ไม่ใช่ช่วงเงียบหน้าไฟล์** — เสียงมาที่ 5ms แต่ไต่ 240ms · และ legato map ที่ PM เผื่อว่าอาจตัดหัวเสียงให้แล้ว = ไม่ได้ใช้ตาม brief §2 → ต้องชดเชยจริง) · ⚠️ **ผมเบี่ยงจากสเปก 1 ข้อ รอ PM ค้าน:** สเปกว่า "time-to-peak" แต่วัดจริงได้ 320–730ms (เลื่อนตามนั้นโน้ตจะมาก่อนจังหวะ) → ใช้ t50 แทน · **🔴 2 เรื่องที่ต้องรู้:** (1) **เชลโลในแอพ = Karoryfer ไม่ใช่ Iowa** (`tools/prepare-samples-cc0.mjs:33`) → **A กับ C ใน brief คือตัวเดียวกัน** ผมเลยใช้ Iowa ตัวจริงจาก `cello-iowa/` บน `dreamy-lumiere` → การทดลองยังเป็น 3 ตัวจริง · ✍️ **แก้คำพูดผมเอง (PM ค้านถูก):** memory `pleng-smplr-vellayer-relative`/`pleng-cello-piano-spike` **ไม่ได้ผิด** (มันพูดถึง Iowa ของ **spike** = ถูกต้อง) · และ `tools/slice-iowa-cello.sh` **มีจริง** (บน `dreamy-lumiere` `4d8175c` · ผมหาแค่ใน working tree เลยสรุปผิด) · ที่สับสนจริงคือ **brief/board จับ shipped=Iowa** เท่านั้น · memory รวมไว้ที่ `pleng-two-cellos-karoryfer-vs-iowa` ของ PM แล้ว (ผมเติมตัวเลขที่วัดได้เข้าไป · ลบไฟล์ซ้ำของผมทิ้ง) · ไวโอลิน shipped = VSCO-2-CE (2) **preset เชลโลที่ shipped เพี้ยน +1 ออกเทฟ** (ประกาศ pitch 24 แต่วัดได้ 36 · ครบ 17/17 ไฟล์) — **ยังไม่กระทบผู้ใช้** (cello `disabled:true` ใน `soundOptions.js`) · ไม่ได้แก้ (นอก scope) · **ห้าม commit sample** (SSO ลิขสิทธิ์) → `_spike/` เข้า .gitignore แล้ว ตรวจแล้วไม่มีไฟล์เสียงใน git · tests 673 ผ่าน (fail 1 = `notationLint.test.mjs` pre-existing)
- **✅ DS menu-parity เสร็จ** (SA · branch `analyze-phrakham-style` `41708f0`) → `docs/ds/menu-parity.md` · เมนู pleng สไตล์พระคำ desktop+mobile: ลิงก์นำทาง inline (desktop) · ☰ drawer + หมวด "เครื่องมือ" (mobile ตามภาพ) · แมป**ตามของจริง** (Aa/ดาวน์โหลด = contextual หน้าเพลง · ตัวอักษรไทย/บัญชี/🔍 = global · pleng ไม่มี ⚙ display-settings แบบพระคำ) · Lucide id + AC ต่อจุด ครบ · **🚩 จุดทับ shell-bar §7:** ต้อง merge หลัง hero/favicon (`task_260b6d03`) + คง Studio teleport (`#shell-left`/`#shell-menus`) → เสนอ desktop หน้าเพลงยุบลิงก์เป็น ▾ · **❓ 4 ข้อรอ P'Aim เคาะ** (🔍 เอาไหม · ย้ายตัวอักษรไทยเข้าเครื่องมือ · desktop เพลงยุบ ▾ · login ใน drawer) · refine ไม่ redesign · **รอ P'Aim review spec ก่อนจ่าย Dev**
- _(shared-core deferred → §📌 · 5 tweaks branch `phrakham-parity` Dev กำลังทำ)_

---

## roster / routing (session id)
- **หลัง LAUNCH: สาย launch archive ได้หมด** (dev `local_3fc2030f` · tester `local_04292fd9` · SA เต็มวง `local_a5d4b566` · research `local_7015b6a7` · icon `local_a08937ac`) — งาน commit/deploy ครบใน git · icon/download-P3 branch ยังอยู่ (asset/plan) เปิดใหม่ได้เมื่อจ่าย
- **ทุกงานใหม่ = spawn worktree + session ใหม่** (1 งาน = 1 worktree = 1 branch) · ไม่ปลุกสายเก่า
- **🧹 archived 26 pleng session ที่งานขึ้น live/merge/superseded แล้ว (pm26 · 15 ก.ค.)** — deploy รอบ 22-27 ทั้งหมด (parity/drawer/notation/badge/audit/audio/GATE/golden-piano ฯลฯ) · branch+commit ยังอยู่ใน git เปิดกลับได้ · **สาย pleng ที่ยัง active/parked (ไม่ปิด):** `charming-cori`(ไวโอลิน parked) · `dreamy-lumiere`(Canon spike · ถือ handoff) · `hopeful-cori`(Remix design พักรอ GATE1) · **ไม่แตะ phrakham.life2 (คนละโปรเจกต์)**
- **env:** GitHub token `OneDrive/4 Personal/claude/.env`→`GITHUB_TOKEN_PLENG` · Supabase `SUPABASE_*_PLENG` · main อยู่ worktree `pleng-natural-tie` · **PM server bug บทเรียน: `npx vite ... &` ทำ vite ตาย (orphan) → ใช้ run_in_background ไม่ใส่ `&`**

**Deploy history:** รอบ11 `84d259c`(B095) · 12 `6e6653d`(B098) · 13 `54eba5c`(B097) · 14 `4513961`(B099) · 15 `6951c19`(B100) · 16 `b7341ee`(B104) · 17 `64c21cb`(B105) · 18 `b161984`(B101) · 19 `359872b`(B102) · 20 `509195c`(B107P1) · 21 `01765cb`(LAUNCH 3-mode audio) · **22 `6cb8e68`(phrakham look&feel) · **23 `bb0c8d3`(round23: role-gate+audit-log+review-badge+notation beam/slur+menu parity+expressive audio) · **24 `2c580a8`(round24: audio round2+STEP0 5-layer+GATE-leak fix+karaoke per-syllable+drawer left pk-drawer core · กู้ hollow) · **25 `bede377`(round25: GATE leak #2 fix — Studio.vue shell picker กรอง verified · bug-only deploy)********

---

## กติกา PM (บทเรียนสะสม — memory มีครบ)
- **PM = คุมกระดาน + จ่ายงาน เท่านั้น** ไม่แก้โค้ดเอง · จ่ายไป session แยกจริง `send_message` (**ห้าม Agent tool**) · triage แค่พอเขียน brief (`feedback_pm_dispatch_real_session`)
- **⭐ audio feature = วัด output จริง (AnalyserNode/OfflineAudioContext peak>0) + ฟังหู** ไม่ใช่ "fire ไม่ error" · unit test = invariant ดักจริง (vel ต้องตกใน layer ที่โหลด · smplr เงียบ >3s ใน offline → วัด ensemble ด้วย LIVE analyser)
- **⭐ UI มือถือ verify บน device emulation จริง (360/412) + per-button right≤viewport** ไม่ใช่ computed 375 · **Chrome font-boosting → pin `text-size-adjust:100%`** (เจอเฉพาะ Chrome มือถือจริง · emulate ไม่ trigger)
- **tester gate = เทียบ spec เต็มทุกข้อ** (`feedback_tester_gate_full_spec`) · UI ทุก control/viewport · P'Aim ไม่ควรจับ spec-gap เอง · print feature = P'Aim verify จาก PDF จริง
- **รายงาน session-agnostic:** dev เขียน `docs/reports/<branch>.md` + §📥 inbox + ping "PM ปัจจุบัน" (อย่า hardcode ชื่อสาย)
- **merge:** cherry-pick เฉพาะ commit โค้ด หรือ merge branch ที่ fork ถูก · git-verify ก่อน gate (`git diff` three-dot จาก merge-base — เลี่ยง phantom deletion) · เช็ก `git branch --show-current` ก่อน commit · rerun test (`--exclude '**/.claude/**'`)
- **deploy:** merge/cherry-pick → base → verify test+build → align main=base + push (`--force-with-lease` ถ้า diverged · verify base ⊇ origin/main ก่อน) → poll live bundle หา launch marker (GitHub build stamp hash เอง) · GATE ทำ public 0 เพลง → editor/audio features ปลอดภัย
- ทุก P'Aim example/บั๊ก → กฎใน `ui-standards.md`/checklist · แก้ที่ process ไม่โทษคน · เก็บภาพ P'Aim ลง `docs/pm/realuse-assets/`

## 🎼 B107 P2 reference (SSOT `docs/ds/instrument-arranger-p2.md` · handoff `docs/reports/b107-p2-sa-handoff.md`)
- **5 เครื่อง:** Grand · Felt (กรอง Grand) · Nylon Guitar · Solo Violin · Cello — arranger 3 ชั้น + instrument-module (§4B) · **launch = piano/guitar solo + ensemble Option 1** (§6b.2: ตัด pad · violinFill call-response · violinCounter chorus · balance เปียโนนำ/ไวโอลิน −12dB/เชลโล −15dB)
- **ยังไม่ทำ:** felt/violin/cello เดี่ยว · ensemble presets อื่น · auto-instrumentation · MP3 P3 · guitar fret-voicing/rubato — architecture เผื่อครบ (เสียบไม่รื้อ)

## 🔁 P'Aim local review รอบ 1 (15 ก.ค. · เจอ 4 gap หลัง dev ว่า "ตรงหมด" — จับได้เอง)
PM วัดเอง getComputedStyle 2 เว็บ live ยืนยันทุกข้อ:
- **#1 ฟอนต์ลิ้นชัก:** CSS เท่ากัน 17.34 · แต่พระคำ text-size-adjust:auto → Android boost ใหญ่กว่า → **พระคำ pin 100% scoped .pk-drawer** (pk pm5 · verify Android)
- **#2 footer:** เพลงมี border-top #e0d6c8 + สแตมป์ size ต่าง · พระคำไม่มีเส้น → **เพลงตัดเส้น + จูน font ตามพระคำ** (dev sad-moser)
- **#3 ปุ่มปิด drawer:** เพลงโชว์ outline 3px ตอน focus-trap เปิด · พระคำไม่ → **เพลงเปลี่ยน :focus→:focus-visible** (แตะไม่ขึ้น · Tab ขึ้น · คง a11y) (dev)
- **#4 ไฮไลต์หน้าปัจจุบัน:** เพลงมี · พระคำไม่ → **P'Aim เลือก align-up: พระคำเพิ่มไฮไลต์ด้วย** (dev เพลงคงไว้ · pk pm5 เพิ่มพระคำ)
- **#5 (ยกไว้):** เพลง=dock คุมขนาดฟอนต์ · พระคำ=ปุ่ม Aa ในลิ้นชัก → normalize เป็น core เดียวทีหลัง (P'Aim สั่งยกไว้)
- **บทเรียน PM:** parity brief พลาด footer/focus/active + verify desktop-only (ข้อ 1 เห็นเฉพาะ Android) → memory `feedback_parity_full_visual_audit` เพิ่ม checklist 4 จุด · จ่ายแก้แล้ว 2 ฝั่ง · **P'Aim จะ re-verify local (Android) ก่อน joint deploy**

## 🔀 CONSOLIDATE parity → 1 session (P'Aim 15 ก.ค.): "ให้คนเดียวทำ จะได้เห็นภาพเดียวกัน" + "ตรวจละเอียดทุกจุด+แนะนำ"
- แยกสาย (audit SA + pleng dev + phrakham dev) → ไม่มีใครเห็นภาพรวม → P'Aim จับ gap 2 รอบ → **รวมเป็น 1 session (`local_81d4f3d9`/sad-moser) เจ้าของ parity ข้ามเว็บ**: เปิด 2 dev server (pleng :5344 + สร้าง phrakham worktree จาก `feat/drawer-parity` รันเอง) · เทียบ element-by-element สองจอ · แก้ทั้ง pleng + phrakham (บน feat/drawer-parity) · exhaustive ทุกจุด + แนะนำ align-up
- **pk pm5 hold สายพระคำ** (ไม่แก้ branch พร้อมกัน · คงสิทธิ์ review+deploy พระคำ) — ส่งแล้ว
- fix ค้าง 4 (#1 Android text-size-adjust พระคำ · #2 footer เพลง · #3 focus-visible เพลง · #4 align-up พระคำเพิ่มไฮไลต์) + หาเพิ่ม → 1 session ถือหมด · verify 2 จอ + Android → ส่ง P'Aim → joint deploy
- lesson: `feedback_parity_full_visual_audit` (1-session cross-site + PM แนะนำเชิงที่ปรึกษา)
- **✅ pleng #2 footer + #3 focus เสร็จ (`a0e937b`):** footer ตัดเส้นขอบ + font 10.94px/stamp 9.30px = พระคำเป๊ะ (วัด live) · #3 `:focus:not(:focus-visible){outline:none}` → แตะไม่มีกรอบ/Tab มี (คง WCAG) · 631 tests · **⚠️ #3 ต้อง P'Aim verify Android จริง (นิ้วแตะ — automation ตั้ง pointer modality ไม่ได้)**
- **⏳ ยังเหลือ (สายเดียวกำลังทำต่อ · คำสั่งรวมสาย queued):** phrakham #1 (text-size-adjust) + #4 (active highlight) บน `feat/drawer-parity` (สร้าง worktree+server เอง) + **exhaustive sweep ทั้ง 2 เว็บ หาที่ตกหล่นเพิ่ม** → เสร็จค่อยส่ง P'Aim เทียบรอบเดียวครบ (รวม Android checks #1+#3)

## 🎉 DEPLOYED รอบ 26 — เพลง `bd53742` LIVE (15 ก.ค. · P'Aim "ได้แล้ว")
whole-site look&feel parity เพลง→พระคำ (พระคำ=master · all flags P'Aim เคาะ) · code-only merge จาก `sad-moser-c66f59` (b07944f..dfb27c8 · ShellBar+styles.css+SongList) · **verify prod:** Actions success · build stamp bd53742 · CSS live มี #545555/radius10/22.5px/footer-no-border · Last-Modified สด · 631 tests+build
- **พระคำ:** GO ให้ pk pm5 deploy `feat/drawer-parity` (↗+toggle+text-size-adjust 17478ce) พร้อมกัน — รอ pk pm5 ยืนยัน live + Android drawer no-boost
- **เหลือ:** พี่เปา/P'Aim verify Android 2 จุด (#1 ลิ้นชักไม่ boost · #3 ปุ่มปิดไม่มีกรอบตอนแตะ) · เก็บ worktree phrakham :8930 ของสาย parity หลัง phrakham deploy
- **#5 normalize dock↔ปุ่ม Aa เป็น core เดียว = ยกไว้ (P'Aim สั่ง) ทำทีหลัง**

**Deploy history:** ...25 `bede377`(GATE leak#2) · **26 `bd53742`(whole-site parity)** · **27 `ad63021`(golden-piano referee+MP3 Grand)** · **28 `09b9712`(guide refresh+About credits · PWA install button · chord→Roman)** · **29 `f573d2d`(app-name ไทย · คู่มือทำเพลง/notation+เมนูดรอปดาวน์ · ไอคอนพื้นขาวทองเฉด E · shell overflow fix · นัชวิลล์ sync · a11y ตาราง · accent knob ออก)**

## ✅✅ PARITY จบ — เหมือนกันบน server ครบทั้ง 2 เว็บ (15 ก.ค.)
- 🟦 เพลง **`bd53742`** LIVE · 🟩 พระคำ **`a0a3021`** LIVE (PM verify เอง: e6fa73b→a0a3021 · pk-ext ↗ live · Last-Modified 10:08)
- พระคำ deploy `feat/drawer-parity`: ↗ + ปุ่มมีหัว/ไม่มีหัว + text-size-adjust:100% scoped .pk-drawer (17478ce) · render 48 หน้าสะอาด
- **เหลืออย่างเดียว:** P'Aim/พี่เปา verify Android จริง 2 จุด — #1 ลิ้นชักพระคำไม่ boost (⚠️ ถ้า Chrome ไม่ยอม un-boost subtree → pk pm5 มี fallback) · #3 ปุ่มปิดไม่มีกรอบตอนแตะ · เพี้ยน→รอบเก็บตก (สาย parity `local_81d4f3d9` ปลุกกลับได้)
- cleanup: สาย parity ปิด :8930 + remove phrakham worktree แล้ว · sad-moser branch archived ได้
- **#5 normalize dock↔Aa เป็น core เดียว = ยกไว้ (P'Aim)**

- 🧹 ค้างลบทีหลัง: โฟลเดอร์ phrakham worktree (sweet-payne) บนดิสก์ลบไม่ได้ (OneDrive lock) · git deregister แล้ว = ไม่กระทบ branch/deploy · ลบเมื่อ lock คลาย

## 🎻🎹 Canon spike จบ (P'Aim เคาะ 15 ก.ค. · `dreamy-lumiere-68292a` · ไม่ merge/deploy)
- **เปียโน = สำเร็จ** (P'Aim "เพราะดี"): Grand 5-layer self-host + ทำนองบนสุด + คอร์ดแตกไหล pedal-ring + variation 16th → ใช้ได้ทั้ง Canon + เพลงจริง #1
- **เชลโล = พัก** (เหมือนไวโอลิน): free Iowa mono + swell = "หวูดเรือ" ไม่เข้ากัน · SA ฟังไม่ได้=เดา → พัก (lesson `pleng-aesthetic-audio-needs-ear`)
- **⭐ follow-up คุ้มสุด (คิวไว้ · รอ P'Aim สั่ง):** ดึงเทคนิคเปียโน (pedal-ring + flow + variation) → ทำ **preset "เปียโนบรรเลง" ใน SongView จริง** ให้เพลงเพราะขึ้น · เชลโล/ไวโอลินปลุกเมื่อมี sample ดีกว่า mono ฟรี

- **ปลุกเต็มวง/เชลโลกลับเมื่อมี:** (ก) sample ดีกว่า mono ฟรี หรือ (ข) multi-agent live-listen หรือ (ค) **phrase-map ที่คนเรียบเรียง (พี่เปา) มาร์กน้ำหนัก/ลายเส้นไว้ก่อน** · โค้ด celloEngine+arranged rules parked ไม่ลบ · ราก (G): static conditional logic ≠ musical empathy + SA ฟังไม่ได้

## 🎹 PRIMED (รอ P'Aim เปิด session) — "เปียโนเดี่ยวร่างทอง" (universal adaptive solo piano · ~400 เพลง)
ต่อยอดจาก Canon spike (เปียโนพิสูจน์แล้ว) → core engine เปียโนเดี่ยว adaptive กับเพลงบรรเลงทั้งหมด (โครง YS/เจียนผู้: ป้ายท่อนร้อง/รับ · คอร์ดรายห้อง · โน้ตเลข)
- **handoff brief:** `docs/reports/piano-golden-handoff.md` (+ spike report · branch spike `dreamy-lumiere-68292a` `8e5f41f` · parked)
- **PM guardrails ตอนตั้ง session (SA ฝาก):** (1) **worktree/branch ใหม่จากฐาน `studio-shell-redesign`** ไม่ทำต่อ branch spike · dev `--host` (2) **ต่อยอด arranger/presets ตัวจริง (playSong) ไม่สร้าง demo Canon ใหม่** = ได้ 400 เพลงจริง (3) traps: resolveContent ก่อนอ่านป้ายท่อน · melody-density fallback · A/B toggle (dev ฟังไม่ได้) · −40% ที่ gain (4) P'Aim iterate ตรง SA (creative/audio) · PM re-engage ตอน gate+publish
- **กลืน follow-up เดิม** (preset เปียโนบรรเลง SongView) เข้างานนี้พอดี · **รอ P'Aim สั่งเปิด → PM spawn worktree+brief**
