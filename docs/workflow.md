# วิธีทำงาน — เพลง.พระคำ.ชีวิต

เอกสาร "วิธีทำงานรวม" — ใช้กับทุกงาน ทุก worktree ในอนาคต (เขียนครั้งเดียว งานใหม่แค่อ้างอิงที่นี่)

## ใครทำอะไร (บทบาท)
- **พี่เอม (เจ้าของ/ลูกค้า)** — ส่ง **user story ภาษาคน** เข้ามา · กลับมาอีกทีตอน **ทดสอบ** · เป็นคนสั่งว่าจะขึ้น `main`/deploy เมื่อไหร่
- **SA (Claude ใน session ที่คุยกับพี่เอม)** — รับ user story → **แปลงเป็น DS + prompt สั่ง dev** · ประสาน dev sessions เอง · เป็นคนตัดสิน **merge กลับฐาน** · คุยกับพี่เอมภาษาคนล้วน · **ไม่เขียนโค้ดเอง**
- **Dev (Claude Code session อื่น)** — อ่าน US/DS/prompt → เขียนโค้ด **+ unit test** ใน 1 worktree → รายงานกลับ SA
- **Testers (พี่เปา และคนอื่น)** — ลองใช้จริงผ่านเว็บพรีวิว (port ของงานนั้น) → บอกว่าตรงตาม story ไหม

**การเชื่อมต่อ SA ↔ dev (ตามจริง):** SA เขียน prompt สั่ง dev → มีคนเปิด session dev แล้ววาง prompt (แค่ก๊อปวาง 1 ที) → dev ทำเสร็จรายงานกลับ → เอารายงานให้ SA review + สั่ง merge. คนแค่ "เปิด session + ส่งต่อ" งานคิดทั้งหมดอยู่ที่ SA. ที่ต้องเป็น session จริง (ไม่ใช่ผู้ช่วยเบื้องหลัง) เพราะพี่เอม/tester ต้องมี **port แยกไว้เปิดดูขนานกัน**

**กติกาภาษา:** คุยกับพี่เอม = ภาษาคนล้วน (ไม่มี git/โค้ดดิบๆ). รายละเอียดเทคนิคไปอยู่ในเอกสาร/prompt ที่ dev อ่าน — พี่เอมไม่ต้องอ่านส่วนนั้น. ถ้าต้องโชว์อะไรเทคนิค ให้อธิบายแนวคิดเป็นภาษาคนก่อน แล้วค่อยใส่บล็อกเทคนิคที่ติดป้าย "สำหรับ dev — พี่เอมข้ามได้"

## ทำงานหลาย session พร้อมกันยังไงไม่ให้ชน (ภาษาคน)
คิดว่าโปรเจกต์ = **สมุดเล่มเดียว** ถ้าหลายคนเขียนลงเล่มเดียวกันพร้อมกัน = เลอะทับกัน
เราเลยให้แต่ละคน **ถ่ายสำเนาสมุดไปไว้ห้องตัวเอง** (โฟลเดอร์แยกบนเครื่อง — git เรียก "worktree") เขียนในสำเนาตัวเองอิสระ ไม่เลอะกัน
แต่ละสำเนา **เปิดเว็บพรีวิวคนละช่อง** (port คนละเลข) → พี่เอมเปิดหลายแท็บ ดูงานทุกคนสดๆ พร้อมกันได้
พอใครเสร็จ ก็ **รวมงานกลับเข้าเล่มหลัก** (merge) — เพราะแต่ละคนเขียนคนละหน้า (คนละไฟล์) การรวมเลยเนียน ไม่ชนกัน

**หัวใจ 3 ข้อ:** 1 งาน = 1 สำเนา (worktree) + 1 ชุดไฟล์ของตัวเอง + 1 ช่องพรีวิว (port) — เจอกันแค่ตอน merge

## ช่องพรีวิว (port) ประจำแต่ละงาน — เปิดดูขนานกันได้
| งาน | localhost |
|---|---|
| ออกแบบ/คุยกับพี่เอม (session SA) | http://localhost:5173 |
| WT-0 ฐาน | http://localhost:5301 |
| WT-A ร้อง/คาราโอเกะ | http://localhost:5302 |
| WT-B พิมพ์แผ่นเพลง | http://localhost:5303 |
| WT-C JSON พกพา | http://localhost:5304 |
| WT-D ทำเพลง→คลัง | http://localhost:5305 |
| wireframe อ้างอิง | http://localhost:5200 |

## วงจรงาน (lifecycle)
```
idea (backlog) → user story ภาษาคน (พี่เอม) → DS ออกแบบ (SA) → prompt สั่งงาน (SA)
   → dev สร้าง + unit test ใน worktree → SA review + merge กลับฐาน studio-shell-redesign
   → testers (พี่เปา ฯลฯ) ลองใช้ผ่าน port → ผ่าน
   → (พี่เอมสั่งค่อยขึ้น main = เว็บจริง)
```
พี่เอมแตะแค่ 2 จุด: **ต้นทาง** (ส่ง user story) กับ **ปลายทาง** (ทดสอบ) — ระหว่างกลาง SA + dev จัดการเอง

## เริ่ม session SA / dev ใหม่
เปิด Claude session ใหม่ในโฟลเดอร์หลัก แล้ววางแค่บรรทัดเดียว:
- SA → **`อ่าน docs/prompts/sa.md`**
- dev → **`อ่าน docs/prompts/dev.md · ผมต้องการ ทำ <งาน>`**

ไฟล์ prompt บอกบทบาทเต็ม + สิ่งที่ต้องอ่านครบ · สถานะงานล่าสุด (ถึงไหนแล้ว) อยู่ที่ `docs/status.md` — SA อัปเดตทุกครั้งที่เปลี่ยน

---

## สำหรับ dev session (พี่เอมข้ามส่วนนี้ได้)

เริ่มงาน 1 worktree (แทน `<ชื่อ>` `<branch>` `<port>` ตามตารางด้านบน):
```sh
git worktree add ../pleng-<ชื่อ> -b <branch> studio-shell-redesign
cd ../pleng-<ชื่อ>
npm install
npm run dev -- --port <port>
```

กติกาขณะทำ (git best practice ให้ขนานไม่ชน):
- แก้ **เฉพาะไฟล์ที่ worktree ตัวเองเป็นเจ้าของ** (ดู DS ของงาน) — ห้ามแตะไฟล์ของ worktree อื่น
- commit ในสาขาตัวเอง · **ห้าม merge `main` · ห้าม deploy**
- ถ้าฐาน (`studio-shell-redesign`) ขยับระหว่างทาง → `git rebase studio-shell-redesign` ในสาขาตัวเอง (ไฟล์ไม่ทับกัน จึงแทบไม่มี conflict)
- เสร็จแล้ว: commit + รายงานว่าแก้ไฟล์ไหนบ้าง → **รอพี่เอมสั่ง merge กลับฐาน** (SA เป็นคนรวม)
- ก่อน commit เช็ก `git branch --show-current` เสมอ (dir หลักใช้ร่วมหลาย session)
