# pleng.phrakham.life — กติกาเฉพาะโปรเจกต์นี้

**กติกาการทำงานไม่ได้อยู่ที่นี่แล้ว** — มีฉบับเดียว ใช้ทั้ง 2 โปรเจกต์ ทุก session:
### 👉 [`pair/pair-sop.md`](../pair/pair-sop.md)
หลักฐานเบื้องหลังแต่ละกฎ → [`pair/evidence.md`](../pair/evidence.md)

ไฟล์นี้เหลือไว้เฉพาะเรื่องที่จริงกับเพลงเท่านั้น
*(20 ก.ค. 2026: เนื้อหากระบวนการทั้งหมดถูกยุบขึ้นไปที่ `pair-sop.md` · ดูของเดิมได้จาก git history)*

---

## 1 · 🔴🔴 งานที่แตะ component จริง = ทำบนโค้ดจริง ⛔ ห้าม mockup ทิ้ง

**(P'Aim 18 ก.ค. · ระดับ "จะเลิกใช้")** ปัญหาที่ต้องเลิก: mockup HTML แยกจากโค้ดจริง → dev สร้างจริงแล้ว**ดริฟต์** = "ไม่ได้ตามนั้น" + สเปกไม่ครบ dev เดา = **P'Aim ต้องพูดซ้ำ**
P'Aim: *"ทำไมไม่ทำเป็น component ที่เอาไป implement ต่อได้จริงเลย"*

- **หน้าตา = ทำเองบนโค้ดจริง** (template + CSS + layout + responsive) เฉพาะไฟล์ฝั่งแอปที่ไม่แชร์
- **ตรรกะ/พฤติกรรม + engine ที่แชร์ 2 เว็บ** = อีกชั้นหนึ่ง · ⛔ **`DockKey.vue` แชร์ 2 host — ห้ามแตะจากฝั่งหน้าตา**
- **ส่งทีเดียวครบ** ปรับ = แก้ branch เดิม ไม่ re-implement ใหม่
- งาน docs/วิเคราะห์ล้วน หรือ mockup คุยทิศในทีม = ยกเว้นได้ (แต่ไม่ใช่ของที่ส่ง P'Aim)

## 2 · 📘 แก้ซอฟต์แวร์ = ต้องอัปคู่มือเสมอ

ทุกครั้งที่ผู้ใช้เห็นความเปลี่ยนแปลง (ฟีเจอร์ใหม่ · ปุ่ม/ตำแหน่ง/ค่าเริ่มต้นเปลี่ยน) → **อัป `src/views/Guide.vue` ในงานเดียวกัน**
- **อยู่ใน DoD** — ยังไม่อัป = งานยังไม่เสร็จ ไม่ผ่านด่าน
- **ภาษาคู่มือ:** ภาษาคนล้วน ระดับอาสาสมัครคริสตจักร (พี่เปาอ่านรู้เรื่อง) · ตามโครง/โทนเดิม ไม่ redesign
- **ยกเว้น:** งานที่ผู้ใช้ไม่เห็นเลย (refactor ภายใน · เทสต์ · CI) — บอกในรายงานว่าทำไมไม่ต้องอัป
- เหตุผล: คู่มือล้าหลัง = ผู้ใช้ไม่รู้ว่ามีฟีเจอร์ + สอนผิด (เจอจริง 20 ก.ค. branch `guide-update` ค้าง 116 commits)

## 3 · เฉพาะทางเทคนิคของเพลง

- **ฐาน = `studio-shell-redesign`** ไม่ใช่ `main` · push ฐาน = `git push origin HEAD:studio-shell-redesign`
- ⛔ **`main` = auto-deploy** merge ผิด = ขึ้นเว็บทันที
- **selective-merge:** ก่อน deploy ถ้า branch แตกมานาน → ฐานแตะไฟล์เดียวกันไหม? ไม่แตะ = checkout เฉพาะไฟล์ · แตะแล้ว = rebase/merge จริง · ⛔ ห้าม full-merge branch เก่า = revert ของที่ live อยู่
- **วิธี deploy ที่ใช้จริง (รอบ 37):** `studio-shell-redesign` แตกจาก `main` (main มี deploy ที่ยังไม่ back-merge เช่น B113/B118) → **cherry-pick เฉพาะ commit ของงาน** ขึ้น worktree ใหม่จาก `origin/main` แล้ว push `HEAD:main` · ได้ของงานอย่างเดียว main เก็บ divergent ของตัวเองไว้ · **build ใน worktree นั้น:** junction `node_modules` มาจาก dir หลัก (`cmd //c mklink //J`) แล้วค่อยลบ junction ก่อน `git worktree remove` (ไม่งั้นลบ node_modules จริง) · **verify:** curl bundle บน live หา commit stamp ไม่ใช่ชื่อไฟล์
- **dev server เปิดด้วย `--host` เสมอ** ให้ P'Aim/พี่เปาทดสอบบนมือถือจริงผ่าน WiFi · **อย่าปิด server ตอนจบงาน** ค้างไว้ให้เปิดตรวจได้ · ใส่ทั้ง URL `localhost` และ Network ท้ายรายงาน
- **Windows:** `vite` หายทั้งที่เมื่อกี้ยังใช้ได้ = `node_modules/.bin` ถูก OneDrive/antivirus กวาด → `npm install` ใหม่
- **Supabase:** การเขียนฐานข้อมูล = P'Aim รัน SQL เอง

## 4 · มาตรฐาน UI ของเพลง

[`ui-standards.md`](ui-standards.md) = SSOT (WCAG 2.2 AA · ARIA APG · HIG/Material 3 · NN/g · Fitts)
+ [`ux-platform-patterns.md`](ux-platform-patterns.md) — แยกท่า touch ≠ pointer ให้ถูก · เปิดโค้ด/DS เดิมก่อนเสนอ แล้วเสนอเฉพาะ **ส่วนต่าง** (ของเดิมไม่รื้อ)

## 5 · คน

- **พี่เปา** — review เพลง/เสียง · **คนเดียวที่พิมพ์เพลงเข้าระบบ = คอขวด** ⇒ UX หน้าแก้ไขสำคัญสูงสุด
- **P'Aim = เสียงคนใช้** (บอกปัญหา + ไอเดีย) **ไม่ใช่ผู้ตัดสินมาตรฐาน** — คำแนะนำของเขาคือ input ที่เราต้องแปลงเป็น "ดีที่สุดระดับโลก" แล้วฟันธงพร้อมอ้างมาตรฐาน
  P'Aim: *"ผมจะมีคุณไปทำไม ถ้าคุณแค่ทำตามผม"*
