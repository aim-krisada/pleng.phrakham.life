# รายงาน Tester — B092 ปุ่มห้อง surface (surface-bar-tools)

**ตรวจโดย:** tester (`tester-qa`) · **ของ:** `surface-bar-tools` `5f46f17` (EditorMode.vue+test · vitest 331) · **ping:** pm7
**วิธี:** vitest + วัดจริง Browser MCP · **หมายเหตุ:** dev server 5417 **ดับ** + 5414 เสิร์ฟโค้ดเก่า → tester ตั้ง server เอง (worktree `pleng-b092` · **`http://localhost:5421`** `--host`) เสิร์ฟโค้ด B092 จริง (5f46f17) แล้ววัด

---

## VERDICT: ✅ ผ่าน (ฟังก์ชัน+มือถือใช้ได้) · 💬 ความเห็น: มือถือ "ลื่นแต่เปลืองแนวตั้ง" → เสนอ responsive-split ให้ P'Aim เคาะ

| เกณฑ์ | ผล | หลักฐานวัดจริง |
|---|---|---|
| ปุ่ม ←ซ้าย/→ขวา/⧉สำเนา/✕ลบ โชว์ตรงในแถบห้อง กดได้เลย | ✅ | foot มี 4 ปุ่มตรง (ย้ายซ้าย/ขวา/สำเนา/ลบ) — ไม่ต้องเปิด ⋯ |
| ⋯ เหลือ checkbox | ✅ | ⋯ relabel เป็น **"เครื่องมือห้องนี้ (ห้องต่อกัน · เล่นซ้ำ · วนกลับ · ห้องจบ)"** (ไม่มี ย้าย/สำเนา/ลบ แล้ว) |
| move/copy/delete ทำงาน | ✅ | สำเนา: ห้อง **1→2** · ลบ: **2→1** |
| edge disabled | ✅ | ห้องเดียว: ย้ายซ้าย+ขวา disabled · 2 ห้อง (ห้องแรก): ย้ายซ้าย disabled · ย้ายขวา enabled |
| vitest · console | ✅ | **331 passed** · console 0 error |

### 🚩 มือถือ 375 (จุดที่ P'Aim ต้องตัดสิน)
- **foot = 92px · พัน 2 แถว** (ตรงที่ dev flag) · **ปุ่มทั้ง 7 ≥44×44px** (touch-min ผ่าน) · **body h-overflow 0 · โน้ตไม่ถูกตัด** (bar overflow 0)
- → **"ใช้ได้จริง ไม่พัง" ยืนยัน** ✅

---

## 💬 ความเห็น tester เรื่อง "หนักบนมือถือไหม" (pm7 ขอ)
**ลื่นขึ้นจริงบน desktop** (ไม่ต้องเปิด ⋯ = เร็ว) · **แต่บนมือถือ 375 = เปลืองแนวตั้งพอควร:** foot 92px/ห้อง · เพลงยาวหลายห้อง = ทุกห้องกินเพิ่ม 92px → หน้ายาว/เลื่อนเยอะ ดันเนื้อลง
- **ข้อเสนอ (follow-up · ไม่บล็อก B092):** **responsive-split** — desktop โชว์ครบ 4 ปุ่ม (คงของดี) · **มือถือ: เก็บ สำเนา/ลบ กลับเข้า ⋯ · โชว์ตรงแค่ ←→ (ย้าย = ใช้บ่อยตอนแต่ง)** → foot มือถือกลับเหลือ 1 แถว (~46px) ลดเปลืองครึ่งนึง คงความลื่น desktop
- **สรุปความเห็น:** ผ่านได้ตามนี้ (ใช้งานได้ + touch OK) · แต่ถ้า P'Aim เน้น mobile-first ผมแนะ responsive-split · **= preference call ของ P'Aim ไม่ใช่ defect**

## หมายเหตุ
- dev server B092 (5417) ดับตอนตรวจ · ผมตั้งเอง `localhost:5421` (`--host` · worktree `pleng-b092`) เสิร์ฟ 5f46f17 จริง — **P'Aim ลองมือถือได้ที่ `<LAN-IP>:5421`** (server ยังรัน) หรือให้ dev เปิด 5417 ใหม่
- launch config `b092` เพิ่มชั่วคราวใน `.claude/launch.json` (ไม่ commit)

## next
- pm7 → B092 ฟังก์ชันเขียว · **ชง P'Aim ตัดสิน: รับตามนี้ (foot 92px มือถือ) หรือทำ responsive-split** · P'Aim ลอง 5421 มือถือประกอบการตัดสิน
