# รายงาน — B073: "ห้องต่อกัน" ข้ามบรรทัด นับจังหวะผิด (11/4)

**branch:** `fix-beat-count-continued` (ฐาน `studio-shell-redesign` · มี `fix-editor-preview-final` merged)
**commit:** `320f4dd`
**worktree:** `.claude/worktrees/charming-haslett-83f9bb` · **dev (--host):** `http://10.215.141.98:5373/`
**สั่งโดย:** pm4

## อาการ
บรรทัด 3 ห้องสุดท้าย กด "ห้องต่อกัน" เชื่อมกับห้องแรกบรรทัด 4 (รวมครบ 4 จังหวะพอดี) แต่ตัวตรวจโชว์ **11/4 แดง**

## ต้นตอจริง (ต่างจากที่ brief สงสัย)
brief สงสัย cont join (`barTokensAt(li+1,0)` ได้โทเคนเกิน) — **ตรวจแล้วไม่ใช่**: `barTokensAt` คืนห้องเดียวถูกต้อง และ cont join รวมแค่ 2 ห้อง = 4 จังหวะ ถูกต้อง

ต้นตอจริงอยู่ที่ **pickup path** (ปุ่ม "↻ ห้องต่อกัน" = ตั้ง `bar.pickup=true`):
- `pickupTotal` (เดิม บรรทัด 556-564) รวมจังหวะของ **ทุกห้องที่ flag pickup ในท่อนนั้นทั้งท่อน** เป็นก้อนเดียว
- เพลงจริงของ P'Aim มี pickup มากกว่า 1 กลุ่มในท่อน (anacrusis ต้นท่อน + คู่ข้ามบรรทัดที่เพิ่งกด) → ผลรวมทั้งท่อน = 11 → ไม่หาร 4 ลงตัว → **ทุกห้อง pickup แดงหมด** รวมคู่ที่ครบจริง
- ข้อความ "11/4" มาจากบรรทัด 598 (`รวมห้องต่อกัน ${sum}/${exp}`) ที่ `sum` = ผลรวมทั้งท่อน (ไม่ใช่ของคู่ที่เชื่อมจริง)

## การแก้ (แตะแค่ `EditorMode.vue` + test)
แทน `pickupTotal` (ผลรวมทั้งท่อน) ด้วย `pickupCheck` ที่ตรวจ **แยกเป็นกลุ่ม**:
- **RUN** — ห้อง pickup ที่ติดกันในลำดับอ่าน (รวมข้ามหัวบรรทัด = เคส B073) นับรวมในกลุ่มเดียว → ต้องครบจำนวนห้องเต็ม
- **ISOLATED** — pickup ที่ไม่ติดกัน (anacrusis ห้องแรก↔ห้องท้าย ที่มีห้องเต็มคั่น = เคส B055 เดิม) รวมเป็นกลุ่ม anacrusis เดียว
- คนละกลุ่มไม่กวนกันอีกต่อไป → คู่ที่ครบจริงเป็น ✓ แม้มี pickup อื่นในท่อนที่ไม่ครบ

ทั้งห้องบน (ท้ายบรรทัด) และห้องล่าง (หัวบรรทัดถัดไป) โชว์สถานะกลุ่มเดียวกัน (ตรงกัน)

## DoD
| เกณฑ์ | ผล |
|---|---|
| `vitest run --exclude .claude --exclude node_modules` | ✅ **270 passed** (ฐาน 268 + B073 ใหม่ 2) · ไฟล์ `notationLint.test.mjs` เป็น standalone script เรียก `process.exit(0)` เอง (quirk เดิม ก่อนแก้ก็เป็น · ไม่เกี่ยว) |
| `npm run build` | ✅ built 1.49s |
| B055 ไม่ regress | ✅ 5 เทสต์เดิมเขียว (in-line anacrusis + cross-line cont) |
| unit test เคสข้ามบรรทัด | ✅ เพิ่ม `describe('B073')` 2 เทสต์ |

## Verify เบราว์เซอร์ (live · `http://10.215.141.98:5373/#/studio`)
| เคส | ก่อนกด | หลังกด "ห้องต่อกัน" |
|---|---|---|
| คู่ข้ามบรรทัด 2+2=4 (ครบ) | แดง `2/4 ❌` ทั้งคู่ | ✅ เขียว `⤷ ห้องต่อกัน 2 จังหวะ ✓` **ทั้งสองห้อง** (ไม่ใช่ 11/4) |
| คู่ข้ามบรรทัด 2+3=5 (ไม่ครบ) | — | 🔴 แดง `⤷ ห้องต่อกัน — รวมห้องต่อกัน 5/4 ❌` (โชว์ผลรวม**กลุ่มตัวเอง** ไม่ใช่ 11) |

ไม่มี console error · แอปบูตด้วยโค้ดใหม่ (build stamp `fb17c76*` + curl พบ `pickupCheck` ที่ served)

## หมายเหตุ / ขอ P'Aim ยืนยัน
- **โครงสร้างที่ทำ dev server ยังรันอยู่** — P'Aim ลองบนมือถือได้ที่ `http://10.215.141.98:5373/` เปิดเพลงจริงที่เจอบั๊ก → มาร์ค "ห้องต่อกัน" คู่ที่ข้ามบรรทัด → ควรเป็น ✓
- ⛔ ยังไม่ merge/deploy — รอ pm4 ตรวจ DoD + P'Aim LAN-test
