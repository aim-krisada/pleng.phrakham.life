# wt-songbook — B059 แผ่นเพลงแบบหนังสือเพลง (โน้ตครั้งเดียว · ข้อซ้ำเนื้อล้วน)

**branch:** `wt-songbook` (ฐาน `studio-shell-redesign`) · **dev server:** `http://192.168.1.124:5380/` (LAN · `--host`)
**งานในสายนี้:** B059 (หนังสือเพลง · เสร็จ+refine spacing) · **B065** (บั๊กโน้ตล้นเส้นกั้นห้อง · เสร็จ) · **B059-refine dedup ลึก = HOLD รอ P'Aim ยืนยันตีความ** (ดูท้ายไฟล์)

## โจทย์ (จาก brief-b059)
- **แผ่นเพลง (sheet):** พิมพ์แบบหนังสือเพลง — โน้ต (+คอร์ด) แสดง **ครั้งเดียวต่อทำนอง (stanza)** ที่ข้อแรกที่ใช้ทำนองนั้น · ข้อถัดไปที่ **ใช้ทำนองซ้ำ = เนื้อล้วน** (เลขข้อ + คำร้องของข้อนั้น)
- **ฝึกร้อง (sing):** ไม่เปลี่ยน — โน้ต + คาราโอเกะ ทุกข้อ

## สิ่งที่ทำ (3 ไฟล์ code + 2 ไฟล์ test)
1. **`src/lib/songModel.js` — `resolveContent`**: ตอน expand arrangement → stanza เพิ่มแท็กรายบรรทัด `_stanza` + `_stanzaFirst` (จุดเดียวในระบบที่รู้ตัวตนของ stanza) · เป็น prop บน array แบบไม่ใช่ index → consumer เดิม (v1 render / midi / print) วน items เหมือนเดิม ไม่กระทบ
2. **`src/components/SongSheet.vue`**: prop ใหม่ `songbook` (default `false`)
   - per-line gate: บรรทัดที่ใช้ทำนองซ้ำ (`_stanzaFirst === false`) → ตัดแถวโน้ต + คอร์ด (รวม bar-line/repeat/volta/end) เหลือเนื้อ
   - เนื้อของบรรทัดซ้ำ fallback ไปใช้ joined lyric text (มีเว้นวรรคคำ) แทน syllable-span ที่กระจายใต้โน้ต → อ่านเป็นข้อความไหลแบบหนังสือเพลง
   - v1 / undefined → `first = true` เสมอ → โน้ตไม่หาย (songbook ตัดเฉพาะ stanza ที่ซ้ำจริง)
3. **`src/views/Studio.vue`**: หน้า **แผ่นเพลง** opt-in `songbook` (บรรทัดเดียว) · หน้าฝึกร้อง (SongViewer) + editor preview ไม่เคยส่ง → คงพฤติกรรมเดิม

> **หมายเหตุขอบเขต:** brief ระบุ "SongViewer.vue (โหมด sheet)" แต่จริงๆ หน้าแผ่นเพลง render `<SongSheet>` **ตรงใน `Studio.vue`** (SongViewer = หน้าฝึกร้องเท่านั้น · ไม่มีโหมด sheet) → จึงแตะ `Studio.vue` 1 บรรทัด (เปิด `songbook`) แทน · **ไม่แตะ SongList/EditorMode/songSearch**

## Verify
- **unit:** `songModel.test.js` (แท็ก stanza-first: first/reuse/v1) + `SongSheet.test.js` (songbook on = ทำนองครั้งเดียว · reuse เนื้อล้วน+เลขข้อ · songbook off = โน้ตทุกข้อ) · **`vitest run` = 206 passed** (`notationLint.test.mjs` fail = ของเดิมบนฐาน · `process.exit` · ไม่เกี่ยว)
- **build:** ✅ ผ่าน (`npm run build`)
- **เบราว์เซอร์ (LAN 5380 · จาก DOM จริง):**
  - **เพลง 77** (ร้อง1·รับ·ร้อง2·ร้อง3·รับ) โหมดแผ่นเพลง → ร้อง1 **มีโน้ต+คอร์ด** · รับ **มีโน้ต+คอร์ด** · ร้อง2/ร้อง3/รับ(ซ้ำ) = **เนื้อล้วน (notes=0)** พร้อมคำร้องของข้อนั้นเอง ✅
  - **เพลง 57** (ร้อง1·รับ·ร้อง2·ร้อง3) → ร้อง1+รับ มีโน้ต · ร้อง2+ร้อง3 เนื้อล้วน ✅
  - **ฝึกร้อง เพลง 57** → ร้อง1/รับ/ร้อง2/ร้อง3 **มีโน้ตครบทุกข้อ** (12/17/12/12) = ไม่เปลี่ยน ✅

## ค้าง — ต้อง P'Aim
- **print/PDF จริง:** ยังไม่ได้พิสูจน์จากไฟล์ PDF (ตาม lesson: ห้ามเคลม print เสร็จจาก DOM) → ขอ P'Aim สั่งพิมพ์หน้าแผ่นเพลง (เพลง 77) → เปิด PDF ดูว่าโน้ตครั้งเดียว + ข้อซ้ำเนื้อล้วน + ไม่ตัดกลางท่อน

## B059-refine spacing (commit `a7a2695`) — เสร็จ
- ระยะบรรทัดข้อเนื้อล้วน **24px → 2px** (`.song-line-lyrics` margin 2px + line-height 1.35) · ซ่อนบรรทัดไร้คำ (ท่อนโน้ตยาว/เอื้อน) ในข้อซ้ำ (`isEmptyLyricLine`) · verify DOM เพลง 77 (gap 2px · 2 บรรทัดเปล่าซ่อน) · unit +1

## B065 (commit `018075e`) — โน้ตล้นเส้นกั้นห้องเมื่อซ่อนคอร์ด — เสร็จ
- **อาการ:** โหมด "เนื้อ+โน้ต"/"โน้ตล้วน" (ซ่อนคอร์ด) → โน้ตโผล่**เหนือ**เส้นกั้นห้อง (img `docs/backlog-assets/B065-bar-overflow-nochord.png`)
- **ต้นเหตุจริง = แนวตั้ง ไม่ใช่ความกว้าง** (ต่างจากที่เดาใน backlog): `.bar-line` มี `margin-top:1em` เผื่อแถวคอร์ดข้างบน · พอไม่มีแถวคอร์ด เส้นกั้นเลยตกลงมา 16px อยู่ใต้โน้ต → โน้ตโผล่เหนือเส้น · (ความกว้างห้องมาจากเนื้อ/โน้ต ไม่ใช่คอร์ด · วัด DOM = โน้ตไม่ล้นแนวนอน)
- **แก้:** root ได้คลาส `.sheet-no-chord` เมื่อ `sn && !sc` (โชว์โน้ต·ซ่อนคอร์ด) → zero margin-top ของ bar-line/bar-final/repeat-mark → เส้นกั้นเริ่มที่แถวโน้ตพอดี
- **verify (เบราว์เซอร์ เพลง 18):** เนื้อ+โน้ต → เส้นกั้นทั้ง 20 เส้น `barTop == noteTop` (เดิม +16) · แผ่นเพลง/ครบ (มีคอร์ด) = ไม่แตะ (คลาสไม่ติด · margin 16px เท่าเดิม) · unit +1 (คลาสติดเฉพาะโหมดไม่มีคอร์ด) · **`vitest run` 208 passed** · build ✅
- ⚠️ print PDF จริง = ยังต้อง P'Aim ยืนยัน (พร้อม B059)

## B059-refine dedup ลึก (ทำนองซ้ำภายในโซนมีโน้ต) — 🛑 HOLD รอ P'Aim ยืนยันตีความ
เจอปมสำคัญจากข้อมูลจริง (เพลง 77) — **naive within-stanza dedup ไม่ปลอดภัย:**
- รับ (stanza B) มี 3 บรรทัดทำนองเดียวกัน: **L1==L2 เป๊ะ · L0 ต่างที่ pickup** (ขึ้นต้น) → "รวมเฉพาะเหมือนเป๊ะ" ยุบได้แค่ L2 เหลือโน้ต 2 บรรทัด (ไม่ถึงครั้งเดียว)
- ข้อ (stanza A) **ก็มีบรรทัดซ้ำเป๊ะ (L0==L2)** แต่เป็น**ข้อร้องต่อเนื่อง** (AABA) ต้องอ่านไล่บนลงล่าง → ถ้ารวม จะดันเนื้อ L2 ไปซ้อนใต้ L0 = **เนื้อสลับที่**
- เพลง 57 = ไม่มีบรรทัดซ้ำภายในเลย (dedup ปัจจุบันพอ)
- **สาเหตุราก = โมเดลข้อมูล** (import อัดทำนองซ้ำในก้อนเดียว แทนโมเดล reuse) → render-level heuristic เสี่ยงทำ 120 เพลงเพี้ยน
- **เสนอ P'Aim 3 ทาง:** A จบที่ stanza-level + แก้โมเดล/import (แนะนำ) · B เพิ่มกฎปลอดภัย (ยุบเฉพาะบรรทัดถัดกันเหมือนเป๊ะ · ข้อไม่พัง · แต่รับไม่เหลือครั้งเดียว) · C dedup เดาวลีคล้าย (ตรงใจแต่เสี่ยงสูง) → **รอ P'Aim เคาะก่อนแตะโค้ด**

## ประสาน (สำหรับ PM)
- SongSheet = ไฟล์เดียวกับ **B043 เฟส2 / B044 / B046** (spacing/A2/print) → ถ้าสายพวกนั้นจะเข้า ให้ merge sequence · การเปลี่ยน B059/B065 = additive (prop/class ใหม่ + per-line gate) ไม่รื้อ layout เดิม
- **ยังไม่เก็บ B044/B046 spacing** (brief ให้แจ้งก่อนทำ) · ถ้า PM ต้องการรวม แจ้งได้
- **นอกสาย (flag):** 119/121 เพลงในฐานไม่มี `content.bpm` → หน้าฝึกร้องโชว์ความเร็ว 92 ทุกเพลง (fallback) = เรื่องข้อมูล/DA import ไม่ใช่บั๊กโค้ด · ไม่ใช่ B059
- ห้าม merge main/deploy (ตามกฎ) · รอ PM ตรวจ DoD + P'Aim accept (LAN + print PDF)
