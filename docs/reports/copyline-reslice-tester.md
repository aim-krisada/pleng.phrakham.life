# รายงาน Tester — B088 คัดลอก/ลบบรรทัด reslice พยางค์ (editor-copyline-reslice)

**ตรวจโดย:** tester (`tester-qa`) · **ของ:** `editor-copyline-reslice` `e496af5` (แตกจาก editor-ux-followup B085/B086 · EditorMode.vue+test · vitest 333) · **ping:** pm7
**วิธี:** vitest + วัดจริง Browser MCP บนเพลง 2 "ของขวัญ" (หลาย verse · ร้อง1=ทำนอง B · รับ/ร้อง2=ทำนอง A) หน้าแก้ไข `5413`

---

## VERDICT: ✅ ผ่าน (reslice ถูกทั้งคัดลอก+ลบ · B086 ยังทำงาน) · ⚠️ 1 ข้อสังเกต UX (native confirm)

| เกณฑ์ | ผล | หลักฐานวัดจริง (badge = paired/total) |
|---|---|---|
| **คัดลอกบรรทัด** → เนื้อทุกข้อ(ทำนองเดียวกัน)ซ้ำตาม | ✅ | note +19 กล่อง (+12 โน้ต) · **รับ 35/69→46/81 · ร้อง2 21/69→33/81** (ทั้งคู่ +12 total+paired) · **ร้อง1(ทำนอง B) 42/43→42/43 คงเดิม** (คนละทำนอง = ไม่แตะ ถูก) |
| **ลบบรรทัด** → เนื้อทุกข้อ slice ออกถูก | ✅ | note −19 กล่อง (−12 โน้ต) · **รับ 35/69→24/57 · ร้อง2 21/69→9/57** (−12 total) · **ร้อง1 คงเดิม** |
| เนื้ออยู่บนโน้ตตรง (ไม่เลื่อนผิด) | ✅ | pairing recompute สอดคล้อง (paired/total ขยับตามจำนวนโน้ตที่เพิ่ม/ลบ พอดี) · verse คนละทำนองไม่กระทบ |
| B086 ย้ายบรรทัด ยังทำงาน (แตกจากสายนั้น) | ✅ | move down เปลี่ยน · move up กลับเป๊ะ (restore=true) |
| vitest · console | ✅ | **333 passed** · console 0 error |

**สรุป:** คัดลอก/ลบ บรรทัดของทำนอง A → verse ที่ใช้ทำนอง A ทั้งหมด (รับ+ร้อง2) reslice ตามพอดี · verse ทำนอง B (ร้อง1) ไม่กระทบ = ตรรกะ reslice ต่อทำนองถูกต้อง

---

## ⚠️ ข้อสังเกต (ไม่ใช่ B088 fail — ฝาก pm7 พิจารณา)
- **ลบบรรทัด ใช้ native `window.confirm()`** — กล่องยืนยันของเบราว์เซอร์ · **ทำ Browser MCP ค้าง (freeze 300s+ · key/navigate ดึงไม่ขึ้น)** จนต้องเปิด pane ใหม่ · ผม override `window.confirm=()=>true` เพื่อทดสอบต่อ
  - **UX (ui-standards §2):** native confirm = จัดสไตล์ไม่ได้ · จังหวะขัด · บนมือถือดูไม่เข้าชุด → **แนะนำเปลี่ยนเป็น in-app dialog ของเว็บ** (ปิด Esc/แตะนอกได้ · ตามแพตเทิร์น popup อื่น) · ไม่บล็อก B088 (ลบทำงานถูก) แต่ควรเก็บเป็นงานปรับ
- ผมลบบรรทัดจริงระหว่างเทส (draft · ไม่ได้ save) → state เพลง 2 บน pane นี้เปลี่ยน · ไม่ลง Supabase

## next
- pm7 → B088 reslice บนจอเขียว · **merge ต่อจาก B085/B086** ตามคิว · P'Aim LAN ยืนยัน save/ดูผล + เคาะเรื่อง native confirm (จะปรับเป็น in-app dialog ไหม)
