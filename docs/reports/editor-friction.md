# รายงาน — ปลดคอขวดพี่เปา (issues 7 / 9 / 11 + ลบร่างตัวเอง)

**สาขา:** `claude/interesting-bun-49b4ec` (ฐาน `studio-shell-redesign`) · commit `d2b1f0f`
**ลองของจริงบนมือถือ:** <http://10.189.195.98:5471/#/studio> (เช็กจาก vite Network line จริง + curl ได้ 200)
**test:** 679 ผ่าน · `notationLint.test.mjs` แดงอยู่ก่อนแล้ว (สคริปต์เรียก `process.exit(0)` — ยืนยันด้วย `git stash` ว่าไม่เกี่ยวกับงานนี้)

---

## สรุปสั้น

พี่เปายื่น 3 ใบในวันเดียวด้วยคำเดียวกัน — และของจริงยืนยันว่ามัน**ไม่ใช่ 3 บั๊ก** แต่เป็นอาการเดียว:
**เครื่องมือที่เขาใช้บ่อยที่สุดถูกวางไว้ในที่ที่กดไม่ถึง หรือกดแล้วไม่เกิดอะไร**

ทุกข้อ**เปิดของจริง รันจริง วัดจริง** ก่อนแตะโค้ด และมี **2 จุดที่ของจริงไม่ตรงกับที่ PM คาด** (เขียนไว้ล่างสุด)

---

## ข้อ 1 · issues7 — "ดูผลทั้งเพลง ตัวเล็กมาก ยืดขยายไม่ได้อีก"

### ต้นเหตุจริง (มี 3 ชั้นซ้อนกัน)

**ชั้นที่ 1 — บั๊กคำนวณ ทำให้ตัวเล็กกว่าที่ออกแบบไว้เอง**
โค้ดวัดความกว้างคอลัมน์อ่านด้วย `parseFloat(cs.maxWidth)` แต่เบราว์เซอร์คืนค่ามาเป็นสูตรดิบ
`"min(1160px, 100%)"` → `parseFloat` = **NaN** → ตกไปใช้ `|| Infinity` → วัดคอลัมน์เป็น **1455px**
ทั้งที่ของจริงกว้าง **1160px เป๊ะ** (วัด `getBoundingClientRect` แล้ว)

ผลข้างเคียงที่ย้อนศร: **จอยิ่งกว้าง ตัวยิ่งเล็ก** — พิสูจน์สดแล้ว จอ 1280 → 9.06px · จอ 1536 → **7.47px**

**ชั้นที่ 2 — ตัวลากขยายเป็นทางตัน (นี่คือคำว่า "ไม่ได้อีก")**
หน้าต่างอยู่มุมขวาบน (`right:16px`) และตัวลากขยายได้แค่ไปทางขวา/ลงเท่านั้น
วัดจริงที่จอ 1536: หน้าต่างกว้าง 720px อยู่ที่ x=785 → **ขยายได้อีกแค่ 27px แล้วชนขอบจอ**
พี่เปาพูดถูกทุกตัวอักษร — ปุ่มที่ดูเหมือนจะขยายได้ แทบไม่ขยับเลย

**ชั้นที่ 3 — หน้าต่างเล็กเกินกว่าจะอ่านออกตั้งแต่ต้น**
แผ่นเพลง**ย่อลงให้พอดีหน้าต่าง** ดังนั้นหน้าต่าง 720px = ตัวหนังสือ ~9px (แผ่นจริง 16px)
นี่คือผลของ parity ที่พี่เปาขอเองใน issues3 (ให้ตกบรรทัดเหมือนหน้าแผ่นเพลง):
จะให้ตกบรรทัดเหมือนคอลัมน์ 1160px ในหน้าต่าง 720px ได้ **ต้องย่อ** — เลี่ยงไม่ได้
**หน้าต่างยิ่งกว้าง = ตัวยิ่งใหญ่** จึงเป็นกลไกของมันอยู่แล้ว แค่ก่อนหน้านี้ทำให้กว้างไม่ได้

### แก้ยังไง
1. ดึงตัวเลข px ออกจากสูตรแทน `parseFloat` ตรงๆ → วัดคอลัมน์ได้ 1160px ตามที่ออกแบบไว้จริง
2. **ตัวลากขยาย: พอชนขอบขวา ให้หน้าต่างเลื่อนไปทางซ้ายแทนการหยุด** → ลากได้จนเต็มจอ
3. ค่าเริ่มต้นหน้าต่าง 720px → **980px** (อ่านออกตั้งแต่เปิด แต่ยังเห็นโต๊ะแก้ไว้ "แก้ไปดูไป")

### ผลที่วัดได้จริง (จอ 1536 · Aa 100%)
| | ก่อน | หลัง |
|---|---|---|
| เปิดมาเฉยๆ | 7.47px | **13.73px** (+84%) |
| ลากตัวขยายสุด | 720→747px (โต 27px) | 980→**1528px** · ตัวหนังสือ **21.75px** |

### ⚠️ ตอบคำถาม PM เรื่อง `readingFontScale` — **ของจริงค้าน PM**
PM ถามว่า "มี `readingFontScale` ใน store — ใช้กับหน้าต่างนี้ไหม?"
**คำตอบ: ใช้อยู่แล้ว และได้ผลจริง** — พิสูจน์สดแล้ว: Aa 100% → 9.06px · **Aa 220% → 19.94px**
แปลว่า **พี่เปามีปุ่มขยายอยู่ในมือมาตลอด แต่ไม่มีทางรู้** เพราะมันชื่อ "Aa" (ขนาดตัวอ่าน) อยู่ในแถบ dock
ไม่มีอะไรบอกว่ามันคุมหน้าต่างนี้ด้วย

**ผมจึงไม่แปะปุ่มซูมเพิ่ม** — เพราะพอตัวลากขยายทำงานจริง **ตัวลากขยาย = ปุ่มซูม** อยู่แล้ว
และมันคือท่าที่พี่เปาลองทำเองตั้งแต่แรก ("ยืดขยาย") ตรงกับความคาดหวังเขาที่สุด

---

## ข้อ 2 · issues9 — "ทำไมต้องกดปักหมุดก่อนถึงจะเซฟร่างได้"

### ต้นเหตุจริง — **แรงกว่าที่ PM คาด**
PM ตั้งโจทย์ว่าเป็นเรื่อง "ค่าเริ่มต้นควรอยู่แถบหลักไหม" ของจริงหนักกว่านั้น:

ในหน้า ⚙ แต่ละแถวจะ render ตัวควบคุมตามชนิด — `menu` ได้ dropdown · `toggle` ได้สวิตช์ · `slider` ได้แถบเลื่อน
แต่ **`btn` ไม่มีสาขาเลย** → "บันทึกร่าง" (เป็น `btn`) แสดงแค่ ไอคอน + ชื่อ + **ช่องว่าง** + หมุด

**คือมันกดใช้จากหน้า ⚙ ไม่ได้จริงๆ — ปักหมุดคือทางเดียวที่จะได้ปุ่มที่กดแล้วทำงาน**
พี่เปาไม่ได้เข้าใจผิด เขาอธิบายพฤติกรรมจริงของระบบเป๊ะๆ

**ภาพพี่เปายืนยันตรงกับโค้ด:** ในภาพ `issues9/20260717-141753.png` แถว "บันทึกร่าง" มีแต่หมุด (ไม่มีสวิตช์)
ส่วนแถว "ดูผลทั้งเพลง" (เป็น `toggle`) มีสวิตช์ — ตรงกับที่โค้ดบอกทุกประการ

### แก้ยังไง (ไม่รื้อระบบปักหมุด ตามที่ PM กำชับ)
1. **"บันทึกร่าง" ได้ที่นั่งถาวรบนแถบหลัก** (row 2) — คำสั่งที่ใช้บ่อยที่สุดของคนพิมพ์ 124 เพลง ต้องกดถึงทันที
   ไม่ต้องตั้งค่าก่อนใช้ · ระบบปักหมุดยังอยู่ครบสำหรับตัวอื่น
2. **ปิดคลาสบั๊ก:** `btn` ในหน้า ⚙ ตอนนี้มีปุ่มกดใช้ได้จริง — ทุกแถวในหน้านั้นทำสิ่งที่มันเขียนไว้
3. **กันปุ่มโผล่ 2 ที่:** พี่เปา**ปักหมุด 'draft' ไว้แล้ว**ในเครื่องเขา (เห็นหมุดแดงในภาพ) ถ้าไม่กัน
   ปุ่มจะขึ้นทั้งแถบหลักและแถวหมุด = ซ้ำ · ตอนนี้ของที่มีที่นั่งบนแถบแล้วจะไม่ถูกวาดซ้ำจากหมุด
   (ล้างค่าเก่าให้เอง ไม่ต้องให้พี่เปาทำอะไร · ตรงตาม ui-standards §2 single source of action)

---

## ข้อ 3 · issues11 (เฉพาะการเข้าถึง) — "ต้องเลื่อนมาบนสุดถึงจะกดใช้ได้"

### ต้นเหตุจริง
`.edhead` (แถบหัวที่ถือเครื่องมือของบรรทัดที่กำลังแก้ — ⋯ · ฮุก · ซ้ำ · ย้ายขึ้น/ลง · ลบบรรทัด)
เป็น `position: static` = **เลื่อนหนีไปกับหน้า** → แก้บรรทัดที่ 20 ต้องเลื่อนกลับขึ้นบนสุดทุกครั้งที่จะใช้เครื่องมือ

### แก้ยังไง
ทำให้แถบ**ติดอยู่ใต้หัวเว็บ** · ค่า `top` **วัดจากความสูงหัวเว็บจริง ไม่ hardcode**
(หัวเว็บสูงไม่เท่ากันแต่ละขนาดจอ — 64px ที่เดสก์ท็อป · 114px ที่ 360px)

> **เจอบั๊กข้างเคียง (ไม่ได้แตะ · นอกขอบเขต):** `NAV.rail` ใช้ `top: 58px` แบบ hardcode
> ทั้งที่หัวเว็บสูง 64–114px → rail มุดใต้หัวเว็บ · ผมเลี่ยงกับดักนี้ด้วยการวัดเอา

### 🔴 ผมทำพังเอง แล้วแก้แล้ว — SA เตือนถูก (และแย่กว่าที่เตือน)
SA ชี้ว่า **`.cshead` (แถบเครื่องมือท่อน) sticky อยู่แล้ว** ที่ `top: 58px` แบบ hardcode (B085 · ไฟล์เดียวกัน)
**ผมไม่ได้ตรวจจุดนี้ในรอบแรก** · พอวัดจริงที่จอ 1536 · scroll 1600:

| | ก่อนแก้ (ที่ผมเกือบส่ง) | หลังแก้ |
|---|---|---|
| edhead | 64→158 | 64→158 |
| cshead | **58→126 (มุดใต้ edhead)** | **158→226** |
| ซ้อนกัน | **62px** | **0px** |
| ปุ่ม cshead กดได้ | **❌ ไม่ได้** (โดน `ed-chip` ของผมบัง) | **✅ ได้** |

**= ผมแลกแถบที่กดไม่ถึงอันหนึ่ง ด้วยการทำอีกอันกดไม่ได้เลย** (ลบล้างเหตุผลทั้งหมดของ B085)

**แก้:** `.cshead` ปักที่ `shellH + edheadH` (วัดจาก DOM จริงทั้งคู่ · ResizeObserver ตาม edhead ที่ wrap ได้)
**⚠️ กับดักซ้อน:** ครั้งแรกใช้ template ref → **ref เป็น null ตอน mount → `edheadH` ค้างที่ 0 → ซ้อนกลับมาเหมือนเดิมเงียบๆ**
จับได้เพราะ**อ่านค่าที่ render ออกมาจริง** (`top: 64px` ทั้งที่ควรเป็น 158) ไม่ใช่เชื่อว่าแก้แล้วจบ → เปลี่ยนไปวัดจาก DOM แบบเดียวกับ `.shell-bar` ที่พิสูจน์แล้วว่าทำงาน

### ผลที่วัดได้จริง (จอ 1536 · หน้ายาว ~2.7k)
scroll 900 / 1600 / 2300 → **ซ้อนกัน 0px ทุกจุด** · **แถบทั้งสองผ่าน hit-test ทั้งคู่** · chrome จบที่ **25% ของจอ** (ต่ำกว่าเพดาน ~30% ที่ SA ตั้ง)

### ⛔ ไม่ได้แตะช่องป้ายเลย
`curLine().label` และถ้อยคำของมันไม่ถูกแตะ — ยกให้สาย `task_c3c169a7` ตามที่ PM กำชับ
(ส่วน *"อ่านแล้วงงว่าทำอะไร"* = ปัญหาของป้าย ไม่ใช่ของเมนู)

### 🔴 ยังเหลือ — sticky **เฉพาะเดสก์ท็อป**
วัดที่ **360px จริง** แล้วพบว่าแถบนี้ **wrap เป็น 6 แถว = สูง 256px = 32% ของจอ**
ถ้าปักไว้บนมือถือ = **กินจอพี่เปาถาวรหนึ่งในสาม** → **ผมตัดสินใจไม่ทำบนมือถือ**
มือถือกลับไปเป็นแบบเดิม (ไม่มีอะไรแย่ลง) และไม่มี h-scroll · ปุ่มไม่หลุดขอบ · ไม่มีปุ่มต่ำกว่า 24px
(ตรวจที่ 360px + 412px จริง)

**จะให้ติดบนมือถือได้ ต้องทำแถบให้เตี้ยลงก่อน** — นั่นเป็นงานออกแบบที่ทับกับสาย ⋯ ขอ PM ตัดสิน
(มือถือจึงไม่มี 2 แถบซ้อนกัน · `.cshead` ใช้ CSS เดิม 58px = **ไม่มีอะไรเปลี่ยนจากก่อนงานนี้** · ตรวจ 360 + 412 จริง)

### 💡 ทำไม sticky แถบนี้สำคัญกว่าที่คิด (หลักฐานจากสาย SA)
พี่เปาพิมพ์ `D.C.` / `FINE` ลง**ช่องคอร์ด** 4 เพลง เพราะช่องคอร์ดอยู่ติดมือทุกห้อง ส่วนช่องป้ายต้องเลื่อนขึ้นบนสุด
(PM รันจริง: `transposeChord('FINE',+2)` → `GINE`) → **ความฝืดนี้ไม่ได้แค่กินเวลา มันทำข้อมูลเสีย**
งานนี้ (sticky) แก้ครึ่งเดียว = ทำให้เมนู "ไปถึงได้" · อีกครึ่ง (ป้ายใช้ง่ายจนไม่ต้องหลบไปช่องคอร์ด) = สาย `task_c3c169a7`
**⛔ ผมไม่แตะช่องคอร์ด/`chords.js`** ตามที่ PM กำชับ — แก้ที่ต้นทาง (ป้าย) ไม่ใช่ที่ปลายทาง (คอร์ด)

---

## ข้อ 4 · ลบร่างของตัวเอง (issues10 · P'Aim อนุมัติแล้ว)

### ต้นเหตุจริง
`deleteDraft` **ไม่มีจริง** — ยืนยันตาม PM · ร่างที่ทิ้งแล้วค้างใน "งานร่างของฉัน" ตลอดไป

### ⚠️ เรื่อง RLS — **ไม่ต้องแตะฐานข้อมูล ไม่ต้องหยุด**
PM สั่งให้เช็กก่อนและหยุดถ้าไม่มี policy · **เช็กแล้ว: มีอยู่แล้ว**
`db/002-draft-review-system.sql` → `create policy "Delete own or as approver" on public.song_drafts
for delete using (author_id = auth.uid() or public.app_role() = 'approver')`
→ **แก้ฝั่งหน้าเว็บอย่างเดียวจบ** · เซิร์ฟเวอร์กันร่างคนอื่นให้อยู่แล้วแม้ UI จะพลาด

**audit log ก็รองรับไว้แล้ว:** `classifyChange()` map DELETE ของ `song_drafts` เป็น `null` = "discard"
(ตั้งใจไม่บันทึก) → ไม่ต้องแก้อะไรเพิ่ม

### แก้ยังไง
- ปุ่มลบ **อยู่บนแถวของร่างนั้นเอง** — แถวนั้นคือร่าง พี่เปาจึงมองหาตรงนั้น ("ทำไมมันหายาก")
- **มี confirm ก่อนลบ** และ**บอกชื่อร่างที่จะโดนลบ** (`ลบร่าง "…" ถาวร? (กู้คืนไม่ได้)`)
- ถ้าลบร่างที่เปิดค้างอยู่ → ปล่อย id ทิ้ง เพื่อไม่ให้ "บันทึกร่าง" ครั้งต่อไปไปอัปเดตแถวที่ถูกลบแล้ว
- ปุ่มเป็น 44px ตาม WCAG 2.2 AA (2.5.8) · สีจางไว้ ไม่แย่งความสนใจจากชื่อเพลง

### ⛔ ไม่รวมลบเพลงที่เผยแพร่แล้ว
ปุ่มเดิม (`v-if="isApprover…"`) **ไม่ถูกแตะ** — P'Aim ยังไม่เคาะ · มี test ล็อกไว้ว่า flow นี้ไม่ยิง `songs.delete`

---

## จุดที่ของจริงค้าน PM (ตามที่ PM สั่งให้ค้านได้)

| PM ว่า | ของจริง |
|---|---|
| `EditorMode.vue` อยู่ `src/views/` | อยู่ **`src/components/`** |
| "issues9 = ค่าเริ่มต้นควรอยู่แถบหลักไหม" | **หนักกว่านั้น** — `btn` ในหน้า ⚙ **กดใช้ไม่ได้เลย** ปักหมุดคือทางเดียวจริงๆ |
| "ถ้า RLS ไม่มี delete → หยุด" | **มีอยู่แล้ว** (`db/002`) → ไม่ต้องแตะฐานข้อมูล ไม่ต้องหยุด |
| "readingFontScale ใช้กับหน้าต่างนี้ไหม" | **ใช้อยู่แล้วและได้ผลจริง** (9.06→19.94px) — ปัญหาคือมองไม่เห็น ไม่ใช่ไม่มี |

## จุดที่ SA/PM ถูก และผมพลาด
| SA/PM ว่า | ผล |
|---|---|
| "`.cshead` sticky อยู่แล้ว (B085) — มี precedent ในไฟล์เดียวกัน" | **ถูก** (`:3357` `top:58px` z-index 4) — ยืนยันเองแล้ว |
| "2 แถบ sticky ซ้อนกัน → ต้องวัดจริง" | **ถูก และแย่กว่าที่เตือน** — ไม่ใช่แค่กินพื้นที่ แต่ cshead **ถูกทับ 62px จนกดไม่ได้เลย** · ผมไม่ได้ตรวจในรอบแรก · แก้แล้ว (ซ้อน 0px) |
| "ถ้าเกิน ~30% → plan B ยุบเข้า `.cshead`" | **ไม่ต้องใช้ plan B** — วัดจริง chrome จบที่ **25%** ของจอเดสก์ท็อป · มือถือไม่ซ้อน (edhead ไม่ sticky) |

## test
- แก้ test เดิม 1 ข้อ (`edhead` resize) ให้ตรงพฤติกรรมใหม่ — หน้าต่างเลื่อนซ้ายแทนการชนหยุด
  (ขอบขวายังอยู่ที่เดิม 1020 · ได้ระยะขอบซ้าย 4px เพิ่มมาให้สมมาตร)
- เพิ่ม test ใหม่ 6 ข้อ ล็อกทุกต้นเหตุ: `btn` ใน ⚙ กดได้ · หมุดค้างไม่ทำให้ปุ่มซ้ำ ·
  บันทึกร่างมีที่นั่งบนแถบ · ลบร่างถาม confirm + ยิงถูกตาราง · กด "ยกเลิก" แล้วไม่ลบ · ล้าง id ที่ตายแล้ว

## ยังเหลือ / ขอ PM ตัดสิน
1. **sticky บนมือถือ** — ต้องทำ `.edhead` ให้เตี้ยกว่า 6 แถวก่อน (ทับสาย ⋯)
2. **บอกพี่เปาว่า "Aa" ขยายหน้าต่างดูผลได้ด้วย** — มันได้ผลจริงมาตลอด เขาแค่ไม่รู้
3. **ลบเพลงที่เผยแพร่แล้ว** — พี่เปาถามใน issues10 ด้วย ("จะลบเพลงทั้งเพลง หาปุ่มไม่เจอ") ยังรอ P'Aim เคาะ
4. `NAV.rail` `top: 58px` hardcode ทั้งที่หัวเว็บสูง 64–114px → มุดใต้หัวเว็บ (บั๊กเดิม ไม่ได้แตะ)
