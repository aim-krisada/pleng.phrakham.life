# wt-b058 — note search reads v2 (B058)

**branch:** `wt-b058` (ฐาน `studio-shell-redesign`) · **commit:** `2a060a2`
**ไฟล์ที่แตะ:** `src/lib/songSearch.js` + `src/lib/songSearch.test.js` เท่านั้น (กันชนตามbrief — ไม่แตะ SongList/EditorMode)

## ปัญหา
กล่องค้นหาโฆษณาว่าค้นโน้ตได้ (placeholder "โน้ตตัวเลข เช่น 5 5 6 1") แต่ `notesText()`
อ่านแค่ v1 (`content.lines`) → 120 เพลง v2 ที่ import (โน้ตอยู่ใน `content.stanzas[].lines[]`)
**ค้นโน้ตไม่เจอเลย**

## แก้
`notesText(content)` — ถ้ามี `content.stanzas` ให้เดินโน้ตจาก `stanzas[].lines[]`
(รูปทรงเดียวกับ v1 lines — array ของ item ต่อบรรทัด · `type:'segment'` ถือ `note`)
ถ้าไม่มีก็ตกกลับ `content.lines` แบบเดิม · strip สัญลักษณ์ชุดเดิม (`. - _ ' ( ) { } # b`)
+ เพิ่ม `.trim()` กันช่องว่างนำหน้าจากช่องว่าง/held box ตัวแรกรั่วออกมา

โครงสร้างจริงยืนยันจากเพลง import จริง (`tools/samples/001.json`):
`stanzas[0].lines[0]` = `[{type:'segment',note:'.5. .5_ .5 .6'},{type:'bar'},...]` — โน้ตมี
จุดออกเทฟ/ขีดค้าง/underscore ที่ต้อง strip เหมือน v1

## Verify
- unit ใหม่: `notesText` v1 (`'1 2 3 5'`) + v2 stanza (strip จุด/ค้าง → `'5 5 6 1 3'`) +
  multi-note v2 segment · ค้น `"5 5 6 1"` เจอเพลง v2 · ค้น `"1 2 3 5"` เจอ v1 (regression)
- **`vitest run --exclude '**/.claude/**'` = 201 passed** (songSearch 26/26) · **build ผ่าน**
- ⚠️ `notationLint.test.mjs` รายงาน "1 failed" = ของเดิมบนฐาน (สคริปต์เรียก `process.exit(0)`
  ที่ vitest ตีเป็น fail — จริงๆ lint pass exit 0) · ไม่เกี่ยวกับ change นี้ · ไม่ได้แตะไฟล์นั้น

## หมายเหตุ
- **UI กล่องค้นหา** (SongList) = สาย catalog/review ทำ · สายนี้แตะแค่ logic — พอ catalog เสียบ
  `filterSongs`/`searchSongs` ก็ได้ค้นโน้ต v2 ฟรีทันที
- **อิสระ merge ได้เลย** (ไม่ชนใคร — logic ล้วนใน songSearch)
