# บันทึกการลองคุย N — งานรายการมาตรฐาน v3 (29 ก.ค. 2026)

> ## ✅ เรื่องนี้จบแล้ว — สาเหตุคือรหัสโน้ตบุ๊กในใบสั่งงานถูกลบไปแล้ว
> PM แจ้งภายหลังว่าโน้ตบุ๊ก **Pleng resource 1** (`7b8ac29d-…`) **ถูกลบไปแล้ววันนี้** ⇒ ข้อความ "หาไม่พบ" ที่เจอด้านล่าง **ถูกต้องตามความจริง ไม่ใช่ตัวเชื่อมพัง**
> เล่มที่ต้องใช้คือ **`Pleng UX 1`** = `f814bbeb-845e-429d-a6b4-d24703287409` (หารหัสด้วย `client.notebooks.list()`) → **ถามสำเร็จ 4 รอบ** · บทสนทนาเต็มอยู่ที่ [`n-consult-2026-07-29-standards-transcript.md`](n-consult-2026-07-29-standards-transcript.md)
> **เก็บไฟล์นี้ไว้เป็นบทเรียน:** อาการ `status code 5 (Not found)` = **โน้ตบุ๊กไม่มีอยู่จริง** ⛔ ไม่ใช่คุกกี้หมดอายุ ⛔ ไม่ใช่บัญชีวิ่งผิด (การรีเฟรชคุกกี้ไม่ได้ช่วยอะไรเลย) ⇒ **ครั้งหน้าเจออาการนี้ ให้เรียก `client.notebooks.list()` ดูก่อนเป็นอย่างแรก** จะรู้ทันทีว่าเล่มยังอยู่ไหม

**ผล (ณ เวลาที่บันทึก): คุย N ไม่สำเร็จ — ล้มเหลว 3 ครั้งด้วยข้อความเดียวกัน แล้วหยุดตามกฎตัดขาดทุน**
⛔ ไม่มีคำตอบจาก N ⇒ **ไม่มีข้อสรุปจาก N ในเอกสารรายการมาตรฐาน** (ตามกฎ: ข้อสรุปที่ไม่มีบทสนทนา = ไม่นับเป็นหลักฐาน)

**โน้ตบุ๊กที่พยายามเรียก:** Pleng resource 1 · `7b8ac29d-9e44-4ecb-8af1-680e0025cb2c`
**ชื่อแชตที่ตั้งไว้:** `pleng-v3-standards-2026-07-29` (ยังไม่เกิดขึ้นจริง เพราะเรียกไม่ผ่าน)

---

## สถานะเครื่องมือก่อนลอง (`bridge.py status`)

```
G (API): key OK · default model gemini-3.6-flash
N (RPC): venv OK · cookie present
registry: C:\gl\.aibridge\registry.json
```
⇒ ตัวเชื่อมพร้อม คุกกี้มีอยู่ · ในทะเบียนมี session เก่าหลายตัวที่เคยใช้โน้ตบุ๊กเดียวกันนี้สำเร็จ (เช่น `pleng-createbtn-2026-07-25`, `pleng-717-editor-2026-07-25`) ⇒ **รหัสโน้ตบุ๊กที่ใช้ถูกต้องแล้ว**

---

## ครั้งที่ 1 — เรียกรายชื่อแหล่งข้อมูลตรงผ่านตัวเชื่อม

**สิ่งที่รัน:** สคริปต์ที่เรียก `client.sources.list("7b8ac29d-9e44-4ecb-8af1-680e0025cb2c")` ด้วย `C:\gl\.aibridge\venv\Scripts\python.exe`
**จุดประสงค์:** เช็กว่าไฟล์ `plengux-` 13 ไฟล์อยู่ในโน้ตบุ๊กแล้วหรือยัง (เงื่อนไขข้อ 🔴 ในใบสั่งงาน)
**ผล — ล้มเหลว:**
```
ERROR [notebooklm._rpc_executor] RPC GET_NOTEBOOK failed after 0.709s: ClientError rpc_code=5
notebooklm.exceptions.ClientError: RPC rLM1Ne returned null result with status code 5 (Not found).
Found IDs: ['rLM1Ne']. If you have multiple Google accounts signed in, this is commonly an
account-routing mismatch — the request defaults to account index 0 when no authuser is set.
```

## ครั้งที่ 2 — ใช้เส้นทางที่ใบสั่งงานกำหนด (`bridge.py ask N`)

**คำสั่งที่รัน:**
```
cd C:\gl\krisada\ceo\tools\aibridge
py -3.14 bridge.py ask N pleng-v3-standards-2026-07-29 "มีแหล่งข้อมูล (source) ไหนในโน้ตบุ๊กนี้ที่ชื่อไฟล์ขึ้นต้นด้วย plengux บ้าง ตอบเป็นรายชื่อไฟล์ทั้งหมดที่พบ ถ้าไม่พบเลยให้ตอบว่า ไม่พบ" --notebook 7b8ac29d-9e44-4ecb-8af1-680e0025cb2c
```
**ผล — ล้มเหลว ข้อความเดียวกัน:**
```
RuntimeError: N RPC failed: ClientError: RPC rLM1Ne returned null result with
status code 5 (Not found). Found IDs: ['rLM1Ne'].
```

## ครั้งที่ 3 — รีเฟรชคุกกี้แล้วลองใหม่

**คำสั่งที่รัน:** `py -3.14 bridge.py bootstrap` → สำเร็จ
```
[bootstrap] wrote 84 cookies (50 google.*) -> C:\Users\aimkr\.notebooklm\profiles\default\storage_state.json
```
แล้วรันคำสั่ง `ask N` เดิมซ้ำ → **ล้มเหลว ข้อความเดียวกันเป๊ะ**

---

## อ่านผลอย่างไร (แยก "เรารู้" กับ "เราคิดว่า")

**เรารู้:**
- ตัวเชื่อมทำงาน คุกกี้เขียนใหม่ได้ 84 รายการ ⇒ ปัญหาไม่ใช่ตัวเชื่อมพัง
- ข้อความที่ได้คือ **"หาไม่พบ" (Not found)** ที่ระดับการเรียกโน้ตบุ๊ก ไม่ใช่ "ไม่มีสิทธิ์" และไม่ใช่ "หมดเวลา"
- ตัวเชื่อมเองแนะว่ากรณีนี้มักเกิดจาก **การล็อกอินหลายบัญชีกูเกิลพร้อมกัน แล้วคำขอวิ่งไปบัญชีลำดับ 0**

**เราคิดว่า (ยังไม่พิสูจน์):**
- น่าจะเป็นเรื่องบัญชีที่คำขอวิ่งไป มากกว่ารหัสโน้ตบุ๊กผิด เพราะรหัสเดียวกันมีประวัติใช้สำเร็จในทะเบียน
- ⛔ **ยังไม่ได้พิสูจน์** และ ⛔ **ไม่ได้แก้** เพราะการแก้ต้องแตะการล็อกอิน/เบราว์เซอร์ ซึ่งใบสั่งงานห้าม (ห้ามแตะพอร์ต 9222/9335/5393 · ห้ามเปิดหน้าต่างเอง)

**⇒ สิ่งที่ยังไม่รู้และต้องรู้:** ไฟล์ `plengux-` 13 ไฟล์ **อยู่ในโน้ตบุ๊กแล้วหรือยัง** — เราตรวจไม่ได้เลยในรอบนี้ (ต้นฉบับอยู่ใน OneDrive ครบ 13 ไฟล์ ตรวจแล้ว แต่นั่นเป็นเรื่องคนละชั้นกับโน้ตบุ๊ก)
