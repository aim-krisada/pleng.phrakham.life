# รายงาน — wt-b055 (สาย editor-urgent-fixes: B055 + B056)

**สาย:** dev · worktree `wt-b055` · ฐาน `studio-shell-redesign` (`5ba1e32`)
**LAN (มือถือ/แท็บเล็ต):** `http://192.168.1.124:5360/` → `#/studio` (dev server `--host` port 5360)
**สถานะ:** ✅ **B055 เสร็จ · test เขียว · build ผ่าน · repro ก่อน/หลังในเบราว์เซอร์แล้ว** · ⏳ **B056 = investigate เสร็จ · ค้าง P'Aim เคาะ UX** (มีของเดิมอยู่แล้วบางส่วน — ดูล่าง)
**คำสั่ง:** แตะ `EditorMode.vue` เท่านั้น (ไม่ต้องแตะ `notation.js`) · ไม่ชน DA/ไม่ชน mobile (Android ยังไม่เริ่ม)

---

## B055 — ตัวตรวจจังหวะแดงปลอมตอนจังหวะข้ามห้อง/ห้องยก ✅

### ปัญหา (ยืนยันจริง)
ตัวตรวจจังหวะนับ **ทีละห้อง** เทียบกับความยาวเต็มห้อง (4/4 = 4 จังหวะ) เสมอ — เพลงที่
**ขึ้นต้นด้วยห้องยก** (anacrusis เช่นเริ่มจังหวะ 3-4 = 2 จังหวะ) แล้วไป **จบด้วยห้องท้ายสั้น**
(จังหวะ 1-2 = 2 จังหวะ) จะขึ้น **แดง "จังหวะไม่ครบ" ทั้ง 2 ห้อง** ทั้งที่ 2+2 = 1 ห้องเต็มพอดี

**ทำไมกลไกเดิมแก้ไม่ได้:** โค้ดมี `line.cont` (⤷ ต่อห้องจากบรรทัดก่อน) ที่รวมจังหวะ **ห้องท้ายบรรทัด
กับห้องแรกบรรทัดถัดไป** ได้ — แต่ห้องยกกับห้องจบ **อยู่คนละที่ ไม่ติดกัน** (ห้องแรกสุด ↔ ห้องท้ายสุด
ของท่อน) `cont` จับคู่ไม่ได้ · และผู้ใช้ **ไม่มีที่ให้บอกว่า "จริงๆครบแล้ว"** (ตรงกับที่ P'Aim พูด)

### แก้ยังไง
เพิ่ม **แฟล็กรายห้อง `pickup` (ห้องยก)** — ห้องที่ติ๊กว่า "ห้องยก" ในท่อนเดียวกันจะถูกตรวจ
**เป็นกลุ่ม**: เอาจังหวะทุกห้องยกมา **รวมกัน** ถ้ารวมได้ครบเป็นจำนวนห้องเต็ม (เช่น 2+2=4) →
เขียวหมด · ครอบทั้ง **ห้องยก↔ห้องจบ (ไม่ติดกัน)** และการแบ่งข้ามบรรทัดที่อยากมาร์คเอง · ส่วน `cont`
เดิมยังอยู่ (ใช้กับการแบ่งข้ามบรรทัดที่ติดกัน)

- **control หาง่าย:** ปุ่มชิป **"↻ ห้องยก"** โผล่ **ข้างสถานะจังหวะ เฉพาะตอนห้องนั้นจังหวะขาด** (ตรงจุดที่
  เห็น ❌ พอดี) → แตะทีเดียวมาร์คได้ · มีใน `⋯ เครื่องมือห้อง` ด้วย (คู่กับ ‖: :‖ / ห้องจบ)
- **ซื่อสัตย์ ไม่กลบ error:** ถ้าติ๊กห้องยกแล้วรวมกัน **ยังไม่ครบห้องเต็ม** (เช่น 2+3=5) → **ยังแดง**
  พร้อมบอก "รวมห้องยก 5/4" (ไม่ใช่แค่ปิดสีแดงทิ้ง)
- **เก็บถาวร:** เซฟเป็น item `{type:'pickup'}` ใน content — ตัว render แผ่นเพลง **มองข้าม** (แผ่นเพลง
  ที่พิมพ์ไม่เปลี่ยน) แต่เปิดเพลงมาแก้ใหม่ค่ายังอยู่ (ไม่กลับมาแดงอีก)

### หลักฐาน repro ในเบราว์เซอร์ (ก่อน/หลัง · 4/4 · 2 ห้องยก 2+2)
| | ห้องแรก (ห้องยก 2 จังหวะ) | ห้องจบ (2 จังหวะ) |
|---|---|---|
| **ก่อน** | `2/4 จังหวะ ❌` (แดงปลอม) | `2/4 จังหวะ ❌` (แดงปลอม) |
| **หลัง** ติ๊ก ↻ ห้องยก | `⤷ ห้องยก 2 จังหวะ ✓` | `⤷ ห้องยก 2 จังหวะ ✓` |
| **เคสรวมผิด** (2+3) | `⤷ ห้องยก 2 — รวมห้องยก 5/4 ❌` | ยังแดง (ถูกต้อง — ยังไม่ครบจริง) |

ไม่มี console error/warning · verify ด้วย DOM state (`.ed-bar-status` text + class `.bad`) เพราะ
`preview_screenshot` timeout (ปัญหาเดิมของเครื่องมือ)

### ไฟล์ที่แก้
- `src/components/EditorMode.vue` — `barShell()` เพิ่ม `pickup` · serialize/deserialize item `pickup` ·
  `pickupTotal` computed (รวมจังหวะห้องยกในท่อน) · `barStatus()` แขนงห้องยก + คืน field `short`/`pickup` ·
  template: ชิป ↻ ห้องยก + toggle ใน `⋯` menu + CSS `.ed-bar-pickup`
- `src/components/EditorMode.beats.test.js` — **ใหม่** · 5 เทส

### ผลทดสอบ
- **unit ใหม่ 5/5:** กลุ่มห้องยก (ห้องแรก↔ห้องจบ ไม่ติดกัน) ไม่แดงปลอม · ห้องสั้นที่ไม่ได้มาร์ค **ยังแดง**
  (ยังจับ error จริง) · มีชิป ↻ ห้องยก บนห้องสั้น · `cont` ข้ามบรรทัดยังทำงาน 2 ห้อง · เซฟ pickup ค้าง round-trip
- **ทั้ง repo:** `npx vitest run --exclude '**/.claude/**'` → **193/193 tests ผ่าน (23/24 suite)** · `npm run build` ผ่าน
- ⚠️ **หมายเหตุ (ไม่ใช่ของ B055):** suite `src/lib/notationLint.test.mjs` โชว์ FAIL เพราะสคริปต์เรียก
  `process.exit(0)` เอง (vitest ไม่ชอบ) — **ของเดิมบนฐาน · ผมไม่แตะ** · รันเดี่ยว `node ...` = 51 passed, 0 failed

---

## B056 — เครื่องหมาย "จบเพลง" (ไม่ใช่แค่จบรอบ 1/2) ⏳ ค้าง P'Aim เคาะ

### สิ่งที่เจอในโค้ด (สำคัญ — มีของอยู่แล้วบางส่วน)
มี **"จบเพลง" ระดับบรรทัดอยู่แล้ว**: checkbox `‖ จบเพลง (Fine)` ใน `⋯ เพิ่มเติม` ของบรรทัด
(`curLine().end`) → render เป็น **เส้นจบเพลงคู่ (final barline)** จริง (`SongSheet` `part.type==='end'` → `.bar-final`)
**ไม่ผูกกับ repeat/volta** เลย → เพลงไม่มีย้อนก็มาร์คจบเพลงได้อยู่แล้ว

ส่วนที่ P'Aim น่าจะสะดุด = dropdown **"ห้องจบ:"** ใน `⋯ เครื่องมือห้อง` (รายห้อง) ที่ให้เลือกแค่
`— ไม่ใช่ — / รอบแรก (จบ 1) / รอบสอง (จบ 2)` — สื่อว่ามีย้อนซ้ำ · **ไม่มีตัวเลือก "จบเพลง"** ตรงนั้น

### ทำไมยังไม่ลงมือ (ขอ P'Aim เคาะ 1 เรื่อง)
"จบเพลง" ของเดิมเป็น **ระดับบรรทัด** แต่ "ห้องจบ" (volta) เป็น **ระดับห้อง** — คนละ field · ถ้าจะเพิ่ม
"จบเพลง" เข้า dropdown ห้องจบ ต้องตัดสินใจว่าจะให้มันไปเซ็ต `end` ของบรรทัด (เหมาะเฉพาะห้องท้ายบรรทัด)
หรือทำ mark ระดับห้องใหม่ · **ไม่อยากเดา UX แล้วทำผิดที่**

**คำถามถึง P'Aim (ผ่าน PM):** ตอนจะมาร์ค "จบเพลง" พี่เอมคาดว่าจะกดตรงไหน?
1. **(แนะนำ)** ใช้ของเดิม `‖ จบเพลง (Fine)` ที่ `⋯ เพิ่มเติม` (ระดับบรรทัด) — แค่ทำให้ **หาง่ายขึ้น**
   (ย้าย/ทำปุ่มให้เห็นชัด) ไม่ต้องเพิ่มที่ใหม่
2. เพิ่ม `จบเพลง` เข้า dropdown **"ห้องจบ:"** รายห้อง (ที่เดียวกับจบรอบ1/2) — กดแล้วเซ็ตจบเพลงให้ห้อง/บรรทัดนั้น
3. อื่นๆ (คำ/ไอคอนที่อยากได้)

พอ P'Aim เคาะ → ทำต่อในสาย/worktree นี้ทันที (แตะ `EditorMode.vue` ไฟล์เดิม ไม่ชนใคร)

---

## กันชน / ข้อควรรู้
- แตะแค่ `EditorMode.vue` (+ test ใหม่) · **ไม่แตะ** `notation.js`/`SongSheet.vue`/DA
- ⛔ ถ้าสาย mobile (Android) จะเริ่มแตะ `EditorMode.vue` → ต้องคุย PM ก่อน (สายนี้ยังเปิดค้าง B056)
- ยังไม่ merge · ไม่ deploy · commit `9e8e864` (B055)
