# B113 — คืนค่า "ชื่อเพลงเต็ม ๆ" ที่หายไปโดยอุบัติเหตุ

**Branch:** `restore-full-song-names` (แตกจาก `origin/main` = `c7f60a3`)
**Network URL (ทดสอบมือถือจริงได้):** <http://192.168.1.124:5391/>

## เรื่องย่อ

commit `ce1022e` (17 ก.ค.) ทำให้ชื่อเพลงแสดงเต็มและรายการกว้างพอดีชื่อที่ยาวสุด
วันเดียวกันเว็บถูก force-reset กลับ `2f4177e` เพราะเรื่องแถบเครื่องมือ — ของชิ้นนี้
เลยโดนถอยติดไปด้วยทั้งที่ไม่เกี่ยวกัน งานนี้เอา **เจตนา** กลับมาใส่ทับโค้ดปัจจุบัน
(ไม่ได้ revert ไฟล์ — `SongList.vue` ถูกแก้มาอีก 3 รอบหลังจากนั้น)

## สิ่งที่เอากลับมา (CSS ล้วน · ไม่แตะ markup / logic)

ไฟล์เดียว: `src/views/SongList.vue`

| file:line | เดิม | ใหม่ |
|---|---|---|
| `SongList.vue:385` | `.song-list { … contain: inline-size; }` | `.song-list { … width: fit-content; max-width: 100%; }` |
| `SongList.vue:389` | `.song-row { align-items: center; }` | `align-items: flex-start;` |
| `SongList.vue:403-407` | (ไม่มี) | `.no, .ref, .key, .row-status { align-self: flex-start; }` — เกาะบรรทัดแรกเมื่อชื่อขึ้นบรรทัดใหม่ |
| `SongList.vue:417-420` | `.ttl { overflow:hidden; text-overflow:ellipsis; white-space:nowrap; }` | `.ttl { white-space: normal; overflow-wrap: anywhere; }` |
| `SongList.vue:448-449` | คอมเมนต์ `.row-status` อ้าง "title truncates" | แก้คอมเมนต์ให้ตรงพฤติกรรมใหม่ |

**ต่างจากของเดิม:** เพิ่ม `.ref` เข้าไปในกลุ่ม `align-self: flex-start` ด้วย — `.ref`
เพิ่งมีทีหลัง `ce1022e` ถ้าไม่ใส่มันจะลอยกลางแถวที่ชื่อ 2 บรรทัด

## ตารางวัดจริง (วัดในเบราว์เซอร์จริง · `getBoundingClientRect` / `getComputedStyle`)

ข้อมูลจริงจาก Supabase · เล่ม "อนุชน" 54 เพลง · ชื่อยาวสุด "หากสิ่งนี้ให้ท่านได้รับประโยชน์มากมาย" (37 ตัวอักษร)

### ชื่อเพลงจริง

| viewport | มี `…` ? | h-scroll ? | ความกว้างรายการ | แถวเท่ากันทุกอัน | ความสูงแถว (ต่ำสุด–สูงสุด) | แถวที่ขึ้นบรรทัดใหม่ |
|---|---|---|---|---|---|---|
| **360** | ไม่มี (0/54) | ไม่มี (345/345) | 321 px | ✅ ทุกแถว 321 | 55 – 84 | 24 แถว |
| **412** | ไม่มี (0/54) | ไม่มี (397/397) | 373 px | ✅ ทุกแถว 373 | 55 – 84 | 9 แถว |
| **768** | ไม่มี (0/54) | ไม่มี (753/753) | 591 px | ✅ ทุกแถว 591 | 55 – 55 | 0 |
| **1280** | ไม่มี (0/54) | ไม่มี (1265/1265) | 591 px | ✅ ทุกแถว 591 | 55 – 55 | 0 |

`.ttl` computed ทุกความกว้าง = `white-space: normal | overflow-wrap: anywhere | text-overflow: clip`

### A2 stress — ชื่อคำเดียวยาว 50 ตัวอักษร ไม่มีช่องว่าง

ใส่ token ไทยติดกัน 50 ตัว (ยืนยัน `length = 50`, `/\s/ = false`) ลงแถวแรก

| viewport | h-scroll ? | มี `…` ? | ความกว้างรายการ | ความสูงแถวที่โดน stress | ขอบขวาแถว |
|---|---|---|---|---|---|
| **360** | ไม่มี (345/345) | ไม่มี | 321 px | 170 px | 333 ≤ 360 ✅ |
| **412** | ไม่มี (397/397) | ไม่มี | 373 px | 112 px | 385 ≤ 412 ✅ |
| **768** | ไม่มี (753/753) | ไม่มี | 700 px | 55 px | 727 ≤ 768 ✅ |
| **1280** | ไม่มี (1265/1265) | ไม่มี | 700 px | 55 px | 983 ≤ 1280 ✅ |

ชื่อยาวทำให้แถว **สูงขึ้น ไม่กว้างขึ้น** ตามเจตนา · ที่ 768/1280 รายการขยาย 591→700 px
เพราะแถว stress กลายเป็นแถวที่ยาวสุด = พฤติกรรม "กว้างพอดีชื่อที่ยาวสุด" ทำงานถูกต้อง

### พิสูจน์ว่าของหายจริง (วัด before/after ที่ 1280)

ฉีด CSS เดิมกลับเข้าไปในหน้าเดียวกันแล้ววัดซ้ำ

| | ความกว้างรายการ | ชื่อที่โดนตัดหาง | `.ttl` computed |
|---|---|---|---|
| **ก่อน (CSS เดิมบน main)** | **306 px** | **41 / 54 แถว** | `nowrap · text-overflow: ellipsis` |
| **หลัง (แก้แล้ว)** | **591 px** | **0 / 54 แถว** | `normal · overflow-wrap: anywhere` |

ตัวเลข 306 px ตรงกับที่ commit message ของ `ce1022e` ระบุไว้เป๊ะ = ยืนยันว่าวินิจฉัยถูกและของหายไปจริง

## A5 — ของที่มาทีหลัง ไม่ถอยหลัง

วัดที่ 360 (แคบสุด = เคสหนักสุด)

| ของ | ผลวัด |
|---|---|
| badge สถานะรีวิว | `align-self: flex-start` · `flex: 0 0 auto` · สูง 24 px (ไม่ถูกบีบจนตกบรรทัด) · อยู่บรรทัดแรก (offset 13 px) · ขอบขวาไม่ล้นจอ |
| ตัวนับ verified | `.count.progress` มาร์กอัปเดิม ไม่ถูกแตะ · `.count.muted` = "54 เพลง" ✅ |
| header/menu สไตล์พระคำ | `.level-head h2` = "อนุชน" · `.crumb` = "← เล่มทั้งหมด" ✅ |
| เลขเพลง | `text-align: right` · `font-variant-numeric: tabular-nums` · สี `rgb(139,69,19)` ✅ |
| คีย์ | "คีย์ E" · `white-space: nowrap` · `align-self: flex-start` ✅ |
| `.ref` | 360 = `display:none` · 768 = แสดง + `nowrap` + `ellipsis` (เหมือนเดิม) ✅ |
| Level 1 bookshelf | 3 เล่ม · "3 เล่ม" · ไม่มี h-scroll ✅ |

> **หมายเหตุความซื่อสัตย์:** badge เป็น team-only (`v-if="loggedIn"`) — session นี้ล็อกอินไม่ได้
> จึงพิสูจน์โดย**ฉีด element ที่มี `data-v-e1f70de1` (scoped-style attribute เดียวกับที่คอมโพเนนต์ปล่อยออกมา)**
> แล้ววัดจริง รอบแรกฉีดโดยไม่ใส่ attribute นี้ → CSS scoped ไม่ match → วัดได้ผลผิด (สูง 86 px)
> ได้แก้วิธีทดสอบแล้ว ตัวเลข 24 px ข้างบนคือค่าที่ถูก **ยังไม่ได้เห็นด้วยตาในสถานะล็อกอินจริง**

## A6 — ขนาดพื้นที่กด

`min-height: var(--touch-min)` = 44 px · วัดจริงแถวต่ำสุด = **55 px** ทุกความกว้าง
เกิน WCAG 2.2 AA (24 px) และเท่าเดิมกับที่ `ce1022e` เคยวัดได้ (55 px)

ไม่มีการใช้ `@media (hover)` หรือ pointer-type gate ในงานนี้เลย

## A7 — เทสต์ + build

- `npx vitest run` → **733 tests ผ่าน · 68/69 suite ผ่าน**
  suite เดียวที่ fail = `src/lib/notationLint.test.mjs` (เรียก `process.exit` เอง) = pre-existing บน base ไม่ใช่ของงานนี้
- `npm run build` → **ผ่าน** (built in 2.03s)

## วิธีวัด (เพื่อให้ตรวจซ้ำได้)

`resize_window` ของ Chrome รายงาน success แต่ `window.innerWidth` ไม่เปลี่ยน (ยังเป็น 2560)
จึงวัดผ่าน **iframe same-origin ที่ตั้งความกว้างเอง** — แอปข้างในได้ viewport จริง
media query ทำงานจริง ไม่ใช่การคำนวณเอา · ชดเชยความกว้าง scrollbar จน `innerWidth`
ตรงเป้าเป๊ะ (360 / 412 / 768 / 1280)

`screenshot` ไม่ได้ใช้ — พิสูจน์ด้วยตัวเลขวัดล้วน

## ความเสี่ยง

1. **แถวสูงขึ้นบนมือถือ** — ที่ 360 มี 24/54 แถวสูงจาก 55 → 84 px รายการยาวขึ้น
   ต้องเลื่อนมากขึ้น เป็นผลที่ตั้งใจ (P'Aim ขอเห็นชื่อเต็ม) แต่ถ้าไม่ชอบต้องกลับมาคุย
2. **สถานะล็อกอิน (ทีม) แถวสูงกว่านี้อีก** — badge กินความกว้างจากชื่อ วัดได้ 141 px
   ที่ 360 ในแถวชื่อยาว ยังไม่มี h-scroll และ badge ไม่แตก แต่ควรให้คนที่ล็อกอินได้ดูจริงสักรอบ
3. **`width: fit-content` ผูกกับ `main.container` ที่เป็น flex item** (`flex: 1 0 auto; min-width: 0`)
   ถ้าอนาคตมีใครแก้ layout ของ shell ต้องวัดหน้านี้ซ้ำ
4. ไม่ได้แตะ `src/lib/notation.js` และ `src/components/NoteRow.vue` เลย (session ขนานที่ `pleng-beam` ถืออยู่)
