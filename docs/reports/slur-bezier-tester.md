# รายงาน Tester — B076 slur/tie Bézier (`slur-bezier`)

**ตรวจโดย:** tester (`tester-qa`) · **ของ:** dev branch `slur-bezier` `c202e13` (แตะ `NoteRow.vue` เดียว) · **ping:** pm7
**วิธีตรวจ:** vitest + audit คณิต + **วัด geometry จริงในเบราว์เซอร์** (dev server `http://127.0.0.1:5376`, ยืนยัน bundle = slur code จริงก่อนวัด)

---

## VERDICT: ✅ ผ่าน — ไม่พบข้อบกพร่อง
เหลือ **1 ข้อที่ผมตรวจแทน P'Aim ไม่ได้** = พิมพ์ PDF จริงบนกระดาษ (กฎเดิม: DOM ไม่พิสูจน์หน้ากระดาษ) → ไม่ใช่ defect · ทุกอย่างก่อนถึงกระดาษเขียวหมด → เสนอ pm7 ให้ P'Aim ดูจอ + สั่ง print 1 ครั้งเป็นด่านท้าย

---

## ตารางผล (✓/✗ · auto/manual · หลักฐาน)

| # | ข้อ (pm7) | วิธี | ผล | หลักฐาน |
|---|---|---|---|---|
| 1a | vitest ไม่ regress | auto | ✅ | **292 passed** (ฐาน+4 · NoteRow 11/11) · "1 failed file" = `notationLint` `process.exit` quirk เดิม ไม่เกี่ยว |
| 1b | slur สั้น (2–3 โน้ต) โค้งเรียบ | manual (วัด path จริง) | ✅ | Guide 8 arc · เช่น W=33 → `d` control ที่ 12/21 = taper 8px (cap 0.32·span) · closed lens · a<b ไม่ม้วน |
| 1c | **slur ยาว (เอื้อน 8 โน้ต) โค้งเดียว ปลายเรียว ไม่บิด/ไม่แบน/ไม่ทู่** | manual (วัด path จริง) | ✅ | พิมพ์ `(1 2 3 4 5 6 7 1')` วัดจริง W=106/112 → **taper = 26px เป๊ะทั้ง 2 ข้าง** · viewBox=clientWidth 1:1 · lens ปิด สมมาตร · a<b (30<76/82) ไม่ loop/ไม่บิด |
| 1d | tie ในห้อง + ข้ามห้อง เรียบ | manual (วัด path จริง) | ✅ | tie 2 ครึ่ง (start/end) mirror กัน · W=19/20 → viewBox 1:1 · path ปิด |
| 2 | ไม่พังการ render โน้ต/คอร์ด/เนื้อ | auto+manual | ✅ | 292 test · console **0 error** (แผ่นเพลง/แก้ไข) · โน้ต 1–7 render ครบ |
| 3 | print PDF เส้นไม่บิด | manual (บางส่วน) | ⏳ | `beforeprint` re-measure ทำงาน ไม่ throw · หลัง event: taper 26/26 · viewBox 1:1 คงเดิม · **กระดาษจริง = ต้อง P'Aim print** |
| 4a | ไม่ลด contrast | manual (วัดสี) | ✅ | fill `#2B6CB0` บนพื้นขาว = **~5.5:1** (เกิน 3:1 กราฟิก · เกิน 4.5:1 ด้วย) |
| 4b | ไม่ทับตัวโน้ต / a11y | manual | ✅ | arc `aria-hidden="true"` (decorative ถูกต้อง) · อยู่แถว arc เหนือเลขโน้ต ไม่คลุมตัวเลข · ไม่มี a11y ใหม่ |

**3 หน้า (ฝึกร้อง/แผ่นเพลง/แก้ไข):** slur วาดด้วย **NoteRow.vue ตัวเดียวกันทั้ง 3 หน้า** · ตรวจจริงบนหน้าแก้ไข (studio) + แผ่นเพลง (ดูผลทั้งเพลง) → geometry ถูก · console 0 error · หน้าฝึกร้องใช้ component เดียวกัน (dev ยืนยันโหลด 3 หน้าไม่ error)

---

## หลักฐาน geometry (วัดจาก DOM จริง — path `d` คือรูปเส้นจริง ไม่ใช่ proxy)

**สั้น** (Guide, W=33): `M4,33 C12,3 21,3 29,33 C21,17 12,17 4,33 Z` · taper 8px · viewBox `0 0 33 40`
**ยาว** (เอื้อน 8, W=112): `M4,33 C30,3 82,3 108,33 C82,17 30,17 4,33 Z` · **taper 26px** · viewBox `0 0 112 40`

> หัวใจของ fix พิสูจน์แล้ว: **taper คงที่ 26px ทุกความกว้าง** (span 76→106→112 ได้ 26px หมด) → ปลายเรียวคมเท่ากันเสมอ, เฉพาะช่วงกลางยืด (แบบ slur ยาวจริงในโน้ตเพลง) · viewBox = ความกว้างจริง → แกน x 1:1 ไม่ถูกยืด (ต้นเหตุ warp เดิม) · `cx = min(26, 0.32·span)` การันตี a<b เสมอ (0.32<0.5) → ไม่มีวันม้วนทับ

## audit คณิต (NoteRow.vue `slurArc`)
- lens ปิด: outer edge (control y=3) apex ~y=10.5 · inner edge (y=17) ~y=21 · ปลาย 2 ข้าง y=33 → หนากลาง เรียวปลาย ✅
- ค่า y คงที่ทุกความกว้าง → ความสูงโค้ง/ความหนาไม่เปลี่ยน ✅
- tie: controls 0.3W/0.7W (scale ตาม W) — กล่องเล็กคงที่ ~1em ไม่เคยเป็นเคส warp ✅

## หมายเหตุ / ขอบเขต
- **จำกัด jsdom:** 4 เทสต์ของ dev รันบน jsdom (clientWidth=0) → ทดสอบ **fallback W=84** ไม่ใช่ความกว้างจริง · เคสยาวจริงพิสูจน์ได้เฉพาะในเบราว์เซอร์ (ทำแล้วในรายงานนี้) · แนะนำ: ถ้าอยากให้เคสยาว auto ใน CI = ต้อง browser-mode (ผูกกับข้อเสนอ Tier-B ของ DockKey)
- **B069 cross-bar overlay** (`SongSheet.vue`) เป็นคนละกลไก dev ไม่ได้แตะ (อยู่ในรั้ว) — ถ้า overlay ข้ามห้องยาวๆ บิดด้วย = งานแยก แจ้ง pm7
- ผม author slur ทดสอบในหน้าแก้ไขเพื่อวัด **ไม่ได้ save** ลง DB

## next
- pm7 → ให้ P'Aim ดูจอ (สั้น/ยาว/tie) + **สั่ง Print → PDF 1 ครั้ง** (เพลงมีเอื้อน) ดูเส้นบนกระดาษ → ถ้าโอเค = merge ได้
- ผ่านด่านโค้ด/เรนเดอร์ครบ · ไม่มี ✗
