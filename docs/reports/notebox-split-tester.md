# รายงาน Tester — B084 ช่องโน้ต space ตัดตรงเคอร์เซอร์ (notebox-split)

**ตรวจโดย:** tester (`tester-qa`) · **ของ:** `notebox-split` `5c25a65` → **scope ขยาย `3029372`** (NoteBoxes.vue+test · vitest 337) · **ping:** pm7
**วิธี:** vitest + วัดจริง Browser MCP (`5415` studio + เพลง 2 · ช่องโน้ตจริง)

---

## 🔁 SCOPE ขยาย — space = แทรก/ดันขวาทุกกรณี (ripple · `3029372`) = ✅ ผ่านเต็ม
space ในช่องโน้ต ทำงานเหมือนช่องเนื้อทุกกรณี:
| เคส | คาดหวัง | ผลจริง | ✓ |
|---|---|---|---|
| caret กลาง "345" | `["34","5"]` | `["34","5"]` | ✅ |
| **caret ท้าย → แทรกช่องว่าง+ripple** | "1 2 3" space ท้าย "2" → `["1","2","","3"]` | `["1","2","","3"]` · โฟกัสช่องว่างใหม่ | ✅ |
| caret ต้น → ดันทั้งก้อนขวา | `["","1",…]` | `["","1","2","","3"]` (นำหน้าด้วยว่าง) | ✅ |
| "4#5" | `["#4","5"]` | `["#4","5"]` | ✅ |
| **⭐ lyric ไม่เพี้ยนตอนแทรกช่องว่างโน้ต** | badge จับคู่คงเดิม | แทรกช่องว่าง (79→80 box) · **badge จับคู่ 3 ข้อคงเดิมเป๊ะ ก่อน=หลังแทรก=หลัง sync** (37/41·41/49·18/49) → ช่องว่างถูก filter · เนื้อไม่เลื่อน | ✅ |
| vitest · console | 337 · 0 | **337 passed** · console 0 | ✅ |

**สรุป ripple:** แทรก/ดันขวา/ต้น/กลาง ถูกทุกกรณี · **ช่องว่างโน้ตไม่ทำเนื้อเลื่อน** (NoteBoxes string ล้วน · filter ว่างตอน sync · pairing คงเดิม — ยืนยันบนเพลง 2 จริง)

### 🔀 FINAL SCOPE — Backspace/Delete = merge (ช่อง `b7ca253` · vitest 337) = ✅ ผ่านเต็ม
| เคส | คาดหวัง | ผลจริง | ✓ |
|---|---|---|---|
| **Backspace ต้นช่อง = รวมเข้าช่องก่อน** | "1 23 45" BS ต้น "23" → `["123","45"]` | `["123","45"]` | ✅ |
| **Delete ท้ายช่อง = ดึงช่องถัดมารวม** | Del ท้าย "123" → `["12345"]` | `["12345"]` | ✅ |
| **merge ไม่รัน fixAccidental (raw concat)** | `["4","#5"]` BS-merge → `["4#5"]` (ไม่ใช่ "#45") | `["4#5"]` | ✅ |

**สรุป final:** โน้ตแก้ลื่นเหมือนช่องเนื้อครบชุด — space แทรก/ripple · Backspace/Delete รวมช่อง · merge เก็บ raw ไม่ normalize # · เนื้อไม่เพี้ยน · vitest **337** · console 0

---

## VERDICT (เวอร์ชันแรก `5c25a65`): ✅ ผ่านเต็ม

| เคส | คาดหวัง | ผลจริง | ✓/✗ |
|---|---|---|---|
| "345" · caret หน้า "5" · space | `["34","5"]` โฟกัส "5" | boxes=`["34","5"]` · **focus index 1 = "5"** | ✅ |
| caret ท้าย + space | ไปช่องถัดไป ไม่ตัด | box count คงที่ (2→2) · **focus ย้ายไปช่องถัดไป "5"** · ไม่มีช่องใหม่ | ✅ |
| "4#5" · caret หน้า "5" · space | `["#4","5"]` (# นำหน้าเลข) | `["#4","5"]` · **normalize # มาก่อนเลขถูก** | ✅ |
| no-regress backspace | ไม่ตัด/ไม่ error | box count ไม่เพิ่ม (ไม่ split ผิด) | ✅ |
| no-regress ArrowRight | ไม่ตัด | box count คงที่ | ✅ |
| vitest · console | 334 · 0 | **334 passed** · console 0 error | ✅ |

**สรุป:** space ที่กลางเคอร์เซอร์ตัดช่องถูก (ก่อน/หลัง caret) + โฟกัสช่องหลัง · space ท้ายช่อง = ไปช่องถัดไป (ไม่ตัด) · normalize `#` ก่อนเลขถูก · arrow/backspace ไม่ regress

## หมายเหตุ
- paste ไม่ได้ทดสอบเชิงลึก (เป็น input event มาตรฐาน · การแก้นี้แตะเฉพาะ keydown space → ไม่กระทบ paste) — ถ้าอยากชัวร์ ฝาก P'Aim วางทับ 1 ที
- ไม่ได้ save

## next
- pm7 → B084 บนจอเขียวครบ · **merge อิสระได้** (ตามลำดับที่ pm7 วางไว้)
