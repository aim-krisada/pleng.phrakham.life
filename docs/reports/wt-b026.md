# รายงาน — B026 lint R4-R7 (dev algorithm)

**branch:** `wt-b026` (ฐาน `studio-shell-redesign` @ `9baddb8`) · **commit:** `24593eb`
**ไฟล์ที่แตะ:** `src/lib/notationLint.js` + `src/lib/notationLint.test.mjs` เท่านั้น (ตามขอบเขต brief)

## ทำอะไร
เพิ่ม 4 กฎ (R4-R7) ต่อจาก R1-R3 เดิม · ทุกกฎเป็น pure/UI-agnostic (ตรวจ "สัญลักษณ์+โครงสร้าง" ไม่ตัดสินทำนอง/คอร์ด/คีย์) · import `beatCount` จาก notation.js เฉยๆ ไม่แก้

| กฎ | code | เตือนเมื่อ | ระดับ |
|---|---|---|---|
| **R4** เอื้อนแยกช่อง | `slur-crosses-bar` | วงเล็บเอื้อน `( )` เปิด-ปิดไม่ครบในห้องเดียว (เอื้อนลากข้ามห้อง หรือวางวงเล็บผิด) | warning |
| **R5** ไทคนละเสียง | `tie-cross-pitch` | ไท `~` เชื่อมโน้ต **คนละขั้น/คนละออกเทฟ** (ไทใช้ต่อเสียงเดียวกันเท่านั้น · คนละเสียง = ต้องใช้เอื้อน) | warning |
| **R6** ตัวหยุดมี #/จุด | `rest-decorated` | ตัวหยุด `0` ติด `#/b/♮` หรือจุดออกเทฟสูง-ต่ำ หรือจุดเพิ่มค่า (ตัวหยุดไม่มีระดับเสียง) | warning |
| **R7** #3/b4/#7/b1 | `accidental-not-in-scale` | `#3`, `b4`, `#7`, `b1` — ชาร์ป/แฟลตที่จริงๆ คือขั้นข้างเคียง (ขั้น 3-4 และ 7-1 ห่างครึ่งเสียง) | warning |

### หมายเหตุการออกแบบ (กันเข้าใจผิด)
- **R5** เทียบเฉพาะ **ขั้น+ออกเทฟ** (`degreeKey`) ไม่เทียบ accidental — เพื่อไม่ false-fire กรณีไทสืบทอดเครื่องหมาย (`#1~ ~1` = ไม่เตือน)
- **R6** ครอบทั้ง accidental / จุดออกเทฟ / จุดเพิ่มค่า ตามที่ brief ระบุ ("rest ห้ามมี sharp/dot") · ถ้า P'Aim อยากอนุญาต "ตัวหยุดจุด" (dotted rest ยืดค่า) ค่อยผ่อนภายหลัง
- **R7** octave-independent (`#7'` ก็เตือน) · เตือนเฉพาะ 4 คู่ที่ enharmonic เป็นขั้นธรรมดา — `#4`, `b3` ฯลฯ (accidental ที่มีจริง) ไม่เตือน
- ทุกกฎรันแม้ห้องมี token อ่านไม่ได้ (เหมือน R1 natural เดิม) · มีเฉพาะ R2 beats ที่ถูกระงับเมื่อ unreadable

## Verify
- `node src/lib/notationLint.test.mjs` → **51 passed, 0 failed** (21 เดิม + 30 เคสใหม่ R4-R7 รวมเคส clean-bar รวม)
- `npm run build` → **ผ่าน** (115 modules · built in ~1.5s)
- scope: `git status` = แตะแค่ 2 ไฟล์ที่อนุญาต · ไม่แตะ notation.js / midi.js / NoteRow.vue / Guide.vue / songSearch.js

## ยังไม่ทำ (ตาม brief = logic ล้วนรอบนี้)
- ยังไม่เสียบเข้า UI/EditorMode (เป็นงานต่อ กันชน mobile/editor)

## merge
- **รอ B027 merge ก่อน** (B027 กำลังเปลี่ยน `beatCount` ใน notation.js) · ถ้าฐานขยับ → rebase `wt-b026` บน studio-shell-redesign ใหม่ แล้วรัน test ซ้ำก่อน merge
- ห้าม merge main / deploy
