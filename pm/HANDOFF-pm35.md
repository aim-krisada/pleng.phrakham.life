# HANDOFF — pm35 → PM คนถัดไป (2026-07-20)

อ่านไฟล์นี้ + `docs/pm/pm.md` + `docs/pm/board.md` แล้วทำต่อได้ทันที
**ตั้งชื่อตัวเอง `pm36`** แล้วอัป `board.md §🎯`

> ⚠️ **P'Aim สั่งหยุดงานทั้งหมด 20 ก.ค. เย็น** — ไม่พอใจคุณภาพงาน PM (ครั้งที่ 2)
> **ห้ามเดินหน้าอะไรจนกว่า P'Aim สั่ง** · P'Aim กำลังพิจารณาเปลี่ยนไปใช้เครื่องมืออื่น
> ทุกอย่างอยู่ใน git + OneDrive แล้ว ยกไปให้เครื่องมือไหนก็ทำต่อได้ ไม่ต้องเริ่มหนึ่ง

---

## 1. LIVE / GIT — สถานะจริง
- **LIVE = รอบ 35 `94199d6`** (bundle `assets/index-0T7X4x_B.js`) — **ไม่มีอะไรเปลี่ยนหลังจากนั้น**
- **`origin/main` = `94199d6`** ยืนยันแล้ว · **B118 ยังไม่ขึ้นเว็บ**
- ⚠️ **verify ด้วย commit stamp ไม่ใช่ชื่อไฟล์** — GH Actions build ได้ hash คนละตัวกับ build ในเครื่อง
  วิธี: `curl` หน้าแรก → หา `assets/index-*.js` → `grep <commit>` ในตัว bundle
- **⛔ ห้าม merge main / deploy จน P'Aim สั่ง**
- deploy = merge เข้า main ผ่าน **worktree ที่ track main** (ห้าม commit main จาก dir หลัก — dir หลักอยู่ `studio-shell-redesign`)

## 2. 🔄 DOING — B118 เส้นโค้ง tie/slur (ค้างกลางอากาศ · ต้องตัดสินก่อน)

**โจทย์ (พี่เปา):** *"เส้น slur โค้งไม่ต่อเนื่องกัน"* — โค้งข้ามห้องขาดเป็น 2 ท่อน
รูป: `OneDrive/4 Personal/pleng.phrakham.life/pleng2-pow-bug-report/isues24/`

**สถานะ:** โค้ดเสร็จ · **merge ไว้ในเครื่องเท่านั้น ยังไม่ push** · branch `b118-slur-continuous` (`f303b26` fix + `661ebd0` report)

**🔴 ข้อมูลขัดกัน — ยังไม่มีใครชี้ขาด:**
| ใคร | บอกว่า |
|---|---|
| dev | แก้แล้ว · #12 ตอ 15px×2 → เส้นเดียว 360.2px |
| tester | **PASS** · base ที่ 360 = ตอ 15px 6 อัน → fix = ตอ 0 · หาเคสเองเจอโค้งเดียว 6 เส้น 4 เพลง (#107 #100 #111 #8) |
| **PM (ผมเอง)** | **วัด #110 ที่ 360 และ 1280 → ยังเห็นเป็นตอสั้น 15px** ไม่เห็นเส้นต่อเนื่อง |

**ทำไม PM ถึงสรุปไม่ได้:** เบราว์เซอร์ agent `visibilityState:hidden` · rAF ไม่ยิง → โค้ดประเภท "วัดแล้ววาดทับ" (ซึ่ง overlay นี้ใช่) เชื่อผลไม่ได้
**tester อาจถูก** (เขาตั้ง server base เทียบจริง) **หรือ PM อาจถูก** — **ต้องให้คนเปิดจอจริงดู 1 ครั้ง จบ**

**🔴 คำเตือนสำคัญ — อย่าส่งงานนี้ด้วยประโยค "ต่อเนื่องเป็นเส้นเดียวแล้ว":**
tester ระบุว่า tie ข้ามห้องในคลัง **ส่วนใหญ่ตกที่รอยต่อห้องคี่→ห้องคู่ = คนละแถวพอดี** → #14 #12 #95 #13 #118 #1 #110 **ยังเห็นเป็น 2 ท่อน** (แบ่งครึ่งตามหลัก engraving = ถูกต้อง)
**บนมือถือ 1 ห้อง/แถว → ทุกเส้นเป็น 2 ครึ่งโดยโครงสร้าง** — พี่เปาจะเห็น "ยังขาดอยู่" ถ้าไม่อธิบายให้ตรง

**ยังไม่ได้ตรวจ:** พิมพ์ PDF จริง (ยืนยันได้แค่ CSSOM ว่า `.ed-arc-layer` มี `no-print`)
**tester ตั้งข้อสังเกต:** โค้งข้ามการ์ด **แบนมาก** — กว้าง 312px โก่งแค่ 9.4px = **3%** (slur ในห้องโก่ง ~18%) · สูตรเดียวกับแผ่นเพลง ไม่ใช่ของใหม่ที่พัง แต่การ์ดหน้าแก้ไขกว้างกว่าเลยแบนกว่า · **ถ้าพี่เปาว่าแบนไป แก้ที่ `slurArcs.js` จุดเดียว จบทั้ง 2 หน้า**

**สิ่งที่ทำไปแล้ว (ถ้าจะไปต่อ):** สร้าง `src/lib/slurArcs.js` ใช้ร่วม 2 หน้า · `NoteRow.vue`/`notation.js`/`midi.js` **ไม่ถูกแตะ 0 บรรทัด** (beam รอบ 35 ปลอดภัย) · MIDI เท่าทุกไบต์ · 766 tests + build ✓ · กวาดคลัง 111 เพลง → **173 จุด · 21 เพลง (2 slur + 171 tie)** · **เพลงร่างไม่ถูกกวาด** (เคสในรูปพี่เปาเองก็เป็นเพลงร่าง หาในคลังไม่เจอ)

**3 ทางเลือก:** (ก) ให้คนดูจอจริง → ผ่านก็ deploy (ข) ทิ้ง branch เริ่มใหม่ (ค) พักไว้ ทำ B112 ก่อน

## 3. 📋 TO DO

| # | งาน | ที่มา | หมายเหตุ |
|---|---|---|---|
| 1 | **B112 เล่นต่อตอนปิดจอ + คุมจากจอล็อก** | **P'Aim ยืนยันเอง + ส่งรูป** | ยังไม่แตะเลย · ดู §4 |
| 2 | **B117 ถามพี่เปา** — โน้ตชุดเร็วอยากให้มัดติดกันไหม | PM เสนอ | คลังมัดคานแค่ **10.9%** · มัดตามจังหวะจะได้ **68.7%** · **ไม่ใช่บั๊ก มี golden test ที่พี่เปาเคยขอ ห้ามเปลี่ยนเองโดยไม่ถาม** |
| 3 | **B115** กด back จากหน้าเพลง เด้งกลับชั้นหนังสือ แทนกลับรายการในเล่ม | tester เจอ | pre-existing |
| 4 | **B108 quirk** เลือก "— ไม่ระบุธีม —" → เผยแพร่แล้วธีมเก่ากลับมา | รอบก่อน | ต้องทำ tri-state |
| 5 | ชิป fermata ทับแป้นตอนโน้ตอยู่ล่างสุดจอเตี้ย | รอบก่อน | ไม่ใช่ regression |
| 6 | `𝄐` บนมือถือขึ้นเป็นกล่อง □ ไหม | รอบก่อน | **กระทบของที่ live แล้ว** · ถ้าเป็นกล่องต้องเปลี่ยนเป็น SVG |
| 7 | **งานใหญ่ยังไม่แตก** — ลูกคู่ multi-voice (ไม่มีในโมเดลเลย) · D.C./Fine/Segno ให้เล่นกระโดดจริง · measure grid · DockKey redesign | คุยกับ G ไว้ | ดู `docs/reports/current-editor-audit.md` · **file เข้า backlog ก่อนแตกงาน อย่าให้บานปลาย** |
| 8 | **DockKey ใหม่** `dock-resize` `f8d77a3` ค้าง ไม่ deploy | — | **P'Aim: ไม่อยากใช้จนกว่าจะนิ่ง** · DockKey = core แชร์พระคำ ถ้า deploy ต้อง gate 2-host ประสาน pk pm |

## 4. B112 — รายละเอียดที่ต้องรู้ก่อนเริ่ม
- P'Aim: *"อยากได้คล้ายตามภาพ"* (จอล็อก: ชื่อ + รูปปก + แถบเวลา + ก่อนหน้า/เล่น/ถัดไป) · แจ้งผ่าน pk pm9
- **ยืม lib กลางพระคำ `assets/pk-audio.js`** (UMD `window.PKAudio` · ไม่ต้องแก้ build · option `media` = ชื่อบนจอล็อกของเราเอง) = **สลับข้างกับ DockKey ที่เราเป็นเจ้าของ**
- 🔴 **ไม่ใช่ drop-in — pk pm9 เตือนเอง ห้ามรับปาก P'Aim ว่าง่ายก่อนสำรวจ:** พระคำ = mp3 อัดไว้ล่วงหน้า · **เราสังเคราะห์สดตอนกด** (`src/lib/audioExport.js`) + ไฮไลต์ **index-based** (`onNote(note, idx)`) ไม่ใช่ time-based → ต้องเลือก: ป้อน blob + timing map เอง **หรือ** ตัดไฮไลต์ เอาแค่ background + lock screen
- **อ่านก่อน scope:** `pm/design-audio-mp3.md` + `pm/report-audio-player.md` (repo พระคำ)
- **ขอแก้ lib ผ่าน pk pm9 เท่านั้น** ห้ามแก้ไฟล์เขาตรง
- 💡 MediaSession **ไม่ต้อง https** (LAN ได้) · ⚠️ ต้องเสิร์ฟด้วย server รองรับ **Range** (`py -m http.server` ไม่รองรับ → แถบเลื่อนเด้ง 0 เหมือนพัง · เขามี `tools/range_server.py`)
- 🤝 **เขาขอตอบแทน 2 อย่าง (ของเรา):** (1) seek bar เข้าถึงได้ (keyboard + `aria-valuetext`) ที่ `SingTransport.vue` ยังไม่มี (2) `aria-label` ปุ่ม ▶ ค้างเป็น `it.name` ตอนเล่น — อยู่ใน `DockKey.vue`

## 5. ✅ DONE — รอบ 35 ขึ้นเว็บแล้ว (20 ก.ค.)
| ของที่ขึ้น | ยืนยัน |
|---|---|
| **B111** เส้นขเบ็ดถูกชั้น (1 ชั้น=1 เส้น · 2 ชั้น=2 เส้น · ขีดสั้น partial beam) | PM วัดบน live: `u1` 1 เส้น · `u2` 2 เส้น · แก้ **9 จุด · 4 เพลง #7 #89 #101 #120** |
| **B113** ชื่อเพลงเต็มบนการ์ด (ของที่หายตอน rollback 17 ก.ค.) | ตัดหาง 0/54 · รายการ 306→591px · **P'Aim ดูแล้ว OK** |
| **B114** เลิกพึ่ง rAF (IntersectionObserver) | ประกัน — **ไม่ใช่บั๊กที่พิสูจน์ได้** ห้ามเคลมว่าแก้บั๊กผู้ใช้ |

**🙏 รอ P'Aim/พี่เปายืนยันบน live:** พี่เปาเปิด 4 เพลงที่แก้ · พิมพ์ PDF จริง · ชื่อเพลงบนมือถือ (360 + ล็อกอิน รายการยาวขึ้น **+60.8%** — P'Aim เห็นตัวเลขแล้วสั่ง deploy · ถ้าพี่เปาบ่น ทางแก้ = จำกัด 2 บรรทัดบนมือถือ)

## 6. 🔴 ทำไมช้า — ตัวเลขจริง (P'Aim ถามตรง)
ทีมทำงานรวม **≈ 3 ชั่วโมง**
| ไปกับอะไร | เวลา | |
|---|---|---|
| ทีมเขียนโค้ด 3 งาน | 89 นาที | คุ้ม |
| **คนตรวจซ้ำ 3 รอบ** | **73 นาที** | **40% ของเวลาทั้งหมด** |
| **ไล่บั๊กที่ไม่มีจริง** (B114 คานหาย + B116 PDF) | **17 นาที** | **PM สั่งเอง เสียเปล่า** |

**คอขวด = การตรวจซ้ำ** (คนที่สองพิสูจน์ใหม่ทั้งหมด ใช้เวลาเกือบเท่าคนทำ)
P'Aim เคยสั่ง **"เอาแบบเร็ว + คนตรวจ"** → PM ปรับให้ tester ตรวจแค่ 4 ข้อแกน ที่เหลืออ้างอิงตัวเลข dev (บันทึกว่า "ไม่ได้ตรวจซ้ำ" ห้ามเขียน PASS)

## 7. 🔴🔴 บทเรียน — PM พลาด 3 เรื่องในวันเดียว (P'Aim: *"ครั้งที่ 2 ที่ claude code fucked up"*)
เขียน memory ถาวรแล้ว **3 ไฟล์ · sync OneDrive แล้ว**

### (ก) `pleng-compare-to-model-not-intuition`
PM เปิด PDF นับเส้นได้เลขถูกทุกตัว แล้ว**สมมติเองว่า "ขีดใต้ติดกัน = ต้องมีคานเชื่อม"** → ประกาศ **"คานหายตอนพิมพ์ 92%"** → **เกือบสั่งรื้อวิธีวาดคาน 3-5 วัน แก้บั๊กที่ไม่มีจริง** + ทำ P'Aim เสียเวลาไปพิมพ์ PDF เทียบ
ความจริง: กฎ `issue8` มัดคานตามพยางค์ **ตั้งใจไม่มัด** · dev รันโมเดลจริง: **8 runs/16 bars ตรงกันเป๊ะ คลาด ≤0.1pt**
→ **ก่อนประกาศว่า output หาย ต้องรันฟังก์ชันที่ผลิตมันออกมาเป็นค่าคาดหวังก่อน · วัดเลขถูก ≠ ข้อสรุปถูก**

### (ข) `pleng-pm-never-relay-cant-do`
agent บอก *"`resize_window` พัง ทดสอบมือถือไม่ได้"* → **PM เชื่อแล้วเอาไปบอก P'Aim ต่อ ทั้งที่ PM เพิ่งย่อจอวัดงาน B113 สำเร็จเองเมื่อชั่วโมงก่อน**
P'Aim ตำหนิตรง: *"ทำไมทดสอบมือถือไม่ได้เลย เมื่อก่อนได้ · pm เพลงเริ่มมั่วสุดๆ"*
PM ตรวจซ้ำ: `resize_window(tabId, 360, 800)` → `innerWidth:360 · mq(max-width:400px):true` = **ใช้ได้ปกติ** (สาเหตุที่ agent เจอปัญหา = **ไม่ส่ง `tabId`**)
→ **ห้าม relay "ทำไม่ได้" โดยไม่ลองเอง · สั่ง agent เสมอว่า "ห้ามเขียนว่าขับไม่ได้ จนกว่าจะแปะ output ที่ล้มเหลวมาให้ดู"**

### (ค) `pleng-agent-browser-not-rendering`
agent browser ทุกตัวรายงาน `visibilityState:"hidden"` · **rAF ไม่ยิงเลย** (PM วัดเอง 0 ครั้งใน 6 วิ) · IntersectionObserver ก็ถูกพัก
เกิดจริง: dev รายงาน "คานหายหลัง 4 วิ" (B114) → tester ยืนยันซ้ำ → **ทั้งคู่วัด env พังอันเดียวกัน** · dev ถอนหลักฐานตัวเองอย่างซื่อสัตย์
→ **static geometry (`getBoundingClientRect`/`getComputedStyle`) เชื่อได้ · แต่ห้ามสรุปบั๊ก timing/observer จาก agent** ต้องให้คนเปิดจอจริงชี้ขาด
**(นี่คือเหตุผลที่ B118 ตัดสินไม่ได้จนถึงตอนนี้)**

### (ง) บทเรียนที่ P'Aim สอนตรง — เจอบั๊กในบ้านเดียวกัน = แก้เลย
P'Aim: *"เจอแล้วทำไมไม่แก้เลย"* — PM แยก B114 ออกด้วยเหตุผล "กันตรวจปนกัน" (**หลักถูก แต่ใช้ผิดที่**)
เกณฑ์: อยู่ไฟล์เดียวกับที่เพิ่งแก้ + คนเดิมยังมี context + **อาการเป็นเรื่องเดียวกับที่ผู้ใช้บ่น** → **แก้ในรอบเดียว**

## 8. กระบวนการที่ใช้อยู่
- **PM สร้าง agent session เองได้** · **1 agent = 1 ฟีเจอร์ full-stack** · **tester = agent แยกเสมอ** (คนสร้างห้าม gate งานตัวเอง)
- **M1 = ทำ "หน้าตา" บนโค้ดจริงแล้วหยุด → P'Aim เคาะ → M2 ต่อ logic** (ห้าม throwaway mockup) · resume agent เดิมด้วย `SendMessage`
- แต่ละ agent: **worktree + port ของตัวเอง** · `--host` + แจ้ง Network URL เสมอ
- **ขนาน 2 สายได้ถ้าคนละไฟล์** — สั่งทั้งคู่ว่า "ห้ามแตะไฟล์ของอีกสาย ถ้าจำเป็น = หยุด ping PM"
- **PM ตัดสินเรื่องมาตรฐานเอง ห้ามถาม P'Aim ว่า "อะไรถูก"** — รอบนี้ตัดสิน 3 เรื่อง (ขอบเขตกวาดคลัง · ความยาวขีดสั้นอ้าง Gould · ทิศขีดสั้น)
- **tester พิสูจน์คุณค่าจริง:** ไม่เชื่อ dev ไปทำวิธีตัวเอง → เจอ base แย่กว่าที่ dev เล่า · เทียบ MIDI แบบ full note object · **ล็อกอินเข้าส่วน team-only ด้วย `import('/src/store.js')` แล้วพลิกธง `session`** (ไม่ปลอม token) — **วิธีนี้ใช้เข้าหน้าแก้ไขได้ PM ยืนยันแล้ว**

## 9. 🧹 ความสะอาด
- **worktree 36 → 25** · ปิด dev server ค้าง 7 ตัว (5380 · 5390-5394 · 5398)
- ⚠️ **ก่อนลบ worktree เช็ก `git status --porcelain` เสมอ** — รอบนี้เจอ **รายงาน tester 2 ไฟล์ที่ไม่เคย commit** (fermata · tablet drawer) เกือบหายถาวร → ย้ายเข้า `docs/reports/` แล้ว
- **25 อันที่เหลือห้ามลบมั่ว** — มี unmerged work: 3 ที่นั่งประจำ (`-sa` `-uxui` `-music` ยังใช้อยู่) · `dock-resize` · mp3 · b107 · cello · b109 · dock-space
- 🔧 **ลบทิ้งได้:** `C:/gl/krisada/pleng-deploy35` (โฟลเดอร์เปล่า ไฟล์ถูกล็อก) · `C:/gl/krisada/pleng-deploy36` (worktree ที่ merge B118 ไว้ **ยังไม่ push** — ถ้าทิ้ง B118 ให้ลบทั้ง worktree + branch)
- worktree ที่ยังอยู่จาก B118: `pleng-slur` (dev) · `pleng-t-b118` + `pleng-t-b118base` (tester)
- memory sync ขึ้น OneDrive แล้ว (3 ไฟล์ใหม่ + MEMORY.md)

---
*pm35 · 2026-07-20 · live รอบ 35 · P'Aim สั่งหยุดงาน — ห้ามเดินหน้าจนกว่าจะสั่ง*
