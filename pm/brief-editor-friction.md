# Brief — ปลดคอขวดพี่เปา: 3 อาการ "ใช้ยาก" + ลบร่างตัวเอง

**ฐาน `studio-shell-redesign`** · worktree + port ของตัวเอง · **⛔ refine ไม่ใช่ redesign** (ต่อยอดของเดิม ห้ามรื้อ editor)

## ทำไมงานนี้สำคัญ

**พี่เปาคือคนเดียวที่พิมพ์เพลงเข้าระบบ · เป้าหมาย 124 เพลง** → **ความฝืดของ editor = คอขวดของทั้งโปรเจกต์**
เขารายงาน 3 ใบในวันเดียว และใช้คำเดียวกันหมด: **"เสียเวลา · หายาก · กดหลายที · อ่านแล้วงง"** → **ไม่ใช่ 3 บั๊กแยกกัน มันคืออาการเดียว**
รายงานต้นฉบับ: `C:\Users\aimkr\OneDrive\4 Personal\pleng.phrakham.life\pleng2-pow-bug-report\issues{7,9,11}\`

---

## 🔴 ความเสี่ยงชนสาย — อ่านก่อนแตะ `EditorMode.vue`

**สาย `task_c3c169a7` (ออกแบบป้ายวนซ้ำ) กำลังออกแบบ *ช่องป้าย* ในเมนูเดียวกันนี้อยู่** (`EditorMode.vue:2473` = `<input placeholder="ป้าย เช่น Fine, D.C. al Fine">`)

| | |
|---|---|
| **สายนั้นทำ** | ออกแบบ **เนื้อหา**ของช่องป้าย (เปลี่ยน text box → ตัวเลือก) · **SA ล้วน ยังไม่เขียนโค้ด** |
| **สายนี้ทำ** | **การเข้าถึง**เมนู (issues11 ส่วน "ต้องเลื่อนขึ้นบนสุด") |
| **⛔ ห้ามแตะ** | **ช่องป้าย (`curLine().label`) และถ้อยคำของมัน** — design กำลังมา · แตะไปจะทิ้ง |

**issues11 มี 2 ส่วน แยกให้ขาด:**
- ✅ **"ต้องเลื่อนมาบนสุดถึงจะกดใช้ได้ เสียเวลา"** → **สายนี้**
- ⛔ **"อ่านแล้วก็งงว่าทำอะไร กดแล้วจะไปโชว์ตรงไหน"** → **สาย `task_c3c169a7`** (นั่นคือปัญหาของ*ป้าย* ไม่ใช่ของ*เมนู*)

---

## งาน 4 ข้อ

### 1. issues7 — "ดูผลทั้งเพลง ตัวเล็กมาก ยืดขยายไม่ได้อีก"
**PM verify:** `EditorMode.vue:1740` = toggle `preview` · `1920` = **หน้าต่างลอย non-modal** แสดงแผ่นทั้งเพลง
พี่เปาบอก **"ยืดขยายไม่ได้อีก"** → **มีตัวขยายอยู่แล้วแต่ชนเพดาน?** หรือ **ไม่มีเลย?** → **เปิดของจริงดูก่อน อย่าเดา** · มี `readingFontScale` ใน store (บรรทัด 47) — ใช้กับหน้าต่างนี้ไหม?
**ต้องการ:** อ่านออกจริงบนจอทำงาน · **แก้ที่ต้นเหตุ ไม่ใช่แค่เพิ่มปุ่มซูม** ถ้าต้นเหตุคือหน้าต่างเล็กเกิน

### 2. issues9 — "ทำไมต้องกดปักหมุดก่อนถึงจะเซฟร่างได้ กดหลายทีกว่าจะเซฟ · ให้เลือกได้เลยที่รูปไอคอน"
**PM verify:** เมนูมีระบบ `pinnable` + `default:'inSetting'` (บรรทัด 1740) = **บางคำสั่งซ่อนในเมนูตั้งค่า ต้องปักหมุดก่อนถึงจะออกมาอยู่แถบหลัก**
**ภาพ:** `issues9/20260717-141753.png` — "บันทึกร่าง" อยู่ในเมนูย่อย มีหมุดสีแดงข้าง ๆ
**ต้องการ:** **บันทึกร่าง = คำสั่งที่ใช้บ่อยที่สุดของคนพิมพ์เพลง → ต้องกดถึงได้ทันที ไม่ต้องตั้งค่าก่อนใช้**
⚠️ **อย่าแก้ด้วยการรื้อระบบปักหมุด** — ถามว่า *"ค่าเริ่มต้นของบันทึกร่าง ควรอยู่แถบหลักตั้งแต่แรกไหม"*

### 3. issues11 (เฉพาะส่วนการเข้าถึง) — "ต้องเลื่อนมาบนสุดถึงจะกดใช้ได้"
เมนู "..." ของบรรทัด (ภาพ `issues11/20260717-142154.png`) · **แก้เรื่องตำแหน่ง/การเข้าถึงเท่านั้น** · ⛔ **ห้ามแตะช่องป้าย**

### 4. 🆕 ลบร่างของตัวเอง — **P'Aim อนุมัติแล้ว (17 ก.ค.)**
พี่เปา (issues10): *"เพลงร่างก็หาที่ลบทั้งเพลงไม่ได้"* → **PM verify: ไม่มีจริง** (`deleteDraft` = 0 hit)
**PM verify โครงสร้าง:** ร่างอยู่ตาราง **`song_drafts`** · `saveDraftRow` (`store.js:314`) มีแค่ **insert/update** · `loadDrafts()` (`EditorMode.vue:1198`) · `currentDraftId`

**ขอบเขตที่ P'Aim อนุมัติ = "ลบร่างตัวเองได้เลย" เท่านั้น**
- ⛔ **ไม่รวมลบเพลงที่เผยแพร่แล้ว** — ปุ่มนั้นมีอยู่แล้ว (`EditorMode.vue:2271`) แต่ `v-if="isApprover..."` = **เฉพาะผู้อนุมัติ** · **P'Aim ยังไม่เคาะเรื่องนี้ → ห้ามแตะ**
- ⚠️ **ต้องเช็ก RLS ของ `song_drafts` ก่อน** — ลบได้เฉพาะร่าง**ของตัวเอง** · **ถ้า policy ยังไม่มี delete = ต้องเพิ่ม → นั่นคือการแตะฐานข้อมูลจริง → หยุด รายงาน PM ก่อน อย่าลุยเอง**
- **ต้องมี confirm ก่อนลบ** (ทำลายถาวร) — ดูแบบของเดิมที่ `EditorMode.vue:1404` เป็นตัวอย่างถ้อยคำ

---

## กติกา
- **⛔ refine ไม่ใช่ redesign** — ต่อยอด ไม่รื้อ (P'Aim 11 ก.ค.)
- **มาตรฐานระดับโลกเป็นค่าเริ่มต้น** (Material/HIG · **WCAG 2.2 AA** · target ≥44px) — `docs/ui-standards.md`
- **ทดสอบที่ 360px + 412px จริง** (พี่เปา/P'Aim ใช้มือถือ · memory `feedback_verify_mobile_real_width`)
- เปิด server `--host` + **เช็ก vite Network line จริงก่อนใส่ URL** (IP เปลี่ยนบ่อย · ตอนนี้ `10.189.195.98`)
- **verify ผลลัพธ์ ไม่ใช่การกระทำ** — วันนี้ PM เดาผิด 6 ครั้งเพราะเชื่อคอมเมนต์/ชื่อ/ตารางในโค้ดโดยไม่รันดูจริง → **เปิดของจริง ค้าน PM ได้และควรค้าน**
- **P'Aim/พี่เปาต้องได้ลองของจริง ไม่ใช่หน้า dev** (memory `feedback_show_real_ui_not_harness`)

## ส่งมอบ
`docs/reports/editor-friction.md` — **แต่ละข้อ: ต้นเหตุจริงคืออะไร (verify แล้ว) · แก้ยังไง · ยังเหลืออะไร** · Network URL จริง · เพิ่มบรรทัดใน `docs/pm/board.md` §📥 PM inbox + ping PM session ปัจจุบัน (ดู `board.md` §🎯 · **อย่า hardcode ชื่อ PM session**)
